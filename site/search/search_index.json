{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Red Hat Enterprise Linux 10 \u2014 Beginner to RHCA","text":"<p>Note: This guide does not promise exam coverage. It teaches the real-world skills that those certifications measure.</p> <p>Welcome. This guide takes you from your very first login on a RHEL 10 system all the way through the skills expected of a Red Hat Certified Architect (RHCA) on the RHEL infrastructure track \u2014 with hands-on labs every step of the way.</p>"},{"location":"#who-this-guide-is-for","title":"Who this guide is for","text":"You are \u2026 Start here Completely new to Linux Install a RHEL 10 Lab VM Comfortable with Linux basics, learning RHEL admin Packages and Repos Preparing for RHCE or wanting automation skills Automation Mindset Going deep on RHEL infrastructure / RHCA Troubleshooting Playbook"},{"location":"#how-the-guide-is-structured","title":"How the guide is structured","text":"<p>The guide is split into four progressive tracks:</p> <pre><code>Onramp  \u2192  RHCSA  \u2192  RHCE  \u2192  RHCA\n</code></pre> <p>You can read start-to-finish or jump directly to any chapter. Each chapter is self-contained and lists its prerequisites up front.</p> Track Covers Onramp Terminal, files, permissions, editors, help system RHCSA Storage, users, systemd, networking, firewall, SSH, SELinux RHCE Bash, Ansible, roles, patching workflows RHCA SELinux deep dive, advanced systemd, networking, Podman ops, performance"},{"location":"#lab-environment","title":"Lab environment","text":"<p>All labs target a single fresh RHEL 10 VM unless explicitly marked <code>(Multi-VM)</code>. Every lab tells you exactly:</p> <ul> <li>what packages to install first</li> <li>the step-by-step commands with expected results</li> <li>how to verify success</li> <li>how to clean up and reset</li> </ul> <p>Read the Lab Workflow page before starting your first lab.</p>"},{"location":"#conventions-at-a-glance","title":"Conventions at a glance","text":"<ul> <li><code>$</code> \u2014 command runs as a regular user</li> <li><code>#</code> \u2014 command runs as root (prefer <code>sudo</code> unless told otherwise)</li> <li><code>sudo command</code> \u2014 escalate a single command (safe default)</li> <li>Highlighted blocks like this:</li> </ul> <p>Do NOT do this</p> <p>A pattern you should avoid, with a short explanation and the correct alternative.</p> <p>Pro tip</p> <p>Useful shortcut or context that helps you understand the \"why\".</p> <p>Full details in Conventions.</p>"},{"location":"#objective-map","title":"Objective map","text":"<p>Every chapter is mapped to RHCSA / RHCE / RHCA-style skill domains in the Objective Map appendix.</p> <p>Note: This guide does not promise exam coverage. It teaches the real-world skills that those certifications measure.</p>"},{"location":"00-preface/about/","title":"About This Guide","text":"<p>Note: This guide does not promise exam coverage. It teaches the real-world skills that those certifications measure.</p>"},{"location":"00-preface/about/#purpose","title":"Purpose","text":"<p>This guide exists to give Linux newcomers and experienced admins alike a single, structured path through Red Hat Enterprise Linux 10 \u2014 from opening a terminal for the first time to designing resilient infrastructure.</p>"},{"location":"00-preface/about/#what-makes-it-different","title":"What makes it different","text":"<ul> <li>Beginner-safe language: every concept is explained before it is used.</li> <li>Lab-driven: you learn by doing, not just reading.</li> <li>Honest about \"don't do this\": common mistakes (disabling SELinux, opening   all firewall ports, running everything as root) are called out explicitly, with   the correct alternative shown.</li> <li>RHCA-aligned depth: the advanced chapters cover the reasoning and design   thinking expected at architect level \u2014 not just \"run this command\".</li> </ul>"},{"location":"00-preface/about/#scope","title":"Scope","text":"In scope Out of scope RHEL 10 (minimal/server install) RHEL 6, 7, or 8 specifics Single-host and small-lab scenarios Large-scale fleet management Podman (rootless containers) Kubernetes / OpenShift Ansible fundamentals Advanced Ansible Tower/AAP Core RHEL networking SDN / OVN"},{"location":"00-preface/about/#versions-and-testing","title":"Versions and testing","text":"<p>Commands in this guide were tested on:</p> <ul> <li>RHEL 10.0 (minimal install, 2 vCPU, 2 GB RAM, 20 GB disk)</li> <li>Each lab page lists the exact package versions used.</li> </ul>"},{"location":"00-preface/about/#contributing","title":"Contributing","text":"<p>Found an error or want to add a lab? Open an issue or pull request in the repository. All contributions must follow the Conventions style guide and include a working <code>Verify</code> section.</p>"},{"location":"00-preface/about/#license","title":"License","text":"<p>This guide is published under the Creative Commons Attribution 4.0 International License.</p>"},{"location":"00-preface/conventions/","title":"Conventions","text":"<p>Consistent formatting makes commands safe to copy and easy to understand. This page defines every convention used throughout the guide.</p>"},{"location":"00-preface/conventions/#shell-prompts","title":"Shell prompts","text":"Prompt Meaning <code>$</code> Regular (unprivileged) user <code>#</code> Root shell \u2014 only when <code>sudo</code> is insufficient or impractical <code>sudo command</code> Preferred way to escalate a single command <p>Example:</p> <pre><code>$ whoami        # shows your username\n$ sudo whoami   # shows: root\n</code></pre> <p>We use <code>sudo command</code> for isolated privileged operations. We introduce <code>sudo -i</code> (interactive root shell) only in chapters where it's clearly needed, with an explicit note about why.</p>"},{"location":"00-preface/conventions/#command-blocks","title":"Command blocks","text":"<p>All commands are copy-paste safe. They do not include the prompt character (<code>$</code> / <code>#</code>) in the text you copy \u2014 only in the annotations.</p> <pre><code># Install a package\nsudo dnf install -y vim\n</code></pre> <p>Lines starting with <code>#</code> inside a code block are comments \u2014 do not run them.</p>"},{"location":"00-preface/conventions/#expected-output","title":"Expected output","text":"<p>Labs use \"look for a line containing\u2026\" style checks, not brittle full-output copies. Example:</p> <pre><code>sudo systemctl status sshd\n</code></pre> <p>Look for a line containing: <code>Active: active (running)</code></p> <p>This is intentional \u2014 minor differences (timestamps, PIDs) should not cause confusion.</p>"},{"location":"00-preface/conventions/#file-paths-and-interface-names","title":"File paths and interface names","text":"Convention Reason Network interface shown as <code>ens3</code> Common in QEMU/KVM VMs; yours may differ (check with <code>ip link</code>) Disk shown as <code>/dev/vda</code> Common in KVM VMs; bare metal often uses <code>/dev/sda</code> Username shown as <code>rhel</code> Replace with your actual username where relevant <p>When a command depends on a value you must substitute, it is shown in angle-bracket uppercase:</p> <pre><code>sudo nmcli con mod &lt;CONNECTION-NAME&gt; ipv4.addresses 192.168.1.10/24\n</code></pre>"},{"location":"00-preface/conventions/#admonition-types","title":"Admonition types","text":"<p>Note</p> <p>Background context or \"good to know\" information.</p> <p>Pro tip</p> <p>Shortcut, pattern, or insight that experienced admins use.</p> <p>Do NOT do this</p> <p>A common but incorrect approach. Followed immediately by the correct alternative.</p> <p>Destructive</p> <p>This command modifies disk partitions, erases data, or changes system boot config. Take a snapshot first.</p> <p>Verify</p> <p>A checkpoint command with expected output. If this fails, stop and troubleshoot before continuing.</p>"},{"location":"00-preface/conventions/#track-badges","title":"Track badges","text":"<p>Each chapter title includes a track label:</p> Badge Meaning <code>(Onramp)</code> Absolute beginner content <code>(RHCSA)</code> Day-to-day system administration <code>(RHCE)</code> Automation-first operations <code>(RHCA)</code> Architecture-level depth"},{"location":"00-preface/conventions/#terminology","title":"Terminology","text":"<ul> <li>host: the physical or virtual machine running RHEL</li> <li>node: same as host, used in automation/multi-host context</li> <li>control node: the machine running Ansible (often your laptop or a jump host)</li> <li>managed node: a host Ansible configures</li> <li>container: a rootless Podman container unless stated otherwise</li> </ul>"},{"location":"00-preface/labs/","title":"Lab Workflow \u2014 Snapshots and Safety","text":""},{"location":"00-preface/labs/#golden-rule","title":"Golden rule","text":"<p>Take a VM snapshot before every lab. If something goes wrong, revert and start over. Snapshots are free and save enormous time.</p>"},{"location":"00-preface/labs/#recommended-vm-setup","title":"Recommended VM setup","text":"Tool Notes GNOME Boxes Easiest snapshot UX on Linux desktop VirtualBox Free; snapshots via Machine \u2192 Take Snapshot libvirt / virt-manager Best for advanced setups; CLI snapshots with <code>virsh snapshot-create-as</code> VMware Workstation / Fusion Commercial; excellent snapshot support"},{"location":"00-preface/labs/#take-a-snapshot-before-each-lab","title":"Take a snapshot before each lab","text":"virt-manager (GUI)virsh (CLI)VirtualBox (CLI) <p>Right-click the VM \u2192 Manage Snapshots \u2192 +</p> <pre><code>virsh snapshot-create-as --domain rhel10-lab \\\n  --name \"before-lab-$(date +%Y%m%d)\" \\\n  --description \"clean state\"\n</code></pre> <pre><code>VBoxManage snapshot \"rhel10-lab\" take \"before-lab-$(date +%Y%m%d)\"\n</code></pre>"},{"location":"00-preface/labs/#restore-a-snapshot","title":"Restore a snapshot","text":"virsh (CLI)VirtualBox (CLI) <pre><code>virsh snapshot-revert --domain rhel10-lab --snapshotname \"before-lab-20260223\"\n</code></pre> <pre><code>VBoxManage snapshot \"rhel10-lab\" restore \"before-lab-20260223\"\n</code></pre>"},{"location":"00-preface/labs/#lab-page-anatomy","title":"Lab page anatomy","text":"<p>Every lab in this guide has the same structure:</p> <pre><code>## Prerequisites\n## Estimated time\n## Success criteria\n## Steps  (numbered, with Verify checkpoints inline)\n## Cleanup\n## Common failures\n## Why this matters in production\n</code></pre> <p>Read the Success criteria section first \u2014 it tells you exactly what \"done\" looks like before you start typing.</p>"},{"location":"00-preface/labs/#verify-checkpoints","title":"Verify checkpoints","text":"<p>Labs use verify blocks to confirm state at key points:</p> <p>Verify</p> <p><pre><code>systemctl is-active myservice\n</code></pre> Expected: <code>active</code></p> <p>If a verify step fails, stop and read the Common failures section for that lab before proceeding.</p>"},{"location":"00-preface/labs/#safety-conventions","title":"Safety conventions","text":"<ul> <li>Every destructive-ish command (anything touching disks, firewall rules, or   SELinux mode) is marked with a warning admonition.</li> <li>Labs do not ask you to run <code>setenforce 0</code> as a fix \u2014 if you see that in a   tutorial elsewhere, treat it as a red flag.</li> <li>Labs do not ask you to disable <code>firewalld</code> \u2014 they show you the correct   rule to add.</li> <li>Cleanup steps are mandatory, not optional. Run them before your next lab.</li> </ul>"},{"location":"00-preface/labs/#single-vm-vs-multi-vm-labs","title":"Single-VM vs Multi-VM labs","text":"Label Topology (no label) Single VM only <code>(Multi-VM)</code> Needs 2\u20133 VMs; instructions provided <p>Multi-VM labs are always optional. The same skill is demonstrated in a single-VM variant where possible.</p>"},{"location":"01-getting-started/first-boot/","title":"First Boot Checklist","text":"<p>Run through this checklist immediately after installing RHEL 10. Each step takes under a minute.</p>"},{"location":"01-getting-started/first-boot/#1-confirm-you-are-not-logged-in-as-root","title":"1 \u2014 Confirm you are NOT logged in as root","text":"<pre><code>$ whoami\n</code></pre> <p>Expected: your regular username. If it says <code>root</code>, log out and log in as your regular user.</p>"},{"location":"01-getting-started/first-boot/#2-check-network-connectivity","title":"2 \u2014 Check network connectivity","text":"<pre><code>$ ping -c 3 8.8.8.8\n</code></pre> <p>Look for: <code>3 packets transmitted, 3 received</code></p> <p>If this fails, see NetworkManager (nmcli).</p>"},{"location":"01-getting-started/first-boot/#3-check-dns-resolution","title":"3 \u2014 Check DNS resolution","text":"<pre><code>$ ping -c 2 access.redhat.com\n</code></pre> <p>Look for: IP address resolved and packets received.</p>"},{"location":"01-getting-started/first-boot/#4-register-the-system-if-not-done-during-install","title":"4 \u2014 Register the system (if not done during install)","text":"<pre><code>sudo subscription-manager register --username &lt;your-redhat-username&gt;\nsudo subscription-manager attach --auto\n</code></pre>"},{"location":"01-getting-started/first-boot/#5-apply-all-available-updates","title":"5 \u2014 Apply all available updates","text":"<pre><code>sudo dnf upgrade -y\n</code></pre> <p>This may take a few minutes on first run. Reboot if the kernel was updated.</p> <pre><code>sudo reboot\n</code></pre>"},{"location":"01-getting-started/first-boot/#6-confirm-selinux-is-enforcing","title":"6 \u2014 Confirm SELinux is enforcing","text":"<pre><code>$ getenforce\n</code></pre> <p>Expected: <code>Enforcing</code></p> <p>Do NOT change this</p> <p>Do not set SELinux to <code>Permissive</code> or <code>Disabled</code>. If something breaks, learn to fix it correctly. See SELinux Fundamentals.</p>"},{"location":"01-getting-started/first-boot/#7-confirm-firewalld-is-running","title":"7 \u2014 Confirm firewalld is running","text":"<pre><code>$ sudo firewall-cmd --state\n</code></pre> <p>Expected: <code>running</code></p>"},{"location":"01-getting-started/first-boot/#8-check-system-time","title":"8 \u2014 Check system time","text":"<pre><code>$ timedatectl\n</code></pre> <p>Look for: <code>System clock synchronized: yes</code> and <code>NTP service: active</code></p> <p>If NTP is not active:</p> <pre><code>sudo timedatectl set-ntp true\n</code></pre>"},{"location":"01-getting-started/first-boot/#9-set-a-hostname-if-not-done-during-install","title":"9 \u2014 Set a hostname (if not done during install)","text":"<pre><code>sudo hostnamectl set-hostname rhel10-lab\n</code></pre>"},{"location":"01-getting-started/first-boot/#10-take-a-vm-snapshot","title":"10 \u2014 Take a VM snapshot","text":"<p>Now is the perfect time to take a clean snapshot before you start making changes. See Lab Workflow for instructions.</p>"},{"location":"01-getting-started/first-boot/#next-step","title":"Next step","text":"<p>\u2192 Accounts, sudo, and Updates</p>"},{"location":"01-getting-started/help-system/","title":"Getting Help \u2014 man, --help, apropos","text":"<p>One of the most important skills on any Linux system is knowing how to find answers without leaving the terminal.</p>"},{"location":"01-getting-started/help-system/#the-help-flag","title":"The <code>--help</code> flag","text":"<p>Almost every command accepts <code>--help</code>. It shows a short summary of options.</p> <pre><code>ls --help\ndnf --help\nsystemctl --help\n</code></pre> <p>Use <code>--help</code> when you just need a quick reminder of a flag.</p>"},{"location":"01-getting-started/help-system/#man-pages","title":"man pages","text":"<p><code>man</code> (manual) provides the full reference documentation for commands, config files, and system calls.</p> <pre><code>man ls\nman dnf\nman 5 fstab     # section 5 = file formats\nman 8 firewalld # section 8 = sysadmin commands\n</code></pre>"},{"location":"01-getting-started/help-system/#navigating-a-man-page","title":"Navigating a man page","text":"Key Action <code>j</code> / <code>k</code> or arrow keys Scroll down / up <code>Space</code> / <code>b</code> Page down / page up <code>/pattern</code> Search forward <code>n</code> / <code>N</code> Next / previous search match <code>q</code> Quit"},{"location":"01-getting-started/help-system/#man-page-sections","title":"Man page sections","text":"Section Content 1 User commands 5 File formats and config files 7 Overviews, conventions 8 System administration commands <pre><code># See all sections available for a keyword\nman -k passwd\n</code></pre>"},{"location":"01-getting-started/help-system/#apropos-search-by-keyword","title":"<code>apropos</code> \u2014 search by keyword","text":"<p>If you don't know the command name, <code>apropos</code> searches man page descriptions:</p> <pre><code>apropos firewall\napropos \"network interface\"\n</code></pre> <p>Update the man database</p> <p>If <code>apropos</code> returns nothing useful, update its index: <pre><code>sudo mandb\n</code></pre></p>"},{"location":"01-getting-started/help-system/#info-pages","title":"<code>info</code> pages","text":"<p>Some GNU tools have richer documentation in <code>info</code> format:</p> <pre><code>info coreutils\ninfo bash\n</code></pre>"},{"location":"01-getting-started/help-system/#usrsharedoc","title":"<code>/usr/share/doc</code>","text":"<p>Package documentation (changelogs, examples, READMEs) lives here:</p> <pre><code>ls /usr/share/doc/\nls /usr/share/doc/bash/\n</code></pre>"},{"location":"01-getting-started/help-system/#online-resources-official","title":"Online resources (official)","text":"Resource URL RHEL 10 documentation https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/10 Red Hat Knowledgebase https://access.redhat.com/solutions Package search https://access.redhat.com/packages"},{"location":"01-getting-started/help-system/#quick-reference-help-commands","title":"Quick reference: help commands","text":"<pre><code># Short help\nls --help\n\n# Full manual\nman ls\n\n# Config file format\nman 5 fstab\n\n# Search all man pages by keyword\napropos partition\n\n# Find which package provides a command\ndnf provides /usr/bin/ss\n</code></pre>"},{"location":"01-getting-started/help-system/#next-step","title":"Next step","text":"<p>\u2192 Shell Basics</p>"},{"location":"01-getting-started/sudo-updates/","title":"Accounts, sudo, and Updates","text":""},{"location":"01-getting-started/sudo-updates/#user-accounts-on-rhel","title":"User accounts on RHEL","text":"<p>RHEL separates regular users from privileged operations through the <code>sudo</code> mechanism. You should almost never need to log in as <code>root</code> directly.</p>"},{"location":"01-getting-started/sudo-updates/#key-concepts","title":"Key concepts","text":"Term Meaning root The superuser \u2014 unlimited system access regular user Day-to-day account; limited privileges sudo Run a single command with root privileges wheel group Members of this group can use <code>sudo</code> on RHEL"},{"location":"01-getting-started/sudo-updates/#check-your-sudo-access","title":"Check your sudo access","text":"<pre><code>$ sudo -l\n</code></pre> <p>Look for a line containing: <code>(ALL) ALL</code> \u2014 this confirms you can run any command via sudo.</p>"},{"location":"01-getting-started/sudo-updates/#add-a-user-to-the-wheel-group","title":"Add a user to the wheel group","text":"<p>If you need to grant another user sudo access:</p> <pre><code>sudo usermod -aG wheel username\n</code></pre> <p>Verify group membership takes effect</p> <p>The user must log out and back in (or open a new shell) for the new group to apply. Check with <code>groups username</code>.</p>"},{"location":"01-getting-started/sudo-updates/#sudo-patterns","title":"sudo patterns","text":"<pre><code># Run a single command as root\nsudo dnf install -y vim\n\n# Edit a protected file safely\nsudo vim /etc/hostname\n\n# Open an interactive root shell (use sparingly)\nsudo -i\n\n# Run as a specific user\nsudo -u nginx id\n</code></pre> <p>sudo -i vs sudo su</p> <p>Prefer <code>sudo -i</code> over <code>sudo su -</code>. It is audited, respects sudoers policy, and does not require the root password.</p>"},{"location":"01-getting-started/sudo-updates/#the-sudoers-file","title":"The sudoers file","text":"<p>The sudoers configuration lives in <code>/etc/sudoers</code> and <code>/etc/sudoers.d/</code>.</p> <p>Always use visudo to edit sudoers</p> <p><pre><code>sudo visudo\n</code></pre> <code>visudo</code> validates syntax before saving. A syntax error in sudoers can lock you out of sudo entirely.</p>"},{"location":"01-getting-started/sudo-updates/#package-updates-with-dnf","title":"Package updates with dnf","text":""},{"location":"01-getting-started/sudo-updates/#check-for-available-updates","title":"Check for available updates","text":"<pre><code>sudo dnf check-update\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#apply-all-updates","title":"Apply all updates","text":"<pre><code>sudo dnf upgrade -y\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#apply-security-updates-only","title":"Apply security updates only","text":"<pre><code>sudo dnf upgrade --security -y\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#check-what-changed-after-an-update","title":"Check what changed after an update","text":"<pre><code>sudo dnf history\nsudo dnf history info &lt;ID&gt;\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#automatic-updates-optional-for-a-lab-vm","title":"Automatic updates (optional for a lab VM)","text":"<p>RHEL ships with <code>dnf-automatic</code>. For a learning VM, manual updates are preferable so you control when things change.</p> <pre><code># Install if you want automatic downloads + notifications\nsudo dnf install -y dnf-automatic\nsudo systemctl enable --now dnf-automatic-install.timer\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#creating-additional-user-accounts","title":"Creating additional user accounts","text":"<pre><code># Create user\nsudo useradd -m -c \"Full Name\" username\n\n# Set password\nsudo passwd username\n\n# Lock an account\nsudo usermod -L username\n\n# Delete account (keeps home dir)\nsudo userdel username\n\n# Delete account AND home dir\nsudo userdel -r username\n</code></pre>"},{"location":"01-getting-started/sudo-updates/#next-step","title":"Next step","text":"<p>\u2192 Getting Help</p>"},{"location":"01-getting-started/vm-install/","title":"Install a RHEL 10 Lab VM","text":""},{"location":"01-getting-started/vm-install/#what-you-need","title":"What you need","text":"Item Minimum Recommended vCPUs 1 2 RAM 1.5 GB 2\u20134 GB Disk 20 GB 40 GB Network NAT (internet access) NAT + Host-only adapter"},{"location":"01-getting-started/vm-install/#step-1-get-a-rhel-10-iso","title":"Step 1 \u2014 Get a RHEL 10 ISO","text":"<ol> <li>Create a free account at Red Hat Developer Portal.</li> <li>Navigate to Downloads \u2192 Red Hat Enterprise Linux.</li> <li>Select RHEL 10 and download the Boot ISO (smaller) or DVD ISO (larger, works offline).</li> </ol> <p>Developer subscription</p> <p>A Red Hat Developer subscription gives you access to RHEL for personal use at no cost. It includes the full package repository \u2014 the same as a paid subscription.</p>"},{"location":"01-getting-started/vm-install/#step-2-create-the-vm","title":"Step 2 \u2014 Create the VM","text":"virt-manager (KVM)VirtualBoxGNOME Boxes <ol> <li>Open virt-manager \u2192 File \u2192 New Virtual Machine</li> <li>Choose Local install media \u2192 select the ISO</li> <li>Set RAM, CPUs, and disk to recommended values above</li> <li>Name it <code>rhel10-lab</code>, check Customize configuration before install</li> <li>Click Finish</li> </ol> <ol> <li>Machine \u2192 New \u2192 name <code>rhel10-lab</code>, type Linux, version Red Hat (64-bit)</li> <li>Assign RAM and disk (use VDI, dynamically allocated)</li> <li>In VM settings \u2192 Storage, attach the ISO to the optical drive</li> <li>In Network, set Adapter 1 to NAT</li> </ol> <ol> <li>Click + \u2192 Create a Virtual Machine</li> <li>Select the ISO file</li> <li>Boxes auto-suggests resources \u2014 accept or raise them</li> <li>Click Create</li> </ol>"},{"location":"01-getting-started/vm-install/#step-3-rhel-10-installer-anaconda","title":"Step 3 \u2014 RHEL 10 installer (Anaconda)","text":"<ol> <li>Select Install Red Hat Enterprise Linux 10</li> <li>Choose your language</li> <li> <p>In the Installation Summary screen, configure:</p> </li> <li> <p>Installation Destination \u2192 select your virtual disk \u2192 Automatic partitioning</p> </li> <li>Software Selection \u2192 choose Minimal Install (recommended for learning)</li> <li>Network &amp; Host Name \u2192 enable the network adapter; set hostname to <code>rhel10-lab</code></li> <li>Root Password \u2192 set a strong password (you will rarely use it directly)</li> <li> <p>User Creation \u2192 create a regular user, check Make this user administrator</p> </li> <li> <p>Click Begin Installation</p> </li> </ol> <p>Why Minimal Install?</p> <p>Minimal gives you only what you need. You install additional packages deliberately, which teaches you the package manager and keeps the system clean.</p>"},{"location":"01-getting-started/vm-install/#step-4-first-login","title":"Step 4 \u2014 First login","text":"<p>After reboot, log in with your regular user account (not root).</p> <pre><code>$ whoami\n</code></pre> <p>Expected: your username (not <code>root</code>)</p>"},{"location":"01-getting-started/vm-install/#step-5-register-with-red-hat","title":"Step 5 \u2014 Register with Red Hat","text":"<pre><code>sudo subscription-manager register --username &lt;your-redhat-username&gt;\nsudo subscription-manager attach --auto\n</code></pre> <p>Verify</p> <p><pre><code>sudo subscription-manager status\n</code></pre> Look for a line containing: <code>Overall Status: Current</code></p> <p>Once registered, your system has access to all RHEL 10 repositories.</p>"},{"location":"01-getting-started/vm-install/#next-step","title":"Next step","text":"<p>\u2192 First Boot Checklist</p>"},{"location":"02-foundations/acls/","title":"ACLs \u2014 When and How","text":"<p>Standard Unix permissions give you three permission sets: user, group, other. Access Control Lists (ACLs) extend this by letting you grant permissions to any number of specific users or groups on a single file or directory.</p>"},{"location":"02-foundations/acls/#when-to-use-acls","title":"When to use ACLs","text":"<p>Use ACLs when standard permissions are not expressive enough. Examples:</p> <ul> <li>A shared project directory where two separate groups each need different access</li> <li>Grant one specific user read access to a file without changing group ownership</li> <li>Set default permissions so all new files in a directory inherit the right ACLs</li> </ul> <p>ACLs don't replace standard permissions</p> <p>ACLs augment standard Unix permissions. The effective permission for a user is the most specific match (user ACL &gt; group ACL &gt; other). When an ACL is present, a mask also applies \u2014 see below.</p>"},{"location":"02-foundations/acls/#check-if-acls-are-supported","title":"Check if ACLs are supported","text":"<p>XFS (the default filesystem on RHEL) supports ACLs natively. No mount options needed.</p> <pre><code>tune2fs -l /dev/vda1 2&gt;/dev/null | grep \"Default mount\" || echo \"XFS - ACLs native\"\n</code></pre>"},{"location":"02-foundations/acls/#install-acl-tools","title":"Install ACL tools","text":"<pre><code>sudo dnf install -y acl\n</code></pre>"},{"location":"02-foundations/acls/#view-acls-getfacl","title":"View ACLs \u2014 <code>getfacl</code>","text":"<pre><code>getfacl /srv/project/\n</code></pre> <p>A file with no ACLs set shows:</p> <pre><code># file: project/\n# owner: root\n# group: developers\nuser::rwx\ngroup::rwx\nother::r-x\n</code></pre> <p>A file with ACLs set shows extra <code>user:</code> or <code>group:</code> lines plus a <code>mask::</code>.</p>"},{"location":"02-foundations/acls/#set-acls-setfacl","title":"Set ACLs \u2014 <code>setfacl</code>","text":"<pre><code># Grant user alice read+write on a file\nsetfacl -m u:alice:rw /srv/project/report.txt\n\n# Grant group qa read-only on a directory\nsetfacl -m g:qa:r-x /srv/project/\n\n# Set a default ACL (applies to newly created files inside the dir)\nsetfacl -m d:u:alice:rw /srv/project/\n\n# Recursive \u2014 apply to directory and all existing contents\nsetfacl -R -m u:alice:rw /srv/project/\n</code></pre>"},{"location":"02-foundations/acls/#remove-acls","title":"Remove ACLs","text":"<pre><code># Remove a specific user ACL\nsetfacl -x u:alice /srv/project/report.txt\n\n# Remove all ACLs (reverts to standard permissions)\nsetfacl -b /srv/project/report.txt\n</code></pre>"},{"location":"02-foundations/acls/#the-acl-mask","title":"The ACL mask","text":"<p>The mask limits the maximum effective permissions for named users and groups (not the owning user or other). It is recalculated automatically when you set ACLs.</p> <pre><code># Force the mask to a specific value\nsetfacl -m mask::r-x /srv/project/\n</code></pre>"},{"location":"02-foundations/acls/#preserve-acls-when-copying","title":"Preserve ACLs when copying","text":"<p>Standard <code>cp</code> does not copy ACLs by default:</p> <pre><code># Copy and preserve ACLs\ncp -a source/ destination/\n\n# Or explicitly\ncp --preserve=all source/ destination/\n</code></pre> <p><code>rsync</code> with <code>-A</code> flag preserves ACLs:</p> <pre><code>rsync -aA /srv/project/ /backup/project/\n</code></pre>"},{"location":"02-foundations/acls/#next-step","title":"Next step","text":"<p>\u2192 Lab: Shared Team Directory</p>"},{"location":"02-foundations/editors/","title":"Editing Files \u2014 nano and vim","text":"<p>You will edit configuration files constantly as a RHEL admin. This chapter covers two editors: nano (beginner-friendly) and vim (the editor you will eventually need for speed and remote sessions).</p>"},{"location":"02-foundations/editors/#nano-the-beginners-editor","title":"nano \u2014 the beginner's editor","text":"<p>nano is simple: open a file, type, save. The help bar at the bottom shows all shortcuts.</p> <pre><code>nano /tmp/testfile.txt\n</code></pre>"},{"location":"02-foundations/editors/#key-shortcuts","title":"Key shortcuts","text":"Keys Action <code>Ctrl+O</code>, <code>Enter</code> Save the file <code>Ctrl+X</code> Exit <code>Ctrl+W</code> Search <code>Ctrl+K</code> Cut the current line <code>Ctrl+U</code> Paste <code>Ctrl+G</code> Show full help <p>sudo nano</p> <p>To edit a system file: <pre><code>sudo nano /etc/hosts\n</code></pre></p>"},{"location":"02-foundations/editors/#vim-the-editor-you-will-need","title":"vim \u2014 the editor you will need","text":"<p>vim is installed on virtually every Linux server. Once you learn its basic modes, it is extremely fast.</p>"},{"location":"02-foundations/editors/#install-vim-on-a-minimal-system","title":"Install vim on a minimal system","text":"<pre><code>sudo dnf install -y vim\n</code></pre>"},{"location":"02-foundations/editors/#the-two-modes-you-must-know","title":"The two modes you must know","text":"Mode How to enter What you can do Normal Press <code>Esc</code> Navigate, copy, paste, delete Insert Press <code>i</code> Type text <p>vim always starts in Normal mode. You cannot type text until you press <code>i</code>.</p>"},{"location":"02-foundations/editors/#getting-started","title":"Getting started","text":"<pre><code>vim /tmp/testvim.txt\n</code></pre> <ol> <li>Press <code>i</code> \u2192 you are now in Insert mode (you see <code>-- INSERT --</code> at the bottom)</li> <li>Type some text</li> <li>Press <code>Esc</code> \u2192 back to Normal mode</li> <li>Type <code>:wq</code> \u2192 save and quit</li> </ol>"},{"location":"02-foundations/editors/#essential-normal-mode-commands","title":"Essential Normal mode commands","text":"Command Action <code>i</code> Insert before cursor <code>A</code> Insert at end of line <code>o</code> Insert new line below <code>dd</code> Delete (cut) current line <code>yy</code> Yank (copy) current line <code>p</code> Paste after cursor <code>u</code> Undo <code>Ctrl+R</code> Redo <code>/pattern</code> Search forward <code>n</code> Next match <code>gg</code> Go to first line <code>G</code> Go to last line <code>:&lt;number&gt;</code> Go to line number <code>:w</code> Save <code>:q</code> Quit (fails if unsaved changes) <code>:wq</code> Save and quit <code>:q!</code> Quit without saving <p>Stuck in vim?</p> <p>If you are stuck and see unexpected characters, press <code>Esc</code> several times then type <code>:q!</code> and press <code>Enter</code>. This exits without saving.</p>"},{"location":"02-foundations/editors/#which-editor-to-use-when","title":"Which editor to use when","text":"Situation Recommended Quick config file edit <code>nano</code> Remote sessions where nano is not installed <code>vim</code> Writing scripts or long files <code>vim</code> Sudoedit (editing protected files safely) <code>sudo EDITOR=nano visudo</code>"},{"location":"02-foundations/editors/#environment-variables-for-editor-preference","title":"Environment variables for editor preference","text":"<p>Set your default editor in <code>~/.bashrc</code>:</p> <pre><code>echo 'export EDITOR=vim' &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre> <p>Commands like <code>crontab -e</code> and <code>visudo</code> will use <code>$EDITOR</code> automatically.</p>"},{"location":"02-foundations/editors/#next-step","title":"Next step","text":"<p>\u2192 Users, Groups, Permissions</p>"},{"location":"02-foundations/files-and-text/","title":"Files and Text \u2014 cp, mv, rm, less","text":""},{"location":"02-foundations/files-and-text/#viewing-files","title":"Viewing files","text":"<pre><code># View a short file\ncat /etc/hostname\n\n# View a long file with scrolling (press q to quit)\nless /etc/os-release\n\n# First 20 lines\nhead -n 20 /var/log/messages\n\n# Last 20 lines\ntail -n 20 /var/log/messages\n\n# Follow a log in real time (Ctrl+C to stop)\ntail -f /var/log/messages\n</code></pre>"},{"location":"02-foundations/files-and-text/#copying-files-cp","title":"Copying files \u2014 <code>cp</code>","text":"<pre><code># Copy a file\ncp /etc/hosts /tmp/hosts.bak\n\n# Copy a directory recursively\ncp -r /etc/sysconfig /tmp/sysconfig-bak\n\n# Copy and preserve permissions/timestamps\ncp -a /etc/hosts /tmp/hosts.bak\n</code></pre>"},{"location":"02-foundations/files-and-text/#moving-and-renaming-mv","title":"Moving and renaming \u2014 <code>mv</code>","text":"<pre><code># Move file to another directory\nmv /tmp/hosts.bak /home/rhel/\n\n# Rename a file\nmv /home/rhel/hosts.bak /home/rhel/hosts.old\n\n# Move directory\nmv /tmp/sysconfig-bak /home/rhel/\n</code></pre>"},{"location":"02-foundations/files-and-text/#removing-files-rm","title":"Removing files \u2014 <code>rm</code>","text":"<pre><code># Remove a file\nrm /tmp/oldfile.txt\n\n# Remove a directory and its contents\nrm -r /tmp/olddir/\n\n# Force removal (no confirmation)\nrm -f /tmp/stubborn.txt\n</code></pre> <p>rm is permanent</p> <p>There is no Trash on the CLI. <code>rm</code> deletes immediately. Double-check your path before running it, especially with <code>-r</code> or wildcards.</p> <p>Do NOT do this</p> <p><pre><code>rm -rf /   # destroys the entire system\nrm -rf /*  # also destroys the entire system\n</code></pre> RHEL has protection against <code>rm -rf /</code> but always be careful with <code>rm -r</code>.</p>"},{"location":"02-foundations/files-and-text/#creating-directories-mkdir","title":"Creating directories \u2014 <code>mkdir</code>","text":"<pre><code># Create one directory\nmkdir /tmp/mydir\n\n# Create nested directories in one command\nmkdir -p /tmp/project/src/lib\n</code></pre>"},{"location":"02-foundations/files-and-text/#finding-files-find","title":"Finding files \u2014 <code>find</code>","text":"<pre><code># Find by name\nfind /etc -name \"*.conf\"\n\n# Find by type (f=file, d=directory)\nfind /var/log -type f -name \"*.log\"\n\n# Find files modified in the last 24 hours\nfind /etc -mtime -1\n</code></pre>"},{"location":"02-foundations/files-and-text/#searching-inside-files-grep","title":"Searching inside files \u2014 <code>grep</code>","text":"<pre><code># Search for a pattern in a file\ngrep \"PermitRootLogin\" /etc/ssh/sshd_config\n\n# Case-insensitive search\ngrep -i \"error\" /var/log/messages\n\n# Show line numbers\ngrep -n \"failed\" /var/log/secure\n\n# Recursive search in a directory\ngrep -r \"192.168.1\" /etc/\n</code></pre>"},{"location":"02-foundations/files-and-text/#counting-lines-words-characters-wc","title":"Counting lines, words, characters \u2014 <code>wc</code>","text":"<pre><code>wc -l /etc/passwd        # number of lines\nwc -w /etc/hosts         # number of words\n</code></pre>"},{"location":"02-foundations/files-and-text/#file-details-stat-and-file","title":"File details \u2014 <code>stat</code> and <code>file</code>","text":"<pre><code># Full metadata (permissions, timestamps, inode)\nstat /etc/hosts\n\n# Identify file type regardless of extension\nfile /usr/bin/bash\nfile /etc/passwd\n</code></pre>"},{"location":"02-foundations/files-and-text/#next-step","title":"Next step","text":"<p>\u2192 Pipes and Redirection</p>"},{"location":"02-foundations/permissions/","title":"Users, Groups, and Permissions","text":"<p>Linux uses a permission model based on users, groups, and a set of read/write/execute bits. Understanding this is essential for everything that follows.</p>"},{"location":"02-foundations/permissions/#the-permission-model","title":"The permission model","text":"<p>Every file and directory has three permission sets:</p> <pre><code>-rw-r--r--  1  rhel  rhel  1234  Feb 23 10:00  file.txt\n\u2502\u2514\u252c\u2518\u2514\u252c\u2518\u2514\u252c\u2518     \u2502     \u2502\n\u2502 \u2502   \u2502   \u2502     \u2502     \u2514\u2500\u2500 Group owner\n\u2502 \u2502   \u2502   \u2502     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 User owner\n\u2502 \u2502   \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 Other (everyone else)\n\u2502 \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 Group permissions\n\u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 User permissions\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 File type (- = file, d = dir, l = symlink)\n</code></pre>"},{"location":"02-foundations/permissions/#permission-bits","title":"Permission bits","text":"Symbol Numeric File meaning Directory meaning <code>r</code> 4 Read file contents List directory contents <code>w</code> 2 Modify file Create/delete files inside <code>x</code> 1 Execute file Enter (cd into) directory <code>-</code> 0 Permission not set Permission not set"},{"location":"02-foundations/permissions/#viewing-permissions-ls-l","title":"Viewing permissions \u2014 <code>ls -l</code>","text":"<pre><code>ls -l /etc/hosts\n</code></pre> <p>Example output:</p> <pre><code>-rw-r--r--. 1 root root 174 Jan 10 09:00 /etc/hosts\n</code></pre> <p>The <code>.</code> after the permission string indicates an SELinux context is set.</p>"},{"location":"02-foundations/permissions/#changing-permissions-chmod","title":"Changing permissions \u2014 <code>chmod</code>","text":"<pre><code># Symbolic form\nchmod u+x script.sh        # add execute for user\nchmod g-w file.txt         # remove write for group\nchmod o= file.txt          # clear all permissions for other\nchmod a+r file.txt         # add read for all (user, group, other)\n\n# Numeric (octal) form\nchmod 644 file.txt         # rw-r--r--\nchmod 755 script.sh        # rwxr-xr-x\nchmod 600 private.key      # rw-------\nchmod 700 private-dir/     # rwx------\n</code></pre>"},{"location":"02-foundations/permissions/#common-permission-patterns","title":"Common permission patterns","text":"Octal Pattern Use case <code>600</code> rw------- Private key, secret file <code>644</code> rw-r--r-- Config file, web content <code>664</code> rw-rw-r-- Group-editable file <code>755</code> rwxr-xr-x Executable script, public directory <code>750</code> rwxr-x--- Script accessible to group only <code>700</code> rwx------ Private directory"},{"location":"02-foundations/permissions/#changing-ownership-chown","title":"Changing ownership \u2014 <code>chown</code>","text":"<pre><code># Change user owner\nsudo chown rhel file.txt\n\n# Change user and group owner\nsudo chown rhel:developers file.txt\n\n# Change group only\nsudo chown :developers file.txt\n\n# Recursive (directory and all contents)\nsudo chown -R rhel:developers /srv/project/\n</code></pre>"},{"location":"02-foundations/permissions/#changing-group-chgrp","title":"Changing group \u2014 <code>chgrp</code>","text":"<pre><code>sudo chgrp developers file.txt\n</code></pre>"},{"location":"02-foundations/permissions/#managing-groups","title":"Managing groups","text":"<pre><code># Create a group\nsudo groupadd developers\n\n# Add a user to a group\nsudo usermod -aG developers rhel\n\n# View your groups\ngroups\n\n# View all groups\ngetent group\n</code></pre> <p>The -aG flag</p> <p><code>-aG</code> means append to groups. Without <code>-a</code>, <code>usermod -G</code> replaces all supplementary groups \u2014 a common mistake that locks users out.</p>"},{"location":"02-foundations/permissions/#umask-default-permissions","title":"umask \u2014 default permissions","text":"<p><code>umask</code> defines permissions that are removed from newly created files and directories.</p> <pre><code>umask          # show current umask (default: 0022)\n</code></pre> <p>With <code>umask 0022</code>: - New files: <code>0666 - 0022 = 0644</code> (rw-r--r--) - New directories: <code>0777 - 0022 = 0755</code> (rwxr-xr-x)</p>"},{"location":"02-foundations/permissions/#special-permission-bits","title":"Special permission bits","text":"Bit On file On directory SUID (<code>4xxx</code>) Run as file owner N/A SGID (<code>2xxx</code>) Run as file group New files inherit group Sticky (<code>1xxx</code>) N/A Only owner can delete their own files <pre><code># SGID on a shared directory (new files inherit group)\nchmod g+s /srv/shared/\n# or\nchmod 2775 /srv/shared/\n\n# Sticky bit (e.g., /tmp)\nls -ld /tmp\n# drwxrwxrwt \u2014 the 't' is the sticky bit\n</code></pre>"},{"location":"02-foundations/permissions/#next-step","title":"Next step","text":"<p>\u2192 ACLs</p>"},{"location":"02-foundations/pipes-redirection/","title":"Pipes and Redirection","text":"<p>Pipes and redirection let you combine simple commands into powerful workflows. This is one of the most useful concepts in Linux.</p>"},{"location":"02-foundations/pipes-redirection/#output-redirection","title":"Output redirection","text":"<pre><code># Write stdout to a file (overwrites)\nls -l /etc &gt; /tmp/etc-listing.txt\n\n# Append stdout to a file\necho \"new line\" &gt;&gt; /tmp/myfile.txt\n\n# Discard output\nsudo dnf upgrade -y &gt; /dev/null\n\n# Redirect stderr (error output) to a file\nls /nonexistent 2&gt; /tmp/errors.txt\n\n# Redirect both stdout and stderr to the same file\nsudo dnf upgrade -y &gt; /tmp/upgrade.log 2&gt;&amp;1\n\n# Short form (bash 4+)\nsudo dnf upgrade -y &amp;&gt; /tmp/upgrade.log\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#input-redirection","title":"Input redirection","text":"<pre><code># Feed a file as input to a command\nsort &lt; /etc/passwd\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#pipes","title":"Pipes \u2014 <code>|</code>","text":"<p>A pipe sends the output of one command as the input to the next.</p> <pre><code># Count lines in a directory listing\nls /etc | wc -l\n\n# Find errors in a log\ncat /var/log/messages | grep -i error\n\n# Page through long output\nsystemctl list-units | less\n\n# Combine multiple pipes\ncat /var/log/secure | grep \"Failed\" | wc -l\n</code></pre> <p>Think of pipes as assembly lines</p> <p>Each command does one small job. Pipes chain them together. The UNIX philosophy: do one thing well.</p>"},{"location":"02-foundations/pipes-redirection/#useful-commands-for-pipes","title":"Useful commands for pipes","text":""},{"location":"02-foundations/pipes-redirection/#sort","title":"<code>sort</code>","text":"<pre><code># Sort alphabetically\ncat /etc/passwd | sort\n\n# Sort by field (delimiter :, field 3 = UID, numeric)\nsort -t: -k3 -n /etc/passwd\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#uniq","title":"<code>uniq</code>","text":"<pre><code># Remove adjacent duplicate lines (usually sort first)\ncat /var/log/messages | grep \"error\" | sort | uniq\n\n# Count occurrences\ncat /var/log/messages | grep \"error\" | sort | uniq -c | sort -rn\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#cut","title":"<code>cut</code>","text":"<pre><code># Extract first field (delimiter :)\ncut -d: -f1 /etc/passwd\n\n# Extract columns from output\nip addr | grep \"inet \" | cut -d/ -f1\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#awk","title":"<code>awk</code>","text":"<pre><code># Print second column of output\ndf -h | awk '{print $2}'\n\n# Print lines where field 3 &gt; 1000\nawk -F: '$3 &gt;= 1000 {print $1}' /etc/passwd\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#tee","title":"<code>tee</code>","text":"<p>Write to both screen and file simultaneously:</p> <pre><code>sudo dnf upgrade -y | tee /tmp/upgrade.log\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#here-strings-and-here-documents","title":"Here-strings and here-documents","text":"<pre><code># Feed a string as stdin\ngrep \"root\" &lt;&lt;&lt; \"root:x:0:0:root:/root:/bin/bash\"\n\n# Feed a multi-line block as stdin (heredoc)\ncat &lt;&lt;EOF\nLine one\nLine two\nEOF\n</code></pre>"},{"location":"02-foundations/pipes-redirection/#next-step","title":"Next step","text":"<p>\u2192 Editing Files</p>"},{"location":"02-foundations/shell-basics/","title":"Shell Basics \u2014 pwd, ls, cd","text":"<p>The shell is your primary interface to RHEL. This chapter covers the commands you will use every single session.</p>"},{"location":"02-foundations/shell-basics/#the-prompt","title":"The prompt","text":"<p>When you log in you see a prompt like:</p> <pre><code>[rhel@rhel10-lab ~]$\n</code></pre> Part Meaning <code>rhel</code> Current username <code>rhel10-lab</code> Hostname <code>~</code> Current directory (<code>~</code> = your home directory) <code>$</code> Regular user (would be <code>#</code> for root)"},{"location":"02-foundations/shell-basics/#where-am-i-pwd","title":"Where am I? \u2014 <code>pwd</code>","text":"<pre><code>$ pwd\n</code></pre> <p>Output: <code>/home/rhel</code> (or wherever you are)</p> <p><code>pwd</code> = print working directory.</p>"},{"location":"02-foundations/shell-basics/#what-is-here-ls","title":"What is here? \u2014 <code>ls</code>","text":"<pre><code># List current directory\nls\n\n# Long format (permissions, owner, size, date)\nls -l\n\n# Include hidden files (names starting with .)\nls -la\n\n# Human-readable file sizes\nls -lh\n\n# List a specific directory\nls -l /etc\n</code></pre>"},{"location":"02-foundations/shell-basics/#moving-around-cd","title":"Moving around \u2014 <code>cd</code>","text":"<pre><code># Go to your home directory\ncd\n\n# Go to a specific path (absolute)\ncd /etc/sysconfig\n\n# Go up one level\ncd ..\n\n# Go up two levels\ncd ../..\n\n# Go to the previous directory\ncd -\n</code></pre> <p>Tab completion</p> <p>Press <code>Tab</code> to auto-complete paths and command names. Press <code>Tab</code> twice to see all options when there are multiple matches. Use it constantly.</p>"},{"location":"02-foundations/shell-basics/#absolute-vs-relative-paths","title":"Absolute vs relative paths","text":"Type Example Meaning Absolute <code>/etc/hosts</code> Always starts from root <code>/</code> Relative <code>../hosts</code> Relative to your current directory"},{"location":"02-foundations/shell-basics/#important-directories-to-know","title":"Important directories to know","text":"Path Contents <code>/</code> Root of the filesystem <code>/home</code> User home directories <code>/etc</code> System configuration files <code>/var</code> Variable data (logs, spool, databases) <code>/usr</code> Installed programs and libraries <code>/tmp</code> Temporary files (cleared on reboot) <code>/boot</code> Kernel and bootloader files <code>/dev</code> Device files <code>/proc</code> Virtual filesystem for kernel/process info <code>/sys</code> Virtual filesystem for hardware/kernel data <code>/run</code> Runtime data (PIDs, sockets) \u2014 cleared on reboot"},{"location":"02-foundations/shell-basics/#useful-shortcuts","title":"Useful shortcuts","text":"Shortcut Effect <code>Ctrl+C</code> Cancel a running command <code>Ctrl+L</code> Clear the screen <code>Ctrl+A</code> Jump to start of line <code>Ctrl+E</code> Jump to end of line <code>Up arrow</code> Previous command <code>!!</code> Repeat last command <code>!$</code> Last argument of previous command <code>history</code> Show command history"},{"location":"02-foundations/shell-basics/#next-step","title":"Next step","text":"<p>\u2192 Files and Text</p>"},{"location":"02-foundations/labs/shared-team-dir/","title":"Lab: Shared Team Directory","text":"<p>Track: Onramp Estimated time: 20 minutes Topology: Single VM</p>"},{"location":"02-foundations/labs/shared-team-dir/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Users, Groups, Permissions and ACLs</li> <li>ACL tools installed: <code>sudo dnf install -y acl</code></li> <li>A fresh VM snapshot taken</li> </ul>"},{"location":"02-foundations/labs/shared-team-dir/#success-criteria","title":"Success criteria","text":"<p>By the end of this lab:</p> <ul> <li>A shared directory <code>/srv/teamspace</code> exists where:</li> <li>Members of group <code>devteam</code> can create and edit files</li> <li>User <code>auditor</code> has read-only access via ACL</li> <li>New files created inside inherit the group and correct permissions (SGID + default ACL)</li> <li>You can demonstrate each access level works correctly</li> </ul>"},{"location":"02-foundations/labs/shared-team-dir/#steps","title":"Steps","text":""},{"location":"02-foundations/labs/shared-team-dir/#1-create-users-and-group","title":"1 \u2014 Create users and group","text":"<pre><code>sudo groupadd devteam\nsudo useradd -m -G devteam alice\nsudo useradd -m -G devteam bob\nsudo useradd -m auditor\n</code></pre> <p>Set passwords for testing:</p> <pre><code>echo \"alice:Password1!\" | sudo chpasswd\necho \"bob:Password1!\" | sudo chpasswd\necho \"auditor:Password1!\" | sudo chpasswd\n</code></pre> <p>Verify</p> <p><pre><code>getent group devteam\n</code></pre> Look for: <code>devteam:x:&lt;GID&gt;:alice,bob</code></p>"},{"location":"02-foundations/labs/shared-team-dir/#2-create-the-shared-directory","title":"2 \u2014 Create the shared directory","text":"<pre><code>sudo mkdir -p /srv/teamspace\nsudo chown root:devteam /srv/teamspace\nsudo chmod 2775 /srv/teamspace\n</code></pre> <p>The <code>2</code> in <code>2775</code> sets the SGID bit \u2014 new files will inherit the <code>devteam</code> group automatically.</p> <p>Verify</p> <p><pre><code>ls -ld /srv/teamspace\n</code></pre> Look for: <code>drwxrwsr-x</code> and group <code>devteam</code></p>"},{"location":"02-foundations/labs/shared-team-dir/#3-set-default-acl-for-auditor","title":"3 \u2014 Set default ACL for auditor","text":"<pre><code># Grant auditor read access to existing directory\nsudo setfacl -m u:auditor:r-x /srv/teamspace\n\n# Default ACL: auditor gets read on all new files/dirs inside\nsudo setfacl -m d:u:auditor:r-x /srv/teamspace\n</code></pre> <p>Verify</p> <p><pre><code>getfacl /srv/teamspace\n</code></pre> Look for lines: <pre><code>user:auditor:r-x\ndefault:user:auditor:r-x\n</code></pre></p>"},{"location":"02-foundations/labs/shared-team-dir/#4-test-alice-devteam-member","title":"4 \u2014 Test alice (devteam member)","text":"<pre><code>sudo -u alice bash -c \"echo 'alice was here' &gt; /srv/teamspace/alice-file.txt\"\n</code></pre> <p>Verify</p> <p><pre><code>ls -l /srv/teamspace/alice-file.txt\n</code></pre> Look for: group is <code>devteam</code> (SGID inherited)</p>"},{"location":"02-foundations/labs/shared-team-dir/#5-test-bob-devteam-member","title":"5 \u2014 Test bob (devteam member)","text":"<p>Bob should be able to read and write alice's file because the group <code>devteam</code> has write permission:</p> <pre><code>sudo -u bob bash -c \"echo 'bob was here' &gt;&gt; /srv/teamspace/alice-file.txt\"\nsudo -u bob cat /srv/teamspace/alice-file.txt\n</code></pre> <p>Verify</p> <p>Output contains both lines.</p>"},{"location":"02-foundations/labs/shared-team-dir/#6-test-auditor-read-only-via-acl","title":"6 \u2014 Test auditor (read-only via ACL)","text":"<pre><code>sudo -u auditor cat /srv/teamspace/alice-file.txt\n</code></pre> <p>Expected: file contents displayed \u2014 auditor can read.</p> <pre><code>sudo -u auditor bash -c \"echo 'auditor writes' &gt;&gt; /srv/teamspace/alice-file.txt\"\n</code></pre> <p>Expected: <code>Permission denied</code> \u2014 auditor cannot write.</p>"},{"location":"02-foundations/labs/shared-team-dir/#7-confirm-new-files-inherit-the-acl","title":"7 \u2014 Confirm new files inherit the ACL","text":"<p>Create a new file as alice and check its ACLs:</p> <pre><code>sudo -u alice bash -c \"echo 'new file' &gt; /srv/teamspace/new-file.txt\"\ngetfacl /srv/teamspace/new-file.txt\n</code></pre> <p>Look for: <code>user:auditor:r--</code> inherited from the default ACL.</p>"},{"location":"02-foundations/labs/shared-team-dir/#cleanup","title":"Cleanup","text":"<pre><code>sudo rm -rf /srv/teamspace\nsudo userdel -r alice\nsudo userdel -r bob\nsudo userdel -r auditor\nsudo groupdel devteam\n</code></pre>"},{"location":"02-foundations/labs/shared-team-dir/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix <code>getent group devteam</code> shows no members User not added to group <code>sudo usermod -aG devteam alice</code> New files do not show <code>devteam</code> group SGID bit not set <code>chmod g+s /srv/teamspace</code> auditor can write ACL mask too permissive Re-run <code>setfacl -m u:auditor:r-x</code> <code>setfacl: command not found</code> acl package missing <code>sudo dnf install -y acl</code>"},{"location":"02-foundations/labs/shared-team-dir/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>Shared project directories in development or CI environments need exactly this pattern: a group-writable directory where all team members can collaborate, new files inherit the right group (SGID), and read-only audit or monitoring accounts can inspect content without write access. Getting this wrong leads to permission errors, files owned by the wrong user, and security gaps.</p>"},{"location":"02-foundations/labs/shared-team-dir/#next-step","title":"Next step","text":"<p>\u2192 Packages and Repos (dnf)</p>"},{"location":"03-rhcsa/dns-resolution/","title":"DNS and Name Resolution","text":"<p>Understanding how RHEL resolves names is essential for troubleshooting connectivity issues. The resolution stack on RHEL 10 uses systemd-resolved integrated with NetworkManager.</p>"},{"location":"03-rhcsa/dns-resolution/#resolution-order","title":"Resolution order","text":"<p>When you <code>ping hostname</code>, RHEL checks:</p> <ol> <li><code>/etc/hosts</code> (local overrides \u2014 checked first)</li> <li><code>systemd-resolved</code> stub resolver (<code>127.0.0.53</code>)</li> <li>Upstream DNS servers (from NetworkManager connection config)</li> </ol>"},{"location":"03-rhcsa/dns-resolution/#etchosts","title":"<code>/etc/hosts</code>","text":"<p>Local static name-to-IP mappings. Takes priority over DNS.</p> <pre><code>cat /etc/hosts\n</code></pre> <pre><code>127.0.0.1   localhost localhost.localdomain\n::1         localhost localhost.localdomain\n192.168.1.10  app01.lab.local app01\n</code></pre> <p>To add a local override:</p> <pre><code>sudo vim /etc/hosts\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#etcresolvconf","title":"<code>/etc/resolv.conf</code>","text":"<p>Points to the stub resolver. On RHEL 10 with systemd-resolved this is managed automatically \u2014 do not edit it by hand.</p> <pre><code>cat /etc/resolv.conf\n</code></pre> <pre><code>nameserver 127.0.0.53\noptions edns0 trust-ad\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#check-dns-resolution","title":"Check DNS resolution","text":"<pre><code># Simple lookup\nhost access.redhat.com\n\n# Detailed A record lookup\ndig access.redhat.com\n\n# Specific record type\ndig MX redhat.com\ndig AAAA access.redhat.com\n\n# Reverse lookup (IP to name)\ndig -x 8.8.8.8\n\n# Short answer only\ndig +short access.redhat.com\n\n# Query a specific DNS server\ndig @8.8.8.8 access.redhat.com\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#resolvectl-systemd-resolved-control","title":"<code>resolvectl</code> \u2014 systemd-resolved control","text":"<pre><code># Show current DNS configuration per interface\nresolvectl status\n\n# Query a name (uses systemd-resolved)\nresolvectl query access.redhat.com\n\n# Flush the DNS cache\nsudo resolvectl flush-caches\n\n# Show statistics\nresolvectl statistics\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#set-dns-servers-per-connection","title":"Set DNS servers per connection","text":"<pre><code>sudo nmcli connection modify \"Wired connection 1\" \\\n  ipv4.dns \"192.168.1.1 8.8.8.8 2001:4860:4860::8888\"\n\nsudo nmcli connection up \"Wired connection 1\"\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#search-domains","title":"Search domains","text":"<pre><code>sudo nmcli connection modify \"Wired connection 1\" \\\n  ipv4.dns-search \"lab.local example.com\"\n\nsudo nmcli connection up \"Wired connection 1\"\n</code></pre> <p>Now <code>ping app01</code> resolves to <code>app01.lab.local</code> automatically.</p>"},{"location":"03-rhcsa/dns-resolution/#etcnsswitchconf","title":"<code>/etc/nsswitch.conf</code>","text":"<p>Controls the resolution order for names, passwords, groups, etc.:</p> <pre><code>grep hosts /etc/nsswitch.conf\n</code></pre> <pre><code>hosts: files dns myhostname\n</code></pre> <ul> <li><code>files</code> = <code>/etc/hosts</code></li> <li><code>dns</code> = DNS resolver</li> <li><code>myhostname</code> = resolve the machine's own hostname</li> </ul>"},{"location":"03-rhcsa/dns-resolution/#troubleshooting-dns","title":"Troubleshooting DNS","text":""},{"location":"03-rhcsa/dns-resolution/#name-not-resolving","title":"\"Name not resolving\"","text":"<pre><code># 1. Is it a DNS or routing problem?\nping 8.8.8.8              # if this works, network is up\nping access.redhat.com    # if this fails, DNS is the problem\n\n# 2. Check what DNS server is being used\nresolvectl status | grep \"DNS Server\"\n\n# 3. Test with a known-good resolver\ndig @8.8.8.8 access.redhat.com\n\n# 4. Check /etc/hosts for stale entries\ngrep access.redhat.com /etc/hosts\n\n# 5. Flush cache and retry\nsudo resolvectl flush-caches\n</code></pre>"},{"location":"03-rhcsa/dns-resolution/#next-step","title":"Next step","text":"<p>\u2192 Firewalling (firewalld)</p>"},{"location":"03-rhcsa/filesystems-fstab/","title":"Filesystems and fstab","text":"<p>This chapter covers XFS filesystem management and persistent mounts via <code>/etc/fstab</code> \u2014 the foundation of storage configuration on RHEL.</p>"},{"location":"03-rhcsa/filesystems-fstab/#xfs-rhels-default-filesystem","title":"XFS \u2014 RHEL's default filesystem","text":"<p>XFS is a high-performance 64-bit journaling filesystem. It is the default on RHEL and the best choice for most workloads.</p> Feature Value Max filesystem size 8 EiB Max file size 8 EiB Journal Metadata only (fast recovery) Growing Supported online (no unmount needed) Shrinking Not supported \u2014 size wisely <p>XFS cannot be shrunk</p> <p>Plan your XFS partition sizes with room to grow. Once created, an XFS filesystem can only be extended, not reduced.</p>"},{"location":"03-rhcsa/filesystems-fstab/#create-and-mount-xfs","title":"Create and mount XFS","text":"<pre><code># Partition (if starting from a raw disk)\nsudo parted /dev/vdb mklabel gpt\nsudo parted /dev/vdb mkpart primary xfs 1MiB 100%\nsudo partprobe /dev/vdb    # inform kernel of new partition table\n\n# Format\nsudo mkfs.xfs /dev/vdb1\n\n# Mount point\nsudo mkdir -p /mnt/data\n\n# Temporary mount (lost on reboot)\nsudo mount /dev/vdb1 /mnt/data\n\n# Verify\ndf -h /mnt/data\n</code></pre>"},{"location":"03-rhcsa/filesystems-fstab/#etcfstab-persistent-mounts","title":"<code>/etc/fstab</code> \u2014 persistent mounts","text":"<p><code>/etc/fstab</code> defines what gets mounted at boot.</p>"},{"location":"03-rhcsa/filesystems-fstab/#format","title":"Format","text":"<pre><code>&lt;device&gt;  &lt;mountpoint&gt;  &lt;fstype&gt;  &lt;options&gt;  &lt;dump&gt;  &lt;pass&gt;\n</code></pre> Field Value Notes device <code>UUID=...</code> Prefer UUID over device name mountpoint <code>/mnt/data</code> Must exist before mounting fstype <code>xfs</code> Filesystem type options <code>defaults</code> Common mount options dump <code>0</code> <code>0</code> = do not back up with dump pass <code>0</code> <code>0</code> = no fsck at boot (XFS handles its own recovery)"},{"location":"03-rhcsa/filesystems-fstab/#add-an-entry","title":"Add an entry","text":"<pre><code># Get the UUID\nsudo blkid /dev/vdb1\n</code></pre> <pre><code>sudo vim /etc/fstab\n</code></pre> <pre><code>UUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  /mnt/data  xfs  defaults  0 0\n</code></pre>"},{"location":"03-rhcsa/filesystems-fstab/#test-immediately-do-not-wait-for-reboot","title":"Test immediately (do not wait for reboot)","text":"<pre><code>sudo mount -a\n</code></pre> <p>Verify</p> <p><pre><code>df -h /mnt/data\n</code></pre> Look for: <code>/mnt/data</code> with the expected size</p>"},{"location":"03-rhcsa/filesystems-fstab/#common-mount-options","title":"Common mount options","text":"Option Meaning <code>defaults</code> rw, suid, exec, auto, nouser, async <code>noexec</code> Prevent executing binaries from this mount <code>nosuid</code> Ignore SUID/SGID bits <code>noatime</code> Don't update access time (performance gain) <code>ro</code> Read-only <code>_netdev</code> Wait for network before mounting (NFS, iSCSI)"},{"location":"03-rhcsa/filesystems-fstab/#growing-an-xfs-filesystem","title":"Growing an XFS filesystem","text":"<p>XFS supports online growth (filesystem stays mounted):</p> <pre><code># First, grow the underlying partition or LV (see LVM chapter)\n# Then grow the filesystem to fill the new space:\nsudo xfs_growfs /mnt/data\n</code></pre>"},{"location":"03-rhcsa/filesystems-fstab/#xfs-tools","title":"XFS tools","text":"<pre><code># View filesystem info\nsudo xfs_info /mnt/data\n\n# Repair (must be unmounted)\nsudo xfs_repair /dev/vdb1\n\n# Dump/restore (for backup)\nsudo xfsdump -l 0 -f /backup/data.dump /mnt/data\nsudo xfsrestore -f /backup/data.dump /mnt/restore\n</code></pre>"},{"location":"03-rhcsa/filesystems-fstab/#diagnosing-full-filesystems","title":"Diagnosing full filesystems","text":"<pre><code># Check disk usage\ndf -h\n\n# Find large files\nsudo du -sh /var/* | sort -rh | head -20\n\n# Find files consuming inodes\ndf -ih\n</code></pre>"},{"location":"03-rhcsa/filesystems-fstab/#next-step","title":"Next step","text":"<p>\u2192 LVM</p>"},{"location":"03-rhcsa/firewalld/","title":"Firewalling with firewalld","text":"<p><code>firewalld</code> is the default firewall manager on RHEL 10. It uses the concept of zones to apply rules based on network trust level.</p>"},{"location":"03-rhcsa/firewalld/#core-concepts","title":"Core concepts","text":"Term Meaning zone A trust level applied to a network interface or source IP service A named rule set (e.g., <code>ssh</code>, <code>http</code>) defined in XML port A direct port/protocol rule (e.g., <code>8080/tcp</code>) rich rule More expressive rule with source/destination/action runtime Currently active rules (lost on reload) permanent Rules saved to disk (survive reload/reboot)"},{"location":"03-rhcsa/firewalld/#check-firewalld-status","title":"Check firewalld status","text":"<pre><code>sudo firewall-cmd --state          # running or not running\nsudo firewall-cmd --list-all       # show active zone + all rules\nsudo firewall-cmd --get-zones      # list all available zones\nsudo firewall-cmd --get-default-zone\nsudo firewall-cmd --get-active-zones\n</code></pre>"},{"location":"03-rhcsa/firewalld/#default-zone","title":"Default zone","text":"<p>The default zone applies to interfaces not explicitly assigned to another zone. On most RHEL VMs the default zone is <code>public</code>.</p> <pre><code>sudo firewall-cmd --get-default-zone\n\n# Change default zone\nsudo firewall-cmd --set-default-zone=home\n</code></pre>"},{"location":"03-rhcsa/firewalld/#allow-a-service","title":"Allow a service","text":"<pre><code># Runtime only (lost on firewalld reload)\nsudo firewall-cmd --add-service=http\n\n# Permanent (survives reload/reboot)\nsudo firewall-cmd --permanent --add-service=http\n\n# Both: add permanently AND reload to activate immediately\nsudo firewall-cmd --permanent --add-service=http\nsudo firewall-cmd --reload\n</code></pre> <p>Best practice: always use --permanent + --reload</p> <p>Using <code>--permanent</code> first and then <code>--reload</code> ensures your rules survive reboots and avoids confusion between runtime and permanent state.</p>"},{"location":"03-rhcsa/firewalld/#remove-a-service","title":"Remove a service","text":"<pre><code>sudo firewall-cmd --permanent --remove-service=http\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/firewalld/#allow-a-port","title":"Allow a port","text":"<pre><code>sudo firewall-cmd --permanent --add-port=8080/tcp\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/firewalld/#remove-a-port","title":"Remove a port","text":"<pre><code>sudo firewall-cmd --permanent --remove-port=8080/tcp\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/firewalld/#list-rules","title":"List rules","text":"<pre><code># All rules in the default zone\nsudo firewall-cmd --list-all\n\n# Rules in a specific zone\nsudo firewall-cmd --list-all --zone=public\n\n# List only services\nsudo firewall-cmd --list-services\n\n# List only ports\nsudo firewall-cmd --list-ports\n</code></pre>"},{"location":"03-rhcsa/firewalld/#predefined-services","title":"Predefined services","text":"<pre><code># See all available predefined services\nsudo firewall-cmd --get-services\n\n# Where service definitions live\nls /usr/lib/firewalld/services/\nls /etc/firewalld/services/   # custom overrides go here\n</code></pre>"},{"location":"03-rhcsa/firewalld/#zones-in-practice","title":"Zones in practice","text":"<p>Common zones and their default posture:</p> Zone Intended use <code>public</code> Untrusted public networks \u2014 only explicitly allowed traffic in <code>home</code> Trusted home network \u2014 less restrictive <code>work</code> Work networks <code>trusted</code> All traffic allowed (use with extreme care) <code>drop</code> All inbound traffic silently dropped <code>block</code> All inbound traffic rejected with ICMP unreachable <code>dmz</code> Servers in a DMZ \u2014 limited inbound <code>internal</code> Internal network"},{"location":"03-rhcsa/firewalld/#assign-an-interface-to-a-zone","title":"Assign an interface to a zone","text":"<pre><code>sudo firewall-cmd --permanent --zone=internal --change-interface=ens3\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/firewalld/#rich-rules-advanced","title":"Rich rules (advanced)","text":"<pre><code># Allow SSH from a specific subnet only\nsudo firewall-cmd --permanent \\\n  --add-rich-rule='rule family=\"ipv4\" source address=\"192.168.1.0/24\" service name=\"ssh\" accept'\n\n# Block a specific IP\nsudo firewall-cmd --permanent \\\n  --add-rich-rule='rule family=\"ipv4\" source address=\"10.0.0.5\" drop'\n\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/firewalld/#masquerading-nat-for-outbound","title":"Masquerading (NAT for outbound)","text":"<pre><code>sudo firewall-cmd --permanent --add-masquerade\nsudo firewall-cmd --reload\n</code></pre> <p>Do NOT do this</p> <p><pre><code>sudo systemctl stop firewalld\nsudo systemctl disable firewalld\n</code></pre> Disabling the firewall is not a fix for a connectivity problem. Diagnose the correct port or service to open instead.</p>"},{"location":"03-rhcsa/firewalld/#next-step","title":"Next step","text":"<p>\u2192 SSH (Keys, Server Basics)</p>"},{"location":"03-rhcsa/logging-journald/","title":"Logs and journalctl","text":"<p>systemd captures all service output in the journal \u2014 a binary, indexed log store. <code>journalctl</code> is your primary tool for reading it.</p>"},{"location":"03-rhcsa/logging-journald/#basic-usage","title":"Basic usage","text":"<pre><code># Show all logs (newest at bottom), page through with less\njournalctl\n\n# Reverse order (newest first)\njournalctl -r\n\n# Follow in real time (like tail -f)\njournalctl -f\n\n# Show only the last 50 lines\njournalctl -n 50\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#filter-by-unit-service","title":"Filter by unit (service)","text":"<pre><code># Logs for one service\njournalctl -u sshd.service\n\n# Follow a service's logs live\njournalctl -f -u sshd.service\n\n# Multiple units\njournalctl -u sshd.service -u firewalld.service\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#filter-by-time","title":"Filter by time","text":"<pre><code># Since a specific time\njournalctl --since \"2026-02-23 08:00:00\"\n\n# Between two times\njournalctl --since \"2026-02-23 08:00:00\" --until \"2026-02-23 09:00:00\"\n\n# Since boot\njournalctl -b\n\n# Previous boot\njournalctl -b -1\n\n# List available boots\njournalctl --list-boots\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#filter-by-priority-log-level","title":"Filter by priority (log level)","text":"Level Meaning <code>emerg</code> (0) System unusable <code>alert</code> (1) Immediate action required <code>crit</code> (2) Critical condition <code>err</code> (3) Error <code>warning</code> (4) Warning <code>notice</code> (5) Normal but significant <code>info</code> (6) Informational <code>debug</code> (7) Debug messages <pre><code># Only errors and above\njournalctl -p err\n\n# Between warning and error\njournalctl -p warning..err\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#kernel-messages","title":"Kernel messages","text":"<pre><code># Journal kernel messages\njournalctl -k\n\n# Since last boot\njournalctl -k -b\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#search-output","title":"Search output","text":"<pre><code># Grep inside journalctl output\njournalctl -u sshd | grep \"Failed\"\n\n# Case-insensitive\njournalctl -u sshd | grep -i \"failed\"\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#persistent-journal","title":"Persistent journal","text":"<p>By default on RHEL, the journal is persistent across reboots (stored in <code>/var/log/journal/</code>). If yours is not:</p> <pre><code>sudo mkdir -p /var/log/journal\nsudo systemd-tmpfiles --create --prefix /var/log/journal\nsudo systemctl restart systemd-journald\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#control-journal-disk-usage","title":"Control journal disk usage","text":"<pre><code># View current disk usage\njournalctl --disk-usage\n\n# Vacuum to keep only last 2 weeks\nsudo journalctl --vacuum-time=2weeks\n\n# Vacuum to 1 GB max\nsudo journalctl --vacuum-size=1G\n</code></pre> <p>Journal retention is configured in <code>/etc/systemd/journald.conf</code>:</p> <pre><code>[Journal]\nSystemMaxUse=2G\nMaxRetentionSec=1month\n</code></pre>"},{"location":"03-rhcsa/logging-journald/#traditional-log-files","title":"Traditional log files","text":"<p>systemd-journald forwards to <code>rsyslog</code> on RHEL, so traditional log files in <code>/var/log/</code> still exist:</p> File Contents <code>/var/log/messages</code> General system messages <code>/var/log/secure</code> Authentication events (SSH, sudo) <code>/var/log/audit/audit.log</code> SELinux AVC denials, audit events <code>/var/log/dnf.log</code> Package operations <code>/var/log/boot.log</code> Boot messages"},{"location":"03-rhcsa/logging-journald/#next-step","title":"Next step","text":"<p>\u2192 Scheduling (timers + cron)</p>"},{"location":"03-rhcsa/lvm/","title":"LVM \u2014 Create, Extend, Reduce Safely","text":"<p>Logical Volume Management (LVM) adds a flexible abstraction layer between physical storage and filesystems. It lets you resize, snapshot, and reorganise storage without downtime.</p>"},{"location":"03-rhcsa/lvm/#lvm-concepts","title":"LVM concepts","text":"<pre><code>Physical disk(s)\n    \u2514\u2500\u2500 Physical Volume (PV)   [pvcreate]\n          \u2514\u2500\u2500 Volume Group (VG) [vgcreate]\n                \u251c\u2500\u2500 Logical Volume (LV) [lvcreate]\n                \u2514\u2500\u2500 Logical Volume (LV)\n                      \u2514\u2500\u2500 Filesystem (mkfs.xfs)\n                            \u2514\u2500\u2500 Mount point\n</code></pre> Term Description PV (Physical Volume) A disk or partition initialised for LVM VG (Volume Group) A pool of storage from one or more PVs LV (Logical Volume) A slice of a VG; behaves like a partition PE (Physical Extent) Smallest allocation unit (default 4 MiB)"},{"location":"03-rhcsa/lvm/#create-an-lvm-setup-from-scratch","title":"Create an LVM setup from scratch","text":"<p>Assumes a raw extra disk <code>/dev/vdb</code> (adjust to your device).</p>"},{"location":"03-rhcsa/lvm/#1-initialise-the-pv","title":"1 \u2014 Initialise the PV","text":"<pre><code>sudo pvcreate /dev/vdb\n</code></pre> <p>Verify</p> <p><pre><code>sudo pvs\n</code></pre> Look for <code>/dev/vdb</code> in the output.</p>"},{"location":"03-rhcsa/lvm/#2-create-a-vg","title":"2 \u2014 Create a VG","text":"<pre><code>sudo vgcreate datavg /dev/vdb\n</code></pre> <p>Verify</p> <p><pre><code>sudo vgs\n</code></pre> Look for: <code>datavg</code> with free space shown.</p>"},{"location":"03-rhcsa/lvm/#3-create-an-lv","title":"3 \u2014 Create an LV","text":"<pre><code># Create a 5 GB logical volume named 'datalv'\nsudo lvcreate -L 5G -n datalv datavg\n\n# Or use 100% of free space\nsudo lvcreate -l 100%FREE -n datalv datavg\n</code></pre> <p>Verify</p> <p><pre><code>sudo lvs\n</code></pre> Look for: <code>datalv</code> in <code>datavg</code> with size <code>5.00g</code>.</p>"},{"location":"03-rhcsa/lvm/#4-format-and-mount","title":"4 \u2014 Format and mount","text":"<pre><code>sudo mkfs.xfs /dev/datavg/datalv\nsudo mkdir -p /mnt/data\nsudo mount /dev/datavg/datalv /mnt/data\n</code></pre>"},{"location":"03-rhcsa/lvm/#5-add-to-fstab","title":"5 \u2014 Add to fstab","text":"<pre><code>sudo blkid /dev/datavg/datalv\n</code></pre> <p>Add to <code>/etc/fstab</code>:</p> <pre><code>/dev/datavg/datalv  /mnt/data  xfs  defaults  0 0\n</code></pre> <p>(LVM device paths are stable \u2014 using the <code>/dev/VG/LV</code> path is fine for LVM.)</p>"},{"location":"03-rhcsa/lvm/#extend-an-lv-online-no-downtime","title":"Extend an LV (online, no downtime)","text":"<pre><code># Extend LV by 2 GB\nsudo lvextend -L +2G /dev/datavg/datalv\n\n# Grow XFS filesystem to fill (online)\nsudo xfs_growfs /mnt/data\n</code></pre> <p>Or extend and grow in one command:</p> <pre><code>sudo lvextend -L +2G -r /dev/datavg/datalv\n</code></pre> <p>(<code>-r</code> = resize filesystem automatically after extending)</p>"},{"location":"03-rhcsa/lvm/#reduce-an-lv-ext4-only-not-xfs","title":"Reduce an LV (ext4 only \u2014 not XFS)","text":"<p>XFS cannot be shrunk</p> <p>Reduction only works with ext4. For XFS, create a new LV at the right size.</p> <pre><code># ext4 only \u2014 unmount first\nsudo umount /mnt/data\nsudo e2fsck -f /dev/datavg/datalv\nsudo resize2fs /dev/datavg/datalv 4G\nsudo lvreduce -L 4G /dev/datavg/datalv\nsudo mount /dev/datavg/datalv /mnt/data\n</code></pre>"},{"location":"03-rhcsa/lvm/#add-a-new-pv-to-an-existing-vg","title":"Add a new PV to an existing VG","text":"<p>When a VG runs low on space:</p> <pre><code>sudo pvcreate /dev/vdc\nsudo vgextend datavg /dev/vdc\nsudo vgs   # confirm new free space\n</code></pre>"},{"location":"03-rhcsa/lvm/#lvm-snapshot-basics","title":"LVM snapshot (basics)","text":"<pre><code># Create a read-only snapshot of datalv (2G COW space)\nsudo lvcreate -L 2G -s -n datalv-snap /dev/datavg/datalv\n\n# Mount the snapshot read-only\nsudo mount -o ro /dev/datavg/datalv-snap /mnt/snap\n\n# Remove when done\nsudo umount /mnt/snap\nsudo lvremove /dev/datavg/datalv-snap\n</code></pre>"},{"location":"03-rhcsa/lvm/#lvm-status-commands","title":"LVM status commands","text":"<pre><code>sudo pvs          # physical volumes (brief)\nsudo pvdisplay    # physical volumes (detailed)\n\nsudo vgs          # volume groups (brief)\nsudo vgdisplay    # volume groups (detailed)\n\nsudo lvs          # logical volumes (brief)\nsudo lvdisplay    # logical volumes (detailed)\n</code></pre>"},{"location":"03-rhcsa/lvm/#next-step","title":"Next step","text":"<p>\u2192 systemd Essentials</p>"},{"location":"03-rhcsa/networking-basics/","title":"Networking Basics \u2014 ip, ss","text":"<p>Before configuring networking, you need to understand the current state of the network stack. These read-only commands are your first tools.</p>"},{"location":"03-rhcsa/networking-basics/#the-ip-command","title":"The <code>ip</code> command","text":"<p><code>ip</code> is the modern replacement for <code>ifconfig</code>, <code>route</code>, and <code>arp</code>.</p>"},{"location":"03-rhcsa/networking-basics/#view-interfaces","title":"View interfaces","text":"<pre><code># List all interfaces with state\nip link show\n\n# Short form\nip l\n\n# Show one interface\nip link show ens3\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#view-addresses","title":"View addresses","text":"<pre><code># Show all IP addresses\nip addr show\n\n# Short form\nip a\n\n# Show one interface\nip addr show ens3\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#view-routes","title":"View routes","text":"<pre><code># Show routing table\nip route show\n\n# Short form\nip r\n\n# Show the route used for a specific destination\nip route get 8.8.8.8\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#view-arpneighbour-cache","title":"View ARP/neighbour cache","text":"<pre><code>ip neigh show\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#network-interface-states","title":"Network interface states","text":"State Meaning <code>UP</code> Administratively enabled <code>LOWER_UP</code> Physical link is up (cable connected or WiFi associated) <code>DOWN</code> Interface disabled <code>NO-CARRIER</code> Interface up but no physical link"},{"location":"03-rhcsa/networking-basics/#the-ss-command","title":"The <code>ss</code> command","text":"<p><code>ss</code> shows socket statistics \u2014 what is listening, what is connected. It replaces <code>netstat</code>.</p> <pre><code># All listening TCP ports\nss -tlnp\n\n# All listening UDP ports\nss -ulnp\n\n# All listening (TCP + UDP)\nss -tlnup\n\n# All established connections\nss -tnp\n\n# Show process name and PID\nss -tlnp\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#output-columns-ss-tlnp","title":"Output columns (ss -tlnp)","text":"Column Meaning State LISTEN, ESTABLISHED, etc. Recv-Q Receive queue size Send-Q Send queue size Local Address:Port Listening address Peer Address:Port Remote address (for established) Process PID and process name"},{"location":"03-rhcsa/networking-basics/#common-patterns","title":"Common patterns","text":"<pre><code># Is SSH listening?\nss -tlnp | grep :22\n\n# What process is using port 80?\nss -tlnp | grep :80\n\n# What ports is nginx listening on?\nss -tlnp | grep nginx\n\n# How many established connections to port 443?\nss -tn state established | grep :443 | wc -l\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#checking-connectivity","title":"Checking connectivity","text":"<pre><code># ICMP reachability\nping -c 4 8.8.8.8\nping -c 4 access.redhat.com\n\n# Traceroute\ntraceroute 8.8.8.8\ntracepath 8.8.8.8   # no root required\n\n# Test TCP port reachability\ncurl -v telnet://192.168.1.1:22\n# or\nnc -zv 192.168.1.1 22\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#hostname","title":"Hostname","text":"<pre><code># Show all hostname info\nhostnamectl\n\n# Set hostname permanently\nsudo hostnamectl set-hostname rhel10-lab\n\n# Show only the static hostname\nhostname\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#network-namespace-quick-view","title":"Network namespace quick view","text":"<pre><code># All network namespaces (relevant when using containers)\nip netns list\n</code></pre>"},{"location":"03-rhcsa/networking-basics/#next-step","title":"Next step","text":"<p>\u2192 NetworkManager (nmcli)</p>"},{"location":"03-rhcsa/networkmanager-nmcli/","title":"NetworkManager and nmcli","text":"<p>NetworkManager is the default network management daemon on RHEL 10. <code>nmcli</code> is its command-line interface \u2014 the tool you will use for all network configuration.</p>"},{"location":"03-rhcsa/networkmanager-nmcli/#key-concepts","title":"Key concepts","text":"Term Meaning device A physical or virtual network interface (e.g., <code>ens3</code>) connection A configuration profile tied to a device active connection A connection currently applied to a device <p>A device can have multiple connections defined (e.g., home, work, static), but only one active at a time.</p>"},{"location":"03-rhcsa/networkmanager-nmcli/#view-current-state","title":"View current state","text":"<pre><code># Short overview of devices and their status\nnmcli\n\n# Device status\nnmcli device status\n\n# Connection profiles (all defined)\nnmcli connection show\n\n# Active connections only\nnmcli connection show --active\n\n# Details of a specific connection\nnmcli connection show \"Wired connection 1\"\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#configure-a-static-ip-address","title":"Configure a static IP address","text":"<pre><code># Step 1 \u2014 identify your connection name\nnmcli connection show\n\n# Step 2 \u2014 modify the connection\nsudo nmcli connection modify \"Wired connection 1\" \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.1.100/24 \\\n  ipv4.gateway 192.168.1.1 \\\n  ipv4.dns \"192.168.1.1 8.8.8.8\"\n\n# Step 3 \u2014 apply changes\nsudo nmcli connection up \"Wired connection 1\"\n</code></pre> <p>Verify</p> <p><pre><code>ip addr show\nip route show\n</code></pre> Look for your new IP and the default gateway route.</p>"},{"location":"03-rhcsa/networkmanager-nmcli/#switch-back-to-dhcp","title":"Switch back to DHCP","text":"<pre><code>sudo nmcli connection modify \"Wired connection 1\" \\\n  ipv4.method auto \\\n  ipv4.addresses \"\" \\\n  ipv4.gateway \"\" \\\n  ipv4.dns \"\"\n\nsudo nmcli connection up \"Wired connection 1\"\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#create-a-new-connection-profile","title":"Create a new connection profile","text":"<pre><code>sudo nmcli connection add \\\n  type ethernet \\\n  con-name \"static-lab\" \\\n  ifname ens3 \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.1.200/24 \\\n  ipv4.gateway 192.168.1.1 \\\n  ipv4.dns \"192.168.1.1\"\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#bring-connections-up-and-down","title":"Bring connections up and down","text":"<pre><code>sudo nmcli connection up \"static-lab\"\nsudo nmcli connection down \"static-lab\"\n\n# Bring up default (auto-connect)\nsudo nmcli device connect ens3\nsudo nmcli device disconnect ens3\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#delete-a-connection-profile","title":"Delete a connection profile","text":"<pre><code>sudo nmcli connection delete \"static-lab\"\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#set-hostname-via-nmcli","title":"Set hostname via nmcli","text":"<pre><code>sudo nmcli general hostname rhel10-lab\nsudo hostnamectl set-hostname rhel10-lab   # equivalent\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#dns-configuration","title":"DNS configuration","text":"<p>DNS servers are set per connection. After modifying DNS:</p> <pre><code>sudo nmcli connection modify \"Wired connection 1\" ipv4.dns \"192.168.1.1 8.8.8.8\"\nsudo nmcli connection up \"Wired connection 1\"\n</code></pre> <p>Check the resolved DNS:</p> <pre><code>resolvectl status\ncat /etc/resolv.conf\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#connection-file-location","title":"Connection file location","text":"<p>NetworkManager stores connection profiles as keyfiles:</p> <pre><code>ls /etc/NetworkManager/system-connections/\n</code></pre> <p>You can edit these directly (then run <code>sudo nmcli connection reload</code>), but nmcli is safer and preferred.</p>"},{"location":"03-rhcsa/networkmanager-nmcli/#useful-nmcli-shortcuts","title":"Useful nmcli shortcuts","text":"<pre><code># Quick status table\nnmcli\n\n# Which IP do I have?\nnmcli -f IP4.ADDRESS device show ens3\n\n# All details about device\nnmcli device show ens3\n</code></pre>"},{"location":"03-rhcsa/networkmanager-nmcli/#next-step","title":"Next step","text":"<p>\u2192 DNS and Name Resolution</p>"},{"location":"03-rhcsa/packages-dnf/","title":"Packages and Repos \u2014 dnf","text":"<p>RHEL uses dnf (Dandified YUM) as its package manager. It handles installing, removing, updating, and searching RPM packages and manages the repositories that provide them.</p>"},{"location":"03-rhcsa/packages-dnf/#concepts","title":"Concepts","text":"Term Meaning RPM The package format (<code>.rpm</code> file with metadata + files) Repository (repo) A server or directory providing packages + metadata dnf The client tool that resolves dependencies and manages RPMs module stream Multiple versions of a package available side-by-side (AppStream)"},{"location":"03-rhcsa/packages-dnf/#searching-for-packages","title":"Searching for packages","text":"<pre><code># Search by name or description\nsudo dnf search nginx\nsudo dnf search \"web server\"\n\n# Show full details about a package\nsudo dnf info nginx\n\n# Find which package provides a file or command\nsudo dnf provides /usr/bin/ss\nsudo dnf provides \"*/nmap\"\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#installing-packages","title":"Installing packages","text":"<pre><code># Install a package\nsudo dnf install -y nginx\n\n# Install multiple packages\nsudo dnf install -y vim git curl\n\n# Install a local .rpm file\nsudo dnf install -y /tmp/package.rpm\n\n# Install without confirmation prompt (-y)\nsudo dnf install -y htop\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#removing-packages","title":"Removing packages","text":"<pre><code># Remove a package (keeps dependencies)\nsudo dnf remove nginx\n\n# Remove a package and unused dependencies\nsudo dnf autoremove nginx\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#updating-packages","title":"Updating packages","text":"<pre><code># Check what updates are available\nsudo dnf check-update\n\n# Update a specific package\nsudo dnf upgrade nginx\n\n# Update everything\nsudo dnf upgrade -y\n\n# Security updates only\nsudo dnf upgrade --security -y\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#package-history-and-rollback","title":"Package history and rollback","text":"<pre><code># View transaction history\nsudo dnf history\n\n# Details of a specific transaction\nsudo dnf history info 5\n\n# Undo the last transaction\nsudo dnf history undo last\n\n# Undo a specific transaction (by ID)\nsudo dnf history undo 5\n</code></pre> <p>Rollback limitations</p> <p><code>dnf history undo</code> restores installed packages but does not restore modified configuration files. Always back up configs before major updates.</p>"},{"location":"03-rhcsa/packages-dnf/#module-streams-appstream","title":"Module streams (AppStream)","text":"<p>RHEL 10 provides multiple versions of some software via AppStream modules:</p> <pre><code># List available modules\nsudo dnf module list\n\n# List streams for a specific module\nsudo dnf module list nodejs\n\n# Enable and install a specific stream\nsudo dnf module enable nodejs:22\nsudo dnf install -y nodejs\n\n# Show currently enabled modules\nsudo dnf module list --enabled\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#repository-management","title":"Repository management","text":"<pre><code># List all enabled repositories\nsudo dnf repolist\n\n# List all repos (enabled + disabled)\nsudo dnf repolist all\n\n# Enable a disabled repo\nsudo dnf config-manager --enable &lt;repo-id&gt;\n\n# Disable a repo\nsudo dnf config-manager --disable &lt;repo-id&gt;\n\n# Add a repo from a URL\nsudo dnf config-manager --add-repo https://example.com/repo.repo\n</code></pre> <p>Repo configuration files live in <code>/etc/yum.repos.d/</code>.</p>"},{"location":"03-rhcsa/packages-dnf/#gpg-key-verification","title":"GPG key verification","text":"<p>RHEL automatically verifies package signatures. Do not disable this.</p> <pre><code># View imported GPG keys\nrpm -q gpg-pubkey --qf '%{NAME}-%{VERSION}-%{RELEASE}\\t%{SUMMARY}\\n'\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#rpm-quick-reference","title":"RPM quick reference","text":"<pre><code># List files installed by a package\nrpm -ql nginx\n\n# Find which package owns a file\nrpm -qf /usr/bin/vim\n\n# List installed packages\nrpm -qa\n\n# Query package info\nrpm -qi bash\n\n# Verify package integrity\nrpm -V nginx\n</code></pre>"},{"location":"03-rhcsa/packages-dnf/#next-step","title":"Next step","text":"<p>\u2192 Storage Overview</p>"},{"location":"03-rhcsa/scheduling/","title":"Scheduling \u2014 systemd Timers and cron","text":"<p>RHEL supports two scheduling mechanisms: systemd timers (modern, preferred) and cron (legacy, still widely used). Know both.</p>"},{"location":"03-rhcsa/scheduling/#systemd-timers-recommended","title":"systemd timers (recommended)","text":"<p>A timer consists of two unit files: - <code>.timer</code> \u2014 defines when to run - <code>.service</code> \u2014 defines what to run</p>"},{"location":"03-rhcsa/scheduling/#create-a-scheduled-job","title":"Create a scheduled job","text":"<p>Step 1 \u2014 write the service unit:</p> <pre><code>sudo vim /etc/systemd/system/cleanup.service\n</code></pre> <pre><code>[Unit]\nDescription=Cleanup old temp files\n\n[Service]\nType=oneshot\nExecStart=/usr/local/bin/cleanup.sh\n</code></pre> <p>Step 2 \u2014 write the timer unit:</p> <pre><code>sudo vim /etc/systemd/system/cleanup.timer\n</code></pre> <pre><code>[Unit]\nDescription=Run cleanup daily at 2am\n\n[Timer]\nOnCalendar=*-*-* 02:00:00\nPersistent=true\n\n[Install]\nWantedBy=timers.target\n</code></pre> <p><code>Persistent=true</code> means: if the system was off at scheduled time, run immediately at next boot.</p> <p>Step 3 \u2014 enable and start the timer:</p> <pre><code>sudo systemctl daemon-reload\nsudo systemctl enable --now cleanup.timer\n</code></pre>"},{"location":"03-rhcsa/scheduling/#timer-schedule-syntax","title":"Timer schedule syntax","text":"<pre><code># Every day at 02:00\nOnCalendar=*-*-* 02:00:00\n\n# Every hour\nOnCalendar=hourly\n\n# Every Monday at 08:30\nOnCalendar=Mon *-*-* 08:30:00\n\n# Every 15 minutes\nOnCalendar=*:0/15\n\n# 5 minutes after boot\nOnBootSec=5min\n\n# Relative to last run\nOnUnitActiveSec=1h\n</code></pre> <p>Test calendar expressions before deploying:</p> <pre><code>systemd-analyze calendar \"*:0/15\"\nsystemd-analyze calendar \"Mon *-*-* 08:30:00\"\n</code></pre>"},{"location":"03-rhcsa/scheduling/#list-and-monitor-timers","title":"List and monitor timers","text":"<pre><code># List all active timers with next trigger time\nsystemctl list-timers\n\n# View timer logs\njournalctl -u cleanup.timer\njournalctl -u cleanup.service\n</code></pre>"},{"location":"03-rhcsa/scheduling/#cron-legacy","title":"cron (legacy)","text":"<p>cron is still present on RHEL and familiar to many. Use it for legacy scripts or when portability matters.</p>"},{"location":"03-rhcsa/scheduling/#crontab-format","title":"crontab format","text":"<pre><code>minute  hour  day  month  weekday  command\n  0       2    *    *        *      /usr/local/bin/cleanup.sh\n</code></pre> Field Values minute 0\u201359 hour 0\u201323 day of month 1\u201331 month 1\u201312 day of week 0\u20137 (0 and 7 = Sunday) <p>Special shortcuts:</p> Shortcut Equivalent <code>@hourly</code> <code>0 * * * *</code> <code>@daily</code> <code>0 0 * * *</code> <code>@weekly</code> <code>0 0 * * 0</code> <code>@reboot</code> On each system startup"},{"location":"03-rhcsa/scheduling/#edit-your-crontab","title":"Edit your crontab","text":"<pre><code>crontab -e     # edit your own crontab\ncrontab -l     # list your current crontab\ncrontab -r     # remove your crontab (careful!)\n</code></pre>"},{"location":"03-rhcsa/scheduling/#system-wide-cron","title":"System-wide cron","text":"<pre><code># Drop scripts directly into these directories:\n/etc/cron.hourly/\n/etc/cron.daily/\n/etc/cron.weekly/\n/etc/cron.monthly/\n\n# Or add to system crontab\nsudo vim /etc/cron.d/myjob\n</code></pre> <p><code>/etc/cron.d/</code> entries include a username field:</p> <pre><code>0 2 * * * root /usr/local/bin/cleanup.sh\n</code></pre>"},{"location":"03-rhcsa/scheduling/#cron-logs","title":"cron logs","text":"<pre><code># cron execution logs\nsudo journalctl -u crond\ngrep CRON /var/log/messages\n</code></pre>"},{"location":"03-rhcsa/scheduling/#systemd-timer-vs-cron-when-to-use-which","title":"systemd timer vs cron: when to use which","text":"Scenario Recommendation New jobs on RHEL systemd timer Existing scripts you don't want to migrate cron Need dependency on network/other services systemd timer (After=) Centralized log review systemd timer (journalctl) Catch up missed runs systemd timer (Persistent=true)"},{"location":"03-rhcsa/scheduling/#next-step","title":"Next step","text":"<p>\u2192 Networking Basics</p>"},{"location":"03-rhcsa/selinux-avc-basics/","title":"SELinux Troubleshooting \u2014 AVCs","text":"<p>An AVC (Access Vector Cache) denial is what SELinux logs when it blocks an action. Learning to read and correctly respond to AVCs is the key skill.</p>"},{"location":"03-rhcsa/selinux-avc-basics/#where-avc-denials-are-logged","title":"Where AVC denials are logged","text":"<pre><code># Primary audit log\nsudo tail -f /var/log/audit/audit.log | grep AVC\n\n# Journal (syslog copy)\nsudo journalctl -f | grep avc\n</code></pre>"},{"location":"03-rhcsa/selinux-avc-basics/#reading-an-avc-denial","title":"Reading an AVC denial","text":"<p>Example AVC from <code>/var/log/audit/audit.log</code>:</p> <pre><code>type=AVC msg=audit(1708692000.123:456): avc:  denied  { read } for\npid=1234 comm=\"httpd\" name=\"index.html\"\nscontext=system_u:system_r:httpd_t:s0\ntcontext=user_u:object_r:user_home_t:s0\ntclass=file permissive=0\n</code></pre> Field Meaning <code>denied { read }</code> httpd tried to read a file and was blocked <code>comm=\"httpd\"</code> The process was httpd <code>name=\"index.html\"</code> The target file <code>scontext=...httpd_t...</code> Source context (httpd process) <code>tcontext=...user_home_t...</code> Target context (file has wrong type) <code>tclass=file</code> The object class being accessed <p>The fix here: the file has <code>user_home_t</code> context but httpd needs <code>httpd_sys_content_t</code>. Fix = <code>restorecon</code>, not <code>chmod 777</code>.</p>"},{"location":"03-rhcsa/selinux-avc-basics/#audit2why-plain-language-explanation","title":"<code>audit2why</code> \u2014 plain-language explanation","text":"<pre><code>sudo audit2why &lt; /var/log/audit/audit.log\n</code></pre> <p>This reads the audit log and explains each denial in plain language, including a suggested fix.</p> <p>audit2why is a diagnostic tool, not a fix generator</p> <p>audit2why may suggest generating a custom policy module with <code>audit2allow</code>. Only do this as a last resort after ruling out a label fix, boolean, or port fix. Custom modules can silently permit unintended access.</p>"},{"location":"03-rhcsa/selinux-avc-basics/#sealert-richer-analysis","title":"<code>sealert</code> \u2014 richer analysis","text":"<pre><code># Analyse the audit log\nsudo sealert -a /var/log/audit/audit.log\n\n# If setroubleshootd is running, check its log\nsudo journalctl -u setroubleshootd\n</code></pre> <p><code>sealert</code> provides: - A human-readable description of the denial - Probability-ranked suggested fixes - The correct <code>semanage</code> or <code>setsebool</code> command to run</p>"},{"location":"03-rhcsa/selinux-avc-basics/#the-fix-taxonomy-choose-the-right-fix","title":"The fix taxonomy (choose the right fix)","text":"<p>When you see an AVC, work through this order:</p> <pre><code>1. Wrong file label?  \u2192 restorecon -Rv &lt;path&gt;\n2. Non-default path?  \u2192 semanage fcontext + restorecon\n3. Boolean covers it? \u2192 setsebool -P &lt;boolean&gt; on\n4. Non-default port?  \u2192 semanage port -a -t &lt;type&gt; -p tcp &lt;port&gt;\n5. None of the above? \u2192 audit2allow (last resort, with caution)\n</code></pre> <p>See SELinux Deep Dive for full coverage of each fix type.</p>"},{"location":"03-rhcsa/selinux-avc-basics/#common-avc-scenarios-and-fixes","title":"Common AVC scenarios and fixes","text":""},{"location":"03-rhcsa/selinux-avc-basics/#web-content-in-wrong-location","title":"Web content in wrong location","text":"<p>Symptom: httpd returns 403, audit log shows <code>denied { read }</code> for your content file.</p> <pre><code>ls -Z /var/www/html/        # should show httpd_sys_content_t\nls -Z /home/rhel/myapp/     # shows user_home_t \u2014 wrong location for web content\n</code></pre> <p>Fix \u2014 option A: move content to <code>/var/www/html/</code> (preferred):</p> <pre><code>sudo cp -r /home/rhel/myapp/* /var/www/html/\nsudo restorecon -Rv /var/www/html/\n</code></pre> <p>Fix \u2014 option B: if the path must stay non-standard, use <code>semanage</code>:</p> <pre><code>sudo semanage fcontext -a -t httpd_sys_content_t \"/srv/myapp(/.*)?\"\nsudo restorecon -Rv /srv/myapp/\n</code></pre>"},{"location":"03-rhcsa/selinux-avc-basics/#service-on-non-default-port","title":"Service on non-default port","text":"<p>Symptom: httpd fails to start on port 8080, AVC shows <code>name_bind denied</code>.</p> <pre><code>sudo semanage port -l | grep http\n# Add the port\nsudo semanage port -a -t http_port_t -p tcp 8080\n# Restart service\nsudo systemctl restart httpd\n</code></pre>"},{"location":"03-rhcsa/selinux-avc-basics/#confirm-selinux-mode-before-assuming-its-a-permission-problem","title":"Confirm SELinux mode before assuming it's a permission problem","text":"<p>Many admins blame SELinux when the actual problem is a missing firewall rule or a standard Unix permission. Always check in this order:</p> <ol> <li><code>systemctl status &lt;service&gt;</code> \u2014 is it even running?</li> <li>Standard file permissions (<code>ls -l</code>)</li> <li>SELinux context (<code>ls -Z</code>)</li> <li>AVC in audit log (<code>sudo ausearch -m avc -ts recent</code>)</li> <li>Firewall (<code>sudo firewall-cmd --list-all</code>)</li> </ol>"},{"location":"03-rhcsa/selinux-avc-basics/#quick-avc-search-with-ausearch","title":"Quick AVC search with <code>ausearch</code>","text":"<pre><code># Recent AVC denials\nsudo ausearch -m avc -ts recent\n\n# Today's AVCs\nsudo ausearch -m avc -ts today\n\n# AVCs for a specific process\nsudo ausearch -m avc -c httpd\n</code></pre>"},{"location":"03-rhcsa/selinux-avc-basics/#next-step","title":"Next step","text":"<p>\u2192 Lab: Fix a SELinux Label Issue</p>"},{"location":"03-rhcsa/selinux-fundamentals/","title":"SELinux Fundamentals","text":"<p>SELinux (Security-Enhanced Linux) is a Mandatory Access Control (MAC) system built into the Linux kernel. It enforces security policies that go far beyond standard Unix permissions.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#mac-vs-dac","title":"MAC vs DAC","text":"Model Name Controls access via DAC Discretionary Access Control File owner sets permissions (<code>chmod</code>) MAC Mandatory Access Control Policy defines what processes can access <p>With SELinux, even if a file has <code>777</code> permissions, a process still needs the correct SELinux type to access it. This is why \"fix it with chmod 777\" is wrong \u2014 SELinux may still block the access.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#selinux-modes","title":"SELinux modes","text":"Mode Behaviour Enforcing Policy is enforced; violations are blocked and logged Permissive Policy is NOT enforced; violations are logged only Disabled SELinux is completely off \u2014 requires reboot to re-enable <pre><code># Check current mode\ngetenforce\n\n# Detailed status\nsestatus\n</code></pre>"},{"location":"03-rhcsa/selinux-fundamentals/#temporarily-switch-modes-no-reboot-not-persistent","title":"Temporarily switch modes (no reboot, not persistent)","text":"<pre><code>sudo setenforce 0    # Permissive (troubleshooting ONLY)\nsudo setenforce 1    # Enforcing (put it back immediately)\n</code></pre> <p>Do NOT set Permissive as a fix</p> <p>Permissive mode is for temporary troubleshooting only. Running in permissive mode defeats the entire point of SELinux. Always fix the root cause and return to enforcing.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#permanent-mode-requires-reboot","title":"Permanent mode (requires reboot)","text":"<pre><code>sudo vim /etc/selinux/config\n</code></pre> <pre><code>SELINUX=enforcing    # recommended\nSELINUX=permissive   # temp troubleshooting only\nSELINUX=disabled     # never in production\n</code></pre> <p>Never set SELINUX=disabled in production</p> <p>Re-enabling after disabling requires a full filesystem relabel at next boot, which takes time and can cause issues if interrupted.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#selinux-contexts","title":"SELinux contexts","text":"<p>Every file, process, and socket has a security context (label):</p> <pre><code>user:role:type:level\n</code></pre> <p>The type is the most important component \u2014 it determines what can access what.</p> <pre><code># Show file context\nls -Z /etc/hosts\nls -Z /var/www/html/\n\n# Show process context\nps -eZ | grep httpd\nps -eZ | grep sshd\n\n# Show your own context\nid -Z\n</code></pre>"},{"location":"03-rhcsa/selinux-fundamentals/#file-contexts-restorecon","title":"File contexts \u2014 <code>restorecon</code>","text":"<p>Files must have the correct context for the service that accesses them. The most common SELinux issue is a file with the wrong context.</p> <pre><code># Restore the correct context (from policy definition)\nsudo restorecon -v /var/www/html/index.html\n\n# Recursive restore\nsudo restorecon -Rv /var/www/html/\n\n# Check what context a path should have (without changing it)\nsudo matchpathcon /var/www/html/index.html\n</code></pre> <p>If you move files with <code>mv</code>, they keep the context from the source. If you copy with <code>cp</code>, they get the context of the destination.</p> <p>Use cp not mv for web content</p> <p>When placing content into <code>/var/www/html/</code>, use <code>cp</code> or <code>restorecon</code> after moving to ensure the correct <code>httpd_sys_content_t</code> context is applied.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#selinux-booleans","title":"SELinux booleans","text":"<p>Booleans are policy switches that enable/disable specific behaviours without writing custom policy.</p> <pre><code># List all booleans\nsudo getsebool -a\n\n# Search booleans related to httpd\nsudo getsebool -a | grep httpd\n\n# Get status of a specific boolean\ngetsebool httpd_can_network_connect\n\n# Turn a boolean ON (runtime only)\nsudo setsebool httpd_can_network_connect on\n\n# Turn a boolean ON persistently (survives reboot)\nsudo setsebool -P httpd_can_network_connect on\n</code></pre> <p>Common booleans for new admins:</p> Boolean What it enables <code>httpd_can_network_connect</code> Apache to make outbound network connections <code>httpd_use_nfs</code> Apache to serve NFS-mounted content <code>samba_enable_home_dirs</code> Samba to share home directories <code>ftpd_anon_write</code> FTP anonymous upload"},{"location":"03-rhcsa/selinux-fundamentals/#install-selinux-tools","title":"Install SELinux tools","text":"<pre><code>sudo dnf install -y policycoreutils-python-utils setroubleshoot-server\n</code></pre> <p>These provide <code>semanage</code>, <code>sealert</code>, and improved audit analysis.</p>"},{"location":"03-rhcsa/selinux-fundamentals/#next-step","title":"Next step","text":"<p>\u2192 SELinux Troubleshooting (AVCs)</p>"},{"location":"03-rhcsa/ssh/","title":"SSH \u2014 Keys, Server Basics","text":"<p>SSH is how you administer RHEL remotely. This chapter covers key-based authentication (the right way) and basic server hardening.</p>"},{"location":"03-rhcsa/ssh/#connect-to-a-remote-host","title":"Connect to a remote host","text":"<pre><code># Connect as the same username\nssh 192.168.1.100\n\n# Connect as a specific user\nssh rhel@192.168.1.100\n\n# Connect on a non-default port\nssh -p 2222 rhel@192.168.1.100\n\n# Connect with a specific key\nssh -i ~/.ssh/id_lab rhel@192.168.1.100\n</code></pre>"},{"location":"03-rhcsa/ssh/#key-based-authentication","title":"Key-based authentication","text":""},{"location":"03-rhcsa/ssh/#generate-a-key-pair","title":"Generate a key pair","text":"<pre><code>ssh-keygen -t ed25519 -C \"rhel-lab-key\"\n</code></pre> <ul> <li>Private key: <code>~/.ssh/id_ed25519</code> \u2014 keep this secret, never share it</li> <li>Public key: <code>~/.ssh/id_ed25519.pub</code> \u2014 share this freely</li> </ul> <p>Key type choices</p> <p><code>ed25519</code> is recommended: modern, fast, small. Avoid <code>rsa</code> with keys shorter than 3072 bits. Never use <code>dsa</code> or <code>ecdsa</code> unless required.</p>"},{"location":"03-rhcsa/ssh/#copy-public-key-to-a-remote-host","title":"Copy public key to a remote host","text":"<pre><code>ssh-copy-id -i ~/.ssh/id_ed25519.pub rhel@192.168.1.100\n</code></pre> <p>Or manually:</p> <pre><code># On the remote host\nmkdir -p ~/.ssh\nchmod 700 ~/.ssh\necho \"&lt;contents of your .pub file&gt;\" &gt;&gt; ~/.ssh/authorized_keys\nchmod 600 ~/.ssh/authorized_keys\n</code></pre>"},{"location":"03-rhcsa/ssh/#test-key-login","title":"Test key login","text":"<pre><code>ssh -i ~/.ssh/id_ed25519 rhel@192.168.1.100\n</code></pre>"},{"location":"03-rhcsa/ssh/#ssh-config-file-sshconfig","title":"SSH config file \u2014 <code>~/.ssh/config</code>","text":"<p>Saves you from typing options every time:</p> <pre><code>Host lab\n    HostName 192.168.1.100\n    User rhel\n    IdentityFile ~/.ssh/id_ed25519\n    Port 22\n\nHost bastion\n    HostName 10.0.0.1\n    User admin\n    IdentityFile ~/.ssh/id_bastion\n</code></pre> <p>Now connect with:</p> <pre><code>ssh lab\n</code></pre>"},{"location":"03-rhcsa/ssh/#ssh-agent-avoid-repeated-passphrase-entry","title":"SSH agent (avoid repeated passphrase entry)","text":"<pre><code>eval \"$(ssh-agent -s)\"\nssh-add ~/.ssh/id_ed25519\n</code></pre>"},{"location":"03-rhcsa/ssh/#sshd-configuration","title":"sshd configuration","text":"<p>The SSH server is configured in <code>/etc/ssh/sshd_config</code>.</p> <p>Always use sshd -t before restarting</p> <p><pre><code>sudo sshd -t    # test config syntax\nsudo systemctl restart sshd\n</code></pre> Restarting sshd with a bad config can lock you out. Always test first.</p>"},{"location":"03-rhcsa/ssh/#recommended-hardening-settings","title":"Recommended hardening settings","text":"<pre><code>sudo vim /etc/ssh/sshd_config\n</code></pre> <p>Key settings to review and set:</p> <pre><code># Disable root login (use sudo instead)\nPermitRootLogin no\n\n# Disable password auth (force key-only)\nPasswordAuthentication no\nPubkeyAuthentication yes\n\n# Disable empty passwords\nPermitEmptyPasswords no\n\n# Limit which users can log in\nAllowUsers rhel admin\n\n# Set a login grace time\nLoginGraceTime 30\n\n# Log level for auditing\nLogLevel VERBOSE\n</code></pre> <pre><code>sudo sshd -t &amp;&amp; sudo systemctl restart sshd\n</code></pre> <p>Disable PasswordAuthentication only after keys work</p> <p>Confirm key login works from another terminal before disabling password auth. Disabling it without working keys will lock you out.</p>"},{"location":"03-rhcsa/ssh/#firewall-allow-ssh","title":"Firewall: allow SSH","text":"<p>SSH is allowed by default in the <code>public</code> zone. If you removed it:</p> <pre><code>sudo firewall-cmd --permanent --add-service=ssh\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"03-rhcsa/ssh/#ssh-tunneling-basics","title":"SSH tunneling (basics)","text":"<pre><code># Local port forwarding: forward local 8080 to remote 80\nssh -L 8080:localhost:80 rhel@192.168.1.100\n\n# SOCKS proxy\nssh -D 1080 rhel@192.168.1.100\n\n# Jump host (ProxyJump)\nssh -J admin@bastion.example.com rhel@10.0.0.50\n</code></pre>"},{"location":"03-rhcsa/ssh/#next-step","title":"Next step","text":"<p>\u2192 SELinux Fundamentals</p>"},{"location":"03-rhcsa/storage-overview/","title":"Storage Overview \u2014 lsblk, blkid, mounts","text":"<p>Before managing storage, you need to understand what devices exist, how they are partitioned, and what is currently mounted.</p>"},{"location":"03-rhcsa/storage-overview/#block-device-hierarchy","title":"Block device hierarchy","text":"<pre><code>Physical disk (e.g., /dev/vda)\n  \u2514\u2500\u2500 Partition (e.g., /dev/vda1, /dev/vda2)\n        \u2514\u2500\u2500 Filesystem (e.g., XFS, ext4)\n              \u2514\u2500\u2500 Mount point (e.g., /, /boot, /home)\n</code></pre> <p>On RHEL VMs you typically see: - <code>/dev/vda</code> \u2014 KVM/QEMU virtual disk - <code>/dev/sda</code> \u2014 SCSI/SATA disk (bare metal or VMware) - <code>/dev/nvme0n1</code> \u2014 NVMe disk (modern bare metal)</p>"},{"location":"03-rhcsa/storage-overview/#list-block-devices-lsblk","title":"List block devices \u2014 <code>lsblk</code>","text":"<pre><code>lsblk\n</code></pre> <p>Example output:</p> <pre><code>NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS\nvda    252:0    0   20G  0 disk\n\u251c\u2500vda1 252:1    0    1G  0 part /boot\n\u251c\u2500vda2 252:2    0    2G  0 part [SWAP]\n\u2514\u2500vda3 252:3    0   17G  0 part /\n</code></pre> <pre><code># Show filesystem type and UUID\nlsblk -f\n\n# Show size in bytes\nlsblk -b\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#identify-filesystems-blkid","title":"Identify filesystems \u2014 <code>blkid</code>","text":"<pre><code>sudo blkid\n</code></pre> <p>Output shows UUID, type, and label for each partition. UUIDs are what <code>/etc/fstab</code> uses to identify devices reliably (device names like <code>/dev/vda1</code> can change).</p> <pre><code># Show info for a specific device\nsudo blkid /dev/vda1\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#view-current-mounts","title":"View current mounts","text":"<pre><code># All currently mounted filesystems\nmount | column -t\n\n# Only real filesystems (not virtual)\nmount | grep -v \" (tmpfs\\|sysfs\\|proc\\|devtmpfs\\|cgroup)\"\n\n# Disk usage per mount\ndf -h\n\n# Inode usage (if disk is \"full\" but df looks OK)\ndf -ih\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#partition-tools","title":"Partition tools","text":""},{"location":"03-rhcsa/storage-overview/#fdisk-mbr-and-gpt-interactive","title":"<code>fdisk</code> (MBR and GPT, interactive)","text":"<pre><code>sudo fdisk /dev/vdb      # interactive mode \u2014 type m for help\nsudo fdisk -l            # list all partition tables\nsudo fdisk -l /dev/vda   # list one disk\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#gdisk-gpt-only","title":"<code>gdisk</code> (GPT only)","text":"<pre><code>sudo gdisk /dev/vdb\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#parted-scriptable-gpt-aware","title":"<code>parted</code> (scriptable, GPT-aware)","text":"<pre><code>sudo parted /dev/vdb print\nsudo parted /dev/vdb mklabel gpt\nsudo parted /dev/vdb mkpart primary xfs 1MiB 100%\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#creating-a-filesystem","title":"Creating a filesystem","text":"<pre><code># XFS (default on RHEL)\nsudo mkfs.xfs /dev/vdb1\n\n# XFS with a label\nsudo mkfs.xfs -L mydata /dev/vdb1\n\n# ext4 (legacy, occasionally needed)\nsudo mkfs.ext4 /dev/vdb1\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#mounting-a-filesystem","title":"Mounting a filesystem","text":"<pre><code># Create mount point\nsudo mkdir -p /mnt/data\n\n# Mount temporarily\nsudo mount /dev/vdb1 /mnt/data\n\n# Mount by UUID\nsudo mount UUID=\"&lt;uuid-from-blkid&gt;\" /mnt/data\n\n# Unmount\nsudo umount /mnt/data\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#persistent-mount-via-etcfstab","title":"Persistent mount via <code>/etc/fstab</code>","text":"<pre><code>sudo blkid /dev/vdb1   # get UUID\nsudo vim /etc/fstab\n</code></pre> <p>Add a line:</p> <pre><code>UUID=&lt;your-uuid&gt;  /mnt/data  xfs  defaults  0 0\n</code></pre> <p>Test without rebooting:</p> <pre><code>sudo mount -a\n</code></pre> <p>Always test fstab before rebooting</p> <p>A bad fstab entry can prevent the system from booting. Always run <code>sudo mount -a</code> after editing fstab to catch errors immediately.</p>"},{"location":"03-rhcsa/storage-overview/#swap","title":"Swap","text":"<pre><code># Check current swap\nswapon --show\n\n# Create a swap partition\nsudo mkswap /dev/vdb2\nsudo swapon /dev/vdb2\n\n# Add to fstab for persistence\nUUID=&lt;swap-uuid&gt;  swap  swap  defaults  0 0\n</code></pre>"},{"location":"03-rhcsa/storage-overview/#next-step","title":"Next step","text":"<p>\u2192 Filesystems and fstab</p>"},{"location":"03-rhcsa/systemd-basics/","title":"systemd Essentials","text":"<p>systemd is the init system and service manager for RHEL 10. Understanding it is non-negotiable for RHEL administration.</p>"},{"location":"03-rhcsa/systemd-basics/#key-concepts","title":"Key concepts","text":"Term Meaning unit A resource systemd manages (service, mount, socket, timer, etc.) service unit A daemon or process managed by systemd (<code>.service</code>) target A group of units representing a system state (e.g., <code>multi-user.target</code>) socket unit Socket activation \u2014 start service on first connection timer unit Scheduled activation (replaces cron for new services) drop-in Override file that extends a unit without modifying the original"},{"location":"03-rhcsa/systemd-basics/#the-systemctl-command","title":"The <code>systemctl</code> command","text":""},{"location":"03-rhcsa/systemd-basics/#start-stop-restart-reload","title":"Start, stop, restart, reload","text":"<pre><code>sudo systemctl start sshd\nsudo systemctl stop sshd\nsudo systemctl restart sshd      # stop + start\nsudo systemctl reload sshd       # reload config without stopping (if supported)\nsudo systemctl reload-or-restart sshd  # try reload, fall back to restart\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#enable-and-disable-start-at-boot","title":"Enable and disable (start at boot)","text":"<pre><code>sudo systemctl enable sshd           # enable at boot\nsudo systemctl disable sshd          # disable at boot\nsudo systemctl enable --now sshd     # enable AND start immediately\nsudo systemctl disable --now sshd    # disable AND stop immediately\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#status-and-introspection","title":"Status and introspection","text":"<pre><code># Rich status view (recent logs included)\nsudo systemctl status sshd\n\n# Is the service running?\nsystemctl is-active sshd\n\n# Is it enabled at boot?\nsystemctl is-enabled sshd\n\n# Did it fail?\nsystemctl is-failed sshd\n\n# List all failed units\nsystemctl --failed\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#listing-units","title":"Listing units","text":"<pre><code># All loaded and active units\nsystemctl list-units\n\n# All service units (loaded)\nsystemctl list-units --type=service\n\n# All units including inactive\nsystemctl list-units --all\n\n# Units that failed\nsystemctl list-units --state=failed\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#system-state-targets","title":"System state (targets)","text":"<pre><code># Current default target (runlevel equivalent)\nsystemctl get-default\n\n# Change default target\nsudo systemctl set-default multi-user.target    # headless (no GUI)\nsudo systemctl set-default graphical.target     # GUI\n\n# Switch to a target immediately (non-persistent)\nsudo systemctl isolate rescue.target\n\n# Common targets\n# poweroff.target  \u2014 shutdown\n# rescue.target    \u2014 single-user repair mode\n# multi-user.target \u2014 full multi-user, no GUI\n# graphical.target  \u2014 multi-user + GUI\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#unit-files","title":"Unit files","text":"<p>Unit files live in:</p> Path Contents <code>/usr/lib/systemd/system/</code> Shipped by packages (do not edit) <code>/etc/systemd/system/</code> Admin-created or admin-modified units <code>/run/systemd/system/</code> Runtime units (ephemeral)"},{"location":"03-rhcsa/systemd-basics/#view-a-unit-file","title":"View a unit file","text":"<pre><code>systemctl cat sshd.service\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#edit-a-unit-use-systemctl-not-vim-directly","title":"Edit a unit (use systemctl \u2014 not vim directly)","text":"<pre><code># Create a drop-in (recommended \u2014 preserves original)\nsudo systemctl edit sshd.service\n\n# Edit the full unit file (replaces shipped file)\nsudo systemctl edit --full sshd.service\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#reload-after-editing","title":"Reload after editing","text":"<pre><code>sudo systemctl daemon-reload\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#creating-a-simple-service-unit","title":"Creating a simple service unit","text":"<pre><code>sudo vim /etc/systemd/system/myscript.service\n</code></pre> <pre><code>[Unit]\nDescription=My Custom Script\nAfter=network.target\n\n[Service]\nType=oneshot\nExecStart=/usr/local/bin/myscript.sh\nRemainAfterExit=yes\n\n[Install]\nWantedBy=multi-user.target\n</code></pre> <pre><code>sudo systemctl daemon-reload\nsudo systemctl enable --now myscript.service\nsudo systemctl status myscript.service\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#power-management","title":"Power management","text":"<pre><code>sudo systemctl poweroff\nsudo systemctl reboot\nsudo systemctl suspend\nsudo systemctl hibernate\n</code></pre>"},{"location":"03-rhcsa/systemd-basics/#next-step","title":"Next step","text":"<p>\u2192 Logs and journalctl</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/","title":"Lab: LVM + XFS Grow","text":"<p>Track: RHCSA Estimated time: 35 minutes Topology: Single VM (requires an extra unpartitioned disk \u2014 add a 10 GB virtual disk to your VM)</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Storage Overview, Filesystems and fstab, and LVM</li> <li>A second virtual disk attached to your VM (e.g., <code>/dev/vdb</code>)</li> <li>VM snapshot taken</li> </ul>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#success-criteria","title":"Success criteria","text":"<ul> <li>An LVM volume group <code>labvg</code> created on <code>/dev/vdb</code></li> <li>A logical volume <code>lablv</code> formatted with XFS, mounted at <code>/mnt/labdata</code></li> <li>Data written, filesystem extended, data verified intact</li> <li>Mount persists across reboot</li> </ul>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#steps","title":"Steps","text":""},{"location":"03-rhcsa/labs/lvm-xfs-grow/#1-confirm-the-second-disk-is-present","title":"1 \u2014 Confirm the second disk is present","text":"<pre><code>lsblk\n</code></pre> <p>Look for: a disk without partitions (e.g., <code>vdb 10G</code>). If you don't see it, add a virtual disk to your VM and reboot.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#2-initialise-the-pv-and-create-vg","title":"2 \u2014 Initialise the PV and create VG","text":"<pre><code>sudo pvcreate /dev/vdb\nsudo vgcreate labvg /dev/vdb\n</code></pre> <p>Verify</p> <p><pre><code>sudo vgs\n</code></pre> Look for: <code>labvg</code> with ~10 GB free.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#3-create-a-4-gb-lv","title":"3 \u2014 Create a 4 GB LV","text":"<pre><code>sudo lvcreate -L 4G -n lablv labvg\n</code></pre> <p>Verify</p> <p><pre><code>sudo lvs\n</code></pre> Look for: <code>lablv</code> in <code>labvg</code> with size <code>4.00g</code>.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#4-format-with-xfs-and-mount","title":"4 \u2014 Format with XFS and mount","text":"<pre><code>sudo mkfs.xfs /dev/labvg/lablv\nsudo mkdir -p /mnt/labdata\nsudo mount /dev/labvg/lablv /mnt/labdata\n</code></pre> <p>Verify</p> <p><pre><code>df -h /mnt/labdata\n</code></pre> Look for: ~4 GB filesystem mounted on <code>/mnt/labdata</code>.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#5-write-test-data","title":"5 \u2014 Write test data","text":"<pre><code>sudo bash -c 'for i in {1..5}; do echo \"file-$i\" &gt; /mnt/labdata/file-$i.txt; done'\nls /mnt/labdata/\n</code></pre>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#6-add-to-fstab-for-persistent-mount","title":"6 \u2014 Add to fstab for persistent mount","text":"<pre><code>echo \"/dev/labvg/lablv  /mnt/labdata  xfs  defaults  0 0\" | sudo tee -a /etc/fstab\n</code></pre> <p>Test:</p> <pre><code>sudo umount /mnt/labdata\nsudo mount -a\n</code></pre> <p>Verify</p> <p><pre><code>df -h /mnt/labdata\nls /mnt/labdata/\n</code></pre> Filesystem remounted and files still present.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#7-extend-the-lv-and-grow-the-filesystem","title":"7 \u2014 Extend the LV and grow the filesystem","text":"<pre><code># Extend LV by 2 GB\nsudo lvextend -L +2G /dev/labvg/lablv\n\n# Grow filesystem online (no unmount needed for XFS)\nsudo xfs_growfs /mnt/labdata\n</code></pre> <p>Verify</p> <p><pre><code>df -h /mnt/labdata\n</code></pre> Look for: filesystem now ~6 GB.</p> <pre><code>ls /mnt/labdata/\n</code></pre> <p>All 5 test files still present.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#8-reboot-and-verify-persistence","title":"8 \u2014 Reboot and verify persistence","text":"<pre><code>sudo reboot\n</code></pre> <p>After login:</p> <pre><code>df -h /mnt/labdata\nls /mnt/labdata/\n</code></pre> <p>Both should show the mounted filesystem and test files.</p>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#cleanup","title":"Cleanup","text":"<pre><code># Remove fstab entry first\nsudo vim /etc/fstab   # delete the /mnt/labdata line\nsudo umount /mnt/labdata\nsudo lvremove /dev/labvg/lablv\nsudo vgremove labvg\nsudo pvremove /dev/vdb\nsudo rmdir /mnt/labdata\n</code></pre>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix <code>pvcreate</code> fails Disk already has partition table <code>sudo wipefs -a /dev/vdb</code> (destructive \u2014 confirm right disk) <code>mount -a</code> fails fstab entry has typo <code>sudo mount -a</code> shows the error; fix fstab Filesystem size unchanged after <code>lvextend</code> Forgot <code>xfs_growfs</code> Run <code>sudo xfs_growfs /mnt/labdata</code> Files gone after reboot fstab entry missing Re-add <code>/dev/labvg/lablv /mnt/labdata xfs defaults 0 0</code>"},{"location":"03-rhcsa/labs/lvm-xfs-grow/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>Running out of disk space on a server is one of the most common (and preventable) issues. LVM lets you extend storage on demand \u2014 adding a new disk to a VG and extending an LV \u2014 without downtime and without losing data. This is a fundamental skill for any RHEL admin.</p>"},{"location":"03-rhcsa/labs/selinux-label-fix/","title":"Lab: Fix a SELinux Label Issue","text":"<p>Track: RHCSA Estimated time: 30 minutes Topology: Single VM</p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed SELinux Fundamentals and SELinux Troubleshooting</li> <li>httpd installed: <code>sudo dnf install -y httpd</code></li> <li>SELinux tools: <code>sudo dnf install -y policycoreutils-python-utils</code></li> <li>VM snapshot taken</li> </ul>"},{"location":"03-rhcsa/labs/selinux-label-fix/#success-criteria","title":"Success criteria","text":"<ul> <li>httpd serves content from <code>/var/www/html/</code> correctly</li> <li>httpd serves content from a custom path <code>/srv/webroot/</code> after correct SELinux fix</li> <li>The fix uses <code>semanage fcontext</code> + <code>restorecon</code> (not <code>setenforce 0</code>, not <code>chmod 777</code>)</li> <li>AVC denials are visible in audit log before the fix and absent after</li> </ul>"},{"location":"03-rhcsa/labs/selinux-label-fix/#steps","title":"Steps","text":""},{"location":"03-rhcsa/labs/selinux-label-fix/#1-start-httpd-and-confirm-selinux-is-enforcing","title":"1 \u2014 Start httpd and confirm SELinux is enforcing","text":"<pre><code>sudo systemctl enable --now httpd\nsudo firewall-cmd --permanent --add-service=http\nsudo firewall-cmd --reload\ngetenforce\n</code></pre> <p>Expected: <code>Enforcing</code></p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#2-create-content-in-the-standard-location-should-work","title":"2 \u2014 Create content in the standard location (should work)","text":"<pre><code>echo \"&lt;h1&gt;Standard location works&lt;/h1&gt;\" | sudo tee /var/www/html/index.html\ncurl http://localhost/\n</code></pre> <p>Verify</p> <p>Output contains: <code>Standard location works</code></p> <p>Check the context:</p> <pre><code>ls -Z /var/www/html/index.html\n</code></pre> <p>Look for: <code>httpd_sys_content_t</code></p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#3-create-a-custom-web-root-with-wrong-context","title":"3 \u2014 Create a custom web root with wrong context","text":"<pre><code>sudo mkdir -p /srv/webroot\necho \"&lt;h1&gt;Custom location&lt;/h1&gt;\" | sudo tee /srv/webroot/index.html\nls -Z /srv/webroot/index.html\n</code></pre> <p>Note the context \u2014 it will be something like <code>default_t</code> or <code>var_t</code>, not <code>httpd_sys_content_t</code>.</p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#4-configure-httpd-to-serve-from-the-custom-path","title":"4 \u2014 Configure httpd to serve from the custom path","text":"<pre><code>sudo vim /etc/httpd/conf.d/custom.conf\n</code></pre> <pre><code>&lt;VirtualHost *:8080&gt;\n    DocumentRoot /srv/webroot\n    &lt;Directory /srv/webroot&gt;\n        Require all granted\n    &lt;/Directory&gt;\n&lt;/VirtualHost&gt;\n</code></pre> <pre><code>sudo firewall-cmd --permanent --add-port=8080/tcp\nsudo firewall-cmd --reload\nsudo semanage port -a -t http_port_t -p tcp 8080 2&gt;/dev/null || \\\n  sudo semanage port -m -t http_port_t -p tcp 8080\nsudo systemctl restart httpd\n</code></pre>"},{"location":"03-rhcsa/labs/selinux-label-fix/#5-test-observe-the-failure","title":"5 \u2014 Test \u2014 observe the failure","text":"<pre><code>curl http://localhost:8080/\n</code></pre> <p>Expected: <code>403 Forbidden</code> or connection refused.</p> <p>Check the audit log:</p> <pre><code>sudo ausearch -m avc -ts recent\n</code></pre> <p>You should see an AVC denial showing httpd was denied access to <code>/srv/webroot/index.html</code>.</p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#6-apply-the-correct-selinux-fix","title":"6 \u2014 Apply the correct SELinux fix","text":"<pre><code># Define the correct context for the custom path\nsudo semanage fcontext -a -t httpd_sys_content_t \"/srv/webroot(/.*)?\"\n\n# Apply the context to existing files\nsudo restorecon -Rv /srv/webroot/\n</code></pre> <p>Verify context</p> <p><pre><code>ls -Z /srv/webroot/index.html\n</code></pre> Look for: <code>httpd_sys_content_t</code></p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#7-test-again-confirm-fix-works","title":"7 \u2014 Test again \u2014 confirm fix works","text":"<pre><code>curl http://localhost:8080/\n</code></pre> <p>Expected: <code>&lt;h1&gt;Custom location&lt;/h1&gt;</code></p> <p>Check audit log again:</p> <pre><code>sudo ausearch -m avc -ts recent\n</code></pre> <p>No new AVC denials for this request.</p>"},{"location":"03-rhcsa/labs/selinux-label-fix/#cleanup","title":"Cleanup","text":"<pre><code>sudo systemctl disable --now httpd\nsudo firewall-cmd --permanent --remove-service=http\nsudo firewall-cmd --permanent --remove-port=8080/tcp\nsudo firewall-cmd --reload\nsudo semanage port -d -t http_port_t -p tcp 8080 2&gt;/dev/null || true\nsudo semanage fcontext -d \"/srv/webroot(/.*)?\" 2&gt;/dev/null || true\nsudo rm -rf /srv/webroot\nsudo rm -f /etc/httpd/conf.d/custom.conf\nsudo systemctl daemon-reload\n</code></pre>"},{"location":"03-rhcsa/labs/selinux-label-fix/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix httpd won't start Port 8080 not in SELinux policy <code>semanage port -a -t http_port_t -p tcp 8080</code> Still 403 after restorecon semanage fcontext not applied first Delete fcontext entry, re-run semanage then restorecon AVC denials still showing Cached denials in audit log Wait 60 seconds or check timestamps \u2014 only look at denials after your fix <code>semanage: command not found</code> Package missing <code>sudo dnf install -y policycoreutils-python-utils</code>"},{"location":"03-rhcsa/labs/selinux-label-fix/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>SELinux label problems are the single most common SELinux issue admins encounter. Whenever content is placed in a non-default path (custom app dirs, mounted volumes, symlinks), the context must be explicitly defined. The correct workflow is always: <code>semanage fcontext -a</code> \u2192 <code>restorecon -Rv</code> \u2014 never <code>setenforce 0</code>.</p>"},{"location":"03-rhcsa/labs/static-ip-dns/","title":"Lab: Static IP + DNS Validation","text":"<p>Track: RHCSA Estimated time: 25 minutes Topology: Single VM</p>"},{"location":"03-rhcsa/labs/static-ip-dns/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed NetworkManager (nmcli) and DNS and Name Resolution</li> <li>Know your VM's current interface name (<code>ip link show</code>)</li> <li>VM snapshot taken</li> </ul>"},{"location":"03-rhcsa/labs/static-ip-dns/#success-criteria","title":"Success criteria","text":"<ul> <li>VM has a manually configured static IP address</li> <li>Default gateway is set correctly</li> <li>DNS resolves external names</li> <li><code>ping access.redhat.com</code> succeeds</li> <li>Configuration survives a reboot</li> </ul>"},{"location":"03-rhcsa/labs/static-ip-dns/#steps","title":"Steps","text":""},{"location":"03-rhcsa/labs/static-ip-dns/#1-identify-your-interface-and-current-connection","title":"1 \u2014 Identify your interface and current connection","text":"<pre><code>nmcli device status\nnmcli connection show\n</code></pre> <p>Note your interface name (e.g., <code>ens3</code>) and active connection name.</p>"},{"location":"03-rhcsa/labs/static-ip-dns/#2-choose-a-static-ip","title":"2 \u2014 Choose a static IP","text":"<p>Pick an IP in your VM's subnet. If your VM is using <code>192.168.122.x</code> (NAT), use something like <code>192.168.122.200/24</code>. Check for conflicts:</p> <pre><code>ping -c 2 192.168.122.200   # should show no response (IP is free)\n</code></pre>"},{"location":"03-rhcsa/labs/static-ip-dns/#3-configure-static-addressing","title":"3 \u2014 Configure static addressing","text":"<p>Replace <code>&lt;CONNECTION-NAME&gt;</code> with your actual connection name:</p> <pre><code>sudo nmcli connection modify \"&lt;CONNECTION-NAME&gt;\" \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.122.200/24 \\\n  ipv4.gateway 192.168.122.1 \\\n  ipv4.dns \"192.168.122.1 8.8.8.8\"\n</code></pre>"},{"location":"03-rhcsa/labs/static-ip-dns/#4-activate-the-new-config","title":"4 \u2014 Activate the new config","text":"<pre><code>sudo nmcli connection up \"&lt;CONNECTION-NAME&gt;\"\n</code></pre> <p>Verify \u2014 IP address</p> <p><pre><code>ip addr show\n</code></pre> Look for: <code>192.168.122.200/24</code> on your interface.</p> <p>Verify \u2014 Default gateway</p> <p><pre><code>ip route show\n</code></pre> Look for: <code>default via 192.168.122.1</code></p> <p>Verify \u2014 DNS</p> <p><pre><code>resolvectl status | grep \"DNS Server\"\n</code></pre> Look for: <code>192.168.122.1</code> and <code>8.8.8.8</code></p>"},{"location":"03-rhcsa/labs/static-ip-dns/#5-test-connectivity","title":"5 \u2014 Test connectivity","text":"<pre><code>ping -c 3 192.168.122.1         # gateway reachable\nping -c 3 8.8.8.8               # internet reachable (IP)\nping -c 3 access.redhat.com     # DNS + internet working\n</code></pre> <p>All three should succeed.</p>"},{"location":"03-rhcsa/labs/static-ip-dns/#6-test-persistence-across-reboot","title":"6 \u2014 Test persistence across reboot","text":"<pre><code>sudo reboot\n</code></pre> <p>After boot, log back in and repeat the verify steps.</p>"},{"location":"03-rhcsa/labs/static-ip-dns/#cleanup-restore-dhcp","title":"Cleanup (restore DHCP)","text":"<pre><code>sudo nmcli connection modify \"&lt;CONNECTION-NAME&gt;\" \\\n  ipv4.method auto \\\n  ipv4.addresses \"\" \\\n  ipv4.gateway \"\" \\\n  ipv4.dns \"\"\n\nsudo nmcli connection up \"&lt;CONNECTION-NAME&gt;\"\n</code></pre>"},{"location":"03-rhcsa/labs/static-ip-dns/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix No IP after <code>connection up</code> Wrong interface or connection name Re-check with <code>nmcli device status</code> <code>ping 8.8.8.8</code> fails Wrong gateway Verify gateway IP matches your network <code>ping access.redhat.com</code> fails but <code>ping 8.8.8.8</code> works Wrong DNS Check <code>ipv4.dns</code> setting Settings lost after reboot Connection not marked auto-connect <code>nmcli connection modify ... connection.autoconnect yes</code>"},{"location":"03-rhcsa/labs/static-ip-dns/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>Servers rarely use DHCP. Static addressing ensures predictable access, stable DNS PTR records, and no surprise IP changes during DHCP lease expiry. Getting comfortable with <code>nmcli</code> is the foundation for all network configuration work that follows.</p>"},{"location":"03-rhcsa/labs/systemd-service/","title":"Lab: Create a systemd Service","text":"<p>Track: RHCSA Estimated time: 30 minutes Topology: Single VM</p>"},{"location":"03-rhcsa/labs/systemd-service/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed systemd Essentials and Logs and journalctl</li> <li>VM snapshot taken</li> </ul>"},{"location":"03-rhcsa/labs/systemd-service/#success-criteria","title":"Success criteria","text":"<ul> <li>A custom systemd service runs a script on boot</li> <li>The service is enabled and starts automatically</li> <li>Logs are visible in <code>journalctl</code></li> <li>A failure scenario is understood and debugged</li> </ul>"},{"location":"03-rhcsa/labs/systemd-service/#steps","title":"Steps","text":""},{"location":"03-rhcsa/labs/systemd-service/#1-create-the-script","title":"1 \u2014 Create the script","text":"<pre><code>sudo vim /usr/local/bin/healthcheck.sh\n</code></pre> <pre><code>#!/usr/bin/env bash\nset -euo pipefail\n\nLOGFILE=/var/log/healthcheck.log\nTIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')\n\necho \"${TIMESTAMP} - Healthcheck: disk=$(df -h / | awk 'NR==2{print $5}') mem=$(free -m | awk '/Mem/{print $3}')MB used\" &gt;&gt; \"${LOGFILE}\"\necho \"${TIMESTAMP} - Healthcheck complete\"\n</code></pre> <pre><code>sudo chmod +x /usr/local/bin/healthcheck.sh\n</code></pre> <p>Test the script manually:</p> <pre><code>sudo /usr/local/bin/healthcheck.sh\n</code></pre> <p>Verify</p> <p>Look for: a line printed to stdout and written to <code>/var/log/healthcheck.log</code>.</p>"},{"location":"03-rhcsa/labs/systemd-service/#2-create-the-service-unit","title":"2 \u2014 Create the service unit","text":"<pre><code>sudo vim /etc/systemd/system/healthcheck.service\n</code></pre> <pre><code>[Unit]\nDescription=System Healthcheck Script\nAfter=network.target\nDocumentation=man:systemd.service(5)\n\n[Service]\nType=oneshot\nExecStart=/usr/local/bin/healthcheck.sh\nStandardOutput=journal\nStandardError=journal\nRemainAfterExit=yes\nUser=root\n\n[Install]\nWantedBy=multi-user.target\n</code></pre>"},{"location":"03-rhcsa/labs/systemd-service/#3-enable-and-start-the-service","title":"3 \u2014 Enable and start the service","text":"<pre><code>sudo systemctl daemon-reload\nsudo systemctl enable --now healthcheck.service\n</code></pre> <p>Verify \u2014 Service active</p> <p><pre><code>systemctl status healthcheck.service\n</code></pre> Look for: <code>Active: active (exited)</code> (oneshot services show \"exited\" not \"running\")</p> <p>Verify \u2014 Logs in journal</p> <p><pre><code>journalctl -u healthcheck.service\n</code></pre> Look for: the healthcheck output line.</p> <p>Verify \u2014 Enabled at boot</p> <p><pre><code>systemctl is-enabled healthcheck.service\n</code></pre> Expected: <code>enabled</code></p>"},{"location":"03-rhcsa/labs/systemd-service/#4-add-a-systemd-timer-to-run-it-every-5-minutes","title":"4 \u2014 Add a systemd timer to run it every 5 minutes","text":"<pre><code>sudo vim /etc/systemd/system/healthcheck.timer\n</code></pre> <pre><code>[Unit]\nDescription=Run healthcheck every 5 minutes\nRequires=healthcheck.service\n\n[Timer]\nOnBootSec=1min\nOnUnitActiveSec=5min\nPersistent=true\n\n[Install]\nWantedBy=timers.target\n</code></pre> <pre><code>sudo systemctl daemon-reload\nsudo systemctl enable --now healthcheck.timer\n</code></pre> <p>Verify</p> <p><pre><code>systemctl list-timers | grep healthcheck\n</code></pre> Look for: <code>healthcheck.timer</code> with a next trigger time shown.</p>"},{"location":"03-rhcsa/labs/systemd-service/#5-simulate-a-failure-and-debug-it","title":"5 \u2014 Simulate a failure and debug it","text":"<p>Break the script on purpose:</p> <pre><code>sudo vim /usr/local/bin/healthcheck.sh\n# Change the first executable line to:\n# /nonexistent-binary\n</code></pre> <p>Restart the service:</p> <pre><code>sudo systemctl restart healthcheck.service\nsystemctl status healthcheck.service\n</code></pre> <p>Look for: <code>Failed</code> status and error details.</p> <pre><code>journalctl -u healthcheck.service -n 20\n</code></pre> <p>Find the error. Fix the script, then:</p> <pre><code>sudo systemctl restart healthcheck.service\nsystemctl status healthcheck.service\n</code></pre>"},{"location":"03-rhcsa/labs/systemd-service/#cleanup","title":"Cleanup","text":"<pre><code>sudo systemctl disable --now healthcheck.timer healthcheck.service\nsudo rm /etc/systemd/system/healthcheck.{service,timer}\nsudo rm /usr/local/bin/healthcheck.sh\nsudo rm -f /var/log/healthcheck.log\nsudo systemctl daemon-reload\n</code></pre>"},{"location":"03-rhcsa/labs/systemd-service/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix <code>daemon-reload</code> needed warning Unit file changed but not reloaded <code>sudo systemctl daemon-reload</code> Service fails immediately Script has syntax error or wrong path <code>bash -n /usr/local/bin/healthcheck.sh</code> <code>is-enabled: disabled</code> Forgot <code>enable</code> step <code>sudo systemctl enable healthcheck.service</code> Timer doesn't trigger Timer unit not enabled <code>sudo systemctl enable --now healthcheck.timer</code>"},{"location":"03-rhcsa/labs/systemd-service/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>This pattern \u2014 a script, a service unit, and a timer \u2014 is the foundation for automated maintenance tasks, health reporting, log rotation triggers, and application-specific watchdogs. Knowing how to create, debug, and manage these units is a daily skill for RHEL administrators.</p>"},{"location":"04-rhce/ansible-patching/","title":"Patch Workflow + Reporting","text":"<p>A repeatable, auditable patch process is one of the most valuable things you can automate with Ansible.</p>"},{"location":"04-rhce/ansible-patching/#the-patch-workflow","title":"The patch workflow","text":"<pre><code>1. Check \u2192 what updates are available?\n2. Snapshot/backup \u2192 take a VM snapshot before patching\n3. Apply \u2192 run the updates\n4. Verify \u2192 services still running, no failed units\n5. Report \u2192 log what changed, when, on which hosts\n6. Reboot \u2192 if kernel was updated (optional, per policy)\n</code></pre>"},{"location":"04-rhce/ansible-patching/#check-available-updates-ad-hoc","title":"Check available updates (ad-hoc)","text":"<pre><code>ansible all -m command -a \"dnf check-update\" --become\n</code></pre> <p>Or use the <code>dnf</code> module:</p> <pre><code>ansible all -m dnf -a \"list=updates\" --become\n</code></pre>"},{"location":"04-rhce/ansible-patching/#patch-playbook","title":"Patch playbook","text":"<pre><code>---\n# patch.yml\n- name: Patch RHEL hosts\n  hosts: \"{{ target_hosts | default('all') }}\"\n  become: true\n  serial: 1                   # patch one host at a time (safe for production)\n\n  pre_tasks:\n    - name: Record pre-patch time\n      ansible.builtin.set_fact:\n        patch_start: \"{{ ansible_date_time.iso8601 }}\"\n\n    - name: Check for available updates\n      ansible.builtin.command: dnf check-update\n      register: dnf_check\n      changed_when: false\n      failed_when: dnf_check.rc not in [0, 100]\n      # rc 100 = updates available (not an error)\n\n    - name: Log that patching is starting\n      ansible.builtin.lineinfile:\n        path: /var/log/ansible-patching.log\n        line: \"{{ ansible_date_time.iso8601 }} - Patching started on {{ inventory_hostname }}\"\n        create: true\n\n  tasks:\n    - name: Apply all updates\n      ansible.builtin.dnf:\n        name: \"*\"\n        state: latest\n        update_cache: true\n      register: dnf_result\n\n    - name: Log packages updated\n      ansible.builtin.lineinfile:\n        path: /var/log/ansible-patching.log\n        line: \"{{ ansible_date_time.iso8601 }} - Updated on {{ inventory_hostname }}: {{ dnf_result.changes | default('none') }}\"\n      when: dnf_result.changed\n\n  post_tasks:\n    - name: Check for failed systemd units\n      ansible.builtin.command: systemctl --failed --no-legend\n      register: failed_units\n      changed_when: false\n\n    - name: Fail if any units failed\n      ansible.builtin.fail:\n        msg: \"Failed units detected after patching: {{ failed_units.stdout }}\"\n      when: failed_units.stdout | length &gt; 0\n\n    - name: Check if reboot is required\n      ansible.builtin.stat:\n        path: /run/reboot-required\n      register: reboot_required\n\n    - name: Report reboot needed\n      ansible.builtin.debug:\n        msg: \"Reboot required on {{ inventory_hostname }}\"\n      when: reboot_required.stat.exists\n\n    - name: Reboot if required (optional \u2014 enable per environment)\n      ansible.builtin.reboot:\n        reboot_timeout: 300\n        msg: \"Rebooting for kernel update\"\n      when:\n        - reboot_required.stat.exists\n        - allow_reboot | default(false) | bool\n</code></pre>"},{"location":"04-rhce/ansible-patching/#run-the-patch-playbook","title":"Run the patch playbook","text":"<pre><code># Patch all hosts (no auto-reboot)\nansible-playbook -i inventory.ini patch.yml\n\n# Patch only webservers\nansible-playbook -i inventory.ini patch.yml -e \"target_hosts=webservers\"\n\n# Patch with auto-reboot enabled\nansible-playbook -i inventory.ini patch.yml -e \"allow_reboot=true\"\n</code></pre>"},{"location":"04-rhce/ansible-patching/#check-mode-preview-without-applying","title":"Check mode \u2014 preview without applying","text":"<pre><code>ansible-playbook -i inventory.ini patch.yml --check\n</code></pre>"},{"location":"04-rhce/ansible-patching/#reporting-view-the-patch-log","title":"Reporting: view the patch log","text":"<pre><code>ansible all -m command -a \"tail -20 /var/log/ansible-patching.log\" --become\n</code></pre>"},{"location":"04-rhce/ansible-patching/#security-patches-only","title":"Security patches only","text":"<pre><code>- name: Apply security updates only\n  ansible.builtin.dnf:\n    name: \"*\"\n    security: true\n    state: latest\n</code></pre>"},{"location":"04-rhce/ansible-patching/#next-step","title":"Next step","text":"<p>\u2192 Lab: Write Your First Playbook</p>"},{"location":"04-rhce/ansible-playbooks/","title":"Ansible Playbooks \u2014 Tasks and Handlers","text":"<p>A playbook is a YAML file that describes the desired state of one or more managed nodes. It is the core artifact of Ansible automation.</p>"},{"location":"04-rhce/ansible-playbooks/#playbook-structure","title":"Playbook structure","text":"<pre><code>---\n- name: Describe what this play does          # Play name\n  hosts: webservers                           # Target inventory group\n  become: true                                # Use sudo\n\n  tasks:\n    - name: Describe what this task does      # Task name\n      module_name:                            # Module\n        option1: value1\n        option2: value2\n</code></pre> <p>Every task should have a <code>name</code>. It appears in output and helps with debugging.</p>"},{"location":"04-rhce/ansible-playbooks/#a-minimal-working-playbook","title":"A minimal working playbook","text":"<pre><code>---\n- name: Ensure vim is installed on all hosts\n  hosts: all\n  become: true\n\n  tasks:\n    - name: Install vim\n      ansible.builtin.dnf:\n        name: vim\n        state: present\n</code></pre> <p>Run it:</p> <pre><code>ansible-playbook -i inventory.ini install-vim.yml\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#common-task-patterns","title":"Common task patterns","text":""},{"location":"04-rhce/ansible-playbooks/#package-management","title":"Package management","text":"<pre><code>- name: Install multiple packages\n  ansible.builtin.dnf:\n    name:\n      - vim\n      - git\n      - curl\n    state: present\n\n- name: Remove a package\n  ansible.builtin.dnf:\n    name: telnet\n    state: absent\n\n- name: Update all packages\n  ansible.builtin.dnf:\n    name: \"*\"\n    state: latest\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#service-management","title":"Service management","text":"<pre><code>- name: Ensure httpd is running and enabled\n  ansible.builtin.service:\n    name: httpd\n    state: started\n    enabled: true\n\n- name: Restart httpd\n  ansible.builtin.service:\n    name: httpd\n    state: restarted\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#file-management","title":"File management","text":"<pre><code>- name: Create a directory\n  ansible.builtin.file:\n    path: /etc/myapp\n    state: directory\n    owner: root\n    group: root\n    mode: '0755'\n\n- name: Copy a file\n  ansible.builtin.copy:\n    src: files/myapp.conf\n    dest: /etc/myapp/myapp.conf\n    owner: root\n    group: root\n    mode: '0644'\n\n- name: Create a symlink\n  ansible.builtin.file:\n    src: /etc/myapp/myapp.conf\n    dest: /etc/myapp.conf\n    state: link\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#user-management","title":"User management","text":"<pre><code>- name: Create application user\n  ansible.builtin.user:\n    name: appuser\n    groups: wheel\n    shell: /bin/bash\n    create_home: true\n    state: present\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#handlers","title":"Handlers","text":"<p>Handlers run once at the end of the play, only if notified. They're used for actions that should only happen when something changes (e.g., restart a service only if its config was modified).</p> <pre><code>---\n- name: Configure sshd\n  hosts: all\n  become: true\n\n  tasks:\n    - name: Set SSH LoginGraceTime\n      ansible.builtin.lineinfile:\n        path: /etc/ssh/sshd_config\n        regexp: '^LoginGraceTime'\n        line: 'LoginGraceTime 30'\n      notify: Restart sshd            # triggers the handler\n\n  handlers:\n    - name: Restart sshd\n      ansible.builtin.service:\n        name: sshd\n        state: restarted\n</code></pre> <p>Key points: - Handlers run after all tasks in the play complete - A handler is only triggered if its notifying task reported <code>changed</code> - Multiple tasks can notify the same handler \u2014 it still runs only once</p>"},{"location":"04-rhce/ansible-playbooks/#check-mode-dry-run","title":"Check mode (dry run)","text":"<pre><code>ansible-playbook -i inventory.ini site.yml --check\n</code></pre> <p>Simulates what would change without making any changes.</p>"},{"location":"04-rhce/ansible-playbooks/#verbose-output","title":"Verbose output","text":"<pre><code>ansible-playbook -i inventory.ini site.yml -v    # basic\nansible-playbook -i inventory.ini site.yml -vvv  # very verbose (shows SSH details)\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#limit-to-specific-hosts","title":"Limit to specific hosts","text":"<pre><code># Run playbook against one host only\nansible-playbook -i inventory.ini site.yml --limit web01\n\n# Run against a group\nansible-playbook -i inventory.ini site.yml --limit webservers\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#tags","title":"Tags","text":"<p>Tags let you run only specific tasks:</p> <pre><code>- name: Install packages\n  ansible.builtin.dnf:\n    name: nginx\n    state: present\n  tags:\n    - packages\n    - nginx\n</code></pre> <pre><code># Run only tagged tasks\nansible-playbook site.yml --tags packages\n\n# Skip tagged tasks\nansible-playbook site.yml --skip-tags packages\n</code></pre>"},{"location":"04-rhce/ansible-playbooks/#next-step","title":"Next step","text":"<p>\u2192 Variables, Templates, and Files</p>"},{"location":"04-rhce/ansible-roles/","title":"Roles and Collections","text":"<p>Roles make playbooks reusable and shareable by organising tasks, variables, templates, and files into a standard directory structure.</p>"},{"location":"04-rhce/ansible-roles/#role-directory-structure","title":"Role directory structure","text":"<pre><code>roles/\n  webserver/\n    defaults/\n      main.yml        # Default variable values (lowest priority)\n    vars/\n      main.yml        # Role variables (higher priority than defaults)\n    tasks/\n      main.yml        # Main list of tasks\n    handlers/\n      main.yml        # Handlers used by this role\n    templates/\n      nginx.conf.j2   # Jinja2 templates\n    files/\n      index.html      # Static files to copy\n    meta/\n      main.yml        # Role metadata and dependencies\n    README.md\n</code></pre>"},{"location":"04-rhce/ansible-roles/#create-a-role-scaffold","title":"Create a role scaffold","text":"<pre><code>ansible-galaxy role init roles/webserver\n</code></pre>"},{"location":"04-rhce/ansible-roles/#example-role-webserver","title":"Example role: webserver","text":"<p><code>roles/webserver/defaults/main.yml</code></p> <pre><code>---\nwebserver_port: 80\nwebserver_root: /var/www/html\nwebserver_user: root\n</code></pre> <p><code>roles/webserver/tasks/main.yml</code></p> <pre><code>---\n- name: Install nginx\n  ansible.builtin.dnf:\n    name: nginx\n    state: present\n\n- name: Create web root\n  ansible.builtin.file:\n    path: \"{{ webserver_root }}\"\n    state: directory\n    mode: '0755'\n\n- name: Deploy nginx config\n  ansible.builtin.template:\n    src: nginx.conf.j2\n    dest: /etc/nginx/nginx.conf\n    mode: '0644'\n  notify: Reload nginx\n\n- name: Open firewall for http\n  ansible.posix.firewalld:\n    service: http\n    state: enabled\n    permanent: true\n    immediate: true\n\n- name: Ensure nginx is started and enabled\n  ansible.builtin.service:\n    name: nginx\n    state: started\n    enabled: true\n</code></pre> <p><code>roles/webserver/handlers/main.yml</code></p> <pre><code>---\n- name: Reload nginx\n  ansible.builtin.service:\n    name: nginx\n    state: reloaded\n</code></pre>"},{"location":"04-rhce/ansible-roles/#use-a-role-in-a-playbook","title":"Use a role in a playbook","text":"<pre><code>---\n- name: Configure web servers\n  hosts: webservers\n  become: true\n  roles:\n    - webserver\n\n- name: Configure web servers with custom port\n  hosts: webservers\n  become: true\n  roles:\n    - role: webserver\n      vars:\n        webserver_port: 8080\n</code></pre>"},{"location":"04-rhce/ansible-roles/#ansible-collections","title":"Ansible Collections","text":"<p>Collections bundle modules, roles, plugins, and playbooks. The key built-in collections for RHEL:</p> Collection Provides <code>ansible.builtin</code> Core modules (dnf, service, file, copy, etc.) <code>ansible.posix</code> firewalld, sysctl, at, cron <code>community.general</code> Wide variety of extras <code>redhat.rhel_system_roles</code> RHEL System Roles (timesync, selinux, etc.)"},{"location":"04-rhce/ansible-roles/#install-a-collection","title":"Install a collection","text":"<pre><code>ansible-galaxy collection install ansible.posix\nansible-galaxy collection install redhat.rhel_system_roles\n</code></pre>"},{"location":"04-rhce/ansible-roles/#rhel-system-roles","title":"RHEL System Roles","text":"<p>RHEL ships pre-certified roles for common administration tasks:</p> <pre><code>sudo dnf install -y rhel-system-roles\n</code></pre> <p>Available roles:</p> Role Task <code>timesync</code> Configure chrony/NTP <code>selinux</code> Configure SELinux mode and booleans <code>network</code> Configure networking <code>firewall</code> Configure firewalld <code>storage</code> Configure LVM and filesystems <code>certificate</code> Manage TLS certificates <p>Example usage:</p> <pre><code>---\n- name: Sync time with NTP\n  hosts: all\n  become: true\n  vars:\n    timesync_ntp_servers:\n      - hostname: pool.ntp.org\n        iburst: true\n\n  roles:\n    - redhat.rhel_system_roles.timesync\n</code></pre>"},{"location":"04-rhce/ansible-roles/#requirementsyml-declare-dependencies","title":"<code>requirements.yml</code> \u2014 declare dependencies","text":"<pre><code># requirements.yml\ncollections:\n  - name: ansible.posix\n  - name: community.general\n\nroles:\n  - name: geerlingguy.nginx\n    src: https://github.com/geerlingguy/ansible-role-nginx\n</code></pre> <p>Install all at once:</p> <pre><code>ansible-galaxy install -r requirements.yml\nansible-galaxy collection install -r requirements.yml\n</code></pre>"},{"location":"04-rhce/ansible-roles/#next-step","title":"Next step","text":"<p>\u2192 Deploy a Service with Ansible</p>"},{"location":"04-rhce/ansible-service-deploy/","title":"Deploy a Service with Ansible \u2014 Firewall + SELinux","text":"<p>A complete real-world playbook that installs nginx, configures firewalld, and handles SELinux correctly \u2014 the three pillars of RHEL service deployment.</p>"},{"location":"04-rhce/ansible-service-deploy/#project-structure","title":"Project structure","text":"<pre><code>web-deploy/\n  ansible.cfg\n  inventory.ini\n  site.yml\n  roles/\n    nginx/\n      defaults/main.yml\n      tasks/main.yml\n      handlers/main.yml\n      templates/nginx.conf.j2\n      files/index.html\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#ansiblecfg","title":"<code>ansible.cfg</code>","text":"<pre><code>[defaults]\ninventory = inventory.ini\nremote_user = rhel\nhost_key_checking = False\nstdout_callback = yaml\n\n[privilege_escalation]\nbecome = True\nbecome_method = sudo\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#inventoryini","title":"<code>inventory.ini</code>","text":"<pre><code>[webservers]\nweb01 ansible_host=192.168.1.101\n\n[webservers:vars]\nnginx_port=80\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#rolesnginxdefaultsmainyml","title":"<code>roles/nginx/defaults/main.yml</code>","text":"<pre><code>---\nnginx_port: 80\nnginx_root: /var/www/html\nnginx_user: nginx\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#rolesnginxtasksmainyml","title":"<code>roles/nginx/tasks/main.yml</code>","text":"<pre><code>---\n- name: Install nginx\n  ansible.builtin.dnf:\n    name: nginx\n    state: present\n  tags: packages\n\n- name: Create web root\n  ansible.builtin.file:\n    path: \"{{ nginx_root }}\"\n    state: directory\n    owner: \"{{ nginx_user }}\"\n    group: \"{{ nginx_user }}\"\n    mode: '0755'\n    setype: httpd_sys_content_t     # SELinux type \u2014 correct for web content\n\n- name: Deploy index.html\n  ansible.builtin.copy:\n    src: index.html\n    dest: \"{{ nginx_root }}/index.html\"\n    owner: \"{{ nginx_user }}\"\n    group: \"{{ nginx_user }}\"\n    mode: '0644'\n    setype: httpd_sys_content_t     # ensure correct SELinux context\n\n- name: Deploy nginx config\n  ansible.builtin.template:\n    src: nginx.conf.j2\n    dest: /etc/nginx/nginx.conf\n    owner: root\n    group: root\n    mode: '0644'\n    validate: nginx -t -c %s       # validate config before applying\n  notify: Reload nginx\n\n- name: Allow nginx port in SELinux (if non-standard)\n  community.general.seport:\n    ports: \"{{ nginx_port }}\"\n    proto: tcp\n    setype: http_port_t\n    state: present\n  when: nginx_port != 80 and nginx_port != 443\n\n- name: Allow http through firewalld\n  ansible.posix.firewalld:\n    service: http\n    state: enabled\n    permanent: true\n    immediate: true\n  when: nginx_port == 80\n\n- name: Allow custom port through firewalld\n  ansible.posix.firewalld:\n    port: \"{{ nginx_port }}/tcp\"\n    state: enabled\n    permanent: true\n    immediate: true\n  when: nginx_port != 80\n\n- name: Ensure nginx is started and enabled\n  ansible.builtin.service:\n    name: nginx\n    state: started\n    enabled: true\n  tags: service\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#rolesnginxhandlersmainyml","title":"<code>roles/nginx/handlers/main.yml</code>","text":"<pre><code>---\n- name: Reload nginx\n  ansible.builtin.service:\n    name: nginx\n    state: reloaded\n\n- name: Restart nginx\n  ansible.builtin.service:\n    name: nginx\n    state: restarted\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#rolesnginxtemplatesnginxconfj2","title":"<code>roles/nginx/templates/nginx.conf.j2</code>","text":"<pre><code>user {{ nginx_user }};\nworker_processes auto;\nerror_log /var/log/nginx/error.log;\npid /run/nginx.pid;\n\nevents {\n    worker_connections 1024;\n}\n\nhttp {\n    include             /etc/nginx/mime.types;\n    default_type        application/octet-stream;\n    sendfile            on;\n    keepalive_timeout   65;\n\n    server {\n        listen       {{ nginx_port }};\n        server_name  {{ inventory_hostname }};\n        root         {{ nginx_root }};\n        index        index.html;\n\n        error_page 404 /404.html;\n        error_page 500 502 503 504 /50x.html;\n    }\n}\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#rolesnginxfilesindexhtml","title":"<code>roles/nginx/files/index.html</code>","text":"<pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;RHEL Lab&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;&lt;h1&gt;Deployed by Ansible&lt;/h1&gt;&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#siteyml","title":"<code>site.yml</code>","text":"<pre><code>---\n- name: Deploy nginx web server\n  hosts: webservers\n  become: true\n\n  roles:\n    - nginx\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#run-it","title":"Run it","text":"<pre><code># Dry run first\nansible-playbook site.yml --check\n\n# Apply\nansible-playbook site.yml\n\n# Apply with verbose output\nansible-playbook site.yml -v\n</code></pre>"},{"location":"04-rhce/ansible-service-deploy/#verify","title":"Verify","text":"<pre><code># From control node\ncurl http://192.168.1.101/\n</code></pre> <p>Expected: <code>Deployed by Ansible</code></p>"},{"location":"04-rhce/ansible-service-deploy/#next-step","title":"Next step","text":"<p>\u2192 Patch Workflow + Reporting</p>"},{"location":"04-rhce/ansible-setup-inventory/","title":"Ansible Setup and Inventory","text":"<p>Ansible is an agentless automation tool that connects to managed nodes over SSH (or WinRM for Windows) and runs tasks. No agent is installed on target hosts.</p>"},{"location":"04-rhce/ansible-setup-inventory/#architecture","title":"Architecture","text":"<pre><code>Control node (your workstation or jump host)\n    \u2502\n    \u2502  SSH\n    \u2502\n    \u251c\u2500\u2500 managed node: web01\n    \u251c\u2500\u2500 managed node: web02\n    \u2514\u2500\u2500 managed node: db01\n</code></pre> Term Meaning Control node Where Ansible is installed and run from Managed node Host that Ansible configures Inventory List of managed nodes (hosts and groups) Playbook YAML file describing what to do Module A built-in task type (e.g., <code>dnf</code>, <code>copy</code>, <code>service</code>) Role A reusable, structured collection of tasks"},{"location":"04-rhce/ansible-setup-inventory/#install-ansible-on-rhel-10","title":"Install Ansible on RHEL 10","text":"<pre><code># Enable the Ansible module stream\nsudo dnf install -y ansible-core\n\n# Verify\nansible --version\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#ssh-key-setup-required","title":"SSH key setup (required)","text":"<p>Ansible connects via SSH. Copy your SSH key to managed nodes first:</p> <pre><code># Generate key if needed\nssh-keygen -t ed25519 -C \"ansible-control\"\n\n# Copy to managed nodes\nssh-copy-id rhel@192.168.1.101\nssh-copy-id rhel@192.168.1.102\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#inventory-file","title":"Inventory file","text":"<p>The inventory lists your managed nodes. Default location: <code>/etc/ansible/hosts</code>. For learning, create a local <code>inventory.ini</code> file.</p>"},{"location":"04-rhce/ansible-setup-inventory/#ini-format","title":"INI format","text":"<pre><code># inventory.ini\n\n[webservers]\nweb01 ansible_host=192.168.1.101\nweb02 ansible_host=192.168.1.102\n\n[dbservers]\ndb01 ansible_host=192.168.1.103\n\n[all:vars]\nansible_user=rhel\nansible_become=true\nansible_become_method=sudo\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#yaml-format-modern","title":"YAML format (modern)","text":"<pre><code># inventory.yaml\nall:\n  vars:\n    ansible_user: rhel\n    ansible_become: true\n  children:\n    webservers:\n      hosts:\n        web01:\n          ansible_host: 192.168.1.101\n        web02:\n          ansible_host: 192.168.1.102\n    dbservers:\n      hosts:\n        db01:\n          ansible_host: 192.168.1.103\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#ansiblecfg","title":"ansible.cfg","text":"<p>Project-level configuration (takes priority over global <code>/etc/ansible/ansible.cfg</code>):</p> <pre><code># ansible.cfg  (in your project directory)\n[defaults]\ninventory = inventory.ini\nremote_user = rhel\nhost_key_checking = False\nstdout_callback = yaml\n\n[privilege_escalation]\nbecome = True\nbecome_method = sudo\nbecome_user = root\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#ad-hoc-commands","title":"Ad-hoc commands","text":"<p>Ad-hoc commands run a single module without a playbook:</p> <pre><code># Ping all hosts\nansible all -m ping\n\n# Ping a specific group\nansible webservers -m ping\n\n# Run a command on all hosts\nansible all -m command -a \"uptime\"\n\n# Install a package\nansible webservers -m dnf -a \"name=vim state=present\"\n\n# Gather facts about a host\nansible web01 -m setup\n\n# Copy a file\nansible all -m copy -a \"src=/etc/hosts dest=/tmp/hosts_copy\"\n</code></pre>"},{"location":"04-rhce/ansible-setup-inventory/#ansible-modules-essential","title":"Ansible modules (essential)","text":"Module What it does <code>ping</code> Test connectivity <code>command</code> Run a command (no shell features) <code>shell</code> Run a command with shell features <code>dnf</code> Manage packages <code>service</code> Manage systemd services <code>copy</code> Copy files to managed nodes <code>template</code> Render Jinja2 templates <code>file</code> Manage file/dir attributes <code>user</code> Manage users <code>group</code> Manage groups <code>firewalld</code> Manage firewalld rules <code>selinux</code> Manage SELinux mode <code>seboolean</code> Manage SELinux booleans"},{"location":"04-rhce/ansible-setup-inventory/#next-step","title":"Next step","text":"<p>\u2192 Ansible Playbooks</p>"},{"location":"04-rhce/ansible-vars-templates/","title":"Variables, Templates, and Files","text":"<p>Variables and templates let you write reusable playbooks that work across different environments (dev, staging, prod) without changing the code.</p>"},{"location":"04-rhce/ansible-vars-templates/#defining-variables","title":"Defining variables","text":""},{"location":"04-rhce/ansible-vars-templates/#in-the-playbook-inline","title":"In the playbook (inline)","text":"<pre><code>---\n- name: Deploy web app\n  hosts: webservers\n  become: true\n  vars:\n    app_port: 8080\n    app_user: webapp\n    app_dir: /srv/webapp\n\n  tasks:\n    - name: Create app directory\n      ansible.builtin.file:\n        path: \"{{ app_dir }}\"\n        state: directory\n        owner: \"{{ app_user }}\"\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#in-a-vars-file","title":"In a vars file","text":"<pre><code># vars/main.yml\napp_port: 8080\napp_user: webapp\napp_dir: /srv/webapp\ndb_host: db01.lab.local\n</code></pre> <pre><code>- name: Deploy web app\n  hosts: webservers\n  vars_files:\n    - vars/main.yml\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#in-the-inventory-hostgroup-vars","title":"In the inventory (host/group vars)","text":"<pre><code># inventory.ini\n[webservers]\nweb01 ansible_host=192.168.1.101 app_port=8080\nweb02 ansible_host=192.168.1.102 app_port=8081\n\n[webservers:vars]\napp_user=webapp\n</code></pre> <p>Or as files:</p> <pre><code>inventory/\n  group_vars/\n    webservers.yml     # applies to all webservers\n    all.yml            # applies to all hosts\n  host_vars/\n    web01.yml          # applies to web01 only\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#variable-precedence-simplified-lowest-to-highest","title":"Variable precedence (simplified, lowest to highest)","text":"<pre><code>role defaults \u2192 inventory group_vars \u2192 inventory host_vars\n  \u2192 playbook vars \u2192 extra vars (-e flag)\n</code></pre> <p>Extra vars (<code>-e</code>) always win:</p> <pre><code>ansible-playbook site.yml -e \"app_port=9090\"\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#variable-usage-in-tasks","title":"Variable usage in tasks","text":"<pre><code>- name: Template a config file\n  ansible.builtin.template:\n    src: templates/nginx.conf.j2\n    dest: /etc/nginx/nginx.conf\n\n- name: Create a user\n  ansible.builtin.user:\n    name: \"{{ app_user }}\"\n    home: \"{{ app_dir }}\"\n</code></pre> <p>Always quote variables that are the entire YAML value to avoid type coercion: <pre><code>path: \"{{ app_dir }}\"    # correct\npath: {{ app_dir }}      # incorrect \u2014 YAML parse error\n</code></pre></p>"},{"location":"04-rhce/ansible-vars-templates/#jinja2-templates","title":"Jinja2 templates","text":"<p>Templates use Jinja2 syntax and are rendered on the managed node.</p> <pre><code>templates/\n  nginx.conf.j2\n</code></pre> <pre><code># templates/nginx.conf.j2\nworker_processes auto;\n\nevents {\n    worker_connections 1024;\n}\n\nhttp {\n    server {\n        listen {{ app_port }};\n        server_name {{ inventory_hostname }};\n\n        root {{ app_dir }};\n\n        access_log /var/log/nginx/{{ inventory_hostname }}_access.log;\n    }\n}\n</code></pre> <p>Deploy the template:</p> <pre><code>- name: Deploy nginx config\n  ansible.builtin.template:\n    src: templates/nginx.conf.j2\n    dest: /etc/nginx/nginx.conf\n    owner: root\n    group: root\n    mode: '0644'\n  notify: Reload nginx\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#jinja2-basics","title":"Jinja2 basics","text":"<pre><code>{# Comment #}\n{{ variable }}\n{{ variable | default(\"fallback\") }}\n{{ variable | upper }}\n\n{% if app_port == 443 %}\nssl on;\n{% endif %}\n\n{% for host in groups['webservers'] %}\nserver {{ host }};\n{% endfor %}\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#the-lineinfile-module","title":"The <code>lineinfile</code> module","text":"<p>Useful for managing single lines in config files:</p> <pre><code>- name: Set MaxSessions in sshd_config\n  ansible.builtin.lineinfile:\n    path: /etc/ssh/sshd_config\n    regexp: '^#?MaxSessions'\n    line: 'MaxSessions 10'\n    state: present\n  notify: Restart sshd\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#the-blockinfile-module","title":"The <code>blockinfile</code> module","text":"<p>Manage a multi-line block:</p> <pre><code>- name: Add hosts entries\n  ansible.builtin.blockinfile:\n    path: /etc/hosts\n    block: |\n      192.168.1.101 web01 web01.lab.local\n      192.168.1.102 web02 web02.lab.local\n    marker: \"# {mark} ANSIBLE MANAGED BLOCK\"\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#ansible-vault-secrets","title":"Ansible Vault (secrets)","text":"<p>Never store passwords in plain-text vars files:</p> <pre><code># Encrypt a vars file\nansible-vault encrypt vars/secrets.yml\n\n# View an encrypted file\nansible-vault view vars/secrets.yml\n\n# Edit an encrypted file\nansible-vault edit vars/secrets.yml\n\n# Run playbook with vault\nansible-playbook site.yml --ask-vault-pass\n\n# Or use a vault password file\nansible-playbook site.yml --vault-password-file ~/.vault_pass\n</code></pre>"},{"location":"04-rhce/ansible-vars-templates/#next-step","title":"Next step","text":"<p>\u2192 Roles and Collections</p>"},{"location":"04-rhce/automation-mindset/","title":"Automation Mindset \u2014 Idempotence","text":"<p>Before writing a single line of Bash or Ansible, you need to think differently about how you manage systems.</p>"},{"location":"04-rhce/automation-mindset/#the-manual-administration-trap","title":"The manual administration trap","text":"<p>When you configure a server by hand:</p> <ul> <li>Steps are only in your head or a runbook</li> <li>You can't repeat the exact steps reliably</li> <li>Drift occurs (the server changes over time, documentation doesn't)</li> <li>Scaling to 10, 100, or 1000 servers is impossible</li> </ul>"},{"location":"04-rhce/automation-mindset/#automation-thinking","title":"Automation thinking","text":"<p>Automation means describing the desired state, not the steps to get there.</p>"},{"location":"04-rhce/automation-mindset/#imperative-vs-declarative","title":"Imperative vs Declarative","text":"Approach Style Example Imperative \"Do these steps\" <code>mkdir /etc/myapp; cp config /etc/myapp/; chmod 640</code> Declarative \"Ensure this state\" \"Directory <code>/etc/myapp</code> exists, file present, mode 640\" <p>Ansible (and most modern config management) is declarative: you describe what you want, the tool figures out how to get there.</p>"},{"location":"04-rhce/automation-mindset/#idempotence","title":"Idempotence","text":"<p>An operation is idempotent if running it multiple times produces the same result as running it once.</p> <pre><code># Not idempotent \u2014 appends a line every time you run it\necho \"server = db01\" &gt;&gt; /etc/myapp.conf\n\n# Idempotent \u2014 only adds the line if it's not already there\ngrep -qxF \"server = db01\" /etc/myapp.conf || echo \"server = db01\" &gt;&gt; /etc/myapp.conf\n</code></pre> <p>Ansible tasks are idempotent by design. The same playbook run ten times produces the same final state.</p>"},{"location":"04-rhce/automation-mindset/#why-idempotence-matters","title":"Why idempotence matters","text":"<ul> <li>Safe to re-run: apply the playbook on a new server OR re-apply it on an   existing server to bring it into compliance.</li> <li>Self-documenting: the playbook is the source of truth for system state.</li> <li>Auditable: run the playbook in check mode to see what would change.</li> <li>Drift detection: if a playbook reports changes on a server that should   be stable, something changed outside of automation.</li> </ul>"},{"location":"04-rhce/automation-mindset/#the-set-euo-pipefail-habit-for-bash","title":"The \"set -euo pipefail\" habit for Bash","text":"<p>Even in scripts, think idempotently and fail safely:</p> <pre><code>#!/usr/bin/env bash\nset -euo pipefail\n\n# -e  exit on any error\n# -u  treat unset variables as errors\n# -o pipefail  fail if any command in a pipe fails\n</code></pre> <p>Without <code>set -e</code>, a script silently continues after errors and can cause hard-to-debug problems.</p>"},{"location":"04-rhce/automation-mindset/#infrastructure-as-code-iac-principles","title":"Infrastructure as Code (IaC) principles","text":"<ol> <li>Version control everything: store playbooks, roles, and configs in Git.</li> <li>Review before apply: treat automation changes like code changes \u2014 PR/review.</li> <li>Test in a lab first: never run untested automation on production.</li> <li>Minimal privilege: automation accounts should have only the access they need.</li> <li>No secrets in code: use Ansible Vault, environment variables, or a secrets manager.</li> </ol>"},{"location":"04-rhce/automation-mindset/#the-4-stage-automation-workflow","title":"The 4-stage automation workflow","text":"<pre><code>Write \u2192 Test (lab) \u2192 Review \u2192 Apply (prod)\n   \u2191                                    |\n   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n              (iterate)\n</code></pre>"},{"location":"04-rhce/automation-mindset/#next-step","title":"Next step","text":"<p>\u2192 Bash Scripting Fundamentals</p>"},{"location":"04-rhce/bash-fundamentals/","title":"Bash Scripting Fundamentals","text":"<p>Bash scripts let you automate repetitive tasks. This chapter teaches safe, readable, idempotent patterns \u2014 not just \"how to write Bash\", but how to write Bash that won't surprise you at 2 AM.</p>"},{"location":"04-rhce/bash-fundamentals/#script-template-always-start-with-this","title":"Script template (always start with this)","text":"<pre><code>#!/usr/bin/env bash\nset -euo pipefail\n\n# Script: describe-what-it-does.sh\n# Usage:  sudo ./describe-what-it-does.sh [args]\n# Notes:  Any important context\n\nmain() {\n  # your logic here\n  echo \"Done.\"\n}\n\nmain \"$@\"\n</code></pre> Header line Why <code>#!/usr/bin/env bash</code> Portable shebang; finds bash in PATH <code>set -e</code> Exit immediately on error <code>set -u</code> Error on unset variable (catches typos) <code>set -o pipefail</code> Catch failures in pipe chains"},{"location":"04-rhce/bash-fundamentals/#variables","title":"Variables","text":"<pre><code>NAME=\"rhel10-lab\"\nPORT=8080\nLOG_FILE=\"/var/log/myapp.log\"\n\n# Use double quotes around variables (prevent word splitting)\necho \"Hostname is: ${NAME}\"\necho \"Port: ${PORT}\"\n\n# Default value if variable is unset\nLOG_DIR=\"${LOG_DIR:-/var/log}\"\n\n# Readonly\nreadonly CONFIG_FILE=\"/etc/myapp/config\"\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#conditionals","title":"Conditionals","text":"<pre><code># if / elif / else\nif [[ -f /etc/myapp.conf ]]; then\n  echo \"Config exists\"\nelif [[ -d /etc/myapp ]]; then\n  echo \"Directory exists, no config\"\nelse\n  echo \"Neither found\"\nfi\n\n# Test flags\n[[ -f file ]]    # file exists and is a regular file\n[[ -d dir ]]     # directory exists\n[[ -z \"$VAR\" ]]  # variable is empty\n[[ -n \"$VAR\" ]]  # variable is non-empty\n[[ \"$A\" == \"$B\" ]]  # string equality\n[[ $N -gt 10 ]]     # numeric greater than\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#loops","title":"Loops","text":"<pre><code># for loop over a list\nfor HOST in web01 web02 web03; do\n  echo \"Processing: ${HOST}\"\ndone\n\n# for loop with range\nfor I in {1..5}; do\n  echo \"Step ${I}\"\ndone\n\n# while loop\nCOUNT=0\nwhile [[ $COUNT -lt 5 ]]; do\n  echo \"Count: ${COUNT}\"\n  (( COUNT++ ))\ndone\n\n# Loop over lines of a file\nwhile IFS= read -r line; do\n  echo \"Line: ${line}\"\ndone &lt; /etc/hosts\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#functions","title":"Functions","text":"<pre><code>log() {\n  local level=\"$1\"\n  local message=\"$2\"\n  echo \"[$(date '+%Y-%m-%d %H:%M:%S')] [${level}] ${message}\"\n}\n\ndie() {\n  log \"ERROR\" \"$1\"\n  exit 1\n}\n\ncheck_root() {\n  if [[ $EUID -ne 0 ]]; then\n    die \"This script must be run as root (use sudo)\"\n  fi\n}\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#input-validation","title":"Input validation","text":"<pre><code>usage() {\n  echo \"Usage: $0 &lt;username&gt; &lt;group&gt;\"\n  exit 1\n}\n\n# Check argument count\nif [[ $# -ne 2 ]]; then\n  usage\nfi\n\nUSERNAME=\"$1\"\nGROUP=\"$2\"\n\n# Validate input\nif ! id \"${USERNAME}\" &amp;&gt;/dev/null; then\n  die \"User '${USERNAME}' does not exist\"\nfi\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#command-substitution","title":"Command substitution","text":"<pre><code># Capture command output into a variable\nOS_VERSION=$(rpm -q --queryformat '%{VERSION}' redhat-release)\nDISK_USAGE=$(df -h / | awk 'NR==2 {print $5}')\n\necho \"RHEL version: ${OS_VERSION}\"\necho \"Root disk usage: ${DISK_USAGE}\"\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#exit-codes","title":"Exit codes","text":"<pre><code># Exit code 0 = success, non-zero = failure\nif ping -c 1 8.8.8.8 &amp;&gt;/dev/null; then\n  echo \"Network OK\"\nelse\n  echo \"Network unreachable\"\n  exit 1\nfi\n\n# Check exit code of last command\nsystemctl is-active sshd\nif [[ $? -ne 0 ]]; then\n  echo \"sshd is not running!\"\nfi\n\n# Shorthand\nsystemctl is-active sshd || echo \"sshd is not running!\"\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#idempotent-patterns","title":"Idempotent patterns","text":"<pre><code># Create directory only if it doesn't exist\n[[ -d /etc/myapp ]] || sudo mkdir -p /etc/myapp\n\n# Add a line only if not already present\ngrep -qxF \"ServerName rhel10-lab\" /etc/httpd/conf/httpd.conf || \\\n  echo \"ServerName rhel10-lab\" | sudo tee -a /etc/httpd/conf/httpd.conf\n\n# Install a package only if not installed\nrpm -q vim &amp;&gt;/dev/null || sudo dnf install -y vim\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#logging-output","title":"Logging output","text":"<pre><code>LOGFILE=\"/var/log/myscript.log\"\n\nlog() {\n  echo \"[$(date '+%Y-%m-%d %H:%M:%S')] $*\" | tee -a \"${LOGFILE}\"\n}\n\nlog \"INFO\" \"Script started\"\n</code></pre>"},{"location":"04-rhce/bash-fundamentals/#next-step","title":"Next step","text":"<p>\u2192 Ansible Setup and Inventory</p>"},{"location":"04-rhce/labs/first-playbook/","title":"Lab: Write Your First Playbook","text":"<p>Track: RHCE Estimated time: 40 minutes Topology: Single VM (using localhost as managed node)</p>"},{"location":"04-rhce/labs/first-playbook/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Ansible Setup and Inventory and Ansible Playbooks</li> <li><code>ansible-core</code> installed: <code>sudo dnf install -y ansible-core</code></li> <li>VM snapshot taken</li> </ul>"},{"location":"04-rhce/labs/first-playbook/#success-criteria","title":"Success criteria","text":"<ul> <li>A local inventory targeting <code>localhost</code> is working</li> <li>A playbook installs packages, creates a user, deploys a config file</li> <li>The playbook is idempotent (run twice, second run shows no changes)</li> <li>A handler triggers correctly when config changes</li> </ul>"},{"location":"04-rhce/labs/first-playbook/#steps","title":"Steps","text":""},{"location":"04-rhce/labs/first-playbook/#1-create-the-project-directory","title":"1 \u2014 Create the project directory","text":"<pre><code>mkdir ~/ansible-lab &amp;&amp; cd ~/ansible-lab\n</code></pre>"},{"location":"04-rhce/labs/first-playbook/#2-create-the-inventory","title":"2 \u2014 Create the inventory","text":"<pre><code>cat &gt; inventory.ini &lt;&lt; 'EOF'\n[local]\nlocalhost ansible_connection=local\nEOF\n</code></pre> <p>Verify</p> <p><pre><code>ansible -i inventory.ini local -m ping\n</code></pre> Expected: <code>localhost | SUCCESS</code></p>"},{"location":"04-rhce/labs/first-playbook/#3-create-the-playbook","title":"3 \u2014 Create the playbook","text":"<pre><code>vim site.yml\n</code></pre> <pre><code>---\n- name: Configure local system\n  hosts: local\n  become: true\n\n  vars:\n    app_user: labuser\n    app_dir: /srv/labapp\n    app_port: 8080\n\n  tasks:\n    - name: Install required packages\n      ansible.builtin.dnf:\n        name:\n          - vim\n          - curl\n          - git\n        state: present\n\n    - name: Create application group\n      ansible.builtin.group:\n        name: appteam\n        state: present\n\n    - name: Create application user\n      ansible.builtin.user:\n        name: \"{{ app_user }}\"\n        groups: appteam\n        shell: /bin/bash\n        create_home: true\n        comment: \"Lab Application User\"\n        state: present\n\n    - name: Create application directory\n      ansible.builtin.file:\n        path: \"{{ app_dir }}\"\n        state: directory\n        owner: \"{{ app_user }}\"\n        group: appteam\n        mode: '0775'\n\n    - name: Deploy application config\n      ansible.builtin.template:\n        src: templates/app.conf.j2\n        dest: \"{{ app_dir }}/app.conf\"\n        owner: \"{{ app_user }}\"\n        group: appteam\n        mode: '0640'\n      notify: Show config deployed\n\n  handlers:\n    - name: Show config deployed\n      ansible.builtin.debug:\n        msg: \"Config deployed to {{ app_dir }}/app.conf\"\n</code></pre>"},{"location":"04-rhce/labs/first-playbook/#4-create-the-template","title":"4 \u2014 Create the template","text":"<pre><code>mkdir -p templates\ncat &gt; templates/app.conf.j2 &lt;&lt; 'EOF'\n# Application configuration\n# Managed by Ansible \u2014 do not edit by hand\n# Generated: {{ ansible_date_time.iso8601 }}\n\n[server]\nport = {{ app_port }}\nuser = {{ app_user }}\ndir  = {{ app_dir }}\n\n[host]\nhostname = {{ inventory_hostname }}\nos       = {{ ansible_distribution }} {{ ansible_distribution_version }}\nEOF\n</code></pre>"},{"location":"04-rhce/labs/first-playbook/#5-run-in-check-mode-first","title":"5 \u2014 Run in check mode first","text":"<pre><code>ansible-playbook -i inventory.ini site.yml --check\n</code></pre> <p>Review what would change. No changes are applied.</p>"},{"location":"04-rhce/labs/first-playbook/#6-apply-the-playbook","title":"6 \u2014 Apply the playbook","text":"<pre><code>ansible-playbook -i inventory.ini site.yml\n</code></pre> <p>Note the tasks that show <code>changed</code> vs <code>ok</code>.</p> <p>Verify</p> <p><pre><code>cat /srv/labapp/app.conf\nid labuser\n</code></pre> Config file present; user created.</p>"},{"location":"04-rhce/labs/first-playbook/#7-run-again-to-confirm-idempotence","title":"7 \u2014 Run again to confirm idempotence","text":"<pre><code>ansible-playbook -i inventory.ini site.yml\n</code></pre> <p>Expected: all tasks show <code>ok</code> \u2014 no changes. This confirms idempotence.</p>"},{"location":"04-rhce/labs/first-playbook/#8-trigger-the-handler","title":"8 \u2014 Trigger the handler","text":"<p>Change the port in the playbook:</p> <pre><code>    app_port: 9090\n</code></pre> <pre><code>ansible-playbook -i inventory.ini site.yml\n</code></pre> <p>Look for: <code>Deploy application config</code> shows <code>changed</code> and the handler runs.</p> <pre><code>grep port /srv/labapp/app.conf\n</code></pre> <p>Expected: <code>port = 9090</code></p>"},{"location":"04-rhce/labs/first-playbook/#cleanup","title":"Cleanup","text":"<pre><code>cd ~\nsudo userdel -r labuser\nsudo groupdel appteam\nsudo rm -rf /srv/labapp\nrm -rf ~/ansible-lab\n</code></pre>"},{"location":"04-rhce/labs/first-playbook/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix <code>ping</code> fails <code>ansible-core</code> not installed <code>sudo dnf install -y ansible-core</code> Permission denied <code>become: true</code> missing Add <code>become: true</code> to the play Handler didn't run Task showed <code>ok</code> not <code>changed</code> Handler only runs when task reports <code>changed</code> Template not found Wrong path Check <code>src: templates/app.conf.j2</code> matches actual file"},{"location":"04-rhce/labs/first-playbook/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>This four-part pattern \u2014 install packages, manage users/dirs, deploy config from template, notify handler \u2014 covers 80% of real-world service deployment tasks. The moment you have this working repeatably, you have a foundation to build every other role on.</p>"},{"location":"04-rhce/labs/role-web-deploy/","title":"Lab: Role-Based Web Service Deploy","text":"<p>Track: RHCE Estimated time: 60 minutes Topology: Single VM (localhost) or two VMs if available</p>"},{"location":"04-rhce/labs/role-web-deploy/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Roles and Collections and Deploy a Service with Ansible</li> <li><code>ansible-core</code> and <code>ansible.posix</code> collection installed</li> <li>VM snapshot taken</li> </ul>"},{"location":"04-rhce/labs/role-web-deploy/#success-criteria","title":"Success criteria","text":"<ul> <li>An <code>nginx</code> role deploys nginx with a custom port (8080)</li> <li>firewalld is configured via Ansible</li> <li>SELinux context is set correctly via Ansible</li> <li>Playbook is idempotent</li> <li>Curl confirms the service is accessible</li> </ul>"},{"location":"04-rhce/labs/role-web-deploy/#steps","title":"Steps","text":""},{"location":"04-rhce/labs/role-web-deploy/#1-install-required-collections","title":"1 \u2014 Install required collections","text":"<pre><code>ansible-galaxy collection install ansible.posix\nansible-galaxy collection install community.general\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#2-create-project-structure","title":"2 \u2014 Create project structure","text":"<pre><code>mkdir ~/role-lab &amp;&amp; cd ~/role-lab\nansible-galaxy role init roles/nginx\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#3-create-ansiblecfg-and-inventory","title":"3 \u2014 Create ansible.cfg and inventory","text":"<pre><code>cat &gt; ansible.cfg &lt;&lt; 'EOF'\n[defaults]\ninventory = inventory.ini\nremote_user = rhel\nhost_key_checking = False\nstdout_callback = yaml\n\n[privilege_escalation]\nbecome = True\nbecome_method = sudo\nEOF\n\ncat &gt; inventory.ini &lt;&lt; 'EOF'\n[webservers]\nlocalhost ansible_connection=local nginx_port=8080\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#4-define-role-defaults","title":"4 \u2014 Define role defaults","text":"<pre><code>cat &gt; roles/nginx/defaults/main.yml &lt;&lt; 'EOF'\n---\nnginx_port: 80\nnginx_root: /var/www/html\nnginx_user: nginx\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#5-write-role-tasks","title":"5 \u2014 Write role tasks","text":"<pre><code>cat &gt; roles/nginx/tasks/main.yml &lt;&lt; 'EOF'\n---\n- name: Install nginx\n  ansible.builtin.dnf:\n    name: nginx\n    state: present\n\n- name: Create web root\n  ansible.builtin.file:\n    path: \"{{ nginx_root }}\"\n    state: directory\n    owner: \"{{ nginx_user }}\"\n    group: \"{{ nginx_user }}\"\n    mode: '0755'\n    setype: httpd_sys_content_t\n\n- name: Deploy index.html\n  ansible.builtin.copy:\n    content: \"&lt;h1&gt;Deployed by Ansible Role&lt;/h1&gt;\\n\"\n    dest: \"{{ nginx_root }}/index.html\"\n    setype: httpd_sys_content_t\n\n- name: Deploy nginx config\n  ansible.builtin.template:\n    src: nginx.conf.j2\n    dest: /etc/nginx/nginx.conf\n    mode: '0644'\n  notify: Reload nginx\n\n- name: Allow nginx non-standard port in SELinux\n  community.general.seport:\n    ports: \"{{ nginx_port }}\"\n    proto: tcp\n    setype: http_port_t\n    state: present\n  when: nginx_port not in [80, 443]\n\n- name: Open custom port in firewalld\n  ansible.posix.firewalld:\n    port: \"{{ nginx_port }}/tcp\"\n    state: enabled\n    permanent: true\n    immediate: true\n  when: nginx_port not in [80, 443]\n\n- name: Open http in firewalld\n  ansible.posix.firewalld:\n    service: http\n    state: enabled\n    permanent: true\n    immediate: true\n  when: nginx_port == 80\n\n- name: Start and enable nginx\n  ansible.builtin.service:\n    name: nginx\n    state: started\n    enabled: true\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#6-write-role-handler","title":"6 \u2014 Write role handler","text":"<pre><code>cat &gt; roles/nginx/handlers/main.yml &lt;&lt; 'EOF'\n---\n- name: Reload nginx\n  ansible.builtin.service:\n    name: nginx\n    state: reloaded\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#7-write-nginx-config-template","title":"7 \u2014 Write nginx config template","text":"<pre><code>cat &gt; roles/nginx/templates/nginx.conf.j2 &lt;&lt; 'EOF'\nuser {{ nginx_user }};\nworker_processes auto;\nerror_log /var/log/nginx/error.log;\n\nevents {\n    worker_connections 1024;\n}\n\nhttp {\n    include       /etc/nginx/mime.types;\n    default_type  application/octet-stream;\n    sendfile      on;\n\n    server {\n        listen      {{ nginx_port }};\n        server_name {{ inventory_hostname }};\n        root        {{ nginx_root }};\n        index       index.html;\n    }\n}\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#8-write-siteyml","title":"8 \u2014 Write site.yml","text":"<pre><code>cat &gt; site.yml &lt;&lt; 'EOF'\n---\n- name: Deploy nginx web server\n  hosts: webservers\n  become: true\n  roles:\n    - nginx\nEOF\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#9-run-check-mode-then-apply","title":"9 \u2014 Run check mode then apply","text":"<pre><code>ansible-playbook site.yml --check\nansible-playbook site.yml\n</code></pre> <p>Verify</p> <p><pre><code>curl http://localhost:8080/\n</code></pre> Expected: <code>Deployed by Ansible Role</code></p> <p><pre><code>sudo semanage port -l | grep http_port\n</code></pre> Look for: <code>8080</code> listed under <code>http_port_t</code></p>"},{"location":"04-rhce/labs/role-web-deploy/#10-confirm-idempotence","title":"10 \u2014 Confirm idempotence","text":"<pre><code>ansible-playbook site.yml\n</code></pre> <p>All tasks: <code>ok</code>. No changes.</p>"},{"location":"04-rhce/labs/role-web-deploy/#cleanup","title":"Cleanup","text":"<pre><code>cd ~\nansible-playbook -i role-lab/inventory.ini role-lab/site.yml \\\n  -e \"nginx_port=8080\" --tags never ||:\nsudo systemctl disable --now nginx\nsudo dnf remove -y nginx\nsudo semanage port -d -t http_port_t -p tcp 8080 2&gt;/dev/null || true\nsudo firewall-cmd --permanent --remove-port=8080/tcp 2&gt;/dev/null || true\nsudo firewall-cmd --reload\nrm -rf ~/role-lab\n</code></pre>"},{"location":"04-rhce/labs/role-web-deploy/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>Roles are the unit of reuse in Ansible. An <code>nginx</code> role can be dropped into any project and parameterised for dev (port 8080) vs prod (port 443). The SELinux and firewalld steps inside the role mean the service is correctly hardened by default \u2014 the role encapsulates all three: software, firewall, and MAC policy.</p>"},{"location":"05-rhca/journald-retention/","title":"Journald Retention and Forwarding","text":"<p>On a RHEL host, the journal is your primary log source. At RHCA level, you need to design journal retention, size policies, and log forwarding correctly.</p>"},{"location":"05-rhca/journald-retention/#journal-storage-location","title":"Journal storage location","text":"Path Meaning <code>/run/log/journal/</code> Volatile (RAM); cleared on reboot <code>/var/log/journal/</code> Persistent; survives reboots <p>RHEL 10 creates <code>/var/log/journal/</code> by default, making the journal persistent.</p>"},{"location":"05-rhca/journald-retention/#journaldconf-configuration-file","title":"<code>journald.conf</code> \u2014 configuration file","text":"<pre><code>sudo vim /etc/systemd/journald.conf\n</code></pre> <p>Key settings:</p> <pre><code>[Journal]\n# Storage location\nStorage=persistent          # persistent (default), volatile, auto, none\n\n# Maximum disk space for the journal\nSystemMaxUse=2G             # default: 10% of filesystem\nSystemKeepFree=1G           # always keep 1G free\n\n# Per-boot limit\nSystemMaxFileSize=128M      # max size of one journal file\n\n# Runtime (volatile) limits\nRuntimeMaxUse=512M\n\n# Max retention by time\nMaxRetentionSec=3months\n\n# Max file size before rotation\nMaxFileSec=1week\n\n# Compression\nCompress=yes                # enabled by default\n\n# Rate limiting (prevent log storms from crashing journald)\nRateLimitIntervalSec=30s\nRateLimitBurst=1000\n</code></pre> <p>After editing:</p> <pre><code>sudo systemctl restart systemd-journald\n</code></pre>"},{"location":"05-rhca/journald-retention/#check-current-journal-disk-usage","title":"Check current journal disk usage","text":"<pre><code>journalctl --disk-usage\n</code></pre>"},{"location":"05-rhca/journald-retention/#manual-vacuum-immediate-cleanup","title":"Manual vacuum (immediate cleanup)","text":"<pre><code># Keep only last 2 weeks\nsudo journalctl --vacuum-time=2weeks\n\n# Keep only 500 MB\nsudo journalctl --vacuum-size=500M\n\n# Keep only 5 files\nsudo journalctl --vacuum-files=5\n</code></pre>"},{"location":"05-rhca/journald-retention/#forwarding-to-syslog-rsyslog","title":"Forwarding to syslog (rsyslog)","text":"<p>By default on RHEL, journald forwards to rsyslog, which writes to <code>/var/log/</code>.</p> <p>Check the forwarding is active:</p> <pre><code>grep \"^ForwardToSyslog\" /etc/systemd/journald.conf\n# If not set, default is 'yes' when rsyslog is installed\n</code></pre> <p>To disable syslog forwarding (if using a centralised log shipper):</p> <pre><code>[Journal]\nForwardToSyslog=no\n</code></pre>"},{"location":"05-rhca/journald-retention/#forwarding-to-a-remote-journal-systemd-journal-remote","title":"Forwarding to a remote journal (systemd-journal-remote)","text":"<p>For centralised collection without a SIEM, RHEL ships <code>systemd-journal-remote</code>:</p> <pre><code># On log collection server\nsudo dnf install -y systemd-journal-remote\nsudo systemctl enable --now systemd-journal-remote.socket\n\n# On sending hosts\nsudo dnf install -y systemd-journal-upload\nsudo vim /etc/systemd/journal-upload.conf\n</code></pre> <pre><code>[Upload]\nURL=http://logserver.lab.local:19532\n</code></pre> <pre><code>sudo systemctl enable --now systemd-journal-upload.service\n</code></pre>"},{"location":"05-rhca/journald-retention/#per-service-log-namespaces","title":"Per-service log namespaces","text":"<p>RHEL 10 supports log namespaces \u2014 a service can write to an isolated journal:</p> <pre><code>[Service]\nLogNamespace=myapp\n</code></pre> <p>View:</p> <pre><code>journalctl --namespace=myapp\n</code></pre> <p>Useful for high-volume services to avoid filling the system journal.</p>"},{"location":"05-rhca/journald-retention/#forwarding-to-a-siem-rsyslog-remote","title":"Forwarding to a SIEM (rsyslog \u2192 remote)","text":"<p>Most enterprise environments forward to a SIEM (Splunk, Elastic, etc.) via rsyslog:</p> <pre><code>sudo vim /etc/rsyslog.d/99-remote.conf\n</code></pre> <pre><code># Forward all logs to remote syslog server (UDP 514)\n*.* @logserver.example.com:514\n\n# TCP (more reliable)\n*.* @@logserver.example.com:514\n</code></pre> <pre><code>sudo systemctl restart rsyslog\n</code></pre>"},{"location":"05-rhca/journald-retention/#next-step","title":"Next step","text":"<p>\u2192 SELinux Deep Dive: Fix Taxonomy</p>"},{"location":"05-rhca/systemd-advanced/","title":"Advanced systemd \u2014 Dependencies, Drop-ins","text":"<p>This chapter goes beyond basic <code>systemctl start/stop</code> to cover unit design, dependency management, and safe customisation via drop-ins.</p>"},{"location":"05-rhca/systemd-advanced/#unit-dependency-directives","title":"Unit dependency directives","text":"<p>systemd uses directives to define ordering and requirements between units:</p> Directive Meaning <code>Requires=</code> Strong dependency \u2014 if the required unit fails, this unit fails too <code>Wants=</code> Weak dependency \u2014 if the required unit fails, this unit continues <code>After=</code> This unit starts after the listed units <code>Before=</code> This unit starts before the listed units <code>BindsTo=</code> Like Requires, but if the dependency stops, this unit stops too <code>PartOf=</code> If parent is restarted/stopped, this unit follows <code>Conflicts=</code> Cannot run simultaneously with the listed unit <p>After= does not imply Requires=</p> <p><code>After=network.target</code> only sets ordering. To ensure the network is present, also add <code>Wants=network-online.target After=network-online.target</code>.</p>"},{"location":"05-rhca/systemd-advanced/#example-service-that-needs-the-network-and-a-database","title":"Example: service that needs the network and a database","text":"<pre><code>[Unit]\nDescription=My Application\nAfter=network-online.target postgresql.service\nWants=network-online.target\nRequires=postgresql.service\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#networktarget-vs-network-onlinetarget","title":"<code>network.target</code> vs <code>network-online.target</code>","text":"Target Meaning <code>network.target</code> Network interfaces configured (may not have connectivity yet) <code>network-online.target</code> Network is up and reachable (NM reports connected) <p>For services that make outbound connections at startup, use:</p> <pre><code>After=network-online.target\nWants=network-online.target\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#drop-in-files-customise-without-modifying-originals","title":"Drop-in files \u2014 customise without modifying originals","text":"<p>Drop-ins override or extend a unit without touching the shipped file:</p> <pre><code># Create a drop-in for sshd.service\nsudo systemctl edit sshd.service\n</code></pre> <p>This creates <code>/etc/systemd/system/sshd.service.d/override.conf</code>.</p> <p>Example \u2014 add a restart policy without touching the original unit:</p> <pre><code>[Service]\nRestart=always\nRestartSec=5s\n</code></pre> <p>View the merged effective unit:</p> <pre><code>systemctl cat sshd.service\n</code></pre> <p>After editing a drop-in:</p> <pre><code>sudo systemctl daemon-reload\nsudo systemctl restart sshd.service\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#service-restart-policies","title":"Service restart policies","text":"Value Meaning <code>no</code> Never restart (default) <code>always</code> Always restart, regardless of exit code <code>on-failure</code> Restart only on non-zero exit or signal <code>on-abnormal</code> Restart on signal, watchdog timeout, or core dump <pre><code>[Service]\nRestart=on-failure\nRestartSec=10s\nStartLimitIntervalSec=60s\nStartLimitBurst=3\n</code></pre> <p>This allows 3 restart attempts in 60 seconds, then gives up.</p>"},{"location":"05-rhca/systemd-advanced/#template-units","title":"Template units","text":"<p>Template units allow one unit file to instantiate multiple services:</p> <pre><code>sshd@.service \u2192 sshd@22.service, sshd@2222.service\n</code></pre> <p>A template unit file uses <code>%i</code> for the instance parameter:</p> <pre><code># /etc/systemd/system/myapp@.service\n[Unit]\nDescription=My App instance %i\n\n[Service]\nExecStart=/usr/bin/myapp --port %i\n</code></pre> <p>Start instances:</p> <pre><code>sudo systemctl start myapp@8080.service\nsudo systemctl start myapp@8081.service\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#target-units-grouping","title":"Target units (grouping)","text":"<pre><code># /etc/systemd/system/myapp.target\n[Unit]\nDescription=All My Application Services\nWants=myapp@8080.service myapp@8081.service\nAfter=network-online.target\n</code></pre> <p>Start everything:</p> <pre><code>sudo systemctl start myapp.target\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#analyse-startup-time","title":"Analyse startup time","text":"<pre><code># Total boot time breakdown\nsystemd-analyze\n\n# Per-unit time\nsystemd-analyze blame | head -20\n\n# Dependency graph (saves SVG)\nsystemd-analyze dot --require | dot -Tsvg &gt; boot.svg\n\n# Critical chain to a unit\nsystemd-analyze critical-chain sshd.service\n</code></pre>"},{"location":"05-rhca/systemd-advanced/#next-step","title":"Next step","text":"<p>\u2192 systemd Hardening Knobs</p>"},{"location":"05-rhca/systemd-hardening/","title":"systemd Hardening Knobs \u2014 Service Sandboxing","text":"<p>systemd provides service-level sandboxing directives that limit what a service can do \u2014 without SELinux and without containers. These are defence-in-depth controls that reduce the blast radius if a service is compromised.</p>"},{"location":"05-rhca/systemd-hardening/#why-sandbox-services","title":"Why sandbox services?","text":"<p>Even with SELinux enforcing, reducing a service's capabilities at the systemd level provides an additional layer:</p> <ul> <li>Limits filesystem visibility</li> <li>Prevents privilege escalation</li> <li>Restricts system call surface</li> <li>Limits network access</li> </ul>"},{"location":"05-rhca/systemd-hardening/#key-hardening-directives","title":"Key hardening directives","text":""},{"location":"05-rhca/systemd-hardening/#user-and-group","title":"User and group","text":"<pre><code>[Service]\nUser=myapp\nGroup=myapp\n</code></pre> <p>Run the service as a dedicated unprivileged user, not root.</p>"},{"location":"05-rhca/systemd-hardening/#filesystem-restrictions","title":"Filesystem restrictions","text":"<pre><code>[Service]\n# Protect the entire filesystem as read-only\nProtectSystem=strict\n\n# Make /home and /root inaccessible\nProtectHome=true\n\n# Writable runtime directory (auto-created as /run/myapp)\nRuntimeDirectory=myapp\nRuntimeDirectoryMode=0750\n\n# Writable state directory (persistent, /var/lib/myapp)\nStateDirectory=myapp\n\n# Writable log directory (/var/log/myapp)\nLogsDirectory=myapp\n\n# Writable cache (/var/cache/myapp)\nCacheDirectory=myapp\n</code></pre> <p>With <code>ProtectSystem=strict</code> and the above directories, the service can only write to its designated directories.</p>"},{"location":"05-rhca/systemd-hardening/#private-tmp","title":"Private /tmp","text":"<pre><code>[Service]\nPrivateTmp=true\n</code></pre> <p>The service gets its own private <code>/tmp</code>. Files there are invisible to other services and cleaned up when the service stops.</p>"},{"location":"05-rhca/systemd-hardening/#network-restrictions","title":"Network restrictions","text":"<pre><code>[Service]\n# No network access at all\nPrivateNetwork=true\n\n# Or restrict to specific address families\nRestrictAddressFamilies=AF_INET AF_INET6\n\n# Block all socket creation\nRestrictAddressFamilies=none\n</code></pre>"},{"location":"05-rhca/systemd-hardening/#capabilities","title":"Capabilities","text":"<p>Linux capabilities break root's all-or-nothing model into granular permissions. Drop all capabilities and add only what's needed:</p> <pre><code>[Service]\n# Drop all capabilities\nCapabilityBoundingSet=\n\n# If the service needs to bind to port &lt; 1024:\nCapabilityBoundingSet=CAP_NET_BIND_SERVICE\nAmbientCapabilities=CAP_NET_BIND_SERVICE\n</code></pre>"},{"location":"05-rhca/systemd-hardening/#prevent-privilege-escalation","title":"Prevent privilege escalation","text":"<pre><code>[Service]\nNoNewPrivileges=true\n</code></pre> <p>Prevents <code>setuid</code> binaries and <code>execve</code> privilege escalation within the service.</p>"},{"location":"05-rhca/systemd-hardening/#system-call-filtering","title":"System call filtering","text":"<pre><code>[Service]\n# Allow only typical server syscalls\nSystemCallFilter=@system-service\nSystemCallErrorNumber=EPERM\n\n# Strict syscall sets:\nSystemCallFilter=@basic-io @file-system @network-io @process\n</code></pre> <p>Syscall groups (<code>@system-service</code>, <code>@basic-io</code>, etc.) are pre-defined sets. View available groups:</p> <pre><code>systemd-analyze syscall-filter\n</code></pre>"},{"location":"05-rhca/systemd-hardening/#protect-kernel-and-host-configuration","title":"Protect kernel and host configuration","text":"<pre><code>[Service]\nProtectKernelTunables=true    # /proc/sys, /sys read-only\nProtectKernelModules=true     # cannot load/unload kernel modules\nProtectKernelLogs=true        # cannot read kernel ring buffer\nProtectControlGroups=true     # cgroup filesystem read-only\nProtectClock=true             # cannot set system clock\nProtectHostname=true          # cannot change hostname\n</code></pre>"},{"location":"05-rhca/systemd-hardening/#full-example-hardened-web-service","title":"Full example: hardened web service","text":"<pre><code>[Unit]\nDescription=Hardened Web Service\nAfter=network-online.target\nWants=network-online.target\n\n[Service]\nType=simple\nUser=webapp\nGroup=webapp\nExecStart=/usr/local/bin/webapp --port 8080\n\n# Filesystem\nProtectSystem=strict\nProtectHome=true\nPrivateTmp=true\nStateDirectory=webapp\nLogsDirectory=webapp\n\n# Privileges\nNoNewPrivileges=true\nCapabilityBoundingSet=CAP_NET_BIND_SERVICE\nAmbientCapabilities=CAP_NET_BIND_SERVICE\n\n# Syscalls\nSystemCallFilter=@system-service\nSystemCallErrorNumber=EPERM\n\n# Kernel\nProtectKernelTunables=true\nProtectKernelModules=true\nProtectControlGroups=true\nProtectClock=true\nProtectHostname=true\n\n# Network\nRestrictAddressFamilies=AF_INET AF_INET6\n\nRestart=on-failure\nRestartSec=5s\n\n[Install]\nWantedBy=multi-user.target\n</code></pre>"},{"location":"05-rhca/systemd-hardening/#verify-restrictions-with-systemd-analyze-security","title":"Verify restrictions with systemd-analyze security","text":"<pre><code># Score a service (lower = more secure)\nsystemd-analyze security sshd.service\nsystemd-analyze security myapp.service\n</code></pre> <p>This gives a security score and a list of hardening improvements available.</p>"},{"location":"05-rhca/systemd-hardening/#next-step","title":"Next step","text":"<p>\u2192 Journald Retention and Forwarding</p>"},{"location":"05-rhca/troubleshooting-playbook/","title":"Troubleshooting Playbook \u2014 First 10 Minutes","text":"<p>When something breaks on a RHEL host, this playbook gives you a repeatable, ordered approach that finds the majority of problems efficiently.</p>"},{"location":"05-rhca/troubleshooting-playbook/#the-mindset","title":"The mindset","text":"<p>Observe before you change. Gather evidence, form a hypothesis, test one thing at a time, and document what you tried.</p> <p>Changing random settings hoping something fixes it wastes time and can make the problem worse.</p>"},{"location":"05-rhca/troubleshooting-playbook/#the-first-10-minutes-checklist","title":"The First 10 Minutes Checklist","text":""},{"location":"05-rhca/troubleshooting-playbook/#0-what-is-the-reported-symptom","title":"0 \u2014 What is the reported symptom?","text":"<ul> <li>What exactly is broken? (service unreachable / command fails / system slow)</li> <li>When did it start?</li> <li>What changed recently? (update, config edit, deploy, cron job)</li> </ul>"},{"location":"05-rhca/troubleshooting-playbook/#1-system-health-30-seconds","title":"1 \u2014 System health (30 seconds)","text":"<pre><code># System load and uptime\nuptime\n\n# Memory pressure\nfree -m\n\n# Disk space (full disk breaks many things silently)\ndf -h\n\n# Inode exhaustion (disk \"full\" but df shows space)\ndf -ih\n\n# Failed systemd units\nsystemctl --failed\n</code></pre> <p>If disk is full \u2192 find and clean large files first. If a unit failed \u2192 jump to Step 3.</p>"},{"location":"05-rhca/troubleshooting-playbook/#2-recent-changes-1-minute","title":"2 \u2014 Recent changes (1 minute)","text":"<pre><code># Last 50 messages across all services\njournalctl -n 50 -r\n\n# Since last hour\njournalctl --since \"1 hour ago\"\n\n# Package changes\nsudo dnf history | head -20\n\n# Recently modified files in /etc\nfind /etc -mtime -1 -type f 2&gt;/dev/null | sort\n</code></pre>"},{"location":"05-rhca/troubleshooting-playbook/#3-service-status-2-minutes","title":"3 \u2014 Service status (2 minutes)","text":"<pre><code># Is the service running?\nsudo systemctl status &lt;service-name&gt;\n\n# Detailed logs\njournalctl -u &lt;service-name&gt; -n 100\n\n# Did it fail to start?\njournalctl -u &lt;service-name&gt; -b | grep -i \"failed\\|error\\|denied\"\n</code></pre> <p>Common causes of service failures:</p> Error in log Likely cause <code>Permission denied</code> File/dir permissions or SELinux <code>avc: denied</code> SELinux AVC denial <code>Address already in use</code> Port conflict <code>No such file or directory</code> Missing binary, config, or data file <code>Failed to bind</code> Port blocked or SELinux port policy"},{"location":"05-rhca/troubleshooting-playbook/#4-selinux-check-1-minute","title":"4 \u2014 SELinux check (1 minute)","text":"<pre><code># Current mode\ngetenforce\n\n# Recent denials\nsudo ausearch -m avc -ts recent\n\n# Denials for specific process\nsudo ausearch -m avc -c httpd -ts today\n</code></pre> <p>If you see AVC denials \u2192 see SELinux Troubleshooting.</p> <p>Do NOT set setenforce 0 as a fix</p> <p>Use permissive mode for diagnosis only, immediately re-enable enforcing, and fix the root cause.</p>"},{"location":"05-rhca/troubleshooting-playbook/#5-network-connectivity-2-minutes","title":"5 \u2014 Network connectivity (2 minutes)","text":"<pre><code># Is the interface up?\nip link show\nip addr show\n\n# Can we reach the gateway?\nip route show\nping -c 2 $(ip route | awk '/default/{print $3}')\n\n# DNS working?\nping -c 2 8.8.8.8          # IP (bypass DNS)\nping -c 2 access.redhat.com # needs DNS\n\n# Is the service listening?\nss -tlnp | grep &lt;port&gt;\n\n# Firewall blocking?\nsudo firewall-cmd --list-all\n</code></pre>"},{"location":"05-rhca/troubleshooting-playbook/#6-the-dns-vs-routing-vs-firewall-decision-tree","title":"6 \u2014 The \"DNS vs routing vs firewall\" decision tree","text":"<pre><code>Can ping 8.8.8.8?\n  No  \u2192 routing/network problem \u2192 check ip route, gateway, interface\n  Yes \u2192 Can ping access.redhat.com?\n          No  \u2192 DNS problem \u2192 check resolvectl, /etc/resolv.conf\n          Yes \u2192 Can curl http://service/?\n                  No  \u2192 Is it listening? (ss -tlnp)\n                          No  \u2192 service not running\n                          Yes \u2192 firewall blocking? (firewall-cmd --list-all)\n                                  Still blocked? \u2192 SELinux? (ausearch -m avc)\n</code></pre>"},{"location":"05-rhca/troubleshooting-playbook/#7-resource-exhaustion-1-minute","title":"7 \u2014 Resource exhaustion (1 minute)","text":"<pre><code># CPU and memory hogs\ntop -b -n 1 | head -30\n\n# Or htop if installed\nhtop\n\n# I/O wait\niostat -x 1 3   # install: sudo dnf install -y sysstat\n\n# Open file limits\nulimit -n\ncat /proc/sys/fs/file-max\n</code></pre>"},{"location":"05-rhca/troubleshooting-playbook/#8-log-files-catch-all","title":"8 \u2014 Log files (catch-all)","text":"<pre><code># Everything since last boot\njournalctl -b | grep -iE \"error|fail|denied|refused\" | tail -50\n\n# Auth failures\nsudo grep \"Failed\\|Invalid\" /var/log/secure | tail -20\n\n# Kernel ring buffer\ndmesg | tail -30\ndmesg | grep -iE \"error|fail|oom\"\n</code></pre>"},{"location":"05-rhca/troubleshooting-playbook/#escalation-checklist","title":"Escalation checklist","text":"<p>Before escalating, document:</p> <ol> <li>The exact error message</li> <li>Steps you already tried</li> <li>Output of: <code>journalctl -u &lt;service&gt; -n 100</code></li> <li>Output of: <code>systemctl status &lt;service&gt;</code></li> <li>Output of: <code>ausearch -m avc -ts recent</code></li> <li>Output of: <code>df -h</code>, <code>free -m</code>, <code>ip addr</code>, <code>firewall-cmd --list-all</code></li> </ol>"},{"location":"05-rhca/troubleshooting-playbook/#next-step","title":"Next step","text":"<p>\u2192 Advanced systemd</p>"},{"location":"05-rhca/containers/podman-fundamentals/","title":"Container Fundamentals \u2014 RHEL View","text":"<p>Containers on RHEL use Podman \u2014 an OCI-compatible, daemonless container engine that is the Red Hat default. Unlike Docker, Podman does not require a root daemon.</p>"},{"location":"05-rhca/containers/podman-fundamentals/#key-concepts","title":"Key concepts","text":"Term Meaning OCI Open Container Initiative \u2014 standard for images and runtimes image Read-only template for a container (layers of filesystem changes) container Running instance of an image registry Server hosting images (quay.io, registry.access.redhat.com, docker.io) tag Version label for an image (e.g., <code>nginx:latest</code>, <code>nginx:1.25</code>) digest Immutable SHA-256 identifier for an image rootless Container running without root privileges (default on RHEL)"},{"location":"05-rhca/containers/podman-fundamentals/#podman-vs-docker","title":"Podman vs Docker","text":"Feature Podman Docker Root daemon required No Yes (dockerd) Rootless containers Native Requires configuration systemd integration Native (<code>podman generate systemd</code>) Via restart policies SELinux integration Native Extra config required Drop-in replacement Nearly full CLI compatibility \u2014 RHEL support Fully supported Not in RHEL repos"},{"location":"05-rhca/containers/podman-fundamentals/#install-podman-on-rhel-10","title":"Install Podman on RHEL 10","text":"<pre><code>sudo dnf install -y podman\npodman --version\n</code></pre>"},{"location":"05-rhca/containers/podman-fundamentals/#basic-workflow","title":"Basic workflow","text":"<pre><code># Search for an image\npodman search nginx\n\n# Pull an image\npodman pull docker.io/library/nginx:latest\n\n# List local images\npodman images\n\n# Run a container\npodman run -d --name webtest -p 8080:80 nginx:latest\n\n# List running containers\npodman ps\n\n# List all containers (including stopped)\npodman ps -a\n\n# View logs\npodman logs webtest\n\n# Execute a command inside a running container\npodman exec -it webtest bash\n\n# Stop and remove\npodman stop webtest\npodman rm webtest\n</code></pre>"},{"location":"05-rhca/containers/podman-fundamentals/#image-management","title":"Image management","text":"<pre><code># Pull from Red Hat's registry (preferred on RHEL)\npodman pull registry.access.redhat.com/ubi9/ubi\n\n# Inspect image layers and metadata\npodman inspect nginx:latest\n\n# Remove an image\npodman rmi nginx:latest\n\n# Remove all unused images\npodman image prune\n\n# Tag an image\npodman tag nginx:latest myregistry.example.com/nginx:1.0\n</code></pre>"},{"location":"05-rhca/containers/podman-fundamentals/#registry-configuration","title":"Registry configuration","text":"<p>RHEL configures trusted registries in <code>/etc/containers/registries.conf</code>:</p> <pre><code>cat /etc/containers/registries.conf\n</code></pre> <p>You can add unqualified search registries:</p> <pre><code># /etc/containers/registries.conf.d/myregs.conf\n[[registry]]\nprefix = \"myapp\"\nlocation = \"myregistry.example.com\"\n</code></pre>"},{"location":"05-rhca/containers/podman-fundamentals/#rootless-vs-rootful","title":"Rootless vs rootful","text":"Feature Rootless Rootful Runs as Your UID root Port binding &lt; 1024 Not allowed Allowed Host filesystem access Mapped UIDs Direct SELinux considerations User namespace mapping Standard labels <p>Use rootless by default. Switch to rootful (<code>sudo podman</code>) only when specifically required (e.g., port 80 binding, direct device access).</p>"},{"location":"05-rhca/containers/podman-fundamentals/#next-step","title":"Next step","text":"<p>\u2192 Rootless Podman</p>"},{"location":"05-rhca/containers/rootless/","title":"Rootless Podman \u2014 Storage and Networking","text":"<p>Rootless containers are Podman's default and the correct way to run containers on RHEL for most workloads.</p>"},{"location":"05-rhca/containers/rootless/#how-rootless-works","title":"How rootless works","text":"<p>Rootless containers use user namespaces: your UID is mapped to a range of sub-UIDs inside the container, so root inside the container is not root on the host.</p> <pre><code># Check your sub-UID and sub-GID mappings\ncat /etc/subuid | grep $(whoami)\ncat /etc/subgid | grep $(whoami)\n</code></pre> <p>Expected: <code>rhel:100000:65536</code> \u2014 65536 UIDs mapped starting at 100000.</p> <p>If missing (fresh system):</p> <pre><code>sudo usermod --add-subuids 100000-165535 --add-subgids 100000-165535 $(whoami)\n</code></pre>"},{"location":"05-rhca/containers/rootless/#user-namespace-mapping-in-practice","title":"User namespace mapping in practice","text":"<p>Inside a rootless container:</p> <pre><code>podman run --rm alpine id\n# uid=0(root) gid=0(root) \u2014 root INSIDE the container\n</code></pre> <p>On the host:</p> <pre><code>ps aux | grep \"podman\\|alpine\"\n# Process runs as YOUR UID \u2014 not root\n</code></pre> <p>Files created by root inside the container appear owned by a high UID on the host:</p> <pre><code>podman run --rm -v /tmp/test:/data:Z alpine sh -c \"echo hello &gt; /data/file\"\nls -ln /tmp/test/file\n# -rw-r--r-- 1 100000 100000 ...   (mapped UID on host)\n</code></pre>"},{"location":"05-rhca/containers/rootless/#rootless-storage","title":"Rootless storage","text":"<p>Rootless containers store images and container data in:</p> <pre><code>~/.local/share/containers/storage/\n</code></pre> <pre><code># View storage usage\npodman system df\n\n# Inspect storage driver\npodman info | grep -A5 store\n</code></pre> <p>RHEL defaults to the overlay storage driver using <code>~/.local/share/containers/</code>.</p>"},{"location":"05-rhca/containers/rootless/#rootless-networking","title":"Rootless networking","text":"<p>Rootless containers use pasta (RHEL 10) or slirp4netns for network namespacing, which provides NAT-like networking.</p> <pre><code># Run container with port mapping\npodman run -d --name web -p 8080:80 nginx:latest\n\n# Test\ncurl http://localhost:8080/\n</code></pre> <p>Ports below 1024</p> <p>Rootless containers cannot bind to ports below 1024 on the host. Use a port above 1024 (e.g., 8080) or set a redirect in firewalld.</p>"},{"location":"05-rhca/containers/rootless/#port-redirect-for-port-80-8080","title":"Port redirect for port 80 \u2192 8080","text":"<pre><code>sudo firewall-cmd --permanent \\\n  --add-forward-port=port=80:proto=tcp:toport=8080:toaddr=127.0.0.1\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"05-rhca/containers/rootless/#rootless-networking-container-to-container","title":"Rootless networking: container-to-container","text":"<p>Containers in the same pod or network can communicate:</p> <pre><code># Create a custom network\npodman network create mynet\n\n# Run containers on it\npodman run -d --name app --network mynet myapp:latest\npodman run -d --name db --network mynet mariadb:latest\n\n# Container 'app' can reach 'db' by hostname\npodman exec app ping -c 2 db\n</code></pre>"},{"location":"05-rhca/containers/rootless/#inspect-networking","title":"Inspect networking","text":"<pre><code># List networks\npodman network ls\n\n# Inspect a network\npodman network inspect mynet\n\n# Show container IP\npodman inspect web | grep IPAddress\n\n# Show port mappings\npodman port web\n</code></pre>"},{"location":"05-rhca/containers/rootless/#clean-up-rootless-storage","title":"Clean up rootless storage","text":"<pre><code># Remove stopped containers\npodman container prune\n\n# Remove unused images\npodman image prune\n\n# Remove everything unused (containers, images, volumes, networks)\npodman system prune\n\n# Full reset (dangerous \u2014 removes everything)\npodman system reset\n</code></pre>"},{"location":"05-rhca/containers/rootless/#next-step","title":"Next step","text":"<p>\u2192 Volumes and Persistent Data</p>"},{"location":"05-rhca/containers/secrets/","title":"Podman Secrets \u2014 Create, Use, Rotate","text":"<p>Podman secrets let you inject sensitive data (passwords, API keys, TLS certs) into containers at runtime without:</p> <ul> <li>Hardcoding values in <code>podman run</code> commands</li> <li>Storing credentials in environment variables (visible via <code>podman inspect</code>)</li> <li>Committing secrets to image layers</li> </ul>"},{"location":"05-rhca/containers/secrets/#how-secrets-work","title":"How secrets work","text":"<p>A secret is stored in Podman's secret store and mounted into the container as a file at <code>/run/secrets/&lt;name&gt;</code>. The application reads it from disk.</p> <pre><code>host secret store \u2192 container /run/secrets/db_password (read-only file)\n</code></pre>"},{"location":"05-rhca/containers/secrets/#create-secrets","title":"Create secrets","text":"<pre><code># From stdin (recommended \u2014 doesn't leave secret in shell history)\nprintf 'supersecretpassword' | podman secret create db_password -\n\n# From a file\npodman secret create db_password /path/to/password.txt\n\n# From an environment variable (use carefully \u2014 may appear in process list)\necho \"${DB_PASSWORD}\" | podman secret create db_password -\n</code></pre>"},{"location":"05-rhca/containers/secrets/#list-and-inspect-secrets","title":"List and inspect secrets","text":"<pre><code># List secrets (metadata only \u2014 values are never shown)\npodman secret ls\n\n# Inspect (metadata only)\npodman secret inspect db_password\n</code></pre> <p>The secret value is never returned by <code>podman secret inspect</code>.</p>"},{"location":"05-rhca/containers/secrets/#use-a-secret-in-a-container","title":"Use a secret in a container","text":"<pre><code>podman run -d \\\n  --name myapp \\\n  --secret db_password \\\n  myapp:latest\n</code></pre> <p>Inside the container, the secret is available as:</p> <pre><code>/run/secrets/db_password\n</code></pre> <p>Read it in your application:</p> <pre><code># Shell example\nDB_PASS=$(cat /run/secrets/db_password)\n\n# Python\nwith open('/run/secrets/db_password') as f:\n    db_pass = f.read().strip()\n</code></pre>"},{"location":"05-rhca/containers/secrets/#mount-with-a-custom-path-or-name","title":"Mount with a custom path or name","text":"<pre><code>podman run -d \\\n  --name myapp \\\n  --secret db_password,target=/etc/myapp/db.passwd,mode=0400 \\\n  myapp:latest\n</code></pre>"},{"location":"05-rhca/containers/secrets/#multiple-secrets","title":"Multiple secrets","text":"<pre><code>printf 'mydbpassword' | podman secret create db_password -\nprintf 'myapikey12345' | podman secret create api_key -\n\npodman run -d \\\n  --name myapp \\\n  --secret db_password \\\n  --secret api_key \\\n  myapp:latest\n</code></pre>"},{"location":"05-rhca/containers/secrets/#secret-rotation-pattern","title":"Secret rotation pattern","text":"<p>Podman does not support updating a secret in-place. The correct pattern:</p> <pre><code># 1. Create the new version\nprintf 'newsecretpassword' | podman secret create db_password_v2 -\n\n# 2. Stop and remove the old container\npodman stop myapp\npodman rm myapp\n\n# 3. Start a new container with the new secret\npodman run -d \\\n  --name myapp \\\n  --secret db_password_v2,target=db_password \\\n  myapp:latest\n\n# 4. Verify the app is working with the new secret\ncurl http://localhost:8080/health\n\n# 5. Remove the old secret\npodman secret rm db_password\n</code></pre> <p>Rotation with zero-downtime</p> <p>For zero-downtime rotation, start the new container first (different name or port), verify it works, switch traffic (e.g., update firewall redirect), then stop the old container.</p>"},{"location":"05-rhca/containers/secrets/#secrets-with-systemd-managed-containers","title":"Secrets with systemd-managed containers","text":"<p>Never put secrets in systemd unit environment variables \u2014 they appear in <code>systemctl show</code> and <code>ps -e</code>.</p> <p>Instead, mount the secret in the container unit:</p> <pre><code>[Service]\nExecStart=podman run \\\n  --name myapp \\\n  --secret db_password \\\n  --replace \\\n  myapp:latest\n</code></pre> <p>The secret value stays inside Podman's store, never in the unit file.</p>"},{"location":"05-rhca/containers/secrets/#rootless-secret-storage-location","title":"Rootless secret storage location","text":"<pre><code># Rootless: secrets stored per-user\nls ~/.local/share/containers/storage/secrets/\n</code></pre> <p>The secret data is stored on disk (not in memory). File permissions restrict access to your UID. On a shared host, each user's secrets are isolated.</p>"},{"location":"05-rhca/containers/secrets/#security-considerations","title":"Security considerations","text":"Practice Why Never use <code>--env</code> for passwords Visible in <code>podman inspect</code> and <code>ps -e</code> Use <code>mode=0400</code> on secret mounts Application should only read, not write Rotate secrets regularly Limits exposure window if compromised Use <code>printf</code> not <code>echo</code> for creating secrets <code>echo</code> adds a trailing newline Remove old secrets after rotation No stale credentials in the store"},{"location":"05-rhca/containers/secrets/#next-step","title":"Next step","text":"<p>\u2192 systemd-Managed Containers</p>"},{"location":"05-rhca/containers/selinux-containers/","title":"Container SELinux Gotchas \u2014 Volumes and Labels","text":"<p>SELinux and containers interact in specific ways that trip up even experienced admins. This chapter covers the common pitfalls and their correct fixes.</p>"},{"location":"05-rhca/containers/selinux-containers/#the-core-issue-volume-mount-labeling","title":"The core issue: volume mount labeling","text":"<p>When you bind-mount a host directory into a container, the container process needs the correct SELinux type to access it. By default, a host directory has a type appropriate for the host \u2014 not for a container process.</p>"},{"location":"05-rhca/containers/selinux-containers/#the-z-and-z-volume-options","title":"The <code>:Z</code> and <code>:z</code> volume options","text":"Option Meaning When to use <code>:Z</code> Relabel the directory for this container (private) One container uses this directory <code>:z</code> Relabel as shared (multiple containers can access) Multiple containers share the directory (none) No relabeling \u2014 usually causes permission denied When the context is already correct <pre><code># Private (one container)\npodman run -d -v ~/myapp/data:/app/data:Z myapp:latest\n\n# Shared (multiple containers)\npodman run -d -v ~/shared/uploads:/uploads:z app1:latest\npodman run -d -v ~/shared/uploads:/uploads:z app2:latest\n</code></pre> <p>Never use :Z on home directories or system paths</p> <p>Using <code>:Z</code> on <code>~</code> or <code>/etc</code> will relabel those paths to a container context, breaking host access. Always bind-mount subdirectories. <pre><code># BAD \u2014 relabels entire home directory\npodman run -v ~:/home/user:Z myapp:latest\n\n# GOOD \u2014 relabels only the specific subdirectory\npodman run -v ~/myapp/data:/home/user/data:Z myapp:latest\n</code></pre></p>"},{"location":"05-rhca/containers/selinux-containers/#what-z-actually-does","title":"What <code>:Z</code> actually does","text":"<p><code>:Z</code> runs <code>chcon -t container_file_t</code> on the host directory.</p> <pre><code># Before :Z\nls -Z ~/myapp/data/\n# user_u:object_r:user_home_t:s0\n\n# Run with :Z\npodman run --rm -v ~/myapp/data:/data:Z ubi9 ls /data\n\n# After :Z\nls -Z ~/myapp/data/\n# system_u:object_r:container_file_t:s0:c123,c456\n</code></pre> <p>The MCS categories (<code>c123,c456</code>) are unique per container \u2014 preventing container-to-container access at the SELinux level.</p>"},{"location":"05-rhca/containers/selinux-containers/#named-volumes-and-selinux","title":"Named volumes and SELinux","text":"<p>Named volumes (managed by Podman) already have the correct SELinux context:</p> <pre><code>podman volume create mydata\npodman volume inspect mydata | grep -i mountpoint\n\nls -Z $(podman volume inspect mydata --format '{{.Mountpoint}}')\n# system_u:object_r:container_file_t:s0\n</code></pre> <p>No <code>:Z</code> needed for named volumes \u2014 they're already labeled correctly.</p>"},{"location":"05-rhca/containers/selinux-containers/#port-binding-and-selinux","title":"Port binding and SELinux","text":"<p>Rootless container port binding above 1024 does not require SELinux policy changes. For rootful containers on non-standard ports:</p> <pre><code># Check what types are allowed for container port binding\nsudo semanage port -l | grep container\n\n# Add a port if needed (rootful containers)\nsudo semanage port -a -t container_port_t -p tcp 9090\n</code></pre>"},{"location":"05-rhca/containers/selinux-containers/#common-avc-denials-with-containers","title":"Common AVC denials with containers","text":""},{"location":"05-rhca/containers/selinux-containers/#permission-denied-on-volume-mount","title":"\"Permission denied\" on volume mount","text":"<pre><code>sudo ausearch -m avc -c \"container\" -ts recent\n</code></pre> <p>Look for: <code>denied { read }</code> or <code>denied { write }</code> with <code>scontext=container_t</code> and <code>tcontext=user_home_t</code> (or similar non-container type).</p> <p>Fix: Add <code>:Z</code> to the volume mount, or relabel manually:</p> <pre><code>sudo chcon -Rt container_file_t ~/myapp/data/\n</code></pre>"},{"location":"05-rhca/containers/selinux-containers/#container-cant-listen-on-port","title":"\"Container can't listen on port\"","text":"<p>Rootless: not an SELinux issue \u2014 it's a kernel restriction. Use port &gt; 1024.</p> <p>Rootful with non-standard port:</p> <pre><code>sudo semanage port -a -t container_port_t -p tcp &lt;port&gt;\n</code></pre>"},{"location":"05-rhca/containers/selinux-containers/#restoring-labels-after-z","title":"Restoring labels after <code>:Z</code>","text":"<p>If <code>:Z</code> labeled a path incorrectly, restore the original context:</p> <pre><code>sudo restorecon -Rv ~/myapp/data/\n</code></pre>"},{"location":"05-rhca/containers/selinux-containers/#audit-containers-specifically","title":"Audit containers specifically","text":"<pre><code># AVCs from container processes\nsudo ausearch -m avc -c \"container\" -ts today\n\n# Specific container by name (get PID first)\npodman inspect myapp | grep Pid\nsudo ausearch -m avc --pid &lt;pid&gt; -ts today\n</code></pre>"},{"location":"05-rhca/containers/selinux-containers/#next-step","title":"Next step","text":"<p>\u2192 Lab: Run Rootless Web App + Persistent Data</p>"},{"location":"05-rhca/containers/systemd-integration/","title":"systemd-Managed Containers","text":"<p>Running containers as systemd services gives you automatic start on boot, restart on failure, journald logging, and integration with <code>systemctl</code>.</p>"},{"location":"05-rhca/containers/systemd-integration/#two-approaches","title":"Two approaches","text":"Approach How Best for Quadlet (RHEL 10 default) Declare containers in <code>.container</code> unit files New workloads \u2014 simpler, declarative podman generate systemd Generate unit file from a running container Migrating existing containers"},{"location":"05-rhca/containers/systemd-integration/#approach-1-quadlet-recommended-for-rhel-10","title":"Approach 1: Quadlet (recommended for RHEL 10)","text":"<p>Quadlet is a systemd generator built into Podman. You write a <code>.container</code> file and systemd handles the rest.</p>"},{"location":"05-rhca/containers/systemd-integration/#rootless-container-user-service","title":"Rootless container (user service)","text":"<pre><code>mkdir -p ~/.config/containers/systemd/\n</code></pre> <pre><code># ~/.config/containers/systemd/webserver.container\n\n[Unit]\nDescription=My Web Server\nAfter=network-online.target\n\n[Container]\nImage=nginx:latest\nPublishPort=8080:80\nVolume=%h/webdata:/usr/share/nginx/html:Z\nSecret=tls_cert,target=/etc/nginx/certs/tls.crt\nEnvironment=NGINX_HOST=localhost\nLabel=app=webserver\n\n[Service]\nRestart=always\nTimeoutStartSec=30\n\n[Install]\nWantedBy=default.target\n</code></pre> <pre><code># Reload systemd user daemon to pick up new file\nsystemctl --user daemon-reload\n\n# Start and enable the service\nsystemctl --user enable --now webserver.service\n\n# Check status\nsystemctl --user status webserver.service\n\n# View logs\njournalctl --user -u webserver.service -f\n</code></pre>"},{"location":"05-rhca/containers/systemd-integration/#system-level-rootful-container","title":"System-level rootful container","text":"<pre><code>sudo mkdir -p /etc/containers/systemd/\nsudo vim /etc/containers/systemd/nginx.container\n</code></pre> <pre><code>[Unit]\nDescription=Nginx Web Server\nAfter=network-online.target\n\n[Container]\nImage=docker.io/library/nginx:latest\nPublishPort=80:80\nVolume=/srv/webroot:/usr/share/nginx/html:Z\nSecret=tls_cert\n\n[Service]\nRestart=on-failure\n\n[Install]\nWantedBy=multi-user.target\n</code></pre> <pre><code>sudo systemctl daemon-reload\nsudo systemctl enable --now nginx.service\n</code></pre>"},{"location":"05-rhca/containers/systemd-integration/#quadlet-file-types","title":"Quadlet file types","text":"Extension What it manages <code>.container</code> A container <code>.volume</code> A named volume <code>.network</code> A Podman network <code>.pod</code> A Podman pod <code>.image</code> An image pull"},{"location":"05-rhca/containers/systemd-integration/#manage-a-volume-with-quadlet","title":"Manage a volume with Quadlet","text":"<pre><code># ~/.config/containers/systemd/dbdata.volume\n[Volume]\nLabel=app=mydb\n</code></pre> <p>Reference it in the container file:</p> <pre><code>Volume=dbdata.volume:/var/lib/mysql:Z\n</code></pre>"},{"location":"05-rhca/containers/systemd-integration/#approach-2-podman-generate-systemd","title":"Approach 2: podman generate systemd","text":"<p>For containers already running, generate a unit file:</p> <pre><code># Generate for an existing container\npodman generate systemd --new --name myapp &gt; myapp.service\n\n# Review the file\ncat myapp.service\n\n# Install it\ncp myapp.service ~/.config/systemd/user/\nsystemctl --user daemon-reload\nsystemctl --user enable --now myapp.service\n</code></pre> <p><code>--new</code> regenerates the container from scratch on each start (preferred over starting an existing container).</p>"},{"location":"05-rhca/containers/systemd-integration/#auto-update-with-podman","title":"Auto-update with Podman","text":"<p>Podman can automatically update containers to newer image digests:</p> <pre><code># In the .container file:\n[Container]\nAutoUpdate=registry\n</code></pre> <pre><code># Enable the auto-update timer\nsystemctl --user enable --now podman-auto-update.timer\n\n# Or trigger manually\npodman auto-update\n</code></pre>"},{"location":"05-rhca/containers/systemd-integration/#lingering-keep-user-services-running-after-logout","title":"Lingering (keep user services running after logout)","text":"<p>By default, user services stop when you log out. Enable lingering for services that must run 24/7:</p> <pre><code>sudo loginctl enable-linger $(whoami)\n</code></pre>"},{"location":"05-rhca/containers/systemd-integration/#next-step","title":"Next step","text":"<p>\u2192 Container SELinux Gotchas</p>"},{"location":"05-rhca/containers/volumes/","title":"Volumes and Persistent Data","text":"<p>Containers are ephemeral by default. Volumes and bind mounts let you persist data across container restarts and replacements.</p>"},{"location":"05-rhca/containers/volumes/#types-of-storage-in-podman","title":"Types of storage in Podman","text":"Type Description Use case Named volume Managed by Podman, stored in container storage Databases, app state Bind mount A host path mapped into the container Config files, logs, dev mounts tmpfs In-memory, cleared on container stop Sensitive temp data"},{"location":"05-rhca/containers/volumes/#named-volumes","title":"Named volumes","text":"<pre><code># Create a volume\npodman volume create mydata\n\n# List volumes\npodman volume ls\n\n# Inspect\npodman volume inspect mydata\n\n# Use in a container\npodman run -d --name db \\\n  -v mydata:/var/lib/mysql:Z \\\n  -e MYSQL_ROOT_PASSWORD=secret \\\n  mariadb:latest\n\n# Remove a volume (only if not in use)\npodman volume rm mydata\n\n# Remove all unused volumes\npodman volume prune\n</code></pre>"},{"location":"05-rhca/containers/volumes/#bind-mounts","title":"Bind mounts","text":"<pre><code># Mount a host directory into the container\nmkdir -p ~/myapp/data\n\npodman run -d --name app \\\n  -v ~/myapp/data:/app/data:Z \\\n  myapp:latest\n</code></pre> <p>The <code>:Z</code> suffix is critical on RHEL \u2014 it sets the correct SELinux context on the host directory for the container to access it. See Container SELinux Gotchas.</p>"},{"location":"05-rhca/containers/volumes/#mount-options","title":"Mount options","text":"Option Meaning <code>:ro</code> Read-only <code>:rw</code> Read-write (default) <code>:Z</code> Relabel for SELinux (private, this container only) <code>:z</code> Relabel for SELinux (shared, multiple containers) <code>:U</code> Map UID/GID to container user"},{"location":"05-rhca/containers/volumes/#tmpfs-mounts","title":"tmpfs mounts","text":"<pre><code>podman run -d --name app \\\n  --tmpfs /app/tmp:rw,size=64m \\\n  myapp:latest\n</code></pre> <p>Contents of <code>/app/tmp</code> inside the container are in RAM and never touch disk. Use for sensitive temporary files.</p>"},{"location":"05-rhca/containers/volumes/#data-persistence-patterns","title":"Data persistence patterns","text":""},{"location":"05-rhca/containers/volumes/#pattern-1-named-volume-for-stateful-service","title":"Pattern 1: Named volume for stateful service","text":"<pre><code>podman volume create pgdata\n\npodman run -d \\\n  --name postgres \\\n  -v pgdata:/var/lib/postgresql/data:Z \\\n  -e POSTGRES_PASSWORD=secret \\\n  -p 5432:5432 \\\n  postgres:16\n\n# Update: stop, remove container, start new version \u2014 data survives\npodman stop postgres\npodman rm postgres\npodman run -d \\\n  --name postgres \\\n  -v pgdata:/var/lib/postgresql/data:Z \\\n  -e POSTGRES_PASSWORD=secret \\\n  -p 5432:5432 \\\n  postgres:16.1\n</code></pre>"},{"location":"05-rhca/containers/volumes/#pattern-2-bind-mount-for-config-injection","title":"Pattern 2: Bind mount for config injection","text":"<pre><code>mkdir -p ~/nginx/conf\ncat &gt; ~/nginx/conf/nginx.conf &lt;&lt; 'EOF'\nevents {}\nhttp {\n  server {\n    listen 8080;\n    location / { root /usr/share/nginx/html; }\n  }\n}\nEOF\n\npodman run -d \\\n  --name nginx \\\n  -v ~/nginx/conf/nginx.conf:/etc/nginx/nginx.conf:ro,Z \\\n  -p 8080:8080 \\\n  nginx:latest\n</code></pre>"},{"location":"05-rhca/containers/volumes/#backup-and-restore-volumes","title":"Backup and restore volumes","text":"<pre><code># Backup a named volume\npodman run --rm \\\n  -v mydata:/data:ro,Z \\\n  -v $(pwd):/backup:Z \\\n  ubi9 tar czf /backup/mydata-backup.tar.gz -C /data .\n\n# Restore a volume\npodman run --rm \\\n  -v mydata:/data:Z \\\n  -v $(pwd):/backup:ro,Z \\\n  ubi9 tar xzf /backup/mydata-backup.tar.gz -C /data\n</code></pre>"},{"location":"05-rhca/containers/volumes/#next-step","title":"Next step","text":"<p>\u2192 Podman Secrets</p>"},{"location":"05-rhca/containers/labs/rootless-web/","title":"Lab - Rootless Web Server with Quadlet","text":""},{"location":"05-rhca/containers/labs/rootless-web/#overview","title":"Overview","text":"<p>Deploy a rootless Nginx container as a persistent systemd user service using a Quadlet <code>.container</code> file. The service survives reboots, restarts on failure, and runs entirely without root privileges.</p> <p>Track: D \u2014 RHCA-style Estimated time: 45 minutes Difficulty: Intermediate</p>"},{"location":"05-rhca/containers/labs/rootless-web/#prerequisites","title":"Prerequisites","text":"<ul> <li>RHEL 10 host with Podman installed</li> <li>A non-root user account (<code>student</code> used throughout)</li> <li>Lingering enabled for the user (<code>loginctl enable-linger student</code>)</li> <li>Port 8080 accessible (firewalld rule added in lab steps)</li> </ul>"},{"location":"05-rhca/containers/labs/rootless-web/#success-criteria","title":"Success Criteria","text":"<ul> <li>[ ] Quadlet <code>.container</code> file created under <code>~/.config/containers/systemd/</code></li> <li>[ ] <code>systemctl --user status nginx-web</code> shows active (running)</li> <li>[ ] <code>curl http://localhost:8080</code> returns the default Nginx page</li> <li>[ ] Service auto-starts after a full reboot (no manual intervention)</li> <li>[ ] Container runs as the student UID (verified with <code>podman ps</code>)</li> </ul>"},{"location":"05-rhca/containers/labs/rootless-web/#background","title":"Background","text":"<p>Quadlet is the RHEL 10\u2013preferred way to run containers under systemd. You write a declarative <code>.container</code> file; systemd generates the unit at runtime. This replaces the older <code>podman generate systemd</code> workflow.</p> <p>Key path: <code>~/.config/containers/systemd/</code> for user-scoped services (no root needed).</p>"},{"location":"05-rhca/containers/labs/rootless-web/#steps","title":"Steps","text":""},{"location":"05-rhca/containers/labs/rootless-web/#1-prepare-the-user-environment","title":"1 \u2014 Prepare the user environment","text":"<pre><code># Confirm linger is on (service must survive logout)\n$ loginctl show-user student | grep Linger\nLinger=yes\n\n# Enable it if not already set\n$ sudo loginctl enable-linger student\n\n# Confirm the XDG_RUNTIME_DIR is available\n$ echo $XDG_RUNTIME_DIR\n/run/user/1000\n</code></pre> <p>Why linger? Without it, systemd user services are stopped when the last session for that user ends.</p>"},{"location":"05-rhca/containers/labs/rootless-web/#2-create-the-content-directory-and-a-custom-index-page","title":"2 \u2014 Create the content directory and a custom index page","text":"<pre><code>$ mkdir -p ~/nginx-web/html\n\n$ cat &gt; ~/nginx-web/html/index.html &lt;&lt;'EOF'\n&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;RHCA Lab - Rootless Nginx&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;Running rootless on RHEL 10&lt;/h1&gt;\n  &lt;p&gt;Served by Nginx in a Podman container, managed by systemd via Quadlet.&lt;/p&gt;\n&lt;/body&gt;\n&lt;/html&gt;\nEOF\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#3-pull-the-image-first-optional-but-recommended","title":"3 \u2014 Pull the image first (optional but recommended)","text":"<pre><code>$ podman pull docker.io/library/nginx:stable-alpine\n\n# Verify the image is cached locally\n$ podman images | grep nginx\ndocker.io/library/nginx  stable-alpine  ...\n</code></pre> <p>Pre-pulling avoids a long first-start delay when systemd activates the service.</p>"},{"location":"05-rhca/containers/labs/rootless-web/#4-create-the-quadlet-container-file","title":"4 \u2014 Create the Quadlet <code>.container</code> file","text":"<pre><code>$ mkdir -p ~/.config/containers/systemd\n\n$ cat &gt; ~/.config/containers/systemd/nginx-web.container &lt;&lt;'EOF'\n[Unit]\nDescription=Rootless Nginx web server (RHCA lab)\nAfter=network-online.target\n\n[Container]\nImage=docker.io/library/nginx:stable-alpine\nPublishPort=8080:80\nVolume=%h/nginx-web/html:/usr/share/nginx/html:Z,ro\nEnvironment=NGINX_ENTRYPOINT_QUIET_LOGS=1\n\n[Service]\nRestart=always\nTimeoutStartSec=60\n\n[Install]\nWantedBy=default.target\nEOF\n</code></pre> <p>Key annotations:</p> Directive Meaning <code>PublishPort=8080:80</code> Map host port 8080 \u2192 container port 80 <code>Volume=...:Z,ro</code> Mount content read-only; <code>:Z</code> sets private SELinux label <code>%h</code> Systemd specifier that expands to the user's home directory <code>Restart=always</code> Restart on any exit (crash, OOM, etc.)"},{"location":"05-rhca/containers/labs/rootless-web/#5-reload-systemd-and-start-the-service","title":"5 \u2014 Reload systemd and start the service","text":"<pre><code># Tell systemd to pick up the new Quadlet file\n$ systemctl --user daemon-reload\n\n# Confirm the generated unit was created\n$ systemctl --user list-unit-files | grep nginx-web\nnginx-web.service  generated\n\n# Start the service\n$ systemctl --user start nginx-web\n\n# Verify it is running\n$ systemctl --user status nginx-web\n\u25cf nginx-web.service - Rootless Nginx web server (RHCA lab)\n     Loaded: loaded (/home/student/.config/containers/systemd/nginx-web.container; generated)\n     Active: active (running) since ...\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#6-open-the-firewall-port","title":"6 \u2014 Open the firewall port","text":"<pre><code># Allow inbound traffic on port 8080\n$ sudo firewall-cmd --add-port=8080/tcp --permanent\n$ sudo firewall-cmd --reload\n\n# Confirm the rule\n$ sudo firewall-cmd --list-ports | grep 8080\n8080/tcp\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#7-test-from-the-host","title":"7 \u2014 Test from the host","text":"<pre><code>$ curl -s http://localhost:8080 | grep -o '&lt;h1&gt;.*&lt;/h1&gt;'\n&lt;h1&gt;Running rootless on RHEL 10&lt;/h1&gt;\n\n# Also test from a remote machine (replace IP accordingly)\n$ curl -s http://192.168.122.10:8080 | grep -o '&lt;h1&gt;.*&lt;/h1&gt;'\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#8-enable-auto-start-and-reboot-test","title":"8 \u2014 Enable auto-start and reboot test","text":"<pre><code># The [Install] section already sets WantedBy=default.target\n# Enable the service so it starts at login/boot\n$ systemctl --user enable nginx-web\n\n# Reboot\n$ sudo reboot\n</code></pre> <p>After reboot, log back in and verify:</p> <pre><code>$ systemctl --user status nginx-web\nActive: active (running) ...\n\n$ curl -s http://localhost:8080 | grep '&lt;h1&gt;'\n&lt;h1&gt;Running rootless on RHEL 10&lt;/h1&gt;\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#verify-checkpoints","title":"Verify Checkpoints","text":"<pre><code># 1. Container is rootless (UID matches your user)\n$ podman ps --format \"{{.Names}} {{.Status}}\"\nsystemd-nginx-web  Up X minutes\n\n$ podman inspect systemd-nginx-web --format '{{.HostConfig.UsernsMode}}'\n# (empty or \"host\" is expected for rootless with default userns)\n\n# 2. No root-owned processes\n$ ps -ef | grep nginx | grep -v grep\nstudent  ...  nginx: master process ...\n\n# 3. SELinux label on the content directory\n$ ls -Z ~/nginx-web/html/\nsystem_u:object_r:container_file_t:s0:cX,cY  index.html\n\n# 4. Journal for the service\n$ journalctl --user -u nginx-web -n 20\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#cleanup","title":"Cleanup","text":"<pre><code># Stop and disable the service\n$ systemctl --user stop nginx-web\n$ systemctl --user disable nginx-web\n\n# Remove the Quadlet file and reload\n$ rm ~/.config/containers/systemd/nginx-web.container\n$ systemctl --user daemon-reload\n\n# Remove the content and image\n$ rm -rf ~/nginx-web\n$ podman rmi docker.io/library/nginx:stable-alpine\n\n# Remove the firewall rule\n$ sudo firewall-cmd --remove-port=8080/tcp --permanent\n$ sudo firewall-cmd --reload\n</code></pre>"},{"location":"05-rhca/containers/labs/rootless-web/#common-failures","title":"Common Failures","text":"Symptom Likely Cause Fix <code>Failed to connect to bus</code> when running <code>systemctl --user</code> <code>DBUS_SESSION_BUS_ADDRESS</code> not set Run as the actual user in a proper session, not via <code>sudo -u student</code> Service starts then stops immediately Image pull failure or wrong image name <code>journalctl --user -u nginx-web -n 50</code>; check image name <code>curl</code> returns connection refused Port not published or firewalld blocking Verify <code>podman ps</code> shows the port mapping; check <code>firewall-cmd --list-ports</code> Content shows default Nginx page, not custom one Volume mount path wrong Check <code>%h</code> expansion: <code>systemctl --user show nginx-web | grep ExecStart</code> Permission denied on volume mount SELinux label missing Ensure <code>:Z</code> is on the volume directive; run <code>restorecon -Rv ~/nginx-web</code> Service not running after reboot Linger disabled <code>sudo loginctl enable-linger student</code>"},{"location":"05-rhca/containers/labs/rootless-web/#why-this-matters-in-production","title":"Why This Matters in Production","text":"<ul> <li>Attack surface reduction: rootless containers cannot escalate to root even if the application is compromised.</li> <li>Quadlet declarative model: <code>.container</code> files are version-controllable and auditable \u2014 no transient shell commands to regenerate units.</li> <li>Systemd lifecycle integration: health checks, restart policies, rate limiting, and dependency ordering are all expressed natively in the unit, not in a wrapper script.</li> <li>Separation of duty: developers ship a container image; the <code>.container</code> file is the ops-owned deployment descriptor.</li> </ul>"},{"location":"05-rhca/containers/labs/rootless-web/#recap","title":"Recap","text":"<p>You deployed an Nginx container as a rootless systemd user service using Quadlet. The deployment:</p> <ul> <li>Survives reboots via <code>WantedBy=default.target</code> and linger</li> <li>Is isolated from the host filesystem using a read-only, SELinux-labeled volume mount</li> <li>Respects RHEL 10 conventions: no <code>podman generate systemd</code>, no Docker socket, no root</li> </ul> <p>Next: Lab - Secrets Rotation</p>"},{"location":"05-rhca/containers/labs/secrets-rotate/","title":"Lab - Podman Secrets Rotation","text":""},{"location":"05-rhca/containers/labs/secrets-rotate/#overview","title":"Overview","text":"<p>Create a containerized application that reads a database password from a Podman secret, then perform a zero-downtime secret rotation: update the secret value, recreate the container, and verify the new credential is in use \u2014 all without ever writing the secret to disk or an environment variable that appears in <code>ps</code> output.</p> <p>Track: D \u2014 RHCA-style Estimated time: 50 minutes Difficulty: Intermediate\u2013Advanced</p>"},{"location":"05-rhca/containers/labs/secrets-rotate/#prerequisites","title":"Prerequisites","text":"<ul> <li>RHEL 10 with Podman \u2265 4.9</li> <li>The <code>rootless-web</code> lab completed (or equivalent Quadlet familiarity)</li> <li>A non-root <code>student</code> user account</li> </ul>"},{"location":"05-rhca/containers/labs/secrets-rotate/#success-criteria","title":"Success Criteria","text":"<ul> <li>[ ] Secret created via <code>podman secret create</code> (never written to a file)</li> <li>[ ] Container mounts the secret at a known path inside the container</li> <li>[ ] <code>podman exec</code> verifies the secret value is readable inside the container</li> <li>[ ] Secret rotated to a new value without downtime (old \u2192 new)</li> <li>[ ] Quadlet <code>.container</code> file drives the service lifecycle</li> <li>[ ] Old secret version confirmed absent after rotation</li> </ul>"},{"location":"05-rhca/containers/labs/secrets-rotate/#background","title":"Background","text":"<p>Podman secrets are stored encrypted in <code>$XDG_DATA_HOME/containers/storage/secrets/</code> (rootless) or <code>/var/lib/containers/storage/secrets/</code> (root). They are injected into the container as a read-only tmpfs mount at <code>/run/secrets/&lt;name&gt;</code> \u2014 never exposed on the command line.</p> <p>Rotation workflow:</p> <ol> <li>Create a new secret with a different name (e.g., <code>db-password-v2</code>)</li> <li>Update the <code>.container</code> file to reference the new secret</li> <li><code>systemctl --user restart</code> the service</li> <li>Verify, then delete the old secret</li> </ol>"},{"location":"05-rhca/containers/labs/secrets-rotate/#steps","title":"Steps","text":""},{"location":"05-rhca/containers/labs/secrets-rotate/#1-create-the-initial-secret","title":"1 \u2014 Create the initial secret","text":"<pre><code># Use printf to avoid a trailing newline in the secret value\n$ printf 'S3cur3P@ssw0rd-v1' | podman secret create db-password -\n\n# Verify the secret is registered (value is NOT shown)\n$ podman secret ls\nID            NAME         DRIVER  CREATED        UPDATED\nabc123def456  db-password  file    5 seconds ago  5 seconds ago\n</code></pre> <p>Why <code>printf</code> not <code>echo</code>? <code>echo</code> appends <code>\\n</code>; that newline becomes part of the secret value, which will cause authentication failures with most databases.</p>"},{"location":"05-rhca/containers/labs/secrets-rotate/#2-build-a-minimal-test-image","title":"2 \u2014 Build a minimal test image","text":"<p>We use a simple Alpine-based image that loops and prints the secret content to its log (for lab verification only \u2014 never do this in production).</p> <pre><code>$ mkdir -p ~/secret-lab\n\n$ cat &gt; ~/secret-lab/Containerfile &lt;&lt;'EOF'\nFROM docker.io/library/alpine:latest\nRUN apk add --no-cache bash\n# Entrypoint reads the secret every 5 seconds and logs the first 4 chars\nCMD [\"bash\", \"-c\", \"while true; do \\\n  val=$(cat /run/secrets/db-password 2&gt;/dev/null || echo 'SECRET_MISSING'); \\\n  echo \\\"[$(date -Iseconds)] password prefix: ${val:0:4}****\\\"; \\\n  sleep 5; \\\ndone\"]\nEOF\n\n$ podman build -t localhost/secret-demo:latest ~/secret-lab/\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#3-write-the-quadlet-container-file","title":"3 \u2014 Write the Quadlet <code>.container</code> file","text":"<pre><code>$ mkdir -p ~/.config/containers/systemd\n\n$ cat &gt; ~/.config/containers/systemd/secret-demo.container &lt;&lt;'EOF'\n[Unit]\nDescription=Secret rotation demo (RHCA lab)\nAfter=network-online.target\n\n[Container]\nImage=localhost/secret-demo:latest\nSecret=db-password\n\n[Service]\nRestart=on-failure\nTimeoutStartSec=30\n\n[Install]\nWantedBy=default.target\nEOF\n</code></pre> <p>The <code>Secret=db-password</code> directive mounts the secret at <code>/run/secrets/db-password</code> inside the container automatically.</p>"},{"location":"05-rhca/containers/labs/secrets-rotate/#4-start-the-service-and-verify-the-initial-secret","title":"4 \u2014 Start the service and verify the initial secret","text":"<pre><code>$ systemctl --user daemon-reload\n$ systemctl --user start secret-demo\n\n# Watch the logs\n$ journalctl --user -u secret-demo -f &amp;\n# Output should show: password prefix: S3cu****\n\n# Exec in to verify directly\n$ podman exec -it systemd-secret-demo cat /run/secrets/db-password\nS3cur3P@ssw0rd-v1\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#5-verify-the-secret-is-not-in-the-process-list","title":"5 \u2014 Verify the secret is NOT in the process list","text":"<pre><code># The secret value must not appear in any process arguments\n$ ps -ef | grep secret-demo\n# You will see the bash loop command \u2014 no password visible\n\n# Also check podman inspect (env vars)\n$ podman inspect systemd-secret-demo --format '{{.Config.Env}}'\n# No password in environment variables\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#6-rotate-the-secret","title":"6 \u2014 Rotate the secret","text":"<p>Step 6a \u2014 Create the new secret version</p> <pre><code>$ printf 'S3cur3P@ssw0rd-v2' | podman secret create db-password-v2 -\n\n$ podman secret ls\nID            NAME             DRIVER  CREATED         UPDATED\nabc123def456  db-password      file    3 minutes ago   3 minutes ago\ndef789abc012  db-password-v2   file    5 seconds ago   5 seconds ago\n</code></pre> <p>Step 6b \u2014 Update the Quadlet file to use the new secret</p> <pre><code>$ sed -i 's/Secret=db-password$/Secret=db-password-v2/' \\\n    ~/.config/containers/systemd/secret-demo.container\n\n# Also update the mount path alias so the app path stays the same\n# Replace the Secret line with a target path form:\n$ cat &gt; ~/.config/containers/systemd/secret-demo.container &lt;&lt;'EOF'\n[Unit]\nDescription=Secret rotation demo (RHCA lab)\nAfter=network-online.target\n\n[Container]\nImage=localhost/secret-demo:latest\nSecret=db-password-v2,target=/run/secrets/db-password\n\n[Service]\nRestart=on-failure\nTimeoutStartSec=30\n\n[Install]\nWantedBy=default.target\nEOF\n</code></pre> <p>Key technique: Using <code>target=/run/secrets/db-password</code> means the application path inside the container stays constant. Only the Podman secret name changes. The app needs no code change.</p> <p>Step 6c \u2014 Restart the service</p> <pre><code>$ systemctl --user daemon-reload\n$ systemctl --user restart secret-demo\n\n# Watch the transition in logs\n$ journalctl --user -u secret-demo -n 20\n# You should now see: password prefix: S3cu**** (same prefix, but v2 value inside)\n\n# Confirm inside container\n$ podman exec -it systemd-secret-demo cat /run/secrets/db-password\nS3cur3P@ssw0rd-v2\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#7-remove-the-old-secret","title":"7 \u2014 Remove the old secret","text":"<pre><code># Only safe to delete after confirming the new secret is working\n$ podman secret rm db-password\n\n# Verify it is gone\n$ podman secret ls\nID            NAME             DRIVER  CREATED         UPDATED\ndef789abc012  db-password-v2   file    2 minutes ago   2 minutes ago\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#8-simulate-a-second-rotation-practice","title":"8 \u2014 Simulate a second rotation (practice)","text":"<p>Repeat the cycle to build muscle memory:</p> <pre><code>$ printf 'S3cur3P@ssw0rd-v3' | podman secret create db-password-v3 -\n\n# Update Quadlet\n$ sed -i 's/db-password-v2/db-password-v3/' \\\n    ~/.config/containers/systemd/secret-demo.container\n\n$ systemctl --user daemon-reload\n$ systemctl --user restart secret-demo\n\n# Verify\n$ podman exec -it systemd-secret-demo cat /run/secrets/db-password\nS3cur3P@ssw0rd-v3\n\n# Cleanup old\n$ podman secret rm db-password-v2\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#verify-checkpoints","title":"Verify Checkpoints","text":"<pre><code># 1. Secret exists and has no readable value via CLI\n$ podman secret ls\n$ podman secret inspect db-password-v3   # shows metadata, NOT the value\n\n# 2. Container is running\n$ systemctl --user status secret-demo\nActive: active (running)\n\n# 3. Secret mounted at correct path\n$ podman exec systemd-secret-demo ls -la /run/secrets/\ntotal 0\n-r--------    1 root     root            17 ...  db-password\n\n# 4. No secret in env or process args\n$ podman inspect systemd-secret-demo --format '{{range .Config.Env}}{{.}}\\n{{end}}'\n# No password lines\n\n# 5. Journal shows new prefix after rotation\n$ journalctl --user -u secret-demo -n 5\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#cleanup","title":"Cleanup","text":"<pre><code>$ systemctl --user stop secret-demo\n$ systemctl --user disable secret-demo\n$ rm ~/.config/containers/systemd/secret-demo.container\n$ systemctl --user daemon-reload\n\n# Remove all lab secrets\n$ podman secret rm db-password-v3 2&gt;/dev/null || true\n$ podman secret rm db-password-v2 2&gt;/dev/null || true\n$ podman secret rm db-password   2&gt;/dev/null || true\n\n# Remove image and build context\n$ podman rmi localhost/secret-demo:latest\n$ rm -rf ~/secret-lab\n</code></pre>"},{"location":"05-rhca/containers/labs/secrets-rotate/#common-failures","title":"Common Failures","text":"Symptom Likely Cause Fix <code>Error: no secret with name or id \"db-password\"</code> on start Secret not created before unit start Create the secret first, then <code>systemctl --user start</code> Secret file is empty inside container <code>echo</code> used instead of <code>printf</code> Delete and recreate: <code>podman secret rm</code>; use <code>printf</code> App still reads old password after rotation Daemon not reloaded or service not restarted <code>systemctl --user daemon-reload &amp;&amp; systemctl --user restart</code> <code>cat /run/secrets/db-password</code> returns <code>SECRET_MISSING</code> Wrong <code>target=</code> path in Quadlet Check the <code>Secret=</code> line; path must match what the app reads Secret value appears in <code>journalctl</code> output Application logging full secret This is a lab-only behavior; in production, never log secret values <code>podman secret rm</code> fails with \"secret in use\" Container still running and using the secret Stop the container first, then remove the old secret"},{"location":"05-rhca/containers/labs/secrets-rotate/#production-notes","title":"Production Notes","text":"<p>In real environments:</p> <ul> <li>Never log secret values. This lab logs a prefix for verification only.</li> <li>Integrate with a secrets manager (HashiCorp Vault, AWS Secrets Manager, RHEL IdM) using an init container or sidecar that populates Podman secrets on start.</li> <li>Use a naming convention for versioned secrets (<code>appname-secrettype-vN</code>) so rotation is auditable.</li> <li>Automate rotation with an Ansible playbook:</li> <li>Generate new credential in the target system (DB, API)</li> <li><code>podman secret create</code> new version</li> <li>Update <code>.container</code> file via template</li> <li><code>systemctl --user restart</code></li> <li>Verify health check passes</li> <li><code>podman secret rm</code> old version</li> </ul>"},{"location":"05-rhca/containers/labs/secrets-rotate/#why-this-matters-in-production","title":"Why This Matters in Production","text":"<ul> <li>Secrets in environment variables are readable by any process in the container and often leaked in logs. The <code>/run/secrets/</code> tmpfs mount is only readable by the container's root process.</li> <li>The rotation procedure requires zero application code changes \u2014 only the Podman secret name and the <code>.container</code> file change.</li> <li>The old secret remains available until you explicitly delete it, giving you a safe rollback window.</li> <li>This pattern is compatible with GitOps: the <code>.container</code> file is committed; only the secret value lives outside Git.</li> </ul>"},{"location":"05-rhca/containers/labs/secrets-rotate/#recap","title":"Recap","text":"<p>You created a secret, injected it into a container via Quadlet, verified it was never exposed on the process list or in environment variables, rotated it to a new version with a constant in-container path, and cleaned up the old version. This complete lifecycle is the production-grade secret management pattern for Podman on RHEL 10.</p>"},{"location":"05-rhca/networking/l2-concepts/","title":"VLAN, Bridge, Bond Concepts (Optional)","text":"<p>This chapter covers the concepts behind three advanced NIC configurations used in RHEL infrastructure: VLANs, bridges, and bonds. Labs are optional because they require specific network environments.</p>"},{"location":"05-rhca/networking/l2-concepts/#bonding-link-aggregation","title":"Bonding \u2014 link aggregation","text":"<p>Bonding combines multiple physical interfaces into one logical interface for redundancy, throughput, or both.</p>"},{"location":"05-rhca/networking/l2-concepts/#bonding-modes","title":"Bonding modes","text":"Mode Name Use case 0 <code>balance-rr</code> Round-robin; throughput, no switch config needed 1 <code>active-backup</code> One active, one standby; simple failover 2 <code>balance-xor</code> XOR hash; switch must support 4 <code>802.3ad</code> (LACP) Dynamic link aggregation; most common in production 5 <code>balance-tlb</code> Adaptive transmit load balancing 6 <code>balance-alb</code> Adaptive load balancing (both directions)"},{"location":"05-rhca/networking/l2-concepts/#create-a-bond-with-nmcli","title":"Create a bond with nmcli","text":"<pre><code># Create bond interface\nsudo nmcli connection add type bond \\\n  con-name bond0 \\\n  ifname bond0 \\\n  bond.options \"mode=active-backup,miimon=100\"\n\n# Add slave interfaces\nsudo nmcli connection add type ethernet \\\n  con-name bond0-slave1 \\\n  ifname ens3 \\\n  master bond0\n\nsudo nmcli connection add type ethernet \\\n  con-name bond0-slave2 \\\n  ifname ens4 \\\n  master bond0\n\n# Configure IP on the bond\nsudo nmcli connection modify bond0 \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.1.100/24 \\\n  ipv4.gateway 192.168.1.1\n\n# Bring it up\nsudo nmcli connection up bond0\nsudo nmcli connection up bond0-slave1\nsudo nmcli connection up bond0-slave2\n</code></pre> <pre><code># Check bond status\ncat /proc/net/bonding/bond0\n</code></pre>"},{"location":"05-rhca/networking/l2-concepts/#vlans-logical-network-segmentation","title":"VLANs \u2014 logical network segmentation","text":"<p>VLANs let one physical interface carry traffic for multiple networks by tagging frames with an 802.1Q VLAN ID. The switch must be configured to allow these tags (trunk port).</p>"},{"location":"05-rhca/networking/l2-concepts/#create-a-vlan-interface","title":"Create a VLAN interface","text":"<pre><code># VLAN 100 on interface ens3\nsudo nmcli connection add type vlan \\\n  con-name vlan100 \\\n  ifname ens3.100 \\\n  dev ens3 \\\n  id 100 \\\n  ipv4.method manual \\\n  ipv4.addresses 10.100.0.10/24 \\\n  ipv4.gateway 10.100.0.1\n\nsudo nmcli connection up vlan100\n</code></pre> <pre><code># Verify\nip addr show ens3.100\n</code></pre>"},{"location":"05-rhca/networking/l2-concepts/#bridges-software-layer-2-switch","title":"Bridges \u2014 software layer 2 switch","text":"<p>A bridge connects multiple interfaces at layer 2. On RHEL, bridges are used for:</p> <ul> <li>KVM/libvirt virtual machine networking</li> <li>Container networking</li> <li>Connecting VLANs</li> </ul>"},{"location":"05-rhca/networking/l2-concepts/#create-a-bridge","title":"Create a bridge","text":"<pre><code># Create bridge\nsudo nmcli connection add type bridge \\\n  con-name br0 \\\n  ifname br0 \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.1.100/24 \\\n  ipv4.gateway 192.168.1.1\n\n# Add ens3 as a bridge slave\nsudo nmcli connection add type ethernet \\\n  con-name br0-slave \\\n  ifname ens3 \\\n  master br0\n\nsudo nmcli connection up br0\nsudo nmcli connection up br0-slave\n</code></pre>"},{"location":"05-rhca/networking/l2-concepts/#combining-bond-vlan-bridge","title":"Combining: bond + VLAN + bridge","text":"<p>A production stack for a hypervisor host:</p> <pre><code>Physical NIC (ens3, ens4)\n    \u2514\u2500\u2500 Bond (bond0, mode=LACP)\n          \u251c\u2500\u2500 VLAN 10 (ens3.10) \u2192 Bridge br-mgmt (management traffic)\n          \u251c\u2500\u2500 VLAN 20 (ens3.20) \u2192 Bridge br-vm   (VM traffic)\n          \u2514\u2500\u2500 VLAN 30 (ens3.30) \u2192 Bridge br-stor  (storage traffic)\n</code></pre> <p>Each bridge is then the network backend for KVM virtual machines.</p>"},{"location":"05-rhca/networking/l2-concepts/#verify-layer-2-config","title":"Verify layer 2 config","text":"<pre><code># Show all bridge information\nbridge link show\nbridge vlan show\n\n# Check bond slaves\ncat /proc/net/bonding/bond0\n\n# Check VLAN interfaces\nip -d link show ens3.100\n</code></pre>"},{"location":"05-rhca/networking/l2-concepts/#next-step","title":"Next step","text":"<p>\u2192 Lab: Debug DNS vs Routing vs Firewall</p>"},{"location":"05-rhca/networking/nmcli-profiles/","title":"nmcli Profiles at Scale","text":"<p>At RHCA level, you need to understand NetworkManager's connection model deeply enough to manage it at scale \u2014 via Ansible, templates, or automation.</p>"},{"location":"05-rhca/networking/nmcli-profiles/#connection-profile-deep-dive","title":"Connection profile deep-dive","text":"<p>A connection profile is a configuration file in <code>/etc/NetworkManager/system-connections/</code>. Modern RHEL 10 uses keyfile format.</p> <pre><code># List all connection files\nsudo ls -la /etc/NetworkManager/system-connections/\n\n# View a profile\nsudo cat \"/etc/NetworkManager/system-connections/Wired connection 1.nmconnection\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#all-important-nmcli-properties","title":"All important nmcli properties","text":"<pre><code># Full list of properties for a connection\nnmcli connection show \"Wired connection 1\"\n\n# Specific property\nnmcli -f ipv4.addresses connection show \"Wired connection 1\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#key-properties-for-static-configuration","title":"Key properties for static configuration","text":"<pre><code>sudo nmcli connection modify \"eth-static\" \\\n  connection.autoconnect yes \\\n  connection.autoconnect-priority 10 \\\n  ipv4.method manual \\\n  ipv4.addresses \"192.168.1.100/24\" \\\n  ipv4.gateway \"192.168.1.1\" \\\n  ipv4.dns \"192.168.1.53 8.8.8.8\" \\\n  ipv4.dns-search \"lab.local example.com\" \\\n  ipv4.ignore-auto-dns yes \\\n  ipv6.method disabled\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#multiple-ip-addresses-on-one-interface","title":"Multiple IP addresses on one interface","text":"<pre><code>sudo nmcli connection modify \"eth-static\" \\\n  ipv4.addresses \"192.168.1.100/24,192.168.1.101/24,192.168.1.102/24\"\n</code></pre> <p>Or add one at a time:</p> <pre><code>sudo nmcli connection modify \"eth-static\" +ipv4.addresses \"192.168.1.103/24\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#multiple-routes-policy-routing-intro","title":"Multiple routes (policy routing intro)","text":"<pre><code># Add a static route via a specific gateway\nsudo nmcli connection modify \"eth-static\" \\\n  +ipv4.routes \"10.10.0.0/16 192.168.1.254\"\n\n# Remove a route\nsudo nmcli connection modify \"eth-static\" \\\n  -ipv4.routes \"10.10.0.0/16 192.168.1.254\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#create-profiles-via-nmcli-batch-scripting","title":"Create profiles via nmcli batch (scripting)","text":"<pre><code>#!/usr/bin/env bash\nset -euo pipefail\n\nCONNECTION_NAME=\"server-eth0\"\nINTERFACE=\"ens3\"\nIP=\"192.168.1.100/24\"\nGW=\"192.168.1.1\"\nDNS=\"192.168.1.53\"\n\n# Delete existing (idempotent)\nnmcli connection delete \"${CONNECTION_NAME}\" 2&gt;/dev/null || true\n\n# Create new\nnmcli connection add \\\n  type ethernet \\\n  con-name \"${CONNECTION_NAME}\" \\\n  ifname \"${INTERFACE}\" \\\n  ipv4.method manual \\\n  ipv4.addresses \"${IP}\" \\\n  ipv4.gateway \"${GW}\" \\\n  ipv4.dns \"${DNS}\" \\\n  ipv6.method disabled \\\n  connection.autoconnect yes\n\nnmcli connection up \"${CONNECTION_NAME}\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#manage-profiles-with-ansible-rhel-system-roles","title":"Manage profiles with Ansible (RHEL System Roles)","text":"<pre><code>sudo dnf install -y rhel-system-roles\n</code></pre> <pre><code>---\n- name: Configure network\n  hosts: all\n  become: true\n  vars:\n    network_connections:\n      - name: eth-static\n        type: ethernet\n        interface_name: ens3\n        state: up\n        autoconnect: true\n        ip:\n          address:\n            - 192.168.1.100/24\n          gateway4: 192.168.1.1\n          dns:\n            - 192.168.1.53\n            - 8.8.8.8\n          dns_search:\n            - lab.local\n\n  roles:\n    - redhat.rhel_system_roles.network\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#reload-profiles-after-manual-keyfile-edits","title":"Reload profiles after manual keyfile edits","text":"<pre><code># If you edit /etc/NetworkManager/system-connections/ directly\nsudo nmcli connection reload\n\n# Activate the updated connection\nsudo nmcli connection up \"&lt;connection-name&gt;\"\n</code></pre>"},{"location":"05-rhca/networking/nmcli-profiles/#next-step","title":"Next step","text":"<p>\u2192 Routing + Troubleshooting Method</p>"},{"location":"05-rhca/networking/routing-method/","title":"Routing + Troubleshooting Method","text":"<p>Routing issues are one of the most common (and most misdiagnosed) network problems. A systematic method makes them fast to resolve.</p>"},{"location":"05-rhca/networking/routing-method/#the-linux-routing-table","title":"The Linux routing table","text":"<p>The kernel consults the routing table to decide where to send each packet.</p> <pre><code># Show all routes\nip route show\n\n# Or\nip r\n</code></pre> <p>Example output:</p> <pre><code>default via 192.168.1.1 dev ens3 proto dhcp metric 100\n192.168.1.0/24 dev ens3 proto kernel scope link src 192.168.1.100\n</code></pre> Field Meaning <code>default via X</code> Default gateway \u2014 used for all non-matching destinations <code>192.168.1.0/24 dev ens3</code> Local subnet \u2014 reach directly via ens3 <code>proto kernel</code> Added by the kernel when an IP is assigned <code>metric 100</code> Route priority (lower = preferred)"},{"location":"05-rhca/networking/routing-method/#route-lookup","title":"Route lookup","text":"<pre><code># Which route would be used for a specific destination?\nip route get 8.8.8.8\n\n# Which route to reach another host?\nip route get 192.168.2.50\n</code></pre>"},{"location":"05-rhca/networking/routing-method/#add-and-remove-static-routes","title":"Add and remove static routes","text":"<pre><code># Temporary (lost on reboot)\nsudo ip route add 10.0.0.0/8 via 192.168.1.254\nsudo ip route del 10.0.0.0/8 via 192.168.1.254\n\n# Persistent via nmcli\nsudo nmcli connection modify \"eth-static\" +ipv4.routes \"10.0.0.0/8 192.168.1.254\"\nsudo nmcli connection up \"eth-static\"\n</code></pre>"},{"location":"05-rhca/networking/routing-method/#the-routing-connectivity-troubleshooting-method","title":"The routing + connectivity troubleshooting method","text":"<p>Work through this in order. Each step eliminates a layer.</p>"},{"location":"05-rhca/networking/routing-method/#step-1-is-the-interface-up","title":"Step 1 \u2014 Is the interface up?","text":"<pre><code>ip link show ens3\n</code></pre> <p>Look for: <code>state UP</code> and <code>LOWER_UP</code> (physical link up).</p> <p>If <code>NO-CARRIER</code>: cable problem, wrong switch port, or VM adapter issue. If <code>DOWN</code>: <code>sudo nmcli device connect ens3</code></p>"},{"location":"05-rhca/networking/routing-method/#step-2-does-the-interface-have-an-ip","title":"Step 2 \u2014 Does the interface have an IP?","text":"<pre><code>ip addr show ens3\n</code></pre> <p>Look for: a <code>inet</code> line with your expected IP.</p> <p>If missing: check if NetworkManager has an active connection: <code>nmcli device status</code></p>"},{"location":"05-rhca/networking/routing-method/#step-3-can-we-reach-the-default-gateway","title":"Step 3 \u2014 Can we reach the default gateway?","text":"<pre><code>ip route show | grep default   # find gateway IP\nping -c 3 &lt;gateway-IP&gt;\n</code></pre> <p>If ping fails but interface is UP with an IP: - Wrong gateway configured? - Firewall on the gateway? - Layer 2 problem (ARP)? \u2192 <code>ip neigh show</code></p>"},{"location":"05-rhca/networking/routing-method/#step-4-can-we-reach-an-external-ip-bypass-dns","title":"Step 4 \u2014 Can we reach an external IP (bypass DNS)?","text":"<pre><code>ping -c 3 8.8.8.8\n</code></pre> <p>If this fails but gateway works: routing problem between you and the internet. Check: is there a route to <code>0.0.0.0/0</code>? Is NAT configured on the router?</p>"},{"location":"05-rhca/networking/routing-method/#step-5-can-we-resolve-names","title":"Step 5 \u2014 Can we resolve names?","text":"<pre><code>ping -c 3 access.redhat.com   # if fails but 8.8.8.8 works \u2192 DNS problem\nresolvectl status\ndig access.redhat.com\ndig @8.8.8.8 access.redhat.com   # test with a known-good resolver\n</code></pre>"},{"location":"05-rhca/networking/routing-method/#step-6-can-we-reach-the-target-service","title":"Step 6 \u2014 Can we reach the target service?","text":"<pre><code># TCP reachability test\ncurl -v --max-time 5 http://192.168.1.50:80/\nnc -zv 192.168.1.50 80\n\n# Is the service listening on the remote?\nssh user@192.168.1.50 \"ss -tlnp | grep :80\"\n\n# Is a local firewall blocking outbound?\nsudo firewall-cmd --list-all\n</code></pre>"},{"location":"05-rhca/networking/routing-method/#useful-tools","title":"Useful tools","text":"<pre><code># Show ARP table (layer 2 neighbours)\nip neigh show\n\n# Trace path\ntraceroute 8.8.8.8\ntracepath 8.8.8.8    # no root required\n\n# Packet loss stats\nmtr 8.8.8.8\n\n# See what interfaces and IPs exist\nip -brief addr\nip -brief link\n</code></pre>"},{"location":"05-rhca/networking/routing-method/#next-step","title":"Next step","text":"<p>\u2192 tcpdump Guided Debugging</p>"},{"location":"05-rhca/networking/tcpdump/","title":"tcpdump Guided Debugging","text":"<p><code>tcpdump</code> captures network packets at the interface level. It is the most powerful network debugging tool available on RHEL without installing anything.</p>"},{"location":"05-rhca/networking/tcpdump/#install","title":"Install","text":"<pre><code>sudo dnf install -y tcpdump\n</code></pre>"},{"location":"05-rhca/networking/tcpdump/#basic-usage","title":"Basic usage","text":"<pre><code># Capture on all interfaces, print to screen\nsudo tcpdump -n\n\n# Capture on a specific interface\nsudo tcpdump -n -i ens3\n\n# Capture and save to file (for later analysis)\nsudo tcpdump -n -i ens3 -w /tmp/capture.pcap\n\n# Read from a saved file\nsudo tcpdump -n -r /tmp/capture.pcap\n</code></pre> <p><code>-n</code> disables hostname resolution (much faster output).</p>"},{"location":"05-rhca/networking/tcpdump/#filters-bpf-syntax","title":"Filters (BPF syntax)","text":"<p>Filters make captures targeted and readable:</p> <pre><code># Only traffic to/from a host\nsudo tcpdump -n -i ens3 host 192.168.1.50\n\n# Only traffic on a port\nsudo tcpdump -n -i ens3 port 80\nsudo tcpdump -n -i ens3 port 53    # DNS\n\n# Only TCP traffic\nsudo tcpdump -n -i ens3 tcp\n\n# Only UDP\nsudo tcpdump -n -i ens3 udp\n\n# Combine filters\nsudo tcpdump -n -i ens3 host 192.168.1.50 and port 443\n\n# Source host\nsudo tcpdump -n -i ens3 src host 10.0.0.1\n\n# Destination host\nsudo tcpdump -n -i ens3 dst host 8.8.8.8\n\n# ICMP only\nsudo tcpdump -n -i ens3 icmp\n</code></pre>"},{"location":"05-rhca/networking/tcpdump/#verbosity-levels","title":"Verbosity levels","text":"<pre><code>-v     # verbose (TTL, IP ID, checksum)\n-vv    # more verbose (full DNS/NTP decode)\n-vvv   # maximum verbosity\n-A     # print payload as ASCII\n-X     # print payload as hex+ASCII\n</code></pre>"},{"location":"05-rhca/networking/tcpdump/#practical-debugging-scenarios","title":"Practical debugging scenarios","text":""},{"location":"05-rhca/networking/tcpdump/#is-my-dns-query-leaving-the-host","title":"\"Is my DNS query leaving the host?\"","text":"<pre><code>sudo tcpdump -n -i ens3 port 53\n</code></pre> <p>Then in another terminal:</p> <pre><code>dig access.redhat.com\n</code></pre> <p>Watch for: outbound query on port 53 and the response. If no packets appear, the query is being intercepted locally (check <code>/etc/hosts</code>, <code>systemd-resolved</code>).</p>"},{"location":"05-rhca/networking/tcpdump/#is-my-http-request-reaching-the-server","title":"\"Is my HTTP request reaching the server?\"","text":"<p>On the server:</p> <pre><code>sudo tcpdump -n -i ens3 port 80 -A\n</code></pre> <p>On the client:</p> <pre><code>curl http://192.168.1.50/\n</code></pre> <p>If packets appear on the server: connection is reaching the server. Problem is in the application (service not running, firewall on server app, SELinux).</p> <p>If no packets appear: the traffic is being blocked before reaching the server (routing, firewall on the way, NAT issues).</p>"},{"location":"05-rhca/networking/tcpdump/#is-there-a-tcp-handshake-completing","title":"\"Is there a TCP handshake completing?\"","text":"<pre><code>sudo tcpdump -n -i ens3 \"tcp[tcpflags] &amp; (tcp-syn|tcp-ack) != 0\"\n</code></pre> <p>Look for: - <code>S</code> (SYN): client sending connection request - <code>S.</code> (SYN-ACK): server accepting - <code>.</code> (ACK): client acknowledging \u2014 handshake complete</p> <p>If you see SYN but no SYN-ACK: server not listening or firewall dropping. If you see SYN, SYN-ACK, RST: server resets the connection (port closed or SELinux).</p>"},{"location":"05-rhca/networking/tcpdump/#capture-traffic-to-a-file-and-inspect-later","title":"\"Capture traffic to a file and inspect later\"","text":"<pre><code># Capture 100 packets on port 443\nsudo tcpdump -n -i ens3 port 443 -c 100 -w /tmp/https.pcap\n\n# Read back with verbose output\nsudo tcpdump -n -r /tmp/https.pcap -v\n</code></pre>"},{"location":"05-rhca/networking/tcpdump/#output-format","title":"Output format","text":"<pre><code>HH:MM:SS.ffffff IP src &gt; dst: flags seq ack win length\n</code></pre> <p>Example:</p> <pre><code>10:05:23.456789 IP 192.168.1.100.54321 &gt; 8.8.8.8.53: Flags [S]\n  seq 1234567, win 64240, length 0\n</code></pre> Part Meaning <code>192.168.1.100.54321</code> Source IP.port <code>8.8.8.8.53</code> Destination IP.port <code>Flags [S]</code> SYN (connection attempt) <code>[S.]</code> SYN-ACK (server accepted) <code>[.]</code> ACK <code>[P.]</code> PUSH+ACK (data payload) <code>[F.]</code> FIN (connection close) <code>[R]</code> RST (connection reset/rejected)"},{"location":"05-rhca/networking/tcpdump/#next-step","title":"Next step","text":"<p>\u2192 VLAN, Bridge, Bond Concepts</p>"},{"location":"05-rhca/networking/labs/debug-triad/","title":"Lab: Debug DNS vs Routing vs Firewall","text":"<p>Track: RHCA Estimated time: 45 minutes Topology: Single VM</p>"},{"location":"05-rhca/networking/labs/debug-triad/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Routing + Troubleshooting Method and tcpdump Guided Debugging</li> <li><code>tcpdump</code> installed: <code>sudo dnf install -y tcpdump</code></li> <li>VM snapshot taken</li> </ul>"},{"location":"05-rhca/networking/labs/debug-triad/#overview","title":"Overview","text":"<p>This lab introduces three deliberate network faults. For each fault: 1. You observe the symptom 2. You apply the troubleshooting method to identify the cause 3. You apply the fix and verify</p> <p>No solutions are given upfront \u2014 work through each fault yourself.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#setup-install-a-simple-web-service","title":"Setup \u2014 install a simple web service","text":"<pre><code>sudo dnf install -y httpd\nsudo systemctl enable --now httpd\necho \"&lt;h1&gt;Lab web server&lt;/h1&gt;\" | sudo tee /var/www/html/index.html\nsudo restorecon -v /var/www/html/index.html\n</code></pre> <p>Verify the baseline works:</p> <pre><code>curl http://localhost/\n</code></pre> <p>Expected: <code>Lab web server</code></p>"},{"location":"05-rhca/networking/labs/debug-triad/#fault-1-dns-broken","title":"Fault 1 \u2014 DNS broken","text":""},{"location":"05-rhca/networking/labs/debug-triad/#introduce-the-fault","title":"Introduce the fault","text":"<pre><code>sudo nmcli connection modify \"$(nmcli -g NAME connection show --active | head -1)\" \\\n  ipv4.dns \"192.168.255.254\"\nsudo nmcli connection up \"$(nmcli -g NAME connection show --active | head -1)\"\n</code></pre>"},{"location":"05-rhca/networking/labs/debug-triad/#observe-the-symptom","title":"Observe the symptom","text":"<pre><code>ping -c 2 access.redhat.com\n</code></pre> <p>Expected: name resolution fails.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#troubleshoot-it","title":"Troubleshoot it","text":"<p>Use the tools from Routing + Troubleshooting Method. Work out: - Can you ping <code>8.8.8.8</code> (IP, no DNS)? - What does <code>resolvectl status</code> show? - What does <code>dig @8.8.8.8 access.redhat.com</code> return? - What DNS server is being used?</p>"},{"location":"05-rhca/networking/labs/debug-triad/#fix-it","title":"Fix it","text":"<p>Restore the correct DNS server using <code>nmcli</code>.</p> <p>Verify</p> <p><pre><code>ping -c 2 access.redhat.com\n</code></pre> Name resolves successfully.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#fault-2-firewall-blocking-http","title":"Fault 2 \u2014 Firewall blocking HTTP","text":""},{"location":"05-rhca/networking/labs/debug-triad/#introduce-the-fault_1","title":"Introduce the fault","text":"<pre><code>sudo firewall-cmd --permanent --remove-service=http\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"05-rhca/networking/labs/debug-triad/#observe-the-symptom_1","title":"Observe the symptom","text":"<p>From a second terminal (or using <code>curl</code> with a timeout):</p> <pre><code>curl --max-time 5 http://127.0.0.1/\n</code></pre> <p>Or test from another host if available.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#troubleshoot-it_1","title":"Troubleshoot it","text":"<p>Work out: - Is httpd running? (<code>systemctl status httpd</code>) - Is it listening? (<code>ss -tlnp | grep :80</code>) - What does <code>firewall-cmd --list-all</code> show? - Is it SELinux? (<code>ausearch -m avc -ts recent</code>)</p>"},{"location":"05-rhca/networking/labs/debug-triad/#fix-it_1","title":"Fix it","text":"<p>Open the correct port/service in firewalld.</p> <p>Verify</p> <p><pre><code>curl http://localhost/\n</code></pre> Returns the web page.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#fault-3-selinux-context-broken-on-web-content","title":"Fault 3 \u2014 SELinux context broken on web content","text":""},{"location":"05-rhca/networking/labs/debug-triad/#introduce-the-fault_2","title":"Introduce the fault","text":"<pre><code>sudo chcon -t var_t /var/www/html/index.html\n</code></pre> <p>This changes the file's SELinux context to the wrong type.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#observe-the-symptom_2","title":"Observe the symptom","text":"<pre><code>curl http://localhost/\n</code></pre> <p>Expected: <code>403 Forbidden</code> (httpd can't read the file).</p>"},{"location":"05-rhca/networking/labs/debug-triad/#troubleshoot-it_2","title":"Troubleshoot it","text":"<p>Work out: - Is httpd running? - Is port 80 open in firewalld? - What does <code>ls -Z /var/www/html/index.html</code> show? - Are there AVC denials? (<code>ausearch -m avc -ts recent</code>)</p>"},{"location":"05-rhca/networking/labs/debug-triad/#fix-it_2","title":"Fix it","text":"<p>Apply the correct SELinux label using <code>restorecon</code>.</p> <p>Verify</p> <p><pre><code>curl http://localhost/\nausearch -m avc -ts recent\n</code></pre> Page loads. No new AVC denials.</p>"},{"location":"05-rhca/networking/labs/debug-triad/#cleanup","title":"Cleanup","text":"<pre><code>sudo systemctl disable --now httpd\nsudo firewall-cmd --permanent --add-service=http 2&gt;/dev/null || true\nsudo firewall-cmd --reload\nsudo restorecon -Rv /var/www/html/\n</code></pre> <p>Restore correct DNS:</p> <pre><code>sudo nmcli connection modify \"$(nmcli -g NAME connection show --active | head -1)\" \\\n  ipv4.dns \"192.168.122.1 8.8.8.8\"\nsudo nmcli connection up \"$(nmcli -g NAME connection show --active | head -1)\"\n</code></pre>"},{"location":"05-rhca/networking/labs/debug-triad/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix <code>curl: (6)</code> name not resolved DNS fault not cleared Re-run DNS fix steps 403 persists after restorecon Wrong context after chcon <code>restorecon -v /var/www/html/index.html</code> http still blocked Permanent firewall rule but no reload <code>sudo firewall-cmd --reload</code>"},{"location":"05-rhca/networking/labs/debug-triad/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>The single most time-consuming class of incidents in RHEL environments is misidentifying the layer of a network problem: \"it must be SELinux\" when it's actually firewalld, or \"restart the service\" when DNS is broken. This lab builds the muscle memory for the diagnostic order: routing \u2192 DNS \u2192 firewall \u2192 SELinux \u2192 application.</p>"},{"location":"05-rhca/perf/recovery-patterns/","title":"Recovery Patterns","text":""},{"location":"05-rhca/perf/recovery-patterns/#overview","title":"Overview","text":"<p>This chapter covers the systematic recovery procedures an RHCA-level administrator must execute under pressure: boot failures, corrupted filesystems, forgotten root passwords, broken SELinux contexts, and unresponsive systemd services. Each pattern follows a Diagnose \u2192 Recover \u2192 Verify \u2192 Prevent structure.</p>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-1-root-password-reset-rhel-10","title":"Pattern 1 \u2014 Root Password Reset (RHEL 10)","text":"<p>Use when: locked out of root account, no sudo access from any other user.</p>"},{"location":"05-rhca/perf/recovery-patterns/#procedure","title":"Procedure","text":"<p>1. Interrupt the boot at GRUB</p> <p>At the GRUB menu, press <code>e</code> to edit the default boot entry.</p> <p>2. Edit the kernel command line</p> <p>Find the line beginning with <code>linux</code> and add <code>rd.break</code> at the end:</p> <pre><code>linux /vmlinuz-... root=/dev/vda3 ro ... rd.break\n</code></pre> <p>Press <code>Ctrl+X</code> to boot with this parameter.</p> <p>3. In the initramfs emergency shell</p> <pre><code># Remount the real root filesystem read-write\nswitch_root:/# mount -o remount,rw /sysroot\n\n# Chroot into the real filesystem\nswitch_root:/# chroot /sysroot\n\n# Reset the root password\nsh-5.1# passwd root\nNew password:\nRetype new password:\npasswd: all authentication tokens updated successfully.\n\n# CRITICAL: tell SELinux to relabel all files on next boot\nsh-5.1# touch /.autorelabel\n\n# Exit chroot and the shell\nsh-5.1# exit\nswitch_root:/# exit\n</code></pre> <p>The system will perform an SELinux relabel (takes 2\u20135 minutes on a full disk), then reboot normally.</p> <p>Why <code>/.autorelabel</code>? The <code>passwd</code> command was run outside of a running SELinux context. Without relabeling, the <code>/etc/shadow</code> file may have the wrong label and logins will fail with a PAM SELinux error.</p>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-2-emergency-and-rescue-targets","title":"Pattern 2 \u2014 Emergency and Rescue Targets","text":""},{"location":"05-rhca/perf/recovery-patterns/#emergency-target","title":"Emergency target","text":"<p>The most minimal boot \u2014 mounts only the root filesystem read-only, no services.</p> <pre><code># Boot into emergency target from GRUB:\n# Append to kernel line:\nsystemd.unit=emergency.target\n\n# In the emergency shell, remount root rw to make changes:\n# mount -o remount,rw /\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#rescue-target","title":"Rescue target","text":"<p>Starts a minimal set of services (filesystems mounted, no network). Good for diagnosing service failures that prevent normal boot.</p> <pre><code># Boot into rescue target:\nsystemd.unit=rescue.target\n\n# Or from a running system:\n$ sudo systemctl isolate rescue.target\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#recovery-decision-tree","title":"Recovery decision tree","text":"<pre><code>System won't boot normally\n  \u251c\u2500\u2500 Reaches GRUB? \u2192 Yes \u2192 use rd.break or systemd.unit=emergency.target\n  \u2502                   No  \u2192 boot from RHEL install media \u2192 Troubleshoot mode\n  \u251c\u2500\u2500 Mounts root?  \u2192 No  \u2192 filesystem corruption \u2192 see Pattern 3\n  \u2514\u2500\u2500 Fails in service start \u2192 journalctl -b -1 -p err \u2192 disable failing unit\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-3-filesystem-corruption-recovery","title":"Pattern 3 \u2014 Filesystem Corruption Recovery","text":""},{"location":"05-rhca/perf/recovery-patterns/#detecting-corruption","title":"Detecting corruption","text":"<pre><code># XFS metadata errors in the kernel journal\n$ journalctl -k | grep -i \"XFS\\|filesystem error\\|I/O error\"\n\n# Mount failure on boot\n$ journalctl -b | grep \"mount\\|fsck\\|EXT4\\|XFS\" | grep -i \"err\\|fail\"\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#xfs-recovery","title":"XFS recovery","text":"<p>XFS is self-healing via its journal (log). Manual <code>fsck</code> is rarely needed. The tool is <code>xfs_repair</code>.</p> <pre><code># UNMOUNT the filesystem first (or use rescue target if it's the root fs)\n$ sudo umount /dev/vda3\n\n# Replay the journal (attempt #1 \u2014 usually sufficient)\n$ sudo xfs_repair /dev/vda3\n\n# If xfs_repair says \"Dirty log \u2014 use -L to force log zeroing\"\n# This LOSES in-flight transactions but gets the FS mountable\n$ sudo xfs_repair -L /dev/vda3\n\n# Remount and verify\n$ sudo mount /dev/vda3 /mountpoint\n$ df -h /mountpoint\n</code></pre> <p>XFS cannot be repaired while mounted. If it is the root filesystem, boot from <code>emergency.target</code> (root is mounted read-only) or use RHEL install media.</p>"},{"location":"05-rhca/perf/recovery-patterns/#ext4-recovery","title":"ext4 recovery","text":"<pre><code>$ sudo umount /dev/vdb1\n$ sudo fsck.ext4 -y /dev/vdb1   # -y = answer yes to all questions\n$ sudo mount /dev/vdb1 /mountpoint\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#lvm-volume-not-appearing","title":"LVM volume not appearing","text":"<pre><code># Scan for volume groups\n$ sudo vgscan --mknodes\n$ sudo vgchange -ay   # activate all VGs\n\n# If PV is missing (degraded mirror or failed disk)\n$ sudo pvs\n  WARNING: Couldn't find device with uuid ...\n  /dev/vdb: open failed: No such file or directory\n$ sudo vgreduce --removemissing &lt;vgname&gt;\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-4-broken-selinux-context-recovery","title":"Pattern 4 \u2014 Broken SELinux Context Recovery","text":"<p>When files have wrong or missing SELinux labels, services fail with AVC denials.</p>"},{"location":"05-rhca/perf/recovery-patterns/#relabel-a-specific-file-or-directory","title":"Relabel a specific file or directory","text":"<pre><code># Restore default context based on fcontext database\n$ sudo restorecon -Rv /var/www/html/\n\n# Verify\n$ ls -Z /var/www/html/\nsystem_u:object_r:httpd_sys_content_t:s0  index.html\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#relabel-a-non-standard-path","title":"Relabel a non-standard path","text":"<pre><code># Add a permanent fcontext rule first\n$ sudo semanage fcontext -a -t httpd_sys_content_t '/srv/web(/.*)?'\n\n# Then relabel (semanage alone does NOT relabel existing files)\n$ sudo restorecon -Rv /srv/web/\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#full-system-relabel","title":"Full system relabel","text":"<p>For situations where large swaths of the filesystem have wrong labels:</p> <pre><code># Option 1: Trigger relabel on next boot\n$ sudo touch /.autorelabel\n$ sudo reboot\n# System relabels all files, then reboots \u2014 allow 5\u201315 minutes\n\n# Option 2: Relabel interactively without reboot (very slow on large filesystems)\n$ sudo restorecon -Rv /\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#selinux-in-permissive-mode-left-over-from-troubleshooting","title":"SELinux in permissive mode left over from troubleshooting","text":"<pre><code># Check current mode\n$ getenforce\nPermissive   \u2190 this should not persist in production\n\n# Return to enforcing\n$ sudo setenforce 1\n\n# Verify it will be enforcing after reboot\n$ grep SELINUX= /etc/selinux/config\nSELINUX=enforcing\n</code></pre> <p>Never leave <code>SELINUX=permissive</code> in <code>/etc/selinux/config</code> as a fix. It disables MAC for the entire system permanently.</p>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-5-unresponsive-systemd-service","title":"Pattern 5 \u2014 Unresponsive systemd Service","text":""},{"location":"05-rhca/perf/recovery-patterns/#diagnose","title":"Diagnose","text":"<pre><code># What is the service's current state?\n$ systemctl status myapp.service\n\n# Full journal for the service (last 50 lines)\n$ journalctl -u myapp.service -n 50\n\n# Is it stuck in a start loop?\n$ systemctl show myapp.service | grep -E \"ActiveState|SubState|Result|NRestarts\"\nActiveState=activating\nNRestarts=15\nResult=start-limit-hit\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#clear-a-start-limit-and-restart","title":"Clear a start-limit and restart","text":"<pre><code># If Result=start-limit-hit, reset the rate limit counter\n$ sudo systemctl reset-failed myapp.service\n$ sudo systemctl start myapp.service\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#force-kill-a-stuck-service","title":"Force-kill a stuck service","text":"<pre><code># Send SIGTERM first\n$ sudo systemctl kill --signal=SIGTERM myapp.service\n\n# If still stuck after a few seconds, SIGKILL\n$ sudo systemctl kill --signal=SIGKILL myapp.service\n\n# Confirm it stopped\n$ systemctl is-active myapp.service\ninactive\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#service-hangs-on-stop-taking-too-long","title":"Service hangs on stop (taking too long)","text":"<pre><code># Check timeout settings\n$ systemctl show myapp.service | grep TimeoutStop\nTimeoutStopUSec=1min 30s\n\n# Override timeout for a specific unit\n$ sudo systemctl edit myapp.service\n# Add:\n[Service]\nTimeoutStopSec=10\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#broken-unit-file-system-wont-boot-past-a-target","title":"Broken unit file \u2014 system won't boot past a target","text":"<pre><code># From emergency.target or rescue.target:\n# Mask the problematic unit temporarily\n$ sudo systemctl mask myapp.service\n\n# Reboot into normal target, then investigate\n$ sudo reboot\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-6-boot-to-last-known-good-kernel","title":"Pattern 6 \u2014 Boot to Last Known Good Kernel","text":"<p>When a kernel update breaks boot (driver issue, panic on boot):</p> <pre><code># At GRUB, select the previous kernel entry, OR:\n# Boot with kernel argument to skip the newest kernel (temporarily):\n# In GRUB editor, select the older kernel entry manually.\n\n# After booting into the older kernel, check installed kernels:\n$ rpm -qa kernel | sort\nkernel-6.12.0-1.el10.x86_64\nkernel-6.12.0-2.el10.x86_64   \u2190 broken\n\n# Set the default kernel (grubby)\n$ sudo grubby --set-default /boot/vmlinuz-6.12.0-1.el10.x86_64\n\n# Verify\n$ sudo grubby --default-kernel\n/boot/vmlinuz-6.12.0-1.el10.x86_64\n\n# Optionally remove the broken kernel\n$ sudo dnf remove kernel-6.12.0-2.el10.x86_64\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-7-network-unreachable-after-config-change","title":"Pattern 7 \u2014 Network Unreachable After Config Change","text":"<pre><code># Check connection state\n$ nmcli connection show\n$ nmcli device status\n\n# Reload connection without losing SSH session\n$ sudo nmcli connection reload\n\n# Bring connection down and up\n$ sudo nmcli connection down ens3 &amp;&amp; sudo nmcli connection up ens3\n\n# If interface has no IP\n$ sudo nmcli device connect ens3\n\n# Verify routing\n$ ip route show\n$ ip addr show ens3\n\n# Check for conflicting static route\n$ ip route show table all | grep -v \"proto kernel\"\n</code></pre> <p>If you lost SSH access entirely: use the VM console (virsh console, IPMI, cloud serial console) to fix the network configuration.</p>"},{"location":"05-rhca/perf/recovery-patterns/#pattern-8-disk-full-recovery","title":"Pattern 8 \u2014 Disk Full Recovery","text":"<pre><code># Identify the full filesystem\n$ df -h\nFilesystem      Size  Used Avail Use% Mounted on\n/dev/vda3        20G   20G     0 100% /\n\n# Step 1: Find large files\n$ sudo find / -xdev -type f -size +100M 2&gt;/dev/null | sort -k5 -rn\n\n# Step 2: Find directories using the most space\n$ sudo du -xh / --max-depth=3 | sort -rh | head -20\n\n# Step 3: Rotated logs taking up space?\n$ sudo journalctl --disk-usage\n$ sudo journalctl --vacuum-size=500M\n\n# Step 4: Old kernels\n$ rpm -qa kernel | sort\n$ sudo dnf remove &lt;old-kernel-nvr&gt;\n\n# Step 5: DNF cache\n$ sudo dnf clean all\n\n# Step 6: Abandoned container images (if Podman in use)\n$ podman image prune -a\n\n# Step 7: Extend the LVM volume (if LVM is in use)\n$ sudo lvextend -r -L +5G /dev/rhel/root   # -r resizes the filesystem too\n</code></pre> <p><code>-r</code> flag on <code>lvextend</code> resizes the filesystem atomically with the LV expansion \u2014 always use it.</p>"},{"location":"05-rhca/perf/recovery-patterns/#recovery-runbook-template","title":"Recovery Runbook Template","text":"<p>Copy this into your runbook for any new recovery scenario:</p> <pre><code>## Recovery - &lt;Scenario Name&gt;\n\n**Trigger:** What condition makes this runbook active  \n**Impact:** What is broken / what users see  \n**RTO target:** Expected time to recover  \n\n### Prerequisites\n- Access method: (SSH / console / IPMI)\n- Required privileges: (root / sudo)\n- Tools needed:\n\n### Steps\n1. Diagnose: confirm this is the right runbook\n2. Isolate: stop the bleeding\n3. Fix: apply the recovery\n4. Verify: success criteria\n5. Communicate: notify stakeholders\n\n### Post-recovery\n- Root cause investigation\n- Prevent recurrence\n- Update monitoring\n\n### Escalation\n- If not resolved in X minutes \u2192 contact ...\n</code></pre>"},{"location":"05-rhca/perf/recovery-patterns/#why-this-matters-in-production","title":"Why This Matters in Production","text":"<p>Recovery procedures that are documented, practiced, and version-controlled reduce MTTR (Mean Time to Recover) dramatically. The difference between a 15-minute recovery and a 4-hour outage is usually:</p> <ol> <li>Knowing the exact commands before you're under pressure</li> <li>Having a tested runbook rather than reasoning from first principles during an incident</li> <li>Practicing in a non-production environment (use the lab VMs!)</li> </ol> <p>RHCA candidates are expected to execute these procedures quickly and correctly under exam conditions. More importantly, production systems depend on administrators who can.</p>"},{"location":"05-rhca/perf/recovery-patterns/#recap","title":"Recap","text":"Scenario Entry Point Key Command Root password lost GRUB <code>rd.break</code> <code>chroot /sysroot; passwd root; touch /.autorelabel</code> Boot failure GRUB <code>emergency.target</code> <code>systemctl list-units --failed</code> XFS corruption Unmount \u2192 repair <code>xfs_repair /dev/vdX</code> SELinux label broken Running system <code>restorecon -Rv &lt;path&gt;</code> Stuck service systemctl <code>systemctl reset-failed; systemctl kill</code> Bad kernel GRUB + grubby <code>grubby --set-default &lt;vmlinuz&gt;</code> Network down nmcli <code>nmcli connection reload; nmcli device connect</code> Disk full df + find + du <code>journalctl --vacuum-size=</code>; <code>dnf clean</code>; <code>lvextend -r</code>"},{"location":"05-rhca/perf/resource-triage/","title":"Performance Resource Triage","text":""},{"location":"05-rhca/perf/resource-triage/#overview","title":"Overview","text":"<p>When a system is slow, the instinct is to immediately tune or add hardware. The disciplined approach is triage first: identify the constrained resource, confirm the bottleneck, then act. This chapter provides a repeatable triage workflow covering CPU, memory, disk I/O, and network.</p>"},{"location":"05-rhca/perf/resource-triage/#the-triage-hierarchy","title":"The Triage Hierarchy","text":"<pre><code>Is the system responding?\n  \u2514\u2500\u2500 Yes \u2192 Are processes stuck or thrashing?\n        \u2514\u2500\u2500 Check CPU / Memory first\n              \u2514\u2500\u2500 High CPU? \u2192 Who owns it? System or user?\n              \u2514\u2500\u2500 High Memory? \u2192 Swapping? OOM events?\n                    \u2514\u2500\u2500 I/O wait high? \u2192 Disk bottleneck\n                          \u2514\u2500\u2500 Network issues? \u2192 Packet drops / retransmits\n</code></pre> <p>Start at the top. Fix the most constrained layer before optimizing anything else.</p>"},{"location":"05-rhca/perf/resource-triage/#quick-assessment-first-60-seconds","title":"Quick Assessment \u2014 First 60 Seconds","text":"<p>Run these in sequence when you first encounter a slow system:</p> <pre><code># 1. Is the system reachable and how loaded?\n$ uptime\n 14:22:05 up 3 days, 2:14,  2 users,  load average: 8.21, 6.44, 4.12\n\n# 2. What is consuming CPU and memory right now?\n$ top -b -n 1 | head -20\n\n# 3. Any OOM kills in recent history?\n$ journalctl -k --since \"1 hour ago\" | grep -i oom\n\n# 4. Disk I/O wait\n$ iostat -x 1 3\n\n# 5. Memory pressure\n$ free -m\n\n# 6. Network errors\n$ ip -s link show ens3\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#reading-load-average","title":"Reading Load Average","text":"Load average vs CPU count Interpretation Load &lt; CPU count System has headroom Load \u2248 CPU count Fully utilized but not overloaded Load &gt; CPU count Runqueue backed up; likely bottleneck Load &gt;&gt; CPU count Severe overload \u2014 expect process starvation <pre><code># CPU count\n$ nproc\n4\n\n# If load average is 8 on a 4-core system \u2192 2\u00d7 overloaded\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#cpu-triage","title":"CPU Triage","text":""},{"location":"05-rhca/perf/resource-triage/#identify-cpu-consumers","title":"Identify CPU consumers","text":"<pre><code># Sort by CPU%, show top 10 processes\n$ ps aux --sort=-%cpu | head -11\n\n# More detailed: show threads, not just processes\n$ top -H   # press 'H' in interactive top to toggle threads\n\n# Per-CPU usage (useful for identifying single-threaded bottlenecks)\n$ mpstat -P ALL 1 5\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#distinguish-user-space-vs-kernel-space","title":"Distinguish user-space vs kernel-space","text":"<pre><code>$ vmstat 1 5\nprocs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----\n r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st\n 6  0      0 123456  12345 678901    0    0     0     2 1234 5678 72 15 12  1  0\n</code></pre> Column Meaning <code>us</code> User-space CPU time <code>sy</code> Kernel/system CPU time <code>id</code> Idle <code>wa</code> I/O wait <code>st</code> Stolen time (relevant in VMs) <p>High <code>sy</code>: kernel is busy \u2014 check for excessive syscalls, IRQ handling, or context switches. High <code>us</code>: application-level problem \u2014 identify the process. High <code>wa</code>: disk or NFS I/O is the real bottleneck, not CPU.</p>"},{"location":"05-rhca/perf/resource-triage/#syscall-overhead","title":"Syscall overhead","text":"<pre><code># How many context switches per second?\n$ vmstat 1 | awk '{print $12, $13}'\n# columns: interrupts/s  context-switches/s\n\n# Which process is making the most syscalls?\n$ strace -c -p &lt;PID&gt;   # attach to running process, Ctrl+C to stop and see summary\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#cpu-throttling-in-containers-cgroups","title":"CPU throttling in containers / cgroups","text":"<pre><code># Is a container being CPU-throttled?\n$ systemctl status &lt;service&gt;\n# Look for: CPUQuota= in the unit\n\n# cgroup v2 throttling stats\n$ cat /sys/fs/cgroup/system.slice/&lt;service.service&gt;/cpu.stat\nnr_throttled 1234\nthrottled_usec 5678000\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#memory-triage","title":"Memory Triage","text":""},{"location":"05-rhca/perf/resource-triage/#baseline-memory-usage","title":"Baseline memory usage","text":"<pre><code>$ free -m\n              total        used        free      shared  buff/cache   available\nMem:           7823        4521         312         234        2990        2834\nSwap:          2047          42        2005\n</code></pre> <p><code>available</code> is the most useful column \u2014 it includes reclaimable cache and shows how much memory a new process could actually get.</p>"},{"location":"05-rhca/perf/resource-triage/#detect-swapping","title":"Detect swapping","text":"<pre><code># Is the system actively swapping? (non-zero si/so is the concern)\n$ vmstat 1 5 | awk 'NR&gt;2 {print \"swap-in:\", $7, \"swap-out:\", $8}'\n\n# Which processes have the most swap usage?\n$ for pid in $(ls /proc | grep -E '^[0-9]+$'); do\n    swap=$(awk '/VmSwap/{print $2}' /proc/$pid/status 2&gt;/dev/null)\n    [ -n \"$swap\" ] &amp;&amp; [ \"$swap\" -gt 0 ] &amp;&amp; \\\n      echo \"$swap kB  $(cat /proc/$pid/comm 2&gt;/dev/null) (PID $pid)\"\n  done | sort -rn | head -10\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#oom-kill-history","title":"OOM kill history","text":"<pre><code># Recent OOM kills from kernel ring buffer\n$ journalctl -k | grep -E \"oom|killed process\" | tail -20\n\n# Identify which cgroup was killed\n$ journalctl -k | grep \"oom_kill_process\"\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#memory-overcommit-settings","title":"Memory overcommit settings","text":"<pre><code>$ sysctl vm.overcommit_memory vm.overcommit_ratio\nvm.overcommit_memory = 0    # 0=heuristic, 1=always allow, 2=never overcommit\nvm.overcommit_ratio = 50\n\n# Committed virtual memory vs physical\n$ cat /proc/meminfo | grep -E \"CommitLimit|Committed_AS\"\nCommitLimit:    5963000 kB\nCommitted_AS:   4812000 kB\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#disk-io-triage","title":"Disk I/O Triage","text":""},{"location":"05-rhca/perf/resource-triage/#iostat-the-primary-tool","title":"iostat \u2014 the primary tool","text":"<pre><code># Extended stats, 1-second interval, 5 iterations\n$ iostat -x 1 5\n\nDevice            r/s     w/s     rkB/s     wkB/s  await  %util\nvda              0.00   145.00      0.00  18560.00  45.23   98.2\n</code></pre> Column Meaning Concern threshold <code>r/s</code> / <code>w/s</code> Read/write operations per second Depends on device <code>await</code> Average I/O wait time in ms &gt; 20ms for HDD, &gt; 2ms for SSD <code>%util</code> Device utilization &gt; 80% sustained = bottleneck <code>svctm</code> Service time (deprecated but still present) \u2014"},{"location":"05-rhca/perf/resource-triage/#identify-the-process-causing-io","title":"Identify the process causing I/O","text":"<pre><code># iotop: per-process I/O (requires root or CAP_NET_ADMIN)\n$ sudo iotop -o -b -n 3 | head -20\n# -o = only show processes doing I/O\n\n# Alternative: pidstat\n$ pidstat -d 1 5\n# Shows per-process disk read/write rates\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#filesystem-saturation","title":"Filesystem saturation","text":"<pre><code># Disk space\n$ df -h\n\n# Inode exhaustion (a full inode table looks like a full disk)\n$ df -i\nFilesystem       Inodes  IUsed   IFree IUse% Mounted on\n/dev/vda3        524288 523900     388   99% /var/log   \u2190 inode exhausted\n\n# Find inode hogs\n$ find /var/log -xdev -printf '%h\\n' | sort | uniq -c | sort -rn | head -10\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#io-scheduler","title":"I/O scheduler","text":"<pre><code># Current scheduler for each block device\n$ cat /sys/block/vda/queue/scheduler\n[none] mq-deadline kyber bfq\n\n# For NVMe/SSDs, 'none' is typically optimal\n# For HDDs, 'mq-deadline' reduces latency for sequential workloads\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#network-triage","title":"Network Triage","text":""},{"location":"05-rhca/perf/resource-triage/#interface-level-statistics","title":"Interface-level statistics","text":"<pre><code># Errors, drops, overruns per interface\n$ ip -s link show ens3\n    RX: bytes  packets  errors  dropped missed  mcast\n    ...        ...      0       12      0       0\n    TX: bytes  packets  errors  dropped carrier collsns\n    ...        ...      0       0       0       0\n\n# Watch in real-time\n$ watch -n 1 'ip -s link show ens3'\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#tcp-retransmits-and-errors","title":"TCP retransmits and errors","text":"<pre><code># Global TCP stats\n$ ss -s\nTotal: 312\nTCP:   45 (estab 18, closed 12, orphaned 0, timewait 12)\n\n# Retransmit counters\n$ netstat -s | grep -i retran\n    3456 segments retransmitted\n\n# Per-socket retransmit info\n$ ss -ti | grep retrans\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#bandwidth-utilization","title":"Bandwidth utilization","text":"<pre><code># Install nload or use iftop for live bandwidth\n$ sudo dnf install -y nload\n$ sudo nload ens3\n\n# Or use sar for historical data\n$ sar -n DEV 1 10\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#connection-table-saturation","title":"Connection table saturation","text":"<pre><code># Are we running out of ephemeral ports or conntrack entries?\n$ sysctl net.ipv4.ip_local_port_range\nnet.ipv4.ip_local_port_range = 32768    60999\n\n$ sysctl net.nf_conntrack_max net.netfilter.nf_conntrack_count\n# If count is close to max, conntrack is saturated\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#putting-it-together-triage-worksheet","title":"Putting It Together \u2014 Triage Worksheet","text":"<p>When responding to a performance issue, capture these data points before making changes:</p> <pre><code>Date/Time of investigation:\nReported symptom:\n\n--- CPU ---\nLoad average (1/5/15):\nCPU count:\nTop CPU consumer (process + %):\nus/sy/id/wa breakdown:\n\n--- Memory ---\nTotal / Used / Available:\nSwap used / active swapping (si/so):\nRecent OOM kills (Y/N):\n\n--- Disk ---\nvda %util:\nawait (ms):\nTop I/O process:\nDisk space / inode usage:\n\n--- Network ---\nRX/TX errors or drops:\nTCP retransmit rate:\nConntrack utilization:\n\n--- Conclusion ---\nPrimary bottleneck:\nSecondary bottleneck (if any):\nRecommended action:\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#useful-one-liners-reference","title":"Useful One-Liners Reference","text":"<pre><code># Top 10 memory-consuming processes\n$ ps aux --sort=-%mem | head -11\n\n# Processes in uninterruptible sleep (D state) \u2014 usually stuck on I/O\n$ ps aux | awk '$8==\"D\"'\n\n# Open file count per process\n$ lsof | awk '{print $1}' | sort | uniq -c | sort -rn | head -10\n\n# Listening ports and owning processes\n$ ss -tlnp\n\n# Which process has a specific port open?\n$ ss -tlnp sport = :8080\n\n# Interrupt distribution across CPUs\n$ cat /proc/interrupts | sort -k2 -rn | head -20\n\n# Huge pages (important for databases)\n$ grep -E \"HugePages|Hugepagesize\" /proc/meminfo\n</code></pre>"},{"location":"05-rhca/perf/resource-triage/#recap","title":"Recap","text":"<p>Effective performance triage is systematic: start with load average, identify the bottleneck layer (CPU \u2192 memory \u2192 disk \u2192 network), then zoom into the specific process or subsystem. Never tune what you haven't measured. Capture a before snapshot, make one change, measure again.</p> <p>Next: tuned profiles and kernel parameter tuning</p>"},{"location":"05-rhca/perf/tuned/","title":"tuned \u2014 Profile-Based System Tuning","text":""},{"location":"05-rhca/perf/tuned/#overview","title":"Overview","text":"<p><code>tuned</code> is a systemd service that applies kernel parameter and device setting profiles appropriate for a given workload. On RHEL 10 it is the first-line tuning tool \u2014 apply the right profile before manually touching any <code>sysctl</code> knobs.</p>"},{"location":"05-rhca/perf/tuned/#how-tuned-works","title":"How tuned Works","text":"<pre><code>tuned daemon\n  \u251c\u2500\u2500 reads /etc/tuned/tuned-main.conf\n  \u251c\u2500\u2500 loads active profile from /etc/tuned/active_profile\n  \u251c\u2500\u2500 applies settings via plugins (sysctl, disk, cpu, net, ...)\n  \u2514\u2500\u2500 monitors system and adjusts dynamically (if dynamic tuning enabled)\n</code></pre> <p>Profile directories: - <code>/usr/lib/tuned/</code> \u2014 shipped profiles (read-only) - <code>/etc/tuned/</code> \u2014 custom profiles (override or extend shipped ones)</p>"},{"location":"05-rhca/perf/tuned/#installing-and-enabling-tuned","title":"Installing and Enabling tuned","text":"<pre><code># Install (usually pre-installed on RHEL 10)\n$ sudo dnf install -y tuned\n\n# Enable and start\n$ sudo systemctl enable --now tuned\n\n# Verify\n$ sudo systemctl status tuned\nActive: active (running)\n</code></pre>"},{"location":"05-rhca/perf/tuned/#listing-and-selecting-profiles","title":"Listing and Selecting Profiles","text":"<pre><code># List all available profiles\n$ tuned-adm list\nAvailable profiles:\n- accelerator-performance\n- balanced\n- desktop\n- hpc-compute\n- latency-performance\n- network-latency\n- network-throughput\n- powersave\n- throughput-performance\n- virtual-guest\n- virtual-host\nCurrent active profile: virtual-guest\n</code></pre>"},{"location":"05-rhca/perf/tuned/#profile-selection-guide","title":"Profile selection guide","text":"Profile Use Case <code>throughput-performance</code> Batch workloads, databases; maximizes I/O and CPU throughput <code>latency-performance</code> Low-latency apps; disables power management, CPU frequency scaling <code>network-latency</code> Network-intensive; adjusts NIC ring buffers, IRQ coalescing <code>network-throughput</code> High-bandwidth transfers; large socket buffers <code>virtual-guest</code> Default for VMs; balances power and performance <code>virtual-host</code> KVM hypervisor hosts; enables THP, optimizes for guest I/O <code>hpc-compute</code> HPC / scientific computing; disables NUMA balancing overhead <code>balanced</code> Default for physical servers; adaptive power/performance <code>powersave</code> Energy-constrained environments; aggressive CPU throttling <pre><code># Switch to a profile\n$ sudo tuned-adm profile throughput-performance\n\n# Verify the switch\n$ tuned-adm active\nCurrent active profile: throughput-performance\n\n# Check what the profile actually sets\n$ tuned-adm profile_info throughput-performance\n</code></pre>"},{"location":"05-rhca/perf/tuned/#inspecting-profile-contents","title":"Inspecting Profile Contents","text":"<pre><code># Read the profile definition\n$ cat /usr/lib/tuned/throughput-performance/tuned.conf\n</code></pre> <p>Example (abbreviated):</p> <pre><code>[main]\nsummary=Throughput performance based on Red Hat Enterprise Linux tuning\n\n[cpu]\nforce_latency=cstate.id:1|3\ngovernor=performance\nenergy_perf_bias=performance\nmin_perf_pct=100\n\n[vm]\ntransparent_hugepages=always\n\n[disk]\nreadahead=&gt;4096\n\n[sysctl]\nkernel.sched_min_granularity_ns=10000000\nkernel.sched_wakeup_granularity_ns=15000000\nvm.dirty_ratio=40\nvm.dirty_background_ratio=10\nnet.core.somaxconn=65536\nnet.core.rmem_max=16777216\nnet.core.wmem_max=16777216\n</code></pre>"},{"location":"05-rhca/perf/tuned/#creating-a-custom-profile","title":"Creating a Custom Profile","text":"<p>Custom profiles live in <code>/etc/tuned/&lt;profile-name&gt;/tuned.conf</code>. The recommended approach is to inherit a base profile and override only what you need:</p> <pre><code>$ sudo mkdir /etc/tuned/rhca-web\n$ sudo tee /etc/tuned/rhca-web/tuned.conf &lt;&lt;'EOF'\n[main]\nsummary=RHCA lab - tuned profile for web server workloads\ninclude=throughput-performance\n\n[sysctl]\n# Increase listen backlog for high-concurrency web\nnet.core.somaxconn=131072\nnet.ipv4.tcp_max_syn_backlog=65536\n\n# Reduce TIME_WAIT socket accumulation\nnet.ipv4.tcp_tw_reuse=1\nnet.ipv4.tcp_fin_timeout=15\n\n# File descriptor limits\nfs.file-max=2097152\n\n[vm]\ntransparent_hugepages=never\n\n[disk]\n# NVMe: use none scheduler; HDD: use mq-deadline\n# scheduler= is applied per-device\nEOF\n</code></pre> <pre><code># Activate your custom profile\n$ sudo tuned-adm profile rhca-web\n\n# Verify\n$ tuned-adm active\nCurrent active profile: rhca-web\n</code></pre>"},{"location":"05-rhca/perf/tuned/#verifying-what-tuned-applied","title":"Verifying What tuned Applied","text":"<pre><code># Check current value of a sysctl that tuned should have set\n$ sysctl net.core.somaxconn\nnet.core.somaxconn = 131072\n\n# Verify CPU governor tuned set\n$ cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor\nperformance\n\n# Transparent Huge Pages\n$ cat /sys/kernel/mm/transparent_hugepage/enabled\nalways madvise [never]   \u2190 'never' is selected (bracketed)\n\n# Disk read-ahead\n$ blockdev --getra /dev/vda\n8192   \u2190 4096 sectors \u00d7 512 bytes = 4096 kB\n</code></pre>"},{"location":"05-rhca/perf/tuned/#dynamic-tuning","title":"Dynamic Tuning","text":"<p>tuned can monitor the system and adjust settings automatically (e.g., reduce CPU power during idle, boost during load spikes):</p> <pre><code># Enable dynamic tuning globally\n$ sudo sed -i 's/^#dynamic_tuning=0/dynamic_tuning=1/' /etc/tuned/tuned-main.conf\n\n# Restart tuned to apply\n$ sudo systemctl restart tuned\n\n# Check sampling interval\n$ grep update_interval /etc/tuned/tuned-main.conf\nupdate_interval=10   # seconds\n</code></pre> <p>Caution: Dynamic tuning can interfere with latency-sensitive workloads. Keep it disabled (<code>dynamic_tuning=0</code>) for <code>latency-performance</code> profiles.</p>"},{"location":"05-rhca/perf/tuned/#recommend-profile-with-tuned-adm","title":"Recommend Profile with tuned-adm","text":"<p>tuned can analyze the system and suggest a profile:</p> <pre><code>$ sudo tuned-adm recommend\nvirtual-guest\n</code></pre> <p>Use this as a starting point \u2014 not necessarily the final choice.</p>"},{"location":"05-rhca/perf/tuned/#persistent-custom-sysctl-supplement-to-tuned","title":"Persistent Custom sysctl (Supplement to tuned)","text":"<p>For settings that don't fit a tuned plugin, use drop-in files under <code>/etc/sysctl.d/</code>:</p> <pre><code>$ sudo tee /etc/sysctl.d/99-rhca-perf.conf &lt;&lt;'EOF'\n# Increase inotify watches for large deployments\nfs.inotify.max_user_watches=524288\nfs.inotify.max_user_instances=512\n\n# Disable IPv6 if not used (reduces kernel overhead)\nnet.ipv6.conf.all.disable_ipv6=1\nnet.ipv6.conf.default.disable_ipv6=1\nEOF\n\n# Apply immediately without reboot\n$ sudo sysctl --system\n</code></pre> <p>Rule of thumb: Use <code>tuned</code> for workload-level profiles; use <code>/etc/sysctl.d/</code> for site-specific tweaks. Never set both in a way that conflicts (tuned will win at activation time).</p>"},{"location":"05-rhca/perf/tuned/#lab-profile-switch-and-verification","title":"Lab \u2014 Profile Switch and Verification","text":"<p>Goal: Switch to <code>latency-performance</code>, verify the CPU governor and THP change, then create a custom profile that inherits it.</p> <p>Estimated time: 20 minutes</p>"},{"location":"05-rhca/perf/tuned/#steps","title":"Steps","text":"<pre><code># 1. Record current state\n$ tuned-adm active\n$ cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor\n$ cat /sys/kernel/mm/transparent_hugepage/enabled\n\n# 2. Switch to latency-performance\n$ sudo tuned-adm profile latency-performance\n\n# 3. Verify changes\n$ cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor\nperformance\n$ cat /sys/kernel/mm/transparent_hugepage/enabled\nalways madvise [never]\n\n# 4. Create a custom profile based on latency-performance\n$ sudo mkdir /etc/tuned/rhca-latency\n$ sudo tee /etc/tuned/rhca-latency/tuned.conf &lt;&lt;'EOF'\n[main]\nsummary=RHCA custom latency profile\ninclude=latency-performance\n\n[sysctl]\n# Reduce scheduling latency\nkernel.sched_min_granularity_ns=3000000\nkernel.sched_wakeup_granularity_ns=4000000\nEOF\n\n# 5. Activate and verify\n$ sudo tuned-adm profile rhca-latency\n$ tuned-adm active\nCurrent active profile: rhca-latency\n\n$ sysctl kernel.sched_min_granularity_ns\nkernel.sched_min_granularity_ns = 3000000\n</code></pre>"},{"location":"05-rhca/perf/tuned/#success-criteria","title":"Success criteria","text":"<ul> <li>[ ] <code>tuned-adm active</code> shows <code>rhca-latency</code></li> <li>[ ] CPU governor is <code>performance</code></li> <li>[ ] THP is <code>never</code></li> <li>[ ] <code>kernel.sched_min_granularity_ns</code> is <code>3000000</code></li> </ul>"},{"location":"05-rhca/perf/tuned/#cleanup","title":"Cleanup","text":"<pre><code>$ sudo tuned-adm profile virtual-guest   # or whatever was active before\n$ sudo rm -rf /etc/tuned/rhca-latency\n</code></pre>"},{"location":"05-rhca/perf/tuned/#common-failures","title":"Common Failures","text":"Symptom Likely Cause Fix <code>tuned-adm: command not found</code> Package not installed <code>sudo dnf install -y tuned</code> Profile switch silently has no effect tuned service not running <code>sudo systemctl start tuned</code> Custom profile not listed Syntax error in <code>tuned.conf</code> Check <code>journalctl -u tuned</code> <code>include=</code> profile not found Typo in base profile name <code>tuned-adm list</code> to verify exact name CPU governor stays at <code>powersave</code> after switch CPU frequency scaling driver doesn't support governor change Check <code>cpupower frequency-info</code> Settings revert on reboot Custom <code>/etc/sysctl.d/</code> file conflicts with tuned Remove duplicates; let tuned own the knob"},{"location":"05-rhca/perf/tuned/#why-this-matters-in-production","title":"Why This Matters in Production","text":"<p>A server running the wrong <code>tuned</code> profile can:</p> <ul> <li>Leave CPU in <code>powersave</code> mode, halving throughput under load</li> <li>Keep Transparent Huge Pages enabled for Redis or MongoDB (causing latency spikes and OOM behavior)</li> <li>Use default socket buffers that cannot sustain 10 GbE line rate</li> </ul> <p>Setting the correct profile at provisioning time (via Ansible: <code>community.general.tuned</code> role or <code>command: tuned-adm profile</code>) eliminates an entire class of unexplained performance problems.</p>"},{"location":"05-rhca/perf/tuned/#recap","title":"Recap","text":"Task Command List profiles <code>tuned-adm list</code> Show active <code>tuned-adm active</code> Switch profile <code>sudo tuned-adm profile &lt;name&gt;</code> Recommend <code>sudo tuned-adm recommend</code> Show profile info <code>tuned-adm profile_info &lt;name&gt;</code> Create custom <code>/etc/tuned/&lt;name&gt;/tuned.conf</code> with <code>include=</code> <p>Next: Recovery Patterns</p>"},{"location":"05-rhca/selinux/audit-workflow/","title":"Audit Workflow \u2014 ausearch, sealert, audit2why","text":"<p>The audit subsystem logs all SELinux denials and security-relevant events. A systematic audit workflow lets you diagnose and fix issues quickly and correctly.</p>"},{"location":"05-rhca/selinux/audit-workflow/#the-audit-subsystem","title":"The audit subsystem","text":"Component Role <code>auditd</code> Audit daemon \u2014 writes events to <code>/var/log/audit/audit.log</code> <code>ausearch</code> Query and filter audit events <code>aureport</code> Generate summary reports from audit log <code>audit2why</code> Explain AVC denials in plain language <code>audit2allow</code> Generate policy from denials (last resort) <code>sealert</code> Rich analysis with ranked fix suggestions <pre><code># Ensure auditd is running\nsudo systemctl status auditd\n</code></pre>"},{"location":"05-rhca/selinux/audit-workflow/#ausearch-targeted-queries","title":"ausearch \u2014 targeted queries","text":"<pre><code># Recent AVC denials (last 10 minutes)\nsudo ausearch -m avc -ts recent\n\n# Today's AVC denials\nsudo ausearch -m avc -ts today\n\n# AVC denials for a specific process\nsudo ausearch -m avc -c httpd\n\n# AVC denials for a specific user\nsudo ausearch -m avc -ui alice\n\n# AVC denials for a specific object (filename)\nsudo ausearch -m avc -f index.html\n\n# Between two timestamps\nsudo ausearch -m avc -ts \"02/23/2026 08:00:00\" -te \"02/23/2026 09:00:00\"\n\n# Raw output (for piping to audit2why)\nsudo ausearch -m avc -ts today --raw\n</code></pre>"},{"location":"05-rhca/selinux/audit-workflow/#read-an-avc-record","title":"Read an AVC record","text":"<pre><code>type=AVC msg=audit(1708692000.123:456): avc:  denied  { read } for\n  pid=1234 comm=\"httpd\" name=\"index.html\"\n  dev=\"vda3\" ino=1234567\n  scontext=system_u:system_r:httpd_t:s0\n  tcontext=user_u:object_r:user_home_t:s0\n  tclass=file permissive=0\n</code></pre> Field Value Meaning <code>denied { read }</code> read The permission being denied <code>comm</code> httpd The process name <code>name</code> index.html The target file <code>scontext</code> <code>httpd_t</code> Source (process) SELinux type <code>tcontext</code> <code>user_home_t</code> Target (file) SELinux type <code>tclass</code> file Object class <code>permissive</code> 0 0=enforcing (was blocked), 1=permissive (was allowed but logged)"},{"location":"05-rhca/selinux/audit-workflow/#audit2why-explain-denials","title":"audit2why \u2014 explain denials","text":"<pre><code># Explain all recent denials\nsudo ausearch -m avc -ts recent | sudo audit2why\n\n# Explain denials for one process\nsudo ausearch -m avc -c httpd | sudo audit2why\n</code></pre> <p><code>audit2why</code> will tell you: - What the denial means in plain language - Whether a boolean fix exists - Whether the target needs a different label</p>"},{"location":"05-rhca/selinux/audit-workflow/#aureport-summary-statistics","title":"aureport \u2014 summary statistics","text":"<pre><code># Summary of all events\nsudo aureport\n\n# AVC denial summary\nsudo aureport --avc\n\n# Failed access summary\nsudo aureport --failed\n\n# Authentication events\nsudo aureport --auth\n\n# Summary for today\nsudo aureport --start today --end now\n</code></pre>"},{"location":"05-rhca/selinux/audit-workflow/#sealert-rich-analysis","title":"sealert \u2014 rich analysis","text":"<p>Requires <code>setroubleshoot-server</code> installed:</p> <pre><code>sudo dnf install -y setroubleshoot-server\n\n# Analyse the audit log\nsudo sealert -a /var/log/audit/audit.log\n</code></pre> <p><code>sealert</code> output shows: - Description of the denial - Probability-ranked list of fixes - The exact command to run for each fix - Whether it is a known policy bug</p>"},{"location":"05-rhca/selinux/audit-workflow/#the-systematic-workflow","title":"The systematic workflow","text":"<pre><code>1. Reproduce the problem\n2. ausearch -m avc -ts recent  \u2192 identify the AVC\n3. audit2why                   \u2192 understand the denial\n4. Apply fix taxonomy (label/boolean/port/policy)\n5. Re-test\n6. ausearch -m avc -ts recent  \u2192 confirm no new denials\n7. Document the fix\n</code></pre>"},{"location":"05-rhca/selinux/audit-workflow/#logging-permissive-denials-dont-miss-them","title":"Logging permissive denials (don't miss them)","text":"<p>Even in permissive mode, denials are logged with <code>permissive=1</code>. This lets you audit what would be denied before switching to enforcing:</p> <pre><code>sudo setenforce 0            # permissive (temporary)\n# reproduce the problem\nsudo ausearch -m avc -ts recent   # see permissive denials\nsudo setenforce 1            # back to enforcing\n</code></pre>"},{"location":"05-rhca/selinux/audit-workflow/#next-step","title":"Next step","text":"<p>\u2192 Lab: Non-Default Port</p>"},{"location":"05-rhca/selinux/fix-taxonomy/","title":"SELinux Fix Taxonomy \u2014 Label vs Boolean vs Port vs Policy","text":"<p>The most important SELinux skill is choosing the correct type of fix for a given AVC denial. Applying the wrong fix either doesn't work or creates a security hole.</p>"},{"location":"05-rhca/selinux/fix-taxonomy/#the-fix-decision-tree","title":"The fix decision tree","text":"<pre><code>AVC denial observed\n        \u2502\n        \u25bc\nIs the file/dir in a non-standard location?\n  Yes \u2192 semanage fcontext + restorecon\n  No  \u2192 Is there a boolean that covers this use case?\n          Yes \u2192 setsebool -P\n          No  \u2192 Is it a non-default port binding?\n                  Yes \u2192 semanage port\n                  No  \u2192 Is this a legitimate access that policy doesn't cover?\n                          Yes \u2192 audit2allow (last resort, with review)\n                          No  \u2192 Fix the application, not the policy\n</code></pre>"},{"location":"05-rhca/selinux/fix-taxonomy/#fix-type-1-file-context-most-common","title":"Fix type 1: File context (most common)","text":"<p>When to use: A file or directory has the wrong SELinux type label. This happens when: - Files are placed in a non-standard path - Files are moved with <code>mv</code> (context follows source, not destination) - Freshly created files in a custom directory</p> <p>Fix:</p> <pre><code># Add a persistent fcontext rule\nsudo semanage fcontext -a -t httpd_sys_content_t \"/srv/myapp(/.*)?\"\n\n# Apply it (run every time files change)\nsudo restorecon -Rv /srv/myapp/\n\n# Verify\nls -Z /srv/myapp/\n</code></pre> <p>View existing rules:</p> <pre><code>sudo semanage fcontext -l | grep /srv\n</code></pre> <p>Remove a rule:</p> <pre><code>sudo semanage fcontext -d \"/srv/myapp(/.*)?\"\n</code></pre>"},{"location":"05-rhca/selinux/fix-taxonomy/#fix-type-2-boolean-targeted-policy-switch","title":"Fix type 2: Boolean (targeted policy switch)","text":"<p>When to use: The access pattern is known and policy already has a pre-built switch for it.</p> <p>Fix:</p> <pre><code># Find relevant boolean\nsudo getsebool -a | grep httpd\n\n# Enable (persistent)\nsudo setsebool -P httpd_can_network_connect on\n\n# Verify\ngetsebool httpd_can_network_connect\n</code></pre> <p>Common booleans (RHEL infra focus):</p> Boolean Enables <code>httpd_can_network_connect</code> Apache outbound network connections <code>httpd_can_network_connect_db</code> Apache to DB (MySQL/PostgreSQL) <code>httpd_use_nfs</code> Apache to serve NFS content <code>httpd_use_cifs</code> Apache to serve CIFS/SMB content <code>httpd_enable_homedirs</code> Apache to serve from user home dirs <code>samba_enable_home_dirs</code> Samba to share home directories <code>container_use_devices</code> Container to access devices <code>virt_use_nfs</code> Virtualization to use NFS <code>ssh_use_tcpd</code> SSH with TCP wrappers"},{"location":"05-rhca/selinux/fix-taxonomy/#fix-type-3-port-label","title":"Fix type 3: Port label","text":"<p>When to use: A service needs to bind to a non-default port.</p> <pre><code># Check what ports are allowed for http\nsudo semanage port -l | grep http_port\n\n# Add a new port\nsudo semanage port -a -t http_port_t -p tcp 8080\n\n# Modify an existing label\nsudo semanage port -m -t http_port_t -p tcp 8080\n\n# Remove\nsudo semanage port -d -t http_port_t -p tcp 8080\n\n# Verify\nsudo semanage port -l | grep 8080\n</code></pre>"},{"location":"05-rhca/selinux/fix-taxonomy/#fix-type-4-audit2allow-last-resort","title":"Fix type 4: audit2allow (last resort)","text":"<p><code>audit2allow</code> generates policy modules from audit logs. Use it only after ruling out all other fix types, and always review what it generates.</p> <pre><code># Step 1: isolate the specific denial you want to fix\nsudo ausearch -m avc -c myapp -ts today &gt; /tmp/myapp-avc.txt\n\n# Step 2: understand what it will allow\nsudo audit2allow -i /tmp/myapp-avc.txt\n\n# Step 3: generate a type enforcement file\nsudo audit2allow -i /tmp/myapp-avc.txt -M myapp-local\n\n# Step 4: REVIEW myapp-local.te before loading\ncat myapp-local.te\n\n# Step 5: load the policy module\nsudo semodule -i myapp-local.pp\n</code></pre> <p>Review audit2allow output carefully</p> <p>audit2allow generates the minimum policy to allow what was denied, but it has no context about whether the access is intended. A module that allows <code>httpd_t</code> to read <code>shadow_t</code> (password hashes) would be generated if httpd somehow triggered that denial \u2014 and it would be wrong.</p>"},{"location":"05-rhca/selinux/fix-taxonomy/#fix-type-5-the-application-is-wrong","title":"Fix type 5: The application is wrong","text":"<p>If the AVC shows a service accessing something it should never need (e.g., a web server reading <code>/etc/shadow</code>), the correct fix is to fix the application configuration, not the SELinux policy.</p> <p>SELinux is revealing a configuration problem or a security issue. Granting access masks it.</p>"},{"location":"05-rhca/selinux/fix-taxonomy/#next-step","title":"Next step","text":"<p>\u2192 semanage Reference</p>"},{"location":"05-rhca/selinux/semanage/","title":"semanage \u2014 fcontext, port, boolean","text":"<p><code>semanage</code> (SELinux Policy Management) is the tool for making persistent SELinux policy customisations without recompiling the full policy.</p>"},{"location":"05-rhca/selinux/semanage/#install","title":"Install","text":"<pre><code>sudo dnf install -y policycoreutils-python-utils\n</code></pre>"},{"location":"05-rhca/selinux/semanage/#semanage-fcontext","title":"semanage fcontext","text":"<p>Manage file context definitions \u2014 what SELinux type a path should have.</p> <pre><code># List all custom fcontext rules\nsudo semanage fcontext -l -C\n\n# List all rules (including defaults) for a path\nsudo semanage fcontext -l | grep /var/www\n\n# Add a rule for a directory and all its contents\nsudo semanage fcontext -a -t httpd_sys_content_t \"/srv/webapp(/.*)?\"\n\n# Add a rule for a specific file\nsudo semanage fcontext -a -t sshd_key_t \"/etc/ssh/keys/id_rsa_custom\"\n\n# Modify an existing custom rule\nsudo semanage fcontext -m -t httpd_sys_rw_content_t \"/srv/webapp/uploads(/.*)?\"\n\n# Delete a custom rule\nsudo semanage fcontext -d \"/srv/webapp(/.*)?\"\n\n# Apply rules to existing files\nsudo restorecon -Rv /srv/webapp/\n</code></pre> <p>semanage fcontext alone is not enough</p> <p><code>semanage fcontext</code> defines the rule in policy. It does not change the labels on existing files. Always follow with <code>restorecon -Rv &lt;path&gt;</code>.</p>"},{"location":"05-rhca/selinux/semanage/#common-types","title":"Common types","text":"Type Used for <code>httpd_sys_content_t</code> Web server read-only content <code>httpd_sys_rw_content_t</code> Web server read-write content (uploads) <code>sshd_key_t</code> SSH private keys <code>postgresql_db_t</code> PostgreSQL data <code>mysqld_db_t</code> MySQL/MariaDB data <code>container_file_t</code> Files accessible by rootless containers <code>var_log_t</code> Log files <code>admin_home_t</code> Admin user home directory"},{"location":"05-rhca/selinux/semanage/#semanage-port","title":"semanage port","text":"<p>Manage port-to-type mappings:</p> <pre><code># List all port definitions\nsudo semanage port -l\n\n# List ports for a specific type\nsudo semanage port -l | grep http_port\n\n# Add a port\nsudo semanage port -a -t http_port_t -p tcp 8080\nsudo semanage port -a -t ssh_port_t -p tcp 2222\n\n# Modify (change type of an existing custom port)\nsudo semanage port -m -t http_port_t -p tcp 8443\n\n# Delete a custom port\nsudo semanage port -d -t http_port_t -p tcp 8080\n\n# View custom (non-default) entries only\nsudo semanage port -l -C\n</code></pre>"},{"location":"05-rhca/selinux/semanage/#semanage-boolean","title":"semanage boolean","text":"<p>Manage SELinux booleans persistently (same as <code>setsebool -P</code>):</p> <pre><code># List all booleans\nsudo semanage boolean -l\n\n# List custom (non-default) booleans\nsudo semanage boolean -l -C\n\n# Set a boolean (persistent)\nsudo semanage boolean -m --on httpd_can_network_connect\nsudo semanage boolean -m --off httpd_can_network_connect\n</code></pre>"},{"location":"05-rhca/selinux/semanage/#semanage-login-and-user","title":"semanage login and user","text":"<p>Map Linux users to SELinux users:</p> <pre><code># List login mappings\nsudo semanage login -l\n\n# Map a Linux user to an SELinux user\nsudo semanage login -a -s staff_u alice\nsudo semanage login -a -s sysadm_u admin\n\n# Delete mapping\nsudo semanage login -d alice\n</code></pre>"},{"location":"05-rhca/selinux/semanage/#export-and-import-policy-customisations","title":"Export and import policy customisations","text":"<pre><code># Export all local customisations\nsudo semanage export -f /tmp/selinux-customisations.txt\n\n# Import on another host\nsudo semanage import -f /tmp/selinux-customisations.txt\n</code></pre> <p>This is very useful for applying the same SELinux customisations across multiple hosts via Ansible.</p>"},{"location":"05-rhca/selinux/semanage/#next-step","title":"Next step","text":"<p>\u2192 Audit Workflow</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/","title":"Lab: Non-Default Port \u2014 Correct SELinux Fix","text":"<p>Track: RHCA Estimated time: 40 minutes Topology: Single VM</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#prerequisites","title":"Prerequisites","text":"<ul> <li>Completed Fix Taxonomy, semanage, and Audit Workflow</li> <li>httpd installed: <code>sudo dnf install -y httpd</code></li> <li>SELinux tools: <code>sudo dnf install -y policycoreutils-python-utils</code></li> <li>VM snapshot taken</li> </ul>"},{"location":"05-rhca/selinux/labs/nondefault-port/#success-criteria","title":"Success criteria","text":"<ul> <li>httpd serves content on port 9090</li> <li>The fix uses <code>semanage port</code> (not <code>setenforce 0</code>, not disabling firewalld)</li> <li>AVC denials are visible before the fix and absent after</li> <li>firewalld is correctly configured for port 9090</li> </ul>"},{"location":"05-rhca/selinux/labs/nondefault-port/#steps","title":"Steps","text":""},{"location":"05-rhca/selinux/labs/nondefault-port/#1-configure-httpd-to-listen-on-port-9090","title":"1 \u2014 Configure httpd to listen on port 9090","text":"<pre><code>sudo vim /etc/httpd/conf/httpd.conf\n</code></pre> <p>Find the <code>Listen 80</code> line and change it to:</p> <pre><code>Listen 9090\n</code></pre>"},{"location":"05-rhca/selinux/labs/nondefault-port/#2-try-to-start-httpd-expect-failure","title":"2 \u2014 Try to start httpd (expect failure)","text":"<pre><code>sudo systemctl restart httpd\nsudo systemctl status httpd\n</code></pre> <p>Expected: httpd fails to start.</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#3-read-the-audit-log","title":"3 \u2014 Read the audit log","text":"<pre><code>sudo ausearch -m avc -ts recent\n</code></pre> <p>You should see a denial like:</p> <pre><code>denied { name_bind } for pid=... comm=\"httpd\" src=9090\nscontext=system_u:system_r:httpd_t:s0\ntcontext=system_u:object_r:unreserved_port_t:s0\n</code></pre> <p>The key: <code>name_bind</code> denied, port 9090, context <code>unreserved_port_t</code>. httpd is allowed to bind <code>http_port_t</code> ports \u2014 but 9090 is <code>unreserved_port_t</code>.</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#4-confirm-with-audit2why","title":"4 \u2014 Confirm with audit2why","text":"<pre><code>sudo ausearch -m avc -ts recent | sudo audit2why\n</code></pre> <p>Look for: \"SELinux is preventing httpd from name_bind access on the tcp_socket port 9090.\"</p> <p>It should suggest a boolean (<code>httpd_can_network_relabelfrom</code>) or a <code>semanage port</code> fix. The correct fix is <code>semanage port</code>.</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#5-apply-the-correct-fix","title":"5 \u2014 Apply the correct fix","text":"<pre><code># Check what's currently assigned to http_port_t\nsudo semanage port -l | grep http_port\n\n# Add port 9090 to http_port_t\nsudo semanage port -a -t http_port_t -p tcp 9090\n</code></pre> <p>Verify policy</p> <p><pre><code>sudo semanage port -l | grep 9090\n</code></pre> Look for: <code>http_port_t  tcp  9090, ...</code></p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#6-open-port-9090-in-firewalld","title":"6 \u2014 Open port 9090 in firewalld","text":"<pre><code>sudo firewall-cmd --permanent --add-port=9090/tcp\nsudo firewall-cmd --reload\n</code></pre>"},{"location":"05-rhca/selinux/labs/nondefault-port/#7-start-httpd-and-verify","title":"7 \u2014 Start httpd and verify","text":"<pre><code>sudo systemctl restart httpd\nsudo systemctl status httpd\n</code></pre> <p>Expected: <code>Active: active (running)</code></p> <pre><code>curl http://localhost:9090/\n</code></pre> <p>Expected: the RHEL test page or 403 (content root is empty) \u2014 but NOT a connection refused.</p> <p>Verify \u2014 No new AVC denials</p> <p><pre><code>sudo ausearch -m avc -ts recent\n</code></pre> No new denials related to httpd and port 9090.</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#8-confirm-with-ss","title":"8 \u2014 Confirm with ss","text":"<pre><code>ss -tlnp | grep :9090\n</code></pre> <p>Look for: httpd/httpd worker listening on <code>:9090</code>.</p>"},{"location":"05-rhca/selinux/labs/nondefault-port/#cleanup","title":"Cleanup","text":"<pre><code>sudo systemctl disable --now httpd\nsudo firewall-cmd --permanent --remove-port=9090/tcp\nsudo firewall-cmd --reload\nsudo semanage port -d -t http_port_t -p tcp 9090\n# Restore httpd.conf\nsudo vim /etc/httpd/conf/httpd.conf   # change Listen 9090 back to Listen 80\n</code></pre>"},{"location":"05-rhca/selinux/labs/nondefault-port/#common-failures","title":"Common failures","text":"Symptom Likely cause Fix httpd still fails after semanage firewalld not opened <code>firewall-cmd --permanent --add-port=9090/tcp</code> <code>semanage port -a</code> fails with \"already defined\" Port has a conflicting existing label Use <code>-m</code> (modify) instead of <code>-a</code> No AVC in audit log auditd not running <code>sudo systemctl start auditd</code> curl returns \"connection refused\" SELinux fix applied but service not restarted <code>sudo systemctl restart httpd</code>"},{"location":"05-rhca/selinux/labs/nondefault-port/#why-this-matters-in-production","title":"Why this matters in production","text":"<p>Microservices, custom applications, and legacy software commonly run on non-standard ports. The correct RHEL workflow is always: identify the port's SELinux type requirement \u2192 <code>semanage port -a</code> \u2192 <code>firewall-cmd --permanent</code>. Skipping either step creates either a security gap or a broken service.</p>"},{"location":"90-labs/","title":"Lab Environments","text":""},{"location":"90-labs/#overview","title":"Overview","text":"<p>The labs in this guide are designed for hands-on practice in a safe, isolated environment. This section describes the two supported configurations:</p> Configuration Best For Single-VM Working through all chapters sequentially; most exercises Multi-VM Ansible labs, networking labs, replication, multi-node scenarios"},{"location":"90-labs/#what-you-need","title":"What You Need","text":""},{"location":"90-labs/#hardware-or-cloud-instance","title":"Hardware or Cloud Instance","text":"Resource Minimum Recommended CPU 2 vCPUs 4 vCPUs RAM 2 GB 4 GB Disk 20 GB 40 GB Network 1 NIC 1 NIC + optional 2nd <p>A KVM/QEMU hypervisor on Linux is the assumed environment. VirtualBox, VMware Workstation, or a cloud provider (AWS, GCP, Azure) also work \u2014 adapt disk device names (<code>/dev/vda</code> \u2192 <code>/dev/sda</code> or <code>/dev/nvme0n1</code>) as needed.</p>"},{"location":"90-labs/#rhel-10-installation-media","title":"RHEL 10 Installation Media","text":"<p>Download from the Red Hat Customer Portal (subscription required) or use a free developer account:</p> <ul> <li>Developer subscription: https://developers.redhat.com</li> <li>ISO: Red Hat Enterprise Linux 10.x DVD ISO</li> </ul>"},{"location":"90-labs/#lab-vm-conventions","title":"Lab VM Conventions","text":"<p>Throughout the labs:</p> Convention Value Primary disk <code>/dev/vda</code> Network interface <code>ens3</code> Non-root user <code>student</code> Hostname (single VM) <code>rhel10.lab.local</code> Hostname (multi-VM controller) <code>controller.lab.local</code> Hostname (multi-VM managed nodes) <code>node1.lab.local</code>, <code>node2.lab.local</code>"},{"location":"90-labs/#quick-start-checklist","title":"Quick-Start Checklist","text":"<p>Before starting Chapter 1:</p> <ul> <li>[ ] RHEL 10 installed and boots successfully</li> <li>[ ] <code>student</code> user created with <code>sudo</code> access</li> <li>[ ] System registered (<code>subscription-manager register</code>) or connected to a local mirror</li> <li>[ ] SSH key-based login working from your workstation to the VM</li> <li>[ ] Internet or local mirror accessible for <code>dnf</code></li> </ul> <p>See Single-VM Setup for the detailed walkthrough.</p>"},{"location":"90-labs/multi-vm/","title":"Multi-VM Lab Setup","text":""},{"location":"90-labs/multi-vm/#overview","title":"Overview","text":"<p>Some labs require multiple machines: Ansible (controller + managed nodes), networking labs (two hosts to route between), and replication scenarios. This page describes a three-VM setup using KVM/libvirt.</p> <p>Topology:</p> <pre><code>KVM Host\n\u251c\u2500\u2500 controller.lab.local  (4 GB RAM, 20 GB disk) \u2014 Ansible control node\n\u251c\u2500\u2500 node1.lab.local       (2 GB RAM, 20 GB disk) \u2014 managed node\n\u2514\u2500\u2500 node2.lab.local       (2 GB RAM, 20 GB disk) \u2014 managed node\n</code></pre> <p>All three VMs share a private <code>labnet</code> libvirt network (192.168.100.0/24) in addition to the default NAT network for internet access.</p>"},{"location":"90-labs/multi-vm/#prerequisites","title":"Prerequisites","text":"<ul> <li>The single-VM lab setup working and understood</li> <li>At least 12 GB RAM free on the KVM host</li> <li>70 GB free disk space</li> <li>RHEL 10 ISO available</li> </ul>"},{"location":"90-labs/multi-vm/#step-1-create-the-private-lab-network","title":"Step 1 \u2014 Create the Private Lab Network","text":"<pre><code># Define a host-only network (no NAT, isolated between VMs)\n$ sudo tee /tmp/labnet.xml &lt;&lt;'EOF'\n&lt;network&gt;\n  &lt;name&gt;labnet&lt;/name&gt;\n  &lt;bridge name=\"virbr-lab\"/&gt;\n  &lt;ip address=\"192.168.100.1\" netmask=\"255.255.255.0\"&gt;\n    &lt;dhcp&gt;\n      &lt;range start=\"192.168.100.10\" end=\"192.168.100.99\"/&gt;\n      &lt;host mac=\"52:54:00:ab:cd:01\" name=\"controller\" ip=\"192.168.100.10\"/&gt;\n      &lt;host mac=\"52:54:00:ab:cd:02\" name=\"node1\"       ip=\"192.168.100.11\"/&gt;\n      &lt;host mac=\"52:54:00:ab:cd:03\" name=\"node2\"       ip=\"192.168.100.12\"/&gt;\n    &lt;/dhcp&gt;\n  &lt;/ip&gt;\n&lt;/network&gt;\nEOF\n\n$ sudo virsh net-define /tmp/labnet.xml\n$ sudo virsh net-start labnet\n$ sudo virsh net-autostart labnet\n\n# Verify\n$ sudo virsh net-list\n Name      State    Autostart   Persistent\n--------------------------------------------\n default   active   yes         yes\n labnet    active   yes         yes\n</code></pre>"},{"location":"90-labs/multi-vm/#step-2-create-the-three-vms","title":"Step 2 \u2014 Create the Three VMs","text":"<p>Run these in parallel (they don't depend on each other):</p> <pre><code># controller\n$ sudo virt-install \\\n  --name controller \\\n  --memory 4096 \\\n  --vcpus 2 \\\n  --disk size=20,format=qcow2,bus=virtio \\\n  --cdrom /path/to/rhel-10.x-x86_64-dvd.iso \\\n  --os-variant rhel10.0 \\\n  --network network=default,model=virtio \\\n  --network network=labnet,model=virtio,mac=52:54:00:ab:cd:01 \\\n  --graphics vnc \\\n  --noautoconsole\n\n# node1\n$ sudo virt-install \\\n  --name node1 \\\n  --memory 2048 \\\n  --vcpus 2 \\\n  --disk size=20,format=qcow2,bus=virtio \\\n  --cdrom /path/to/rhel-10.x-x86_64-dvd.iso \\\n  --os-variant rhel10.0 \\\n  --network network=default,model=virtio \\\n  --network network=labnet,model=virtio,mac=52:54:00:ab:cd:02 \\\n  --graphics vnc \\\n  --noautoconsole\n\n# node2\n$ sudo virt-install \\\n  --name node2 \\\n  --memory 2048 \\\n  --vcpus 2 \\\n  --disk size=20,format=qcow2,bus=virtio \\\n  --cdrom /path/to/rhel-10.x-x86_64-dvd.iso \\\n  --os-variant rhel10.0 \\\n  --network network=default,model=virtio \\\n  --network network=labnet,model=virtio,mac=52:54:00:ab:cd:03 \\\n  --graphics vnc \\\n  --noautoconsole\n</code></pre>"},{"location":"90-labs/multi-vm/#step-3-install-rhel-10-on-each-vm","title":"Step 3 \u2014 Install RHEL 10 on Each VM","text":"<p>Open three <code>virt-manager</code> console windows and complete the Anaconda installer on each:</p> Setting controller node1 node2 Hostname <code>controller.lab.local</code> <code>node1.lab.local</code> <code>node2.lab.local</code> User <code>student</code> (wheel) <code>student</code> (wheel) <code>student</code> (wheel) Software Minimal Minimal Minimal Disk Auto partition Auto partition Auto partition <p>Both network interfaces will be configured post-install.</p>"},{"location":"90-labs/multi-vm/#step-4-configure-static-ips-on-the-lab-network","title":"Step 4 \u2014 Configure Static IPs on the lab Network","text":"<p>On each VM, configure the second NIC (connected to <code>labnet</code>) with a static IP:</p> <p>controller:</p> <pre><code>$ sudo nmcli connection add \\\n    con-name lab-static \\\n    type ethernet \\\n    ifname ens4 \\\n    ipv4.method manual \\\n    ipv4.addresses 192.168.100.10/24 \\\n    ipv4.gateway \"\" \\\n    connection.autoconnect yes\n\n$ sudo nmcli connection up lab-static\n</code></pre> <p>node1:</p> <pre><code>$ sudo nmcli connection add \\\n    con-name lab-static \\\n    type ethernet \\\n    ifname ens4 \\\n    ipv4.method manual \\\n    ipv4.addresses 192.168.100.11/24 \\\n    ipv4.gateway \"\" \\\n    connection.autoconnect yes\n\n$ sudo nmcli connection up lab-static\n</code></pre> <p>node2:</p> <pre><code>$ sudo nmcli connection add \\\n    con-name lab-static \\\n    type ethernet \\\n    ifname ens4 \\\n    ipv4.method manual \\\n    ipv4.addresses 192.168.100.12/24 \\\n    ipv4.gateway \"\" \\\n    connection.autoconnect yes\n\n$ sudo nmcli connection up lab-static\n</code></pre>"},{"location":"90-labs/multi-vm/#step-5-etchosts-on-all-three-vms","title":"Step 5 \u2014 /etc/hosts on All Three VMs","text":"<p>Add lab hostnames to <code>/etc/hosts</code> so Ansible inventory works without DNS:</p> <pre><code># Run on all three VMs\n$ sudo tee -a /etc/hosts &lt;&lt;'EOF'\n192.168.100.10  controller.lab.local  controller\n192.168.100.11  node1.lab.local       node1\n192.168.100.12  node2.lab.local       node2\nEOF\n</code></pre>"},{"location":"90-labs/multi-vm/#step-6-ssh-key-distribution-from-controller","title":"Step 6 \u2014 SSH Key Distribution from controller","text":"<p>Ansible communicates over SSH. Set up passwordless SSH from controller to both nodes:</p> <pre><code># On controller: generate a key\n$ ssh-keygen -t ed25519 -N \"\" -f ~/.ssh/id_ed25519\n\n# Copy to node1 and node2\n$ ssh-copy-id student@node1\n$ ssh-copy-id student@node2\n\n# Test\n$ ssh student@node1 hostname\nnode1.lab.local\n\n$ ssh student@node2 hostname\nnode2.lab.local\n</code></pre>"},{"location":"90-labs/multi-vm/#step-7-install-ansible-on-controller","title":"Step 7 \u2014 Install Ansible on controller","text":"<pre><code>$ sudo dnf install -y ansible-core\n\n$ ansible --version\nansible [core 2.x.x]\n</code></pre>"},{"location":"90-labs/multi-vm/#step-8-baseline-ansible-inventory","title":"Step 8 \u2014 Baseline Ansible Inventory","text":"<pre><code>$ mkdir -p ~/ansible/inventory\n\n$ tee ~/ansible/inventory/lab.ini &lt;&lt;'EOF'\n[controller]\ncontroller.lab.local\n\n[nodes]\nnode1.lab.local\nnode2.lab.local\n\n[all:vars]\nansible_user=student\nansible_become=true\nEOF\n\n# Smoke test\n$ ansible all -i ~/ansible/inventory/lab.ini -m ping\nnode1.lab.local | SUCCESS =&gt; ...\nnode2.lab.local | SUCCESS =&gt; ...\ncontroller.lab.local | SUCCESS =&gt; ...\n</code></pre>"},{"location":"90-labs/multi-vm/#step-9-snapshot-all-three-vms","title":"Step 9 \u2014 Snapshot All Three VMs","text":"<pre><code>$ for vm in controller node1 node2; do\n    sudo virsh snapshot-create-as $vm \\\n      --name \"baseline\" \\\n      --description \"RHEL 10 minimal + student + lab network configured\"\n  done\n\n# Verify\n$ for vm in controller node1 node2; do\n    echo \"=== $vm ===\"; sudo virsh snapshot-list $vm\n  done\n</code></pre>"},{"location":"90-labs/multi-vm/#quick-reference-multi-vm-management","title":"Quick Reference \u2014 Multi-VM Management","text":"<pre><code># Start all lab VMs\n$ for vm in controller node1 node2; do sudo virsh start $vm; done\n\n# Shutdown all\n$ for vm in controller node1 node2; do sudo virsh shutdown $vm; done\n\n# Revert all to baseline\n$ for vm in controller node1 node2; do\n    sudo virsh snapshot-revert $vm baseline\n  done\n\n# Status of all\n$ sudo virsh list --all\n</code></pre>"},{"location":"90-labs/multi-vm/#success-criteria","title":"Success Criteria","text":"<ul> <li>[ ] All three VMs boot and reach a login prompt</li> <li>[ ] <code>ping node1</code> from controller succeeds via the 192.168.100.x network</li> <li>[ ] <code>ansible all -m ping</code> returns SUCCESS for all three hosts</li> <li>[ ] Baseline snapshots exist for all three VMs</li> </ul>"},{"location":"90-labs/multi-vm/#troubleshooting","title":"Troubleshooting","text":"Problem Fix Second NIC not visible as <code>ens4</code> Interface name may differ \u2014 check <code>ip link</code> and update <code>nmcli</code> commands <code>ansible ping</code> fails with <code>UNREACHABLE</code> Verify SSH key was copied; test <code>ssh student@node1</code> manually VMs can't reach each other on <code>labnet</code> Confirm <code>sudo virsh net-list</code> shows labnet active; check <code>ens4</code> IP with <code>ip addr</code> DHCP not assigning the static MAC address The MAC in <code>virt-install</code> must exactly match the MAC in <code>labnet.xml</code> <code>subscription-manager</code> needed on all VMs Register each VM or point all three to a local mirror; automate with Ansible"},{"location":"90-labs/single-vm/","title":"Single-VM Lab Setup","text":""},{"location":"90-labs/single-vm/#overview","title":"Overview","text":"<p>A single RHEL 10 VM covers 95% of the labs in this guide. This page walks through creating it with KVM/QEMU, performing a minimal installation, and configuring the baseline student environment.</p> <p>Estimated time: 30\u201345 minutes What you get: A fully registered, SSH-accessible RHEL 10 VM ready for all Track A\u2013D labs.</p>"},{"location":"90-labs/single-vm/#prerequisites","title":"Prerequisites","text":"<ul> <li>A Linux workstation with KVM and <code>libvirt</code> installed</li> <li>At least 8 GB RAM available on the host (4 GB for the VM, rest for the host)</li> <li>40 GB free disk space</li> <li>RHEL 10 ISO downloaded</li> </ul>"},{"location":"90-labs/single-vm/#step-1-install-kvm-and-virt-install-host","title":"Step 1 \u2014 Install KVM and virt-install (Host)","text":"<pre><code># On the KVM host (Fedora/RHEL)\n$ sudo dnf install -y qemu-kvm libvirt virt-install virt-manager\n\n$ sudo systemctl enable --now libvirtd\n\n# Verify KVM is functional\n$ sudo virsh list --all\n Id   Name   State\n--------------------\n</code></pre>"},{"location":"90-labs/single-vm/#step-2-create-the-vm","title":"Step 2 \u2014 Create the VM","text":"<pre><code>$ sudo virt-install \\\n  --name rhel10-lab \\\n  --memory 4096 \\\n  --vcpus 4 \\\n  --disk size=40,format=qcow2,bus=virtio \\\n  --cdrom /path/to/rhel-10.x-x86_64-dvd.iso \\\n  --os-variant rhel10.0 \\\n  --network network=default,model=virtio \\\n  --graphics vnc \\\n  --noautoconsole\n</code></pre> <p>Open the VM console:</p> <pre><code>$ virt-manager &amp;\n# or\n$ sudo virsh console rhel10-lab\n</code></pre>"},{"location":"90-labs/single-vm/#step-3-rhel-10-installation-anaconda","title":"Step 3 \u2014 RHEL 10 Installation (Anaconda)","text":"<p>Follow the graphical installer with these settings:</p>"},{"location":"90-labs/single-vm/#installation-destination","title":"Installation Destination","text":"<ul> <li>Select <code>/dev/vda</code> (40 GB virtio disk)</li> <li>Partitioning: Automatic (for initial setup)</li> <li>Or custom:</li> </ul> Mount point Size Filesystem <code>/boot</code> 1 GB xfs <code>/boot/efi</code> 600 MB vfat (if UEFI) <code>swap</code> 4 GB swap <code>/</code> remainder xfs"},{"location":"90-labs/single-vm/#software-selection","title":"Software Selection","text":"<p>Choose Minimal Install \u2014 we install packages on-demand throughout the guide.</p>"},{"location":"90-labs/single-vm/#network-and-host-name","title":"Network and Host Name","text":"<ul> <li>Hostname: <code>rhel10.lab.local</code></li> <li>Enable the Ethernet interface (it will get a DHCP address initially)</li> </ul>"},{"location":"90-labs/single-vm/#root-password","title":"Root Password","text":"<p>Set a strong root password. Then also create a user account:</p> <ul> <li>Username: <code>student</code></li> <li>Check \"Make this user administrator\" (adds to <code>wheel</code> group)</li> </ul>"},{"location":"90-labs/single-vm/#step-4-first-boot","title":"Step 4 \u2014 First Boot","text":"<p>After reboot, log in as <code>student</code> via the console or SSH.</p>"},{"location":"90-labs/single-vm/#confirm-ssh-access","title":"Confirm SSH access","text":"<pre><code># From the KVM host, get the VM's IP\n$ sudo virsh domifaddr rhel10-lab\n Name       MAC address          Protocol     Address\n-------------------------------------------------------------------------------\n vnet0      52:54:00:xx:xx:xx    ipv4         192.168.122.10/24\n\n# SSH in\n$ ssh student@192.168.122.10\n</code></pre>"},{"location":"90-labs/single-vm/#register-the-system","title":"Register the system","text":"<pre><code>$ sudo subscription-manager register --username &lt;rhn-username&gt; --password &lt;rhn-password&gt;\n$ sudo subscription-manager attach --auto\n$ sudo dnf update -y\n</code></pre> <p>If you have a Developer Subscription, use the same credentials. Alternatively, configure a local Satellite or DNF mirror.</p>"},{"location":"90-labs/single-vm/#step-5-baseline-configuration","title":"Step 5 \u2014 Baseline Configuration","text":"<pre><code># Set hostname persistently\n$ sudo hostnamectl set-hostname rhel10.lab.local\n\n# Verify sudo works\n$ sudo whoami\nroot\n\n# Install commonly needed tools (used across labs)\n$ sudo dnf install -y \\\n    bash-completion \\\n    bind-utils \\\n    tcpdump \\\n    net-tools \\\n    lsof \\\n    strace \\\n    vim \\\n    tmux \\\n    git \\\n    python3 \\\n    podman\n\n# Enable and start firewalld (should already be active)\n$ sudo systemctl enable --now firewalld\n$ sudo firewall-cmd --state\nrunning\n</code></pre>"},{"location":"90-labs/single-vm/#step-6-ssh-key-setup-recommended","title":"Step 6 \u2014 SSH Key Setup (Recommended)","text":"<p>Generate an SSH key pair on your workstation and copy the public key to the lab VM for passwordless access:</p> <pre><code># On your workstation\n$ ssh-keygen -t ed25519 -C \"rhca-lab\"\n\n$ ssh-copy-id student@192.168.122.10\n</code></pre>"},{"location":"90-labs/single-vm/#step-7-snapshot-the-vm-strongly-recommended","title":"Step 7 \u2014 Snapshot the VM (Strongly Recommended)","text":"<p>Take a snapshot before starting any lab so you can roll back quickly:</p> <pre><code>$ sudo virsh snapshot-create-as rhel10-lab \\\n    --name \"baseline\" \\\n    --description \"Clean RHEL 10 install, student user, tools installed\"\n\n# List snapshots\n$ sudo virsh snapshot-list rhel10-lab\n\n# Restore to snapshot\n$ sudo virsh snapshot-revert rhel10-lab baseline\n</code></pre>"},{"location":"90-labs/single-vm/#disk-layout-for-storage-labs","title":"Disk Layout for Storage Labs","text":"<p>LVM and filesystem labs (Chapters 03-rhcsa) need extra disk space. Add a second virtual disk:</p> <pre><code># Attach a second 10 GB disk to the running VM\n$ sudo virsh attach-disk rhel10-lab \\\n    /var/lib/libvirt/images/rhel10-lab-extra.qcow2 \\\n    vdb \\\n    --driver qemu \\\n    --subdriver qcow2 \\\n    --persistent\n\n# Create the image file first if it doesn't exist\n$ sudo qemu-img create -f qcow2 \\\n    /var/lib/libvirt/images/rhel10-lab-extra.qcow2 10G\n\n# Inside the VM, confirm the new disk\n$ lsblk\nNAME   MAJ:MIN RM SIZE RO TYPE MOUNTPOINTS\nvda    252:0    0  40G  0 disk\n...\nvdb    252:16   0  10G  0 disk   \u2190 second disk for storage labs\n</code></pre>"},{"location":"90-labs/single-vm/#quick-reference-vm-management","title":"Quick Reference \u2014 VM Management","text":"<pre><code># Start VM\n$ sudo virsh start rhel10-lab\n\n# Graceful shutdown\n$ sudo virsh shutdown rhel10-lab\n\n# Force off\n$ sudo virsh destroy rhel10-lab\n\n# Delete VM and its disk (irreversible)\n$ sudo virsh undefine rhel10-lab --remove-all-storage\n\n# Get console (use Ctrl+] to detach)\n$ sudo virsh console rhel10-lab\n</code></pre>"},{"location":"90-labs/single-vm/#success-criteria","title":"Success Criteria","text":"<p>Before moving on to Chapter 1:</p> <ul> <li>[ ] <code>ssh student@&lt;vm-ip&gt;</code> works without a password (key auth)</li> <li>[ ] <code>sudo dnf update -y</code> completes without errors</li> <li>[ ] <code>podman --version</code> returns a version string</li> <li>[ ] A baseline snapshot named <code>baseline</code> exists</li> <li>[ ] <code>lsblk</code> shows <code>/dev/vdb</code> (second disk attached)</li> </ul>"},{"location":"90-labs/single-vm/#troubleshooting","title":"Troubleshooting","text":"Problem Fix No IP address on <code>ens3</code> <code>sudo nmcli connection up ens3</code>; check if DHCP server on the <code>default</code> libvirt network is running <code>subscription-manager</code> fails Check network connectivity; verify RHN credentials; try <code>--force</code> SSH connection refused Confirm <code>sshd</code> is running: <code>sudo systemctl start sshd</code> Disk not visible as <code>/dev/vda</code> Different hypervisor uses <code>/dev/sda</code> \u2014 update disk references in labs accordingly <code>virt-install</code> fails with <code>No space left</code> Free up disk space on the host in <code>/var/lib/libvirt/images/</code>"},{"location":"98-reference/cheatsheets/","title":"Command Cheatsheets","text":"<p>Quick-reference cards for the most-used command groups. Each section is designed to fit on a single screen.</p>"},{"location":"98-reference/cheatsheets/#dnf-package-management","title":"dnf \u2014 Package Management","text":"<pre><code># Search and info\ndnf search &lt;keyword&gt;\ndnf info &lt;package&gt;\ndnf provides &lt;file&gt;               # Which package owns a file?\ndnf repoquery --list &lt;package&gt;    # List files in a package\n\n# Install / Remove\nsudo dnf install -y &lt;package&gt;\nsudo dnf remove &lt;package&gt;\nsudo dnf reinstall &lt;package&gt;\nsudo dnf autoremove               # Remove unneeded dependencies\n\n# Updates\nsudo dnf check-update\nsudo dnf upgrade -y\nsudo dnf upgrade-minimal          # Security patches only\n\n# History and rollback\ndnf history list\ndnf history info &lt;id&gt;\nsudo dnf history undo &lt;id&gt;\n\n# Repositories\ndnf repolist\ndnf repolist --all\nsudo dnf config-manager --enable &lt;repo-id&gt;\nsudo dnf config-manager --disable &lt;repo-id&gt;\n</code></pre>"},{"location":"98-reference/cheatsheets/#systemctl-service-management","title":"systemctl \u2014 Service Management","text":"<pre><code># Status and control\nsystemctl status &lt;unit&gt;\nsudo systemctl start &lt;unit&gt;\nsudo systemctl stop &lt;unit&gt;\nsudo systemctl restart &lt;unit&gt;\nsudo systemctl reload &lt;unit&gt;      # Reload config without restart\n\n# Enable / Disable at boot\nsudo systemctl enable &lt;unit&gt;\nsudo systemctl disable &lt;unit&gt;\nsudo systemctl enable --now &lt;unit&gt;   # Enable AND start\n\n# Inspect\nsystemctl list-units --failed\nsystemctl list-unit-files --state=enabled\nsystemctl show &lt;unit&gt;             # All unit properties\nsystemctl cat &lt;unit&gt;              # Show unit file\nsudo systemctl edit &lt;unit&gt;        # Edit drop-in override\n\n# Targets\nsystemctl get-default\nsudo systemctl set-default multi-user.target\nsudo systemctl isolate rescue.target\n\n# User services (rootless)\nsystemctl --user status &lt;unit&gt;\nsystemctl --user start &lt;unit&gt;\nsystemctl --user enable &lt;unit&gt;\nsystemctl --user daemon-reload\n</code></pre>"},{"location":"98-reference/cheatsheets/#journalctl-log-inspection","title":"journalctl \u2014 Log Inspection","text":"<pre><code># Basic queries\njournalctl -u &lt;unit&gt;              # Logs for a service\njournalctl -u &lt;unit&gt; -f           # Follow (tail -f equivalent)\njournalctl -u &lt;unit&gt; -n 50        # Last 50 lines\njournalctl -p err                 # Errors and above\njournalctl -p err..warning        # Range of priorities\njournalctl -k                     # Kernel messages only\njournalctl -b                     # Current boot\njournalctl -b -1                  # Previous boot\n\n# Time filtering\njournalctl --since \"1 hour ago\"\njournalctl --since \"2026-02-01\" --until \"2026-02-02\"\njournalctl --since today\n\n# Output formats\njournalctl -o json-pretty -u sshd -n 5\njournalctl --no-pager &gt; /tmp/journal.txt\n\n# Disk usage and cleanup\njournalctl --disk-usage\nsudo journalctl --vacuum-size=500M\nsudo journalctl --vacuum-time=30d\n</code></pre>"},{"location":"98-reference/cheatsheets/#nmcli-network-management","title":"nmcli \u2014 Network Management","text":"<pre><code># Status\nnmcli device status\nnmcli connection show\nnmcli connection show &lt;name&gt;\n\n# Create a static IPv4 connection\nsudo nmcli connection add \\\n  con-name &lt;name&gt; type ethernet ifname &lt;nic&gt; \\\n  ipv4.method manual \\\n  ipv4.addresses 192.168.1.10/24 \\\n  ipv4.gateway 192.168.1.1 \\\n  ipv4.dns \"8.8.8.8 1.1.1.1\" \\\n  connection.autoconnect yes\n\n# Modify existing connection\nsudo nmcli connection modify &lt;name&gt; ipv4.dns \"8.8.8.8\"\n\n# Activate / deactivate\nsudo nmcli connection up &lt;name&gt;\nsudo nmcli connection down &lt;name&gt;\nsudo nmcli connection reload\n\n# Delete\nsudo nmcli connection delete &lt;name&gt;\n</code></pre>"},{"location":"98-reference/cheatsheets/#firewall-cmd-firewall-management","title":"firewall-cmd \u2014 Firewall Management","text":"<pre><code># Status\nsudo firewall-cmd --state\nsudo firewall-cmd --list-all\nsudo firewall-cmd --get-active-zones\n\n# Ports\nsudo firewall-cmd --add-port=8080/tcp --permanent\nsudo firewall-cmd --remove-port=8080/tcp --permanent\nsudo firewall-cmd --list-ports\n\n# Services\nsudo firewall-cmd --add-service=http --permanent\nsudo firewall-cmd --remove-service=http --permanent\nsudo firewall-cmd --list-services\n\n# Apply permanent changes\nsudo firewall-cmd --reload\n\n# Rich rules\nsudo firewall-cmd --add-rich-rule='rule family=ipv4 source address=10.0.0.0/8 accept' --permanent\n\n# Temporary (testing, no --permanent)\nsudo firewall-cmd --add-port=9090/tcp\nsudo firewall-cmd --remove-port=9090/tcp\n</code></pre>"},{"location":"98-reference/cheatsheets/#selinux","title":"SELinux","text":"<pre><code># Status\ngetenforce\nsestatus\n\n# Toggle mode\nsudo setenforce 1          # enforcing\nsudo setenforce 0          # permissive (temporary ONLY \u2014 diagnostic use)\n\n# File contexts\nls -Z &lt;path&gt;\nps -Z\nid -Z\n\n# Restore default context\nsudo restorecon -Rv &lt;path&gt;\n\n# Manage fcontext rules (persistent)\nsudo semanage fcontext -a -t httpd_sys_content_t '/srv/web(/.*)?'\nsudo restorecon -Rv /srv/web\n\n# Port labels\nsudo semanage port -l | grep http\nsudo semanage port -a -t http_port_t -p tcp 8080\nsudo semanage port -d -t http_port_t -p tcp 8080\n\n# Booleans\ngetsebool -a | grep httpd\nsudo setsebool -P httpd_can_network_connect on\n\n# AVC denials\nsudo ausearch -m avc -ts recent\nsudo audit2why &lt; /var/log/audit/audit.log\nsudo audit2allow -a          # REVIEW before applying\n</code></pre>"},{"location":"98-reference/cheatsheets/#lvm","title":"LVM","text":"<pre><code># Inspect\npvs / vgs / lvs\npvdisplay / vgdisplay / lvdisplay\nlsblk\n\n# Create\nsudo pvcreate /dev/vdb\nsudo vgcreate datavg /dev/vdb\nsudo lvcreate -L 5G -n datalv datavg\n\n# Filesystem + mount\nsudo mkfs.xfs /dev/datavg/datalv\nsudo mkdir /data\necho '/dev/datavg/datalv /data xfs defaults 0 0' | sudo tee -a /etc/fstab\nsudo mount -a\n\n# Extend (online, XFS)\nsudo lvextend -r -L +2G /dev/datavg/datalv   # -r resizes FS too\n# Or explicitly:\nsudo lvextend -L +2G /dev/datavg/datalv\nsudo xfs_growfs /data\n\n# Snapshots\nsudo lvcreate -s -n snap1 -L 1G /dev/datavg/datalv\nsudo lvremove /dev/datavg/snap1\n</code></pre>"},{"location":"98-reference/cheatsheets/#podman","title":"Podman","text":"<pre><code># Images\npodman pull docker.io/library/nginx:stable-alpine\npodman images\npodman rmi &lt;image&gt;\npodman image prune -a\n\n# Run\npodman run -d --name web -p 8080:80 nginx:stable-alpine\npodman run -it --rm alpine sh          # interactive, auto-remove\n\n# Container lifecycle\npodman ps\npodman ps -a                           # including stopped\npodman stop &lt;name&gt;\npodman start &lt;name&gt;\npodman restart &lt;name&gt;\npodman rm &lt;name&gt;\n\n# Inspect and logs\npodman logs -f &lt;name&gt;\npodman inspect &lt;name&gt;\npodman exec -it &lt;name&gt; /bin/sh\n\n# Secrets\nprintf 'mypassword' | podman secret create db-pass -\npodman secret ls\npodman secret rm db-pass\n\n# Volumes\npodman volume create mydata\npodman run -v mydata:/data:Z alpine\npodman volume ls\npodman volume rm mydata\n\n# Build\npodman build -t myapp:latest .\n</code></pre>"},{"location":"98-reference/cheatsheets/#quadlet-systemd-container-integration","title":"Quadlet (systemd container integration)","text":"<pre><code># Files location\n# User (rootless): ~/.config/containers/systemd/\n# Root:            /etc/containers/systemd/\n\n# Minimal .container file\ncat &gt; ~/.config/containers/systemd/myapp.container &lt;&lt;'EOF'\n[Unit]\nDescription=My app\n\n[Container]\nImage=docker.io/library/nginx:stable-alpine\nPublishPort=8080:80\nVolume=%h/data:/data:Z\n\n[Service]\nRestart=always\n\n[Install]\nWantedBy=default.target\nEOF\n\n# Reload and manage\nsystemctl --user daemon-reload\nsystemctl --user start myapp\nsystemctl --user enable myapp\nsystemctl --user status myapp\njournalctl --user -u myapp -f\n</code></pre>"},{"location":"98-reference/cheatsheets/#ssh","title":"SSH","text":"<pre><code># Connect\nssh user@host\nssh -p 2222 user@host\nssh -i ~/.ssh/mykey user@host\n\n# Key management\nssh-keygen -t ed25519 -C \"label\"\nssh-copy-id user@host\nssh-add ~/.ssh/mykey\n\n# Config shortcut (~/.ssh/config)\nHost lab\n  HostName 192.168.122.10\n  User student\n  IdentityFile ~/.ssh/id_ed25519\n\n# File transfer\nscp localfile user@host:/remote/path\nscp user@host:/remote/file .\nrsync -avz localdir/ user@host:/remote/\n\n# Tunnels\nssh -L 8080:localhost:80 user@host    # local forward\nssh -R 9090:localhost:9090 user@host  # remote forward\n\n# Server management\nsudo sshd -t                           # syntax check (ALWAYS before restart)\nsudo systemctl restart sshd\n</code></pre>"},{"location":"98-reference/cheatsheets/#performance-triage-quick-reference","title":"Performance Triage Quick Reference","text":"<pre><code># Load and CPU\nuptime\ntop -b -n 1 | head -20\nps aux --sort=-%cpu | head -11\nmpstat -P ALL 1 3\nvmstat 1 5\n\n# Memory\nfree -m\nvmstat 1 5 | awk 'NR&gt;2 {print \"si:\",$7,\"so:\",$8}'\n\n# Disk I/O\niostat -x 1 3\nsudo iotop -o -b -n 3\ndf -h &amp;&amp; df -i\n\n# Network\nip -s link show ens3\nss -s\nss -tlnp\nnetstat -s | grep -i retran\n\n# Processes\nps aux | awk '$8==\"D\"'     # uninterruptible sleep (I/O stuck)\nlsof | awk '{print $1}' | sort | uniq -c | sort -rn | head -10\n</code></pre>"},{"location":"98-reference/cheatsheets/#tuned-quick-reference","title":"tuned Quick Reference","text":"<pre><code>tuned-adm list\ntuned-adm active\nsudo tuned-adm profile throughput-performance\nsudo tuned-adm recommend\ntuned-adm profile_info &lt;name&gt;\n\n# Custom profile\nsudo mkdir /etc/tuned/myprofile\nsudo tee /etc/tuned/myprofile/tuned.conf &lt;&lt;'EOF'\n[main]\ninclude=throughput-performance\n\n[sysctl]\nnet.core.somaxconn=131072\nEOF\nsudo tuned-adm profile myprofile\n</code></pre>"},{"location":"98-reference/further-reading/","title":"Further Reading","text":"<p>Curated resources to deepen your understanding beyond this guide. All Red Hat documentation is freely accessible with a free Red Hat Developer account.</p>"},{"location":"98-reference/further-reading/#official-red-hat-documentation","title":"Official Red Hat Documentation","text":"Resource URL When to Use RHEL 10 Product Documentation access.redhat.com/documentation/en-us/red_hat_enterprise_linux/10 Authoritative reference for everything Configuring and managing networking link nmcli deep dive, bonding, bridges, VLANs SELinux User's and Administrator's Guide link Policy deep-dive, confined services, troubleshooting Managing file systems link XFS, ext4, LVM, VDO Configuring basic system settings link tuned, performance co-pilot, cgroups Building, running, and managing containers link Podman, Buildah, Quadlet, rootless Automating system administration with Ansible link RHEL System Roles"},{"location":"98-reference/further-reading/#red-hat-exam-prep","title":"Red Hat Exam Prep","text":"Resource Notes RHCSA EX200 Exam Objectives Official, always check before exam RHCE EX294 Exam Objectives Ansible exam objectives RHCA Infrastructure concentration Overview of specialist credentials Red Hat Learning Subscription Video courses, hands-on labs in the cloud"},{"location":"98-reference/further-reading/#ansible","title":"Ansible","text":"Resource Notes Ansible Documentation Module reference, playbook language reference Ansible Galaxy Community roles and collections Red Hat Ansible Automation Platform Docs AAP-specific; useful background for EX294 Ansible: Up and Running (O'Reilly) Book \u2014 excellent Ansible fundamentals"},{"location":"98-reference/further-reading/#systemd","title":"systemd","text":"Resource Notes <code>man systemd.unit</code> The definitive reference \u2014 read it <code>man systemd.service</code> Service-specific directives <code>man systemd.exec</code> Sandboxing directives (<code>PrivateTmp</code>, <code>NoNewPrivileges</code>, etc.) systemd.io Upstream documentation and blog How Linux Works by Brian Ward Book \u2014 excellent systemd and boot process coverage"},{"location":"98-reference/further-reading/#selinux","title":"SELinux","text":"Resource Notes SELinux Project Wiki Policy reference, upstream docs <code>man semanage-fcontext</code> fcontext management <code>man audit2allow</code> Policy module generation <code>man sepolicy</code> Introspect SELinux policy SELinux System Administration (Packt) Book \u2014 comprehensive guide"},{"location":"98-reference/further-reading/#containers-and-podman","title":"Containers and Podman","text":"Resource Notes Podman Documentation Official Podman reference Quadlet documentation Full <code>.container</code> file directive reference OCI Image Specification What a container image actually is Red Hat Container Catalog Certified base images for RHEL"},{"location":"98-reference/further-reading/#performance-and-observability","title":"Performance and Observability","text":"Resource Notes <code>man iostat</code>, <code>man vmstat</code>, <code>man mpstat</code> sysstat tools reference Brendan Gregg's Linux Performance The definitive performance resource bpftrace Dynamic tracing for deep performance analysis Systems Performance by Brendan Gregg Book \u2014 essential for RHCA-level performance work Red Hat Performance Co-Pilot (PCP) RHEL's observability framework"},{"location":"98-reference/further-reading/#linux-fundamentals","title":"Linux Fundamentals","text":"Resource Notes <code>man hier</code> The filesystem hierarchy standard \u2014 read once The Linux Command Line Free book \u2014 excellent for Track A learners UNIX and Linux System Administration Handbook (Evi Nemeth et al.) The essential sysadmin reference; 5th ed. covers Linux well Linux Kernel Documentation Authoritative but dense; useful for proc/sys tuning"},{"location":"98-reference/further-reading/#networking","title":"Networking","text":"Resource Notes <code>man ip</code>, <code>man ip-route</code>, <code>man ip-address</code> iproute2 reference NetworkManager documentation nmcli and connection file formats TCP/IP Illustrated, Vol. 1 by W. Richard Stevens Classic \u2014 fundamental TCP/IP knowledge for tcpdump work tcpdump.org Filter syntax reference"},{"location":"98-reference/further-reading/#practice-environments","title":"Practice Environments","text":"Resource Notes Red Hat Developer Sandbox Free OpenShift/cloud environment (not RHEL, but useful) KVM/QEMU on Fedora Best local environment for RHEL labs Vagrant + libvirt Scriptable multi-VM environments learn.redhat.com Red Hat's official learning platform"},{"location":"98-reference/further-reading/#community","title":"Community","text":"Resource Notes r/redhat Community discussion r/linuxadmin Broader sysadmin community Red Hat Discussion Forums Official community forums RHCSA/RHCE Study Group (r/redhat) Community study resources and experience reports"},{"location":"98-reference/glossary/","title":"Glossary","text":"<p>Key terms used throughout this guide, ordered alphabetically.</p>"},{"location":"98-reference/glossary/#a","title":"A","text":"<p>ACL (Access Control List) An extended permission mechanism that allows fine-grained access control beyond the standard owner/group/other model. Managed with <code>getfacl</code> and <code>setfacl</code>. See ACLs.</p> <p>Ansible An agentless IT automation engine that uses SSH to execute declarative playbooks (YAML) on managed nodes. The RHCE exam (EX294) tests Ansible on RHEL 10. See Ansible Setup and Inventory.</p> <p>Ansible Galaxy The upstream community hub for sharing Ansible roles and collections. Accessed via <code>ansible-galaxy</code>.</p> <p>Ansible Vault An Ansible feature for encrypting sensitive data (passwords, API keys) within playbook variables and files.</p> <p>AVC (Access Vector Cache) The SELinux kernel subsystem that caches policy decisions for performance. AVC denials appear in <code>/var/log/audit/audit.log</code> and <code>journalctl -k</code>. See SELinux AVC Basics.</p>"},{"location":"98-reference/glossary/#b","title":"B","text":"<p>Boolean (SELinux) A runtime switch that toggles a subset of SELinux policy rules without recompiling the policy. Managed with <code>getsebool</code> and <code>setsebool</code>. See SELinux Fundamentals.</p> <p>Boot target A systemd unit of type <code>target</code> (e.g., <code>multi-user.target</code>, <code>graphical.target</code>) that groups dependencies for a system state. Replaces SysVinit runlevels.</p>"},{"location":"98-reference/glossary/#c","title":"C","text":"<p>cgroup (Control Group) A Linux kernel feature that limits, accounts for, and isolates resource usage (CPU, memory, I/O, network) for collections of processes. Systemd and Podman both use cgroups. RHEL 10 uses cgroup v2 exclusively.</p> <p>Condition (systemd) A unit file directive (<code>ConditionPathExists=</code>, <code>ConditionVirtualization=</code>, etc.) that prevents a unit from starting if the condition is not met, without reporting a failure.</p> <p>Container An isolated process (or group of processes) sharing the host kernel but with separate namespaces (PID, network, mount, user, IPC, UTS). On RHEL 10 this means Podman. See Podman Fundamentals.</p> <p>Context (SELinux) A label attached to every file, process, and socket of the form <code>user:role:type:level</code>. The type field (e.g., <code>httpd_sys_content_t</code>) is what most policy rules check. See SELinux Fundamentals.</p>"},{"location":"98-reference/glossary/#d","title":"D","text":"<p>DAC (Discretionary Access Control) The traditional Unix permission model (owner/group/other + rwx). Called \"discretionary\" because owners can grant access to others at their discretion. Contrasts with MAC.</p> <p>DBus An IPC mechanism used by systemd for inter-process communication. Required for <code>systemctl --user</code> in rootless Podman / Quadlet contexts.</p> <p>dnf The package manager for RHEL 10 (Dandified YUM). Handles installation, updates, removal, and dependency resolution of RPM packages. See Packages and DNF.</p> <p>Drop-in (systemd) An override file placed in <code>/etc/systemd/system/&lt;unit&gt;.d/*.conf</code> that supplements (rather than replaces) the original unit definition. Created via <code>systemctl edit &lt;unit&gt;</code>.</p>"},{"location":"98-reference/glossary/#e","title":"E","text":"<p>ens3 The default virtio network interface name used in KVM/QEMU VMs throughout this guide. May differ in other hypervisors or physical hardware (e.g., <code>ens192</code>, <code>eth0</code>).</p> <p>ext4 A widely-used Linux journaling filesystem. On RHEL 10, XFS is the default for data partitions; ext4 is used for <code>/boot</code> in some configurations.</p>"},{"location":"98-reference/glossary/#f","title":"F","text":"<p>fcontext An SELinux file context mapping \u2014 a rule that defines what SELinux label a file path should have. Managed with <code>semanage fcontext</code>. Must be followed by <code>restorecon</code> to apply to existing files.</p> <p>firewalld The RHEL 10 firewall manager, using <code>nftables</code> as its backend. Manages zones, services, ports, and rich rules. See firewalld.</p> <p>fstab <code>/etc/fstab</code> \u2014 the filesystem table. Defines which filesystems and network mounts are mounted at boot. See Filesystems and fstab.</p>"},{"location":"98-reference/glossary/#g","title":"G","text":"<p>GRUB2 The GRand Unified Bootloader version 2. The bootloader used on RHEL 10. Configuration is managed via <code>grubby</code>. The bootloader menu allows kernel selection and parameter editing at boot.</p> <p>grubby A command-line tool for managing GRUB2 boot entries. Used to set the default kernel, add/remove kernel parameters.</p>"},{"location":"98-reference/glossary/#i","title":"I","text":"<p>inode A data structure in a filesystem that stores metadata about a file (permissions, timestamps, ownership, data block pointers) but not the filename. Inode exhaustion (not disk space exhaustion) is a common cause of \"disk full\" errors on <code>/var/log</code>.</p> <p>I/O wait The percentage of CPU time spent waiting for I/O operations to complete (the <code>wa</code> column in <code>vmstat</code>). High I/O wait indicates a disk or NFS bottleneck, not a CPU bottleneck.</p>"},{"location":"98-reference/glossary/#j","title":"J","text":"<p>journald The systemd journal daemon (<code>systemd-journald</code>). Collects log data from the kernel, services, and applications into a structured binary format queryable with <code>journalctl</code>. See Logging and journald.</p> <p>journalctl The command-line interface for reading the systemd journal. Supports filtering by service (<code>-u</code>), priority (<code>-p</code>), time (<code>--since</code>/<code>--until</code>), and kernel (<code>-k</code>).</p>"},{"location":"98-reference/glossary/#l","title":"L","text":"<p>Linger (systemd) A per-user setting (<code>loginctl enable-linger</code>) that keeps a user's systemd user instance and services running after the user logs out. Required for rootless Podman Quadlet services.</p> <p>load average A Unix metric representing the average number of processes in the run queue (running or waiting to run) over the last 1, 5, and 15 minutes. Compare to CPU count to assess system load.</p> <p>LVM (Logical Volume Manager) A storage abstraction layer that allows flexible resizing and management of logical volumes spanning one or more physical disks. See LVM.</p>"},{"location":"98-reference/glossary/#m","title":"M","text":"<p>MAC (Mandatory Access Control) A security model where access decisions are made by policy, not by the resource owner. SELinux implements MAC on RHEL. Contrast with DAC.</p> <p>MLS (Multi-Level Security) The sensitivity/clearance component of a full SELinux context (the <code>s0:cX,cY</code> part). Most RHEL deployments use <code>s0</code> (the default sensitivity level) and only use the category fields for container isolation.</p>"},{"location":"98-reference/glossary/#n","title":"N","text":"<p>NetworkManager The RHEL 10 network configuration service. Manages connections via <code>nmcli</code> (CLI), <code>nmtui</code> (TUI), or connection files in <code>/etc/NetworkManager/system-connections/</code>. See NetworkManager and nmcli.</p> <p>nmcli The command-line interface for NetworkManager. Used to create, modify, activate, and inspect network connections.</p>"},{"location":"98-reference/glossary/#p","title":"P","text":"<p>pasta The default network stack for rootless Podman on RHEL 10 (replacing slirp4netns). pasta provides better performance and supports IPv6. See Rootless Podman.</p> <p>PE (Physical Extent) The basic allocation unit in LVM. A Physical Volume is divided into fixed-size PEs (default 4 MB). Logical Volumes are allocated in multiples of PEs.</p> <p>Playbook (Ansible) A YAML file containing one or more plays, each targeting a group of hosts and defining a list of tasks to execute. The central Ansible artifact.</p> <p>Podman A daemonless container engine for OCI containers on RHEL 10. Supports both root and rootless operation. See Podman Fundamentals.</p> <p>Podman secret An encrypted blob stored by Podman and injected into containers at <code>/run/secrets/&lt;name&gt;</code> as a read-only tmpfs file. Never exposed in environment variables or process arguments. See Secrets.</p> <p>PV (Physical Volume) A disk or partition initialized for use by LVM with <code>pvcreate</code>.</p>"},{"location":"98-reference/glossary/#q","title":"Q","text":"<p>Quadlet A systemd generator shipped with Podman on RHEL 10 that converts <code>.container</code>, <code>.volume</code>, <code>.network</code>, and <code>.pod</code> files into systemd unit files at runtime. The preferred way to run containers under systemd. See systemd Integration.</p>"},{"location":"98-reference/glossary/#r","title":"R","text":"<p>RHCA (Red Hat Certified Architect) The highest Red Hat certification tier. Earned by achieving five or more Red Hat Certified Specialist credentials. This guide targets the RHEL infrastructure concentration.</p> <p>RHCE (Red Hat Certified Engineer) The second-tier Red Hat certification (EX294). Tests Ansible automation skills on RHEL. Prerequisite: RHCSA.</p> <p>RHCSA (Red Hat Certified System Administrator) The entry-level Red Hat certification (EX200). Tests core RHEL administration skills. See Exam Objective Map.</p> <p>restorecon A SELinux tool that relabels files to their default context based on the fcontext database. Must be run after <code>semanage fcontext</code> to apply new rules to existing files. See semanage.</p> <p>Role (Ansible) A structured way to organize playbooks, tasks, variables, templates, and handlers into a reusable unit. Roles follow a standard directory layout. See Ansible Roles.</p> <p>RPM The underlying package format for RHEL. <code>rpm</code> provides low-level package management; <code>dnf</code> adds dependency resolution and repository management on top.</p>"},{"location":"98-reference/glossary/#s","title":"S","text":"<p>SELinux (Security-Enhanced Linux) A Linux Security Module implementing Mandatory Access Control. Enforces type enforcement policy on every file access, network connection, and process action. See SELinux Fundamentals.</p> <p>semanage A SELinux policy management tool for persistent changes to fcontext mappings, port labels, and booleans. Part of the <code>policycoreutils-python-utils</code> package. See semanage.</p> <p>slirp4netns The previous user-space network stack for rootless Podman (pre-RHEL 10). Replaced by pasta on RHEL 10.</p> <p>socket activation A systemd feature where a service is started on-demand when a connection arrives on its socket, rather than at boot. Improves startup time and resource usage.</p> <p>subscription-manager The RHEL tool for registering systems with Red Hat Subscription Management (RHSM) and entitling them to content repositories.</p> <p>sudo A program that allows a permitted user to execute commands as another user (typically root) after authentication. RHEL configures sudo via <code>/etc/sudoers</code> and <code>/etc/sudoers.d/</code>. See sudo and Updates.</p> <p>swap Disk space used to extend virtual memory when physical RAM is exhausted. Chronic swapping (high <code>si</code>/<code>so</code> in <code>vmstat</code>) indicates insufficient RAM for the workload.</p>"},{"location":"98-reference/glossary/#t","title":"T","text":"<p>target (systemd) A unit type that groups other units for synchronization and ordering. Acts as milestones in the boot process (e.g., <code>network-online.target</code>, <code>multi-user.target</code>).</p> <p>tcpdump A command-line network packet analyzer. Used for network troubleshooting and protocol analysis. See tcpdump.</p> <p>THP (Transparent Huge Pages) A kernel feature that automatically uses 2 MB huge memory pages instead of 4 KB standard pages. Improves throughput for some workloads (databases may prefer it disabled). Managed via <code>tuned</code> profiles.</p> <p>tuned A systemd service on RHEL 10 that applies kernel parameter and device setting profiles optimized for specific workloads. See tuned.</p>"},{"location":"98-reference/glossary/#u","title":"U","text":"<p>Unit (systemd) The basic configuration object in systemd. Types include <code>.service</code>, <code>.socket</code>, <code>.timer</code>, <code>.target</code>, <code>.mount</code>, <code>.path</code>, <code>.slice</code>, and <code>.scope</code>.</p> <p>userns (User Namespace) A Linux kernel feature that maps a range of host UIDs to a different range inside the namespace. Used by rootless Podman to give containers apparent root access without actual root privileges on the host.</p>"},{"location":"98-reference/glossary/#v","title":"V","text":"<p>VG (Volume Group) A pool of storage in LVM created from one or more Physical Volumes. Logical Volumes are allocated from VGs.</p> <p>virsh The command-line interface for libvirt/KVM VM management. Used for VM lifecycle, snapshot management, and network configuration.</p> <p>virt-install A command-line tool for creating and provisioning KVM virtual machines.</p>"},{"location":"98-reference/glossary/#x","title":"X","text":"<p>XFS The default filesystem for RHEL 10 data partitions. High-performance, scalable journaling filesystem. Can be grown online but cannot be shrunk. See Filesystems and fstab.</p>"},{"location":"98-reference/glossary/#z","title":"Z","text":"<p>:z / :Z (Podman volume mount flags) SELinux relabeling options for Podman volume mounts. <code>:z</code> (lowercase) = shared label (multiple containers can read). <code>:Z</code> (uppercase) = private label (only this container can read). See Volumes.</p>"},{"location":"98-reference/objective-map/","title":"Exam Objective Map","text":"<p>This page maps each chapter and lab to the official Red Hat exam objectives for RHCSA (EX200), RHCE (EX294), and the RHCA infrastructure concentration exams.</p> <p>Disclaimer: Exam objectives change. Always cross-reference with the current Red Hat exam page at access.redhat.com/training/exam-objectives.</p>"},{"location":"98-reference/objective-map/#rhcsa-ex200-rhel-10","title":"RHCSA (EX200) \u2014 RHEL 10","text":""},{"location":"98-reference/objective-map/#understand-and-use-essential-tools","title":"Understand and use essential tools","text":"Objective Chapter Lab Access a shell prompt and issue commands with correct syntax Shell Basics \u2014 Use input-output redirection Pipes and Redirection \u2014 Use grep and regular expressions to analyze text Files and Text \u2014 Access remote systems using SSH SSH Static IP &amp; DNS Log in and switch users in multiuser targets Shell Basics \u2014 Archive, compress, unpack, and uncompress files Files and Text \u2014 Create and edit text files Editors \u2014 Create, delete, copy, and move files and directories Files and Text \u2014 Create hard and soft links Files and Text \u2014 List, set, and change standard file permissions Permissions \u2014 Locate, read, and use system documentation Help System \u2014"},{"location":"98-reference/objective-map/#create-simple-shell-scripts","title":"Create simple shell scripts","text":"Objective Chapter Lab Conditionals, loops Bash Fundamentals \u2014 Process exit codes, test command Bash Fundamentals \u2014"},{"location":"98-reference/objective-map/#operate-running-systems","title":"Operate running systems","text":"Objective Chapter Lab Boot, reboot, shut down systemd Basics \u2014 Interrupt the boot process to gain access to a system Recovery Patterns \u2014 Identify CPU/memory intensive processes and kill Resource Triage \u2014 Adjust process scheduling Resource Triage \u2014 Manage tuning profiles tuned \u2014 Locate and interpret system log files and journals Logging and journald \u2014 Preserve system journals across reboots journald Retention \u2014 Start, stop, and check status of services systemd Basics Systemd Service Securely transfer files SSH \u2014"},{"location":"98-reference/objective-map/#configure-local-storage","title":"Configure local storage","text":"Objective Chapter Lab List, create, delete partitions Storage Overview \u2014 Create and remove physical volumes, VGs, LVs LVM LVM XFS Grow Configure systems to mount file systems at boot Filesystems and fstab \u2014 Create and use swap space Storage Overview \u2014 Create and manage POSIX ACLs ACLs Shared Team Dir"},{"location":"98-reference/objective-map/#create-and-configure-file-systems","title":"Create and configure file systems","text":"Objective Chapter Lab Create, mount, unmount, and use vfat, ext4, xfs file systems Filesystems and fstab \u2014 Mount and unmount network file systems using NFS Filesystems and fstab \u2014 Configure automount Filesystems and fstab \u2014 Extend existing LVs LVM LVM XFS Grow Create and configure set-GID directories for collaboration Permissions Shared Team Dir"},{"location":"98-reference/objective-map/#deploy-configure-and-maintain-systems","title":"Deploy, configure, and maintain systems","text":"Objective Chapter Lab Schedule tasks using at and cron Scheduling \u2014 Start and stop services, configure services to start automatically systemd Basics Systemd Service Configure systems to boot into a specific target systemd Advanced \u2014 Configure time service clients Networking Basics \u2014 Install and update software packages from a repository Packages and DNF \u2014 Modify the system bootloader Recovery Patterns \u2014"},{"location":"98-reference/objective-map/#manage-basic-networking","title":"Manage basic networking","text":"Objective Chapter Lab Configure IPv4 and IPv6 addresses NetworkManager and nmcli Static IP &amp; DNS Configure hostname resolution DNS Resolution \u2014 Configure network services to start automatically NetworkManager and nmcli \u2014 Restrict network access using firewall-cmd firewalld \u2014"},{"location":"98-reference/objective-map/#manage-users-and-groups","title":"Manage users and groups","text":"Objective Chapter Lab Create, delete, and modify local user accounts Shell Basics \u2014 Change passwords and adjust password aging Shell Basics \u2014 Create, delete, and modify local groups Permissions \u2014 Configure superuser access sudo and Updates \u2014"},{"location":"98-reference/objective-map/#manage-security","title":"Manage security","text":"Objective Chapter Lab Configure firewall settings using firewall-cmd firewalld \u2014 Manage default file permissions Permissions \u2014 Configure key-based authentication for SSH SSH \u2014 Set enforcing and permissive modes for SELinux SELinux Fundamentals \u2014 List and identify SELinux file and process context SELinux Fundamentals \u2014 Restore default file contexts SELinux AVC Basics SELinux Label Fix Manage SELinux port labels semanage Non-default Port Use Boolean settings to modify system SELinux settings SELinux Fundamentals \u2014 Diagnose and address routine SELinux policy violations SELinux AVC Basics SELinux Label Fix"},{"location":"98-reference/objective-map/#rhce-ex294-ansible-on-rhel-10","title":"RHCE (EX294) \u2014 Ansible on RHEL 10","text":"Objective Chapter Lab Install and configure Ansible on a control node Ansible Setup and Inventory \u2014 Create and use static inventories Ansible Setup and Inventory First Playbook Create and use playbooks Ansible Playbooks First Playbook Create and use variables, facts, and templates Ansible Vars and Templates \u2014 Create and use roles Ansible Roles Role Web Deploy Create and use logic in playbooks Ansible Playbooks \u2014 Use Ansible to configure target machines Ansible Service Deploy \u2014 Download and use roles from Ansible Galaxy Ansible Roles \u2014 Use Ansible Vault for secrets Ansible Vars and Templates \u2014 Manage content with collections Ansible Roles \u2014 Automate patching and updates Ansible Patching \u2014"},{"location":"98-reference/objective-map/#rhca-infrastructure-concentration-indicative-topics","title":"RHCA Infrastructure Concentration (indicative topics)","text":"Topic Chapter Advanced systemd unit configuration and hardening systemd Advanced, systemd Hardening SELinux audit workflow and policy development SELinux Audit Workflow, semanage Advanced networking \u2014 multiple profiles, routing nmcli Profiles, Routing Method Network diagnostics \u2014 tcpdump tcpdump Rootless Podman and container lifecycle Rootless, Volumes Container secrets management and rotation Secrets, Lab - Secrets Rotate Quadlet / systemd container integration systemd Integration, Lab - Rootless Web SELinux for containers SELinux Containers Performance triage Resource Triage tuned profile management tuned System recovery procedures Recovery Patterns journald retention and forwarding journald Retention Structured troubleshooting methodology Troubleshooting Playbook"}]}